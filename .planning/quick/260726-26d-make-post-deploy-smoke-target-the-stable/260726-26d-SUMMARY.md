---
quick_task: 260726-26d
title: Make post-deploy smoke target the stable public Scholar Scout Vercel production domain
status: complete
completed: 2026-07-26
commit: 830d702
requirements:
  - OPS-04
files_modified:
  - .github/workflows/post-deploy-smoke.yml
  - scripts/test-production-tooling.mjs
  - docs/vercel-deployment.md
---

# Quick Task 260726-26d Summary

Post-deploy smoke checks now use the configured stable public production domain while retaining the protected deployment URL and deployed SHA as dispatch evidence.

## Completed Work

- Bound `SCHOLARSCOUT_SMOKE_BASE_URL` in the production-only post-deploy workflow to the existing GitHub Actions `SCHOLARSCOUT_SMOKE_BASE_URL` secret.
- Preserved the dispatch URL for concurrency and failed-incident evidence, and preserved the dispatch SHA for checkout and incident evidence.
- Added a focused workflow-contract test that prevents a regression back to using the dispatch URL as the smoke target.
- Documented the stable public value `https://scholar-scout-web.vercel.app/`, the repository-secret configuration point, and the required update before a production-domain change.

## Verification

- Passed the plan contract assertion for the secret-backed smoke target, dispatch URL/SHA evidence, and documented stable public domain.
- Passed the focused `post-deploy smoke targets the stable secret while retaining dispatch evidence` Node test.
- Passed `git diff --check` before commit.
- `pnpm run test:production-tooling` could not start because the execution runner supplies Node 24.14.0 and pnpm 11.9.0, while this repository requires Node 20.x and pnpm 10.34.5.
- A direct full-suite run exercised 17 passing tests; its unrelated portable-Corepack wrapper test failed because sandboxing denied `lstat` access to `C:\\Users\\judge`.

## Decisions

- Kept Vercel Standard Protection enabled and introduced no deployment-access bypass, credentials, secret output, or production-data handling.
- Used the existing repository secret as the single maintainable public smoke target; dynamic deployment URLs remain evidence only.

## Deviations

None. The implementation followed the planned scope exactly.

## Commit

- `830d702` `fix(260726-26d): target post-deploy smoke at stable domain`
