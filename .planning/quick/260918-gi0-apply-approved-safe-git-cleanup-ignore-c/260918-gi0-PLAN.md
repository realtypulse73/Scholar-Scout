---
quick_task: 260918-gi0
title: Apply approved safe Git cleanup and ignore confirmed local artifacts
status: planned
autonomous: true
must_haves:
  truths:
    - The inspected tracked work is committed without staging unrelated untracked feature, asset, report, or planning paths.
    - The repository ignores only the four approved local/generated artifact paths.
    - The current branch is normally pushed after the two commits, with no history rewrite or force push.
  artifacts:
    - .gitignore
    - a content-work commit containing exactly the approved tracked content-diff files
    - a separate ignore-policy commit when .gitignore has the approved entries to add
  key_links:
    - .gitignore -> .worktrees/
    - .gitignore -> tmp/
    - .gitignore -> dev-server.log
    - .gitignore -> output/pdf/
---

# Quick Task 260918-gi0: Apply approved safe Git cleanup

## Goal

Commit the previously inspected tracked work with an explicit staging allowlist, separately record the approved local-artifact ignore policy, and normally publish the current branch.

## Scope and safety boundary

- Do not delete, discard, move, stash, drop a stash, change branches, rewrite history, rebase, reset, amend, or force-push.
- Do not stage any untracked feature directories, assets, reports, or planning material.
- The only ignore entries permitted are `.worktrees/`, `tmp/`, `dev-server.log`, and `output/pdf/`.
- Exclude `.gitignore`, `apps/web/components/feed/PathCard.tsx`, and `apps/web/lib/platform.ts` from the content-work commit because the X-ray found no content diff for those three paths.

## Tasks

### 1. Verify the working-tree boundary and stage only the approved content-diff files

**Files:**

- `.planning/ROADMAP.md`
- `.planning/STATE.md`
- `.planning/config.json`
- `.planning/debug/fixture-lifecycle-transport.md`
- `apps/web/__tests__/lib/programmes.test.ts`
- `apps/web/app/globals.css`
- `apps/web/app/layout.tsx`
- `apps/web/app/programmes/[id]/page.tsx`
- `apps/web/app/schools/[slug]/page.tsx`
- `apps/web/components/programmes/ProgrammeResults.tsx`
- `apps/web/lib/programmes.ts`
- `output/gsd-effective-use-training.html`
- `scripts/prelaunch-rehearsal.mjs`
- `scripts/test-production-tooling.mjs`

**Action:** Before staging, run `git status --short`, `git diff --name-only`, and `git diff --cached --name-only`. Confirm the index is empty and that the fourteen paths listed above remain the only paths with intended content changes. Stage them only through an explicit `git add --` allowlist. Review `git diff --cached --name-only` and `git diff --cached --check`; if the staged file list differs from the allowlist or whitespace validation fails, stop without committing and report the discrepancy. Keep all untracked paths and the three no-content-diff files out of the index.

**Verify:** `git diff --cached --name-only` outputs exactly the fourteen listed paths, and `git diff --cached --check` exits successfully.

**Done:** The index contains only the approved tracked content work and the working tree still retains all unrelated/untracked paths.

### 2. Commit the staged work, then add and commit the narrowly approved ignore policy

**Files:**

- `.gitignore`

**Action:** Commit the staged content work first with a descriptive conventional commit message that reflects the actual staged changes. After confirming that commit succeeds, inspect `.gitignore` and add only the four approved lines: `.worktrees/`, `tmp/`, `dev-server.log`, and `output/pdf/`. Do not alter existing ignore rules. Stage `.gitignore` by its explicit path, inspect `git diff --cached -- .gitignore`, and make a separate cleanup commit. Do not include the plan itself, any untracked directory, or a no-content-diff path in either commit.

**Verify:** `git show --name-only --format='' HEAD~1` lists only `.gitignore`; `git show --name-only --format='' HEAD` lists exactly the fourteen approved content paths; and `git check-ignore -q .worktrees tmp dev-server.log output/pdf/example.pdf` succeeds for each approved local/generated artifact target.

**Done:** There are two new commits in order: the approved content work first and the four-entry ignore-policy cleanup second.

### 3. Push normally and capture the post-push repository state

**Files:**

- `.planning/quick/260918-gi0-apply-approved-safe-git-cleanup-ignore-c/260918-gi0-SUMMARY.md`

**Action:** Run a plain `git push` from the unchanged current branch after both commits are locally verified. Do not use force-related flags or change the upstream. Capture the branch name, the two commit IDs, push result, final ahead/behind status, and the remaining untracked paths in the quick-task summary. If push fails because of authorization or a non-fast-forward remote update, leave local commits intact and report the precise blocker rather than retrying with a destructive command.

**Verify:** `git status --short --branch` reports the expected current branch with no ahead/behind divergence after a successful push; otherwise the summary records the push error and preserves the local commit IDs.

**Done:** The current branch has been normally pushed when authorized, or the safe stopping condition and retained local state are documented.

## Expected outcome

The approved content changes are committed without absorbing unrelated work; only the four explicitly authorized local/generated paths become ignored; and the branch is published through an ordinary fast-forward push.
