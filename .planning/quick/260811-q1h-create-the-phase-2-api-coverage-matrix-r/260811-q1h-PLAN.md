# Quick Task 260811-q1h: Create the Phase 2 API coverage matrix

## Goal

Record a complete, explicit coverage decision for every external API capability in Phase 2 so the blocking API-coverage pre-verification gate can validate the completed phase.

## Tasks

1. Create the canonical Phase 2 matrix at `.planning/phases/02-authentication-api-ai-and-webhook-controls/COVERAGE.md` (not in this quick-task directory). Use exactly one non-empty, human-editable Markdown table with the `capability`, `decision`, and `reason` columns; give every row a unique, service-qualified capability name and use only `INTEGRATE` or `OPT-OUT`. Base the inventory on `02-CONTEXT.md`, `02-RESEARCH.md`, `02-08-PLAN.md`, `02-09-PLAN.md`, `02-02-PLAN.md`, the completed verification report, and the live advisor, limiter, and webhook-runner implementations.

   - Enumerate the OpenAI Responses surface: the bounded, non-streaming structured response creation used by the advisor is `INTEGRATE`; streaming, response persistence/retrieval/cancellation, continuation/conversations, background mode, tools (including web search, file search, code interpreter, computer use, image generation, and remote MCP), function calling, and non-advisor multimodal generation are reasoned `OPT-OUT`s because D-09--D-12 require a direct, single-turn, server-owned advisor with no tools or browsing.
   - Enumerate the Upstash rate-limit surface: fixed-window atomic reservation is `INTEGRATE` for advisor, credentials, and registration; sliding-window, token-bucket, cache, analytics, and general Redis data-store operations are reasoned `OPT-OUT`s because Phase 2 uses the provider only for fail-closed reservations before protected work.
   - Enumerate the GitHub webhook and Issues surfaces: signed `issues` delivery for the configured repository with `opened`/`labeled` and approved labels, plus posting the qualified issue comment, are `INTEGRATE`; every other event/action, webhook administration, issue mutation other than the job-packet comment, and unverified delivery are reasoned `OPT-OUT`s per D-13--D-16.
   - Enumerate the externally configured Codex-agent endpoint: authenticated bounded job-packet `POST` dispatch is `INTEGRATE`; unauthenticated dispatch, inbound callbacks, polling, retries, and unbounded/bidirectional job control are reasoned `OPT-OUT`s because the runner only supports the one-way bearer-authenticated 10-second dispatch contract.
   - Enumerate the existing GitHub and Google OAuth provider capabilities Phase 2 depends on for returned sessions: authorization-code sign-in/callback identity is `INTEGRATE`; provider-management APIs, refresh-token/background access, organization/repository data access, and additional profile scopes are reasoned `OPT-OUT`s because Phase 2 retains optional NextAuth sign-in only to trigger server-bound guest migration.
   - Do not add Vercel Blob, the HTTP data adapter, or other integrations whose capabilities are not part of the Phase 2 control scope. Every `OPT-OUT` must have a concise, single-line reason; `INTEGRATE` rows may have an empty reason.

2. Validate the committed artifact with the same blocking gate used at Phase 2 pre-verification, then correct only matrix-format defects reported by that check: `node "$env:USERPROFILE\\.codex\\gsd-core\\bin\\gsd-tools.cjs" query api-coverage.verify-pre ".planning\\phases\\02-authentication-api-ai-and-webhook-controls"`. Confirm it reports a passing valid matrix and that no duplicate capability, invalid decision, missing opt-out reason, pipe-containing cell, or empty matrix remains.

## Expected outcome

`.planning/phases/02-authentication-api-ai-and-webhook-controls/COVERAGE.md` is the gate-recognized, complete Phase 2 external-API decision record, and `api-coverage.verify-pre` passes without changing production code, secrets, or unrelated planning artifacts.
