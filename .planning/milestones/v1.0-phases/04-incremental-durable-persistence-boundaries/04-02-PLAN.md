---
phase: 04-incremental-durable-persistence-boundaries
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - apps/web/lib/server/student-records.ts
  - apps/web/lib/server/data-store.ts
  - apps/web/app/api/account/onboarding/route.ts
  - apps/web/app/api/account/shortlist/route.ts
  - apps/web/__tests__/lib/student-records.test.ts
  - apps/web/__tests__/api/account-guest-routes.test.ts
autonomous: true
requirements: [DATA-01, DATA-02]
must_haves:
  truths:
    - "Student account, onboarding, shortlist, and shortlist-plan changes use server-derived ownership and bounded conditional operations."
    - "A stale student replacement preserves the newer state and returns an explicit safe retry/conflict outcome."
    - "Shortlist IDs and shortlist plans commit as one student-intent change rather than two independently overwritable documents."
  artifacts:
    - path: "apps/web/lib/server/student-records.ts"
      provides: "Bounded account/profile/shortlist operations over the Phase 4 CAS port"
    - path: "apps/web/__tests__/lib/student-records.test.ts"
      provides: "Ownership, atomicity, and interleaved conflict coverage"
  key_links:
    - from: "apps/web/app/api/account/shortlist/route.ts"
      to: "apps/web/lib/server/student-records.ts"
      via: "one combined server-derived actor operation for shortlist and plans"
    - from: "apps/web/lib/server/student-records.ts"
      to: "apps/web/lib/server/persistence-operations.ts"
      via: "non-retrying conditional replacement mutation"
---

<objective>
Migrate student-owned account and pathway-progress writes to bounded, ownership-safe conditional operations.

Purpose: Prevent stale profile or shortlist changes from erasing newer student intent while completing the student domain portion of DATA-02.
Output: Student record operations, atomic shortlist-state persistence, safe route conflicts, and focused tests.
</objective>

<execution_context>@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md</execution_context>

<context>
@.planning/phases/04-incremental-durable-persistence-boundaries/04-01-SUMMARY.md
@.planning/phases/04-incremental-durable-persistence-boundaries/04-CONTEXT.md
@.planning/phases/04-incremental-durable-persistence-boundaries/04-RESEARCH.md
@apps/web/lib/server/data-store.ts
@apps/web/app/api/account/onboarding/route.ts
@apps/web/app/api/account/shortlist/route.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bound account provisioning and onboarding profile mutations</name>
  <files>apps/web/lib/server/student-records.ts, apps/web/lib/server/data-store.ts, apps/web/__tests__/lib/student-records.test.ts</files>
  <behavior>
    - Credential and OAuth account creation cannot erase an account committed after the same initial read.
    - Onboarding replacement touches only the server-selected student's profile and required audit data.
    - Onboarding conflict returns without applying an older profile and without leaking another student's data.
  </behavior>
  <action>Move the mutation logic for `createUser`, `findOrCreateOAuthUser`, and `saveOnboardingProfile` behind named bounded student operations while retaining existing exported compatibility functions in `data-store.ts`. Every operation must read one version, validate uniqueness/ownership against that snapshot, compose only its owned student slice plus required audit evidence, and commit conditionally. Treat account/profile replacements as non-commutative: return a stable internal conflict instead of blind retry. Preserve existing password handling, OAuth identity behavior, server-derived storage keys, and route validation.</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web run test -- --runInBand __tests__/lib/student-records.test.ts __tests__/api/account-guest-routes.test.ts</automated></verify>
  <done>Account provisioning and onboarding writes are bounded, ownership-safe, and reject interleaved stale replacements without data loss.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Commit shortlist IDs and plans as one conflict-safe student operation</name>
  <files>apps/web/lib/server/student-records.ts, apps/web/lib/server/data-store.ts, apps/web/app/api/account/onboarding/route.ts, apps/web/app/api/account/shortlist/route.ts, apps/web/__tests__/lib/student-records.test.ts, apps/web/__tests__/api/account-guest-routes.test.ts</files>
  <behavior>
    - One shortlist request updates programme IDs and plan statuses in one conditional commit.
    - Interleaved shortlist/onboarding replacements return 409 with a safe reload/retry category and preserve the winning state.
    - Browser-supplied identity never selects the stored student slice.
  </behavior>
  <action>Create a combined bounded shortlist-state operation and change the shortlist route from sequential `saveShortlist`/`saveShortlistPlans` calls to that one operation. Keep the individual exported helpers only for compatible internal callers, implemented through the same conditional student boundary. Map the general persistence conflict to a stable `409` response that gives the caller a reload/retry action but no provider token or other student's contents. Update onboarding similarly. Do not auto-retry replacement intent or weaken the existing actor/auth/input guards.</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web run test -- --runInBand __tests__/lib/student-records.test.ts __tests__/api/account-guest-routes.test.ts</automated></verify>
  <done>Student profile and shortlist journeys use bounded conditional writes, preserve server-derived ownership, and expose explicit safe conflicts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| account route -> student operation | Authenticated/opaque guest actor identity selects the only mutable student slice. |
| student operation -> CAS adapter | Replacement intent is conditional and never automatically replayed over newer choices. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-05 | Tampering | student replacements | high | mitigate | Non-retrying CAS and interleaving tests. |
| T-04-06 | Elevation of Privilege | student ownership | critical | mitigate | Retain server-derived actor/storage keys and cross-user negative tests. |
| T-04-07 | Information Disclosure | conflict response | high | mitigate | Return only safe retry category/status; no current foreign record or provider token. |
| T-04-SC | Tampering | package installs | high | mitigate | No package install. |
</threat_model>

<verification>Focused student operation and account route tests pass, including cross-owner and two-writer interleavings.</verification>
<success_criteria>Student account, onboarding, shortlist, and plan writes satisfy DATA-01/02 without changing auth semantics or production state.</success_criteria>
<output>Create `04-02-SUMMARY.md` when done.</output>
