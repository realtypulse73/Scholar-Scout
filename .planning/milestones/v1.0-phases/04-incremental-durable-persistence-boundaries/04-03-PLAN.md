---
phase: 04-incremental-durable-persistence-boundaries
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - apps/web/lib/server/operational-records.ts
  - apps/web/lib/server/data-store.ts
  - apps/web/lib/server/platform-store.ts
  - apps/web/__tests__/lib/operational-records.test.ts
  - apps/web/__tests__/lib/platform-store.test.ts
autonomous: true
requirements: [DATA-01, DATA-02]
must_haves:
  truths:
    - "Privileged audit, guest lifecycle, recovery lifecycle, and platform events use bounded conditional operations."
    - "A whitelisted commutative append may retry exactly once only with a stable pre-generated event ID; every other conflict is returned."
    - "Memory, simulation, decision, migration, and other replacement transforms return conflict instead of replaying stale intent."
    - "Required audit evidence commits with its domain change, never in a separate adapter write."
  artifacts:
    - path: "apps/web/lib/server/operational-records.ts"
      provides: "Classified bounded operational append, lifecycle, and replacement operations"
    - path: "apps/web/__tests__/lib/operational-records.test.ts"
      provides: "Retry classification, idempotency, and audit atomicity coverage"
  key_links:
    - from: "apps/web/lib/server/platform-store.ts"
      to: "apps/web/lib/server/operational-records.ts"
      via: "delegation of append and replacement mutations"
    - from: "apps/web/lib/server/operational-records.ts"
      to: "apps/web/lib/server/persistence-operations.ts"
      via: "explicit commutative-retry versus replacement-conflict policy"
---

<objective>
Migrate operational and platform write paths to bounded conditional operations with explicit, testable retry classification.

Purpose: Complete the operational domain of DATA-02 without turning conflict handling into duplicate events or stale replacement loss.
Output: Operational record operations, migrated platform-store delegates, and concurrency/idempotency tests.
</objective>

<execution_context>@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md</execution_context>

<context>
@.planning/phases/04-incremental-durable-persistence-boundaries/04-02-SUMMARY.md
@.planning/phases/04-incremental-durable-persistence-boundaries/04-RESEARCH.md
@apps/web/lib/server/data-store.ts
@apps/web/lib/server/platform-store.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bound lifecycle and privileged audit operations</name>
  <files>apps/web/lib/server/operational-records.ts, apps/web/lib/server/data-store.ts, apps/web/__tests__/lib/operational-records.test.ts</files>
  <behavior>
    - Privileged audit append and guest/recovery lifecycle changes conditionally commit their domain record and required audit evidence together.
    - Duplicate stable event IDs remain idempotent across a bounded retry.
    - Lifecycle replacements and migrations surface conflicts and never replay stale transforms automatically.
  </behavior>
  <action>Inventory and migrate the operational mutations currently exported from `data-store.ts`, including guest registration/migration bindings, privileged operation audit, recovery lifecycle/outcome records, and other server-owned operational records. Resolve the retry research question with an explicit allowlist: privileged audit and recovery lifecycle/outcome appends may retry exactly once (two total CAS attempts) only when their event ID is generated before the first read and duplicate-ID application is a no-op. Guest lifecycle registration, guest/account migration, incident-hold replacement, and any operation without a stable ID never retry. Encode this classification in named operation policy, not comments alone. Compose audit evidence in the same conditional mutation as the affected record. Preserve existing public exports and privacy-minimal audit shapes.</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web run test -- --runInBand __tests__/lib/operational-records.test.ts __tests__/lib/data-store.test.ts</automated></verify>
  <done>Operational lifecycle and audit writes are bounded, atomic with required evidence, and have explicit tested retry classifications.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate platform append and replacement writes</name>
  <files>apps/web/lib/server/operational-records.ts, apps/web/lib/server/platform-store.ts, apps/web/__tests__/lib/operational-records.test.ts, apps/web/__tests__/lib/platform-store.test.ts</files>
  <behavior>
    - Feed, analytics, referral, and share appends retain unique IDs and cannot lose an interleaved append.
    - Simulation, memory, decision, and guest-owned record migrations reject stale replacements without blind retry.
    - Existing reads and recommendation/metric behavior remain compatible.
  </behavior>
  <action>Replace every direct platform-store read/modify/`writeScholarScoutData` mutation with a named bounded operational call. Keep analytics in the existing shared compatibility store; separate analytics storage and background jobs are out of scope. Extend the explicit retry allowlist to feed interaction, analytics event, referral, and share appends: generate the ID before the first read, retry exactly once after CAS conflict, and make duplicate-ID application a no-op. Simulation result, memory, decision run, guest-owned migration, and any operation lacking a stable ID never retry and must surface conflict. Test both allowlist and denylist behavior. Preserve read normalization, ownership rules, and current exported platform-store signatures where callers rely on them.</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web run test -- --runInBand __tests__/lib/operational-records.test.ts __tests__/lib/platform-store.test.ts</automated></verify>
  <done>All operational/platform mutation families use bounded conditional operations and interleaved writes cannot silently erase or duplicate records.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| platform caller -> operational operation | Server-owned record invariants and stable IDs govern retry eligibility. |
| operational operation -> adapter | One CAS commit contains both domain and required audit changes. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-08 | Tampering | concurrent operational writes | high | mitigate | CAS plus interleaved append/replacement tests. |
| T-04-09 | Repudiation | audit/domain split | high | mitigate | Compose both in one conditional mutation. |
| T-04-10 | Denial of Service | contention retry | medium | mitigate | Small bounded retries only for stable-ID idempotent appends. |
| T-04-11 | Information Disclosure | operational conflicts | medium | mitigate | Stable categories without event payloads or provider metadata. |
| T-04-SC | Tampering | package installs | high | mitigate | No package install. |
</threat_model>

<verification>Focused operational and platform tests prove no lost append, no duplicate retry, no blind replacement replay, and preserved read behavior.</verification>
<success_criteria>Operational records complete DATA-02 coverage while DATA-04/05 remain explicitly deferred and production remains untouched.</success_criteria>
<output>Create `04-03-SUMMARY.md` when done.</output>
