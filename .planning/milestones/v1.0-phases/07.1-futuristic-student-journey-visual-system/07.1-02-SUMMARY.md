---
phase: 07.1-futuristic-student-journey-visual-system
plan: 02
subsystem: visual-system
tags: [nextjs, react, accessibility, css-motion, regression-tests]
requires:
  - phase: 07.1-01
    provides: StudentJourneyScene and shared red-white-silver visual tokens.
provides:
  - School-locker and peer-community adoption of the reusable journey scene.
  - Regression tests for foreground content, clipped decorative layers, and consumer actions.
affects: [school-locker, peer-community, visual-system]
tech-stack:
  added: []
  patterns:
    - Presentation-only route consumers use StudentJourneyScene instead of local image overlays.
    - Scene decoration containment has a stable data attribute separate from foreground content.
key-files:
  created:
    - apps/web/__tests__/components/lifestyle-scenes.test.tsx
  modified:
    - apps/web/app/schools/[slug]/page.tsx
    - apps/web/components/peer-community/PeerCommunity.tsx
    - apps/web/components/visual/StudentJourneyScene.tsx
    - apps/web/__tests__/components/StudentJourneyScene.test.tsx
key-decisions:
  - "Preserve each consumer's data, error, and interaction flow; only replace its local scene presentation."
  - "Expose the clipped, non-interactive decoration container with data-scene-decorations for stable regression coverage."
requirements-completed: [D-01, D-02, D-03, D-04, D-05, D-06, D-07]
duration: 16min
completed: 2026-09-22
status: complete
---

# Phase 07.1 Plan 02: Shared Lifestyle Scene Adoption Summary

**School Locker and Peer Community now share the same calm, accessible StudentJourneyScene without changing their application behavior.**

## Accomplishments

- Replaced the school-locker route's local transition styling with the server-safe reusable scene wrapper while retaining its governed-programme lookup and `notFound()` behavior.
- Replaced Peer Community's inline blue image overlay with the reusable scene, preserving client-side selection, API request, and error behavior.
- Added regression coverage for the consumer-visible heading and sign-in action, foreground stacking, right-side clipping boundary, and non-interactive decorative hooks.

## Verification

- `corepack pnpm --filter @scholar-scout/web test --runInBand --runTestsByPath __tests__/components/lifestyle-scenes.test.tsx __tests__/components/StudentJourneyScene.test.tsx` — passed (2 suites, 3 tests).
- `corepack pnpm --filter @scholar-scout/web run lint` — passed.
- `corepack pnpm --filter @scholar-scout/web run typecheck` — passed.
- `corepack pnpm --filter @scholar-scout/web test --runInBand` — completed successfully. Next.js reported its existing multiple-lockfile workspace-root warning, but no test failure.

## Commits

- `0c06e56` — RED: added failing consumer scene coverage.
- `607787f` — GREEN: routed school and peer consumers through the shared scene.
- `e78dbf7` — RED: defined clipping and foreground isolation coverage.
- `80c6a30` — GREEN: added the stable decoration-container hook.

## Deviations from Plan

None - plan executed as written. The stable `data-scene-decorations` hook is the explicit class/data hook required by Task 2 to verify the existing clipping and reduced-motion boundary.

## Self-Check: PASSED

- Confirmed all planned source and test artifacts exist.
- Confirmed all four task commits exist.
- Confirmed no tracked files were deleted by this plan.
