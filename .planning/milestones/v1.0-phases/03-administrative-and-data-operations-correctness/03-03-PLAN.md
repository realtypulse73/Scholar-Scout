---
phase: 03-administrative-and-data-operations-correctness
plan: 03
type: execute
wave: 2
depends_on: [03-02]
files_modified:
  - apps/web/app/api/admin/data/backups/route.ts
  - apps/web/app/api/admin/data/backups/[id]/plan/route.ts
  - apps/web/app/api/admin/data/backups/[id]/restore/route.ts
  - apps/web/app/api/admin/data/backups/[id]/hold/release/route.ts
  - apps/web/app/api/admin/data/status/route.ts
  - apps/web/__tests__/api/admin-data-routes.test.ts
autonomous: true
requirements: [OPS-02, OPS-03, DATA-03]
must_haves:
  truths:
    - "D-01/D-02: backup operations advertised by capabilities have implemented authorized handlers and exact safe response contracts."
    - "D-05–D-09: backup preview/apply requires a bound plan, non-empty reason, typed phrase, fresh version check, one-write recovery backup, and minimal audit."
    - "D-10–D-13: unavailable reads make status/backups/apply return safe retryable 503 and never empty arrays, zeros, or writes."
    - "D-15/D-17: a freshly authorized server-only operator route can release an active incident hold with a bounded reason and one composed write/audit, while no Phase 3 UI capability advertises that operation."
  artifacts:
    - "Backup list, preview, apply, incident-hold release, and compatibility status routes delegate to the recovery service."
  key_links:
    - "backup ID -> server-issued plan token -> exact apply contract -> recovery service"
    - "backup ID + incident ID + bounded reason -> freshly authorized hold-release route -> fresh healthy read -> one composed recovery-service write with lifecycle audit"
---
<objective>Wire the complete backup recovery route family to the safe recovery domain and canonical capability contract (D-01, D-02, D-05–D-13, D-15, D-17).</objective>
<execution_context>@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03-administrative-and-data-operations-correctness/03-02-SUMMARY.md
@.planning/phases/03-administrative-and-data-operations-correctness/03-PATTERNS.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Return verified backup summaries and count-only plans</name>
  <read_first>apps/web/app/api/admin/data/backups/route.ts, apps/web/app/api/admin/data/backups/[id]/plan/route.ts, apps/web/app/api/admin/data/status/route.ts, apps/web/__tests__/api/admin-data-routes.test.ts</read_first>
  <files>apps/web/app/api/admin/data/backups/route.ts, apps/web/app/api/admin/data/backups/[id]/plan/route.ts, apps/web/app/api/admin/data/status/route.ts, apps/web/__tests__/api/admin-data-routes.test.ts</files>
  <behavior>
    - Authorization happens before storage; list is deterministic newest-first and exposes metadata/counts/hold state but never backup data.
    - Missing backup is 404; healthy empty is explicit; read failure is safe 503 with incident ID rather than an empty list.
    - Preview contains plan token/expiry/source/current version and category deltas only.
  </behavior>
  <action>Delegate route policy to data-recovery. Keep status compatible only as a projection of the canonical fresh health contract, avoiding a second source of truth. Map typed expected failures precisely and let unexpected errors propagate. After staff authorization, route failed/no-write outcomes through the independent operational-evidence sink and test only actor/action/safe category/incident ID/timestamp/outcome are present, with zero application writes and no provider/student/snapshot data. Cover OPS-03 adjacency/empty/ordering probes through equality-boundary, stable-order, zero/one/many, and failure-not-empty tests.</action>
  <verify><automated>pnpm --filter @scholar-scout/web run test -- --runInBand __tests__/api/admin-data-routes.test.ts</automated></verify>
  <acceptance_criteria>Authorized list/plan paths expose only safe summaries; denial/read failures perform zero writes and provider strings never reach JSON.</acceptance_criteria>
  <done>Authorized list/plan paths expose only safe summaries; denial/read failures perform zero writes and provider strings never reach JSON.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Apply backup plans through an exact bounded request</name>
  <read_first>apps/web/app/api/admin/data/backups/[id]/restore/route.ts, apps/web/lib/api-request.ts, apps/web/__tests__/api/admin-data-routes.test.ts</read_first>
  <files>apps/web/app/api/admin/data/backups/[id]/restore/route.ts, apps/web/__tests__/api/admin-data-routes.test.ts</files>
  <behavior>
    - Only exact bounded {planToken, reason, confirmation} is accepted after authorization.
    - Invalid 400, conflict 409, expired/consumed 410, oversized 413, unavailable 503 all write zero times.
    - Successful apply writes once and returns operation/recovery-backup/audit identifiers without snapshot content.
  </behavior>
  <action>Remove backup ID or snapshot as apply authority. Authorize before bounded parsing, bind the route source to token claims, and call the common apply service. Reject whitespace reason and wrong phrase before write. Map safe typed outcomes, retain retry guidance, and assert every authorized rejection emits privacy-minimal operational evidence through the independent sink while application-store write count remains zero.</action>
  <verify><automated>pnpm --filter @scholar-scout/web run test -- --runInBand __tests__/api/admin-data-routes.test.ts __tests__/lib/data-recovery.test.ts</automated></verify>
  <acceptance_criteria>Backup apply cannot bypass preview or binding and cannot partially write or disclose data.</acceptance_criteria>
  <done>Backup apply cannot bypass preview or binding and cannot partially write or disclose data.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Release an incident hold through an exact authorized operator route</name>
  <read_first>apps/web/app/api/admin/data/backups/route.ts, apps/web/lib/server/active-staff.ts, apps/web/lib/api-request.ts, apps/web/lib/server/data-recovery.ts, apps/web/__tests__/api/admin-data-routes.test.ts</read_first>
  <files>apps/web/app/api/admin/data/backups/[id]/hold/release/route.ts, apps/web/__tests__/api/admin-data-routes.test.ts</files>
  <behavior>
    - POST `/api/admin/data/backups/[id]/hold/release` calls fresh `requireActiveStaff` before body parsing or storage access and accepts only an exact bounded `{incidentId, reason}` body with non-empty trimmed values.
    - The recovery service validates the route backup ID and supplied incident ID identify an active unresolved hold using one fresh healthy document read; malformed/unknown identity is 400/404, a resolved or changed hold is 409, and unhealthy storage is a safe retryable 503.
    - Success performs exactly one composed application-document write that releases the hold and includes the D-17 lifecycle audit with actor, action, backup/incident identity, schema/digest/timestamps/outcome/safe reason category, but no snapshot or student content.
    - Every rejection performs zero application-document writes; authorized operational failures emit independent privacy-minimal evidence with safe category and incident ID, never provider, snapshot, package, or student content.
    - The capability response and Phase 3 admin UI do not advertise or render incident-hold release; this is an operator/server route only.
  </behavior>
  <action>Create the exact hold-release POST route and route-level tests per D-15/D-17. Invoke fresh `requireActiveStaff` before calling the bounded exact-body parser or recovery service. Bind the route `[id]` backup identity and body `incidentId` to an active unresolved hold found by a fresh validated read; reject whitespace/oversized/extra-field bodies and mismatched identities before write. Delegate the state transition to the existing recovery service so success composes the hold release and privacy-minimal lifecycle audit into exactly one `write()` call. Map typed outcomes to safe 400, 404, 409, and retryable 503 responses; after authorization, send failures to the independent operational-evidence sink even when the application store is unhealthy. Add tests that assert authorization ordering, exact/bounded input, unknown backup/incident, inactive/mismatched hold, unhealthy read, version/state conflict, success response, D-17 audit fields/redaction, and zero writes on every rejection versus one write on success. Keep this route out of the capability operation list and add a route/capability assertion proving no Phase 3 UI control can be derived for it.</action>
  <verify><automated>pnpm --filter @scholar-scout/web run test -- --runInBand __tests__/api/admin-data-routes.test.ts __tests__/lib/data-recovery.test.ts</automated></verify>
  <acceptance_criteria>The exact operator route is freshly authorized, bounded, identity- and health-checked, one-write audited on success, zero-write and privacy-safe on all failures, and absent from Phase 3 UI capabilities.</acceptance_criteria>
  <done>An authorized operator can release only the named active incident hold with a reason; all safe status mappings, write counts, audit content, redaction, and capability non-exposure are enforced by route tests.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| staff HTTP -> backup routes | Authorization and bounded exact parsing. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-08 | Elevation | backup routes | high | mitigate | Fresh staff guard before parse/read. |
| T-03-09 | Tampering | restore POST | high | mitigate | Token is sole authority and checked by service. |
| T-03-10 | Information Disclosure | summaries/errors | high | mitigate | Counts/metadata only and provider redaction. |
| T-03-10A | Elevation/Tampering | incident-hold release route | high | mitigate | Fresh staff authorization before parsing/storage, exact bounded identity/reason DTO, active-hold validation, and one composed audited write. |
## ASVS Level 1 Control Mapping
| ASVS area | Task/verification mapping | Disposition |
|---|---|---|
| V2/V3 Authentication and Session | Both tasks require fresh active-staff authorization; route tests prove guard ordering. | mitigate |
| V4 Access Control | Capability-advertised handlers, actor/source-bound plan checks, and fresh operator authorization for the non-UI hold-release route. | mitigate |
| V5 Validation | Exact bounded apply and hold-release DTOs, identity/state validation, and typed status mapping tests. | mitigate |
| V7 Error/Logging | All tasks assert privacy-minimal authorized failure evidence, safe responses, and D-17 lifecycle audit content. | mitigate |
| V8 Data Protection | Summary/result exclusion assertions prevent snapshot/student disclosure. | mitigate |
| V12/V13 Files and API | Backup package boundary and authenticated route contract tests. | mitigate |
</threat_model>
<verification>Focused route/recovery tests plus full web suite/typecheck at wave end.</verification>
<success_criteria>Every advertised backup capability maps to a real authorized route with explicit safe failure and recovery semantics.</success_criteria>
<output>Create `03-03-SUMMARY.md` when done.</output>
