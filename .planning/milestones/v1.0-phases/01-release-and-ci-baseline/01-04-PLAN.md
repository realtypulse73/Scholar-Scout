---
phase: 01-release-and-ci-baseline
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - README.md
  - docs/docker-free-development.md
  - docs/http-data-adapter-runbook.md
  - docs/vercel-blob-data-adapter.md
  - services/http-data-service/README.md
  - docs/vercel-deployment.md
  - docs/vercel-permissions-handoff.md
  - docs/vercel-docker-workaround.md
  - .github/PULL_REQUEST_TEMPLATE.md
autonomous: true
requirements:
  - OPS-05
must_haves:
  truths:
    - "Contributors have one documented frozen pnpm command path for install, development, validation, service fixtures, and Vercel builds."
    - "Vercel operators can configure Corepack, root build commands, and the production branch without consulting obsolete npm instructions."
    - "Pull-request guidance is consistent with the deployed package-manager and CI contract."
  artifacts:
    - path: README.md
      provides: "primary pnpm contributor command reference"
    - path: docs/vercel-deployment.md
      provides: "Vercel Corepack/frozen-install deployment instructions"
    - path: docs/vercel-permissions-handoff.md
      provides: "Vercel access and production configuration handoff"
  key_links:
    - from: README.md
      to: package.json
      via: "documented pnpm commands invoke retained root scripts"
    - from: docs/vercel-deployment.md
      to: vercel.json
      via: "operator instructions exactly match committed frozen pnpm commands"
---

<objective>
Publish the contributor and Vercel documentation half of the pnpm migration so local and hosted execution follow one immutable path.

Purpose: Complete OPS-05 for the daily developer, service-fixture, Docker-free, and Vercel workflows rather than leaving correct configuration undocumented.
Output: revised root, development, data-adapter, Vercel, and pull-request documentation with one pnpm vocabulary.
</objective>

<execution_context>
@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-release-and-ci-baseline/01-CONTEXT.md
@.planning/phases/01-release-and-ci-baseline/01-RESEARCH.md
@.planning/phases/01-release-and-ci-baseline/01-VALIDATION.md
@.planning/phases/01-release-and-ci-baseline/01-01-SUMMARY.md
@.planning/phases/01-release-and-ci-baseline/01-03-SUMMARY.md
@README.md
@docs/vercel-deployment.md
@docs/docker-free-development.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert contributor and local-service guides to frozen pnpm</name>
  <files>README.md, docs/docker-free-development.md, docs/http-data-adapter-runbook.md, docs/vercel-blob-data-adapter.md, services/http-data-service/README.md</files>
  <read_first>
    - `README.md` and `docs/docker-free-development.md`: retain supported local-development and Docker-free entry points while replacing package-manager prerequisites and commands.
    - `docs/http-data-adapter-runbook.md`, `docs/vercel-blob-data-adapter.md`, and `services/http-data-service/README.md`: retain adapter configuration, URLs, token guidance, and HTTP-service contract instructions while translating only command execution.
    - `package.json` and `scripts/pnpm-portable.ps1`: use the exact finalized root/filter commands and portable helper name.
  </read_first>
  <action>Per D-01, D-02, D-05, and D-14, replace npm prerequisites, install commands, root scripts, and workspace service commands with the exact Corepack pnpm path. State that pnpm 10.34.5 is selected by the root `packageManager`, that contributors run `corepack enable`, and that installation uses `pnpm install --frozen-lockfile --ignore-scripts`; document the pnpm portable helper where the portable Node flow is described. Preserve all existing data-adapter, Docker-free, and service-contract guidance, but ensure every command invokes the equivalent root script or `pnpm --filter` workspace command.</action>
  <verify>
    <automated>rg -n 'pnpm@10.34.5|corepack enable|pnpm install --frozen-lockfile --ignore-scripts|pnpm --filter @scholar-scout/http-data-service' README.md docs/docker-free-development.md docs/http-data-adapter-runbook.md docs/vercel-blob-data-adapter.md services/http-data-service/README.md</automated>
  </verify>
  <acceptance_criteria>
    - All five guides state the root-selected pnpm 10.34.5/Corepack prerequisite and use the frozen no-lifecycle install where installation is described.
    - Every local/web/service command maps to an existing root pnpm script or `@scholar-scout/http-data-service` filter command.
    - Adapter configuration, Docker-free steps, service URLs, token guidance, and HTTP contract content remain intact.
  </acceptance_criteria>
  <done>Contributor and local-service documentation identifies pnpm as the only supported manager and supplies frozen, executable equivalents for every former npm path.</done>
</task>

<task type="auto">
  <name>Task 2: Document Vercel's protected Corepack deployment contract</name>
  <files>docs/vercel-deployment.md, docs/vercel-permissions-handoff.md, docs/vercel-docker-workaround.md</files>
  <read_first>
    - `docs/vercel-deployment.md`: preserve deployment-specific setup, smoke, and troubleshooting content while aligning its commands to `vercel.json`.
    - `docs/vercel-permissions-handoff.md`: retain access ownership and handoff boundaries while adding the exact protected-main/ruleset evidence requirements.
    - `docs/vercel-docker-workaround.md`: preserve the documented Docker workaround boundary; change package-manager commands only.
    - `.github/workflows/ci.yml` and `vercel.json`: copy the finalized check names, installation command, and build command exactly.
  </read_first>
  <action>Update the Vercel guides per D-02, D-05, D-10, D-11, and D-14 to match `vercel.json`: repository root, Node 20, `ENABLE_EXPERIMENTAL_COREPACK=1` in the Production environment, `pnpm install --frozen-lockfile --ignore-scripts`, and `pnpm build:vercel`. Document that Vercel's Git integration deploys the protected `main` production branch only after required checks pass; provide the exact GitHub ruleset/branch-protection configuration using the six fixed ScholarScout check names, pull-request requirement, up-to-date branch, and direct-push restriction. Instruct operators to select checks only after successful runs have appeared, and keep no manual alternate deployment path.</action>
  <verify>
    <automated>rg -n 'ENABLE_EXPERIMENTAL_COREPACK|pnpm install --frozen-lockfile --ignore-scripts|pnpm build:vercel|ScholarScout / Web typecheck|protected.*main|ruleset' docs/vercel-deployment.md docs/vercel-permissions-handoff.md docs/vercel-docker-workaround.md</automated>
  </verify>
  <acceptance_criteria>
    - The three Vercel guides agree with committed `vercel.json` on repository root, Node 20, Corepack production variable, frozen install, and `build:vercel` command.
    - The guides name all six exact ScholarScout checks and require PRs, up-to-date branches, and direct-push restriction before Vercel may deploy `main` to production.
    - The documentation requires maintainers to retain ruleset evidence after the checks have successfully appeared and exposes no manual alternate production deployment path.
  </acceptance_criteria>
  <done>Vercel and GitHub setup documentation matches committed configuration and instructs maintainers how to protect main with exactly the new Scholar Scout checks.</done>
</task>

<task type="auto">
  <name>Task 3: Keep pull-request instructions synchronized after documentation migration</name>
  <files>.github/PULL_REQUEST_TEMPLATE.md</files>
  <read_first>
    - `.github/PULL_REQUEST_TEMPLATE.md`: start from the checklist produced by Plan 02 and preserve non-command safety prompts.
    - `package.json` and `.github/workflows/ci.yml`: compare the finalized root/filter commands and displayed check names before correcting spelling or labels.
  </read_first>
  <action>Review the pnpm checklist created in Plan 02 against the finalized root script and filter names. Correct only command spelling, check labels, and frozen-install prerequisites necessary for D-14 documentation consistency; preserve the safety checklist and do not alter product review criteria.</action>
  <verify>
    <automated>rg -n 'pnpm --filter @scholar-scout/web (typecheck|lint|test|build)|pnpm --filter @scholar-scout/http-data-service test|pnpm test:production-tooling' .github/PULL_REQUEST_TEMPLATE.md</automated>
  </verify>
  <acceptance_criteria>
    - Every template command exists in `package.json` or is the finalized `@scholar-scout/web`/HTTP-service filter command used by CI.
    - The six labels match `.github/workflows/ci.yml` exactly and the frozen-install prerequisite is present.
    - No safety prompt or product review criterion is removed or broadened.
  </acceptance_criteria>
  <done>The PR template lists only commands that exist in the pnpm root contract and maps them to CI's six named checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation to contributor environment | Operators copy commands that determine dependency resolution and release behavior. |
| Documentation to GitHub/Vercel settings | A maintainer configures merge and deployment controls outside the repository. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-09 | Tampering | contributor install instructions | medium | mitigate | Publish one pinned Corepack pnpm command and explicit frozen install in every active installation guide. |
| T-01-10 | Elevation of privilege | GitHub/Vercel setup instructions | high | mitigate | Document the exact required checks, PR/up-to-date/direct-push controls, production branch, and Corepack environment variable. |
| T-01-11 | Repudiation | operational handoff | low | accept | Documentation cannot enforce external settings, but it names the dashboard locations and verification evidence required at phase completion. |
| T-01-SC | Tampering | documented package-manager path | high | mitigate | Documentation points only to the reviewed Corepack pnpm pin and committed frozen lockfile. |
</threat_model>

<verification>
Search each active contributor/deployment guide for stale npm commands and validate every documented pnpm command against `package.json`. Execute the documented frozen root install and one filtered HTTP service command. At phase completion, retain evidence of the GitHub ruleset and Vercel settings described in the Plan 03 human check.
</verification>

<success_criteria>
- OPS-05 is documented consistently for development, service fixtures, Docker-free use, and Vercel deployment.
- Main protection instructions name the exact six independently reported Scholar Scout checks.
- No current contributor or Vercel guide asks maintainers to use npm after migration.
</success_criteria>

<output>
Create `.planning/phases/01-release-and-ci-baseline/01-04-SUMMARY.md` when done.
</output>
