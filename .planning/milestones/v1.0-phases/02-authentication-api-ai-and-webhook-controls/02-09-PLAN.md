---
phase: 02-authentication-api-ai-and-webhook-controls
plan: 09
type: tdd
wave: 1
depends_on: []
files_modified: [services/codex-webhook-runner/src/server.mjs, services/codex-webhook-runner/test/server.test.mjs, services/codex-webhook-runner/package.json, services/codex-webhook-runner/README.md]
autonomous: true
requirements: [SEC-04]
must_haves:
  truths:
    - Health remains observable while a missing/invalid signature secret rejects webhook delivery without side effects.
    - D-14: Only configured Scholar Scout repository issue events with an opened or labeled action and an approved codex or automation label may qualify for dispatch.
    - Valid configured Scholar Scout issue events produce only an authenticated, size-bounded, timed agent dispatch.
  artifacts: [services/codex-webhook-runner/test/server.test.mjs]
  key_links: ["raw 64KiB body -> secret/HMAC -> repo/event/label allowlist -> 16KiB packet -> bearer dispatch"]
---
<objective>
Harden the standalone GitHub webhook runner at its raw-input and outbound-dispatch boundaries.

Purpose: Implement D-13--D-16 with real HTTP regression tests.
Output: Testable runner factory, bounded HMAC verification, authenticated dispatch, and operational documentation.
</objective>
<execution_context>@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/02-authentication-api-ai-and-webhook-controls/02-RESEARCH.md
@services/codex-webhook-runner/src/server.mjs
@services/http-data-service/test/server.test.mjs</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Make the runner injectable and fail closed on raw webhook input</name>
  <files>services/codex-webhook-runner/src/server.mjs, services/codex-webhook-runner/test/server.test.mjs</files>
  <behavior>- GET health is 200 even with no secret; webhook POST is 503 and has no GitHub/agent call when secret is missing (D-13).
- Invalid signature also returns 503 without parsing or GitHub/agent side effects; valid signature accepts only configured repository issues opened/labeled with approved label (D-13, D-14).
- Streamed body over 64KiB is rejected before JSON parse (D-16).</behavior>
  <action>Write real HTTP tests first and refactor to export `createCodexWebhookRunner(config)` with injected fetch/config, keeping environment loading/listen only at the executable edge. Enforce raw byte cap while reading, require configured secret, validate signature prefix/length before constant-time comparison, and return safe 503 for both absent-secret and invalid-signature POSTs. Parse only after valid HMAC, then allow only configured Scholar Scout repository, issues event, opened/labeled action, and codex/automation labels. Tests must prove each 503 path performs no JSON parse, GitHub comment, or agent dispatch; rejected/ignored deliveries must not post comments or dispatch.</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/codex-webhook-runner exec node --test test/server.test.mjs</automated></verify>
  <done>Missing/invalid/oversize/nonqualifying webhook traffic cannot create external side effects.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Bound and authenticate outbound job dispatch</name>
  <files>services/codex-webhook-runner/src/server.mjs, services/codex-webhook-runner/test/server.test.mjs</files>
  <behavior>- Missing agent bearer configuration prevents dispatch (D-15).
- A sanitized UTF-8 job packet stays within 16KiB and dispatch uses Bearer authentication plus ten-second abort (D-15, D-16).
- GitHub comments retain their configured-token behavior and failed external calls are handled safely.</behavior>
  <action>Build the job packet from selected validated issue fields, enforce an encoded 16KiB maximum, require endpoint and `CODEX_AGENT_BEARER_TOKEN` before any dispatch, set Authorization Bearer and `AbortSignal.timeout(10_000)`, and map dispatch failures to safe response/logging without secrets. Test headers, timeout signal, missing token, packet size, and that nonqualifying input makes no calls (D-15, D-16).</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/codex-webhook-runner exec node --test test/server.test.mjs</automated></verify>
  <done>Only authenticated, bounded, qualifying webhook jobs can reach the agent endpoint.</done>
</task>
<task type="auto">
  <name>Task 3: Document the hardened service contract</name>
  <files>services/codex-webhook-runner/package.json, services/codex-webhook-runner/README.md</files>
  <action>Add the Node test script and document required secret, configured repository, agent bearer token, body/packet/timeout limits, health-versus-webhook behavior, and the supported test command. Do not document optional unauthenticated dispatch as supported behavior.</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/codex-webhook-runner exec node --test test/server.test.mjs</automated></verify>
  <done>The operational README and package script match the fail-closed runtime contract.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| GitHub to runner | Raw external webhook input is untrusted. |
| runner to GitHub/agent | Runner can cause external side effects. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-02-18 | Spoofing | webhook HMAC | high | mitigate | Required secret, raw-body HMAC, constant-time comparison, and 503 for absent or invalid signature. |
| T-02-19 | Denial of service | request/packet/dispatch | high | mitigate | 64KiB request cap, 16KiB packet cap, ten-second abort. |
| T-02-20 | Elevation of privilege | agent dispatch | high | mitigate | Repository/event/label allowlist and required bearer token. |
</threat_model>
<verification>Run the Node service test and the service package script; full runner behavior is covered with injected HTTP/fetch fakes.</verification>
<success_criteria>Webhook automation is observable for health but cannot authenticate, parse, or dispatch unsafe traffic.</success_criteria>
<output>Create `.planning/phases/02-authentication-api-ai-and-webhook-controls/02-09-SUMMARY.md` when done</output>
