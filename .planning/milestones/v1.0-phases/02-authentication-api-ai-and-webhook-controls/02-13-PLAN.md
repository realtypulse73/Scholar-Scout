---
phase: 02-authentication-api-ai-and-webhook-controls
plan: 13
type: tdd
wave: 5
depends_on: [02-06]
files_modified: [apps/web/app/api/decisions/route.ts, apps/web/app/admin/ops/page.tsx, apps/web/app/admin/feed/page.tsx, apps/web/__tests__/api/decision-boundary.test.ts]
autonomous: true
requirements: [SEC-01, SEC-02]
must_haves:
  truths:
    - A public request to /api/decisions cannot run the global decision engine, write decision logs, or receive engagement-derived decision data.
    - Only an actively allowlisted staff member can cause a server-rendered operations or feed dashboard to read global metrics or display decision logs.
    - Removed, unauthenticated, or malformed-config staff access reaches a safe denial path before any global-metric read or decision write.
  artifacts:
    - apps/web/app/api/decisions/route.ts
    - apps/web/__tests__/api/decision-boundary.test.ts
  key_links:
    - The disabled public route has no dependency on the platform decision engine.
    - Admin dashboard pages call Plan 06 active-staff authorization before getPlatformMetrics.
---

<objective>
Remove the identityless public decision mutation boundary and limit its remaining server-only metrics consumers to active staff.

Purpose: Close SEC-01's unplanned /api/decisions exposure without altering the decision engine or the existing authorized dashboard product path.
Output: Audited caller inventory in the execution summary, a disabled public endpoint, active-staff-first server dashboards, and focused regression tests.
</objective>

<execution_context>@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/02-authentication-api-ai-and-webhook-controls/02-CONTEXT.md
@.planning/phases/02-authentication-api-ai-and-webhook-controls/02-RESEARCH.md
@.planning/phases/02-authentication-api-ai-and-webhook-controls/02-06-SUMMARY.md
@apps/web/app/api/decisions/route.ts
@apps/web/lib/server/platform-store.ts
<interfaces>Plan 06 supplies `requireActiveStaff` with current allowlist checking and minimal allowed/denied audit behavior. `getPlatformMetrics` remains server-only and may invoke the global decision engine only after that guard allows an administrative dashboard request.</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Audit and disable the public decision-engine route</name>
  <files>apps/web/app/api/decisions/route.ts, apps/web/__tests__/api/decision-boundary.test.ts</files>
  <behavior>- The source/caller audit records every in-tree `/api/decisions`, `runAndStoreDecisions`, and `getPlatformMetrics` reference before modification; an unexpected public caller stops this contained change for plan revision.
- GET `/api/decisions` returns a stable 404-style safe response and never reads engagement data, runs the decision engine, or writes shared decision logs.
- The public response contains neither decision entries nor aggregate engagement-derived metrics.</behavior>
  <action>Before editing, rerun a repository-wide source/caller audit for the endpoint URL, the decision-engine function, and the server metrics function across `apps/web`; record the exact callers and the conclusion in `02-13-SUMMARY.md`. The currently audited contract has no in-tree browser caller, while the two admin pages consume only `getPlatformMetrics`; if a new public caller appears, stop and revise this plan rather than silently preserving global data exposure. Replace the route handler with a disabled, stable 404-style response that has no import or invocation path to platform-store. Preserve the server-only decision-engine and metrics functions for the guarded dashboards; do not expose a replacement public route, an aggregate response, or a write trigger. Write the route regression tests first, including an assertion that the disabled request cannot reach the platform mutation seam.</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test --runInBand --runTestsByPath __tests__/api/decision-boundary.test.ts</automated></verify>
  <done>Public decision requests receive only a safe disabled response and cannot expose engagement-derived data or mutate shared decision logs.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Guard server-rendered global decision dashboards</name>
  <files>apps/web/app/admin/ops/page.tsx, apps/web/app/admin/feed/page.tsx, apps/web/__tests__/api/decision-boundary.test.ts</files>
  <behavior>- An unauthenticated, removed, or malformed-config staff request reaches a safe denial path before `getPlatformMetrics` or its decision write path can run (D-05, D-06, D-08).
- An actively allowlisted staff request receives the unchanged operations/feed dashboard after current authorization and records only the minimal privileged-operation audit metadata (D-05, D-07).
- No denied server-rendered response includes metric totals or decision-log entries.</behavior>
  <action>Write the denial-before-metrics tests first. In each admin page, invoke Plan 06's `requireActiveStaff` with a distinct action and route label before calling `getPlatformMetrics`; use its safe denial branch to return the established non-sensitive authorization outcome without rendering metric values or decision logs. Keep the existing dashboard presentation and server-only metric calculation unchanged for an actively allowlisted staff member. The tests must mock allowed, removed, unauthenticated, and malformed-allowlist outcomes; prove that denied cases never invoke the metrics/decision mutation seam and allowed cases do so only after authorization. Do not authorize from a JWT role, turn either dashboard into a public aggregate, or add a new analytics API (D-05--D-08).</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test --runInBand --runTestsByPath __tests__/api/decision-boundary.test.ts</automated></verify>
  <done>Global decision metrics remain available only to freshly authorized staff, with safe denials that neither read/write global data nor render it.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| public request to decision route | An unauthenticated request previously triggered global engagement aggregation and a shared write. |
| staff session to server dashboards | A stale or malformed privilege state must not reveal or mutate global operational metrics. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-29 | Information disclosure / Tampering | public decision route | high | mitigate | Disable the public handler, remove its platform-store path, and test the no-mutation response. |
| T-02-30 | Elevation of privilege / Information disclosure | admin decision dashboards | high | mitigate | Plan-06 active-staff guard and minimal audit precede every global-metrics read or write; test denied and allowed paths. |
</threat_model>

<verification>Run the focused decision-boundary test after each task, then `corepack pnpm --filter @scholar-scout/web run lint` and `corepack pnpm --filter @scholar-scout/web run typecheck`.</verification>
<success_criteria>No public code path can obtain or mutate global decision data, and the authorized operational dashboards retain their behavior only behind fresh staff authorization.</success_criteria>
<output>Create `.planning/phases/02-authentication-api-ai-and-webhook-controls/02-13-SUMMARY.md` when done</output>
