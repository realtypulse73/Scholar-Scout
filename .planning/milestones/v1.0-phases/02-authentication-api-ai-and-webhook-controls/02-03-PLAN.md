---
phase: 02-authentication-api-ai-and-webhook-controls
plan: 03
type: tdd
wave: 3
depends_on: [02-02]
files_modified: [apps/web/lib/server/data-store.ts, apps/web/lib/server/platform-store.ts, apps/web/lib/server/student-actor.ts, apps/web/app/api/account/guest-migration/route.ts, apps/web/__tests__/lib/data-store.test.ts, apps/web/__tests__/api/auth-controls.test.ts]
autonomous: true
requirements: [SEC-01, SEC-03]
must_haves:
  truths:
    - A guest has a unique opaque seven-day server-recognized identity and cannot act as staff.
    - D-02: The seven-day guest trial cannot grant staff capability or access to another student's private data.
    - Same-device sign-in transfers only eligible guest-owned records once, clears the credential, and preserves the active guest quota binding.
  artifacts:
    - apps/web/lib/server/student-actor.ts
    - apps/web/app/api/account/guest-migration/route.ts
  key_links:
    - Every user-owned route can receive an account or guest storage key only from the server actor resolver.
---
<objective>
Create the opaque guest/account actor boundary and idempotent guest migration contract.

Purpose: Implement D-01--D-04 and the D-12 guest-window carry-over without trusting browser identity.
Output: Server-only actor resolution, lifecycle persistence, and route/store tests.
</objective>
<execution_context>@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/02-authentication-api-ai-and-webhook-controls/02-CONTEXT.md
@apps/web/lib/server/data-store.ts
@apps/web/lib/server/platform-store.ts
<interfaces>`getServerSession(authOptions)` is the current account boundary. `PlatformData` owns memory, simulations, referrals, shares, analytics, and feed records keyed by strings.</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Persist guest lifecycle and allowlisted record migration</name>
  <files>apps/web/lib/server/data-store.ts, apps/web/lib/server/platform-store.ts, apps/web/__tests__/lib/data-store.test.ts</files>
  <behavior>- A generated guest secret is stored only as a hash/HMAC lookup with seven-day expiry (D-01, D-02).
- A migration transfers only documented actor-owned records once and marks the guest migrated (D-03).
- Migration retains the active guest quota binding through reset and does not modify Phase-5 community visibility (D-03, D-04, D-12).</behavior>
  <action>Write store tests first. Extend normalized persisted data with guest lifecycle metadata, a migration marker, quota-binding metadata, and a minimal migration audit record; keep raw guest secrets out of storage. Add explicit idempotent transfer helpers for only `onboardingProfiles`, `shortlists`, `shortlistPlans`, `feedInteractions`, `simulationResults`, `memoryRecords`, `referralRecords`, `shareRecords`, and `analyticsEvents` owned by the guest actor. Explicitly exclude `users`, `programmeRecords`, `outcomeMetricRecords`, `auditEvents`, `restoreBackups`, `decisionLogs`, `peerConnectionRequests`, `campusNotes`, and `uploaderInboxRequests`; tests must prove each allowlisted record transfers once while the excluded Phase-5 community collections remain unchanged. Do not copy whole unrelated collections, expose guest records, or alter community publishing/moderation semantics reserved for Phase 5.</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test --runInBand --runTestsByPath __tests__/lib/data-store.test.ts</automated></verify>
  <done>Only the enumerated private activity collections migrate once to the authenticated account; excluded community and operational collections remain unchanged.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Resolve only server-derived account or guest actors</name>
  <files>apps/web/lib/server/student-actor.ts, apps/web/__tests__/api/auth-controls.test.ts</files>
  <behavior>- An account actor uses only the current session ID.
- A guest actor is issued from a random HttpOnly, Secure, SameSite=Lax, Path=/, seven-day cookie and server lookup (D-01, D-02).
- No actor result grants staff capability or accepts a browser-provided identifier (D-01, D-02).</behavior>
  <action>Write failing actor-contract tests, then implement a server-only resolver/issuer with a discriminated account-or-guest actor and namespaced storage key. Read the signed-in session first; otherwise validate only the trusted cookie against lifecycle state and issue a fresh opaque credential when the route policy permits a guest. Provide cookie-clear support for migration. Keep staff authorization outside this helper.</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test --runInBand --runTestsByPath __tests__/api/auth-controls.test.ts</automated></verify>
  <done>Routes can obtain a private actor identity without any client-selected key or shared guest fallback.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Expose session-bound guest migration</name>
  <files>apps/web/app/api/account/guest-migration/route.ts, apps/web/__tests__/api/auth-controls.test.ts</files>
  <behavior>- Unauthenticated migration is rejected.
- A signed-in request migrates the cookie-resolved guest once, clears it, and returns stable success on an idempotent retry (D-03).
- JSON cannot nominate a guest/account identity or bypass the active guest quota binding (D-03, D-12).</behavior>
  <action>Add a POST-only route that derives both the current account session and guest credential from server state, invokes the allowlisted migration, persists the guest quota carry-over, and emits a safe result before clearing the cookie. Accept no request body identity. Include regression tests for unauthenticated, absent/migrated guest, success, and retry paths.</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test --runInBand --runTestsByPath __tests__/api/auth-controls.test.ts</automated></verify>
  <done>Same-device migration is authenticated, idempotent, credential-invalidating, and cannot be steered by the browser.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser cookie/session to actor resolver | Credentials determine private storage ownership. |
| guest to account migration | State crosses identity namespaces after authentication. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-04 | Spoofing / Information disclosure | student actor | high | mitigate | Opaque cookie lookup, server-only storage key, expiry, and no JSON identity. |
| T-02-05 | Tampering | guest migration | high | mitigate | Session-bound allowlist transfer, idempotency marker, cookie invalidation, and tests. |
| T-02-06 | Elevation of privilege | guest actor | medium | mitigate | Actor has no staff capability; active staff is a separate guard. |
</threat_model>
<verification>Run focused auth/store tests, then web typecheck.</verification>
<success_criteria>A seven-day guest trial is private and migrates safely without giving guests staff access or bypassing the day’s guest advisor allowance.</success_criteria>
<output>Create `.planning/phases/02-authentication-api-ai-and-webhook-controls/02-03-SUMMARY.md` when done</output>
