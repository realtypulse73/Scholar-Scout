---
phase: 02-authentication-api-ai-and-webhook-controls
plan: 05
type: tdd
wave: 4
depends_on: [02-03]
files_modified: [apps/web/app/api/referrals/route.ts, apps/web/app/api/feed-events/route.ts, apps/web/app/api/share/route.ts, apps/web/app/api/ab-testing/assign/route.ts, apps/web/__tests__/api/engagement-routes.test.ts]
autonomous: true
requirements: [SEC-01]
must_haves:
  truths:
    - Referrals, feed events, shares, and A/B assignment are attributed to only the resolved actor.
    - Public callers cannot enumerate all referral records.
  artifacts: [apps/web/__tests__/api/engagement-routes.test.ts]
  key_links: ["Actor storage key is supplied to referral/platform functions after exact input validation."]
---
<objective>
Migrate the remaining user-keyed engagement routes to server-derived ownership and bounded input.

Purpose: Close SEC-01 across the referral, engagement, share, and assignment routes without turning public discovery into an auth wall.
Output: Actor-scoped route contracts and regression tests.
</objective>
<execution_context>@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/02-authentication-api-ai-and-webhook-controls/02-PATTERNS.md
@apps/web/lib/server/student-actor.ts
@apps/web/lib/api-request.ts</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Scope referrals and feed engagement to the actor</name>
  <files>apps/web/app/api/referrals/route.ts, apps/web/app/api/feed-events/route.ts, apps/web/__tests__/api/engagement-routes.test.ts</files>
  <behavior>- Referral GET returns only actor-owned records and POST uses the actor as referrer (D-01).
- Feed events use actor ownership and reject invalid IDs, event types, numeric values, and excess metadata.</behavior>
  <action>Write route tests first. Resolve the actor before reads/writes, remove browser-selected referral ownership and the broad referral-list response, and validate bounded feed-event fields before platform calls. Preserve discovery reads elsewhere; do not add community publishing, moderation, or author representations reserved for Phase 5 (D-04).</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test --runInBand --runTestsByPath __tests__/api/engagement-routes.test.ts</automated></verify>
  <done>Referral and feed records cannot be created, listed, or attributed across student identities.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Scope shares and experiment assignment to the actor</name>
  <files>apps/web/app/api/share/route.ts, apps/web/app/api/ab-testing/assign/route.ts, apps/web/__tests__/api/engagement-routes.test.ts</files>
  <behavior>- A share uses only the actor key with finite target type/ID validation.
- Assignment cannot be steered by a supplied identity and accepts only supported bounded experiment input (D-01).</behavior>
  <action>Replace request ownership fields with the resolved actor key, validate target and experiment values against server-owned finite/bounded rules, then preserve the existing analytics/assignment behavior with actor ownership. Add cross-actor and malformed-input tests. Do not use a shared guest identity or expand Phase-5 community release scope (D-01, D-04).</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test --runInBand --runTestsByPath __tests__/api/engagement-routes.test.ts</automated></verify>
  <done>Shares and experiment analytics cannot be forged for another student.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser to engagement routes | Request fields can attempt identity and event-data tampering. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-02-09 | Spoofing / Tampering | referrals and engagement | high | mitigate | Server actor plus exact bounded schemas and cross-actor tests. |
| T-02-10 | Information disclosure | referrals GET | high | mitigate | Actor-scoped collection response only. |
</threat_model>
<verification>Run `corepack pnpm --filter @scholar-scout/web test --runInBand --runTestsByPath __tests__/api/engagement-routes.test.ts`.</verification>
<success_criteria>Every remaining user-keyed engagement route receives ownership from server state, not the browser.</success_criteria>
<output>Create `.planning/phases/02-authentication-api-ai-and-webhook-controls/02-05-SUMMARY.md` when done</output>
