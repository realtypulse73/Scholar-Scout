---
phase: 02-authentication-api-ai-and-webhook-controls
plan: 12
type: tdd
wave: 4
depends_on: [02-03]
files_modified: [apps/web/app/api/account/onboarding/route.ts, apps/web/app/api/account/shortlist/route.ts, apps/web/__tests__/api/account-guest-routes.test.ts]
autonomous: true
requirements: [SEC-01]
must_haves:
  truths:
    - A distinct opaque guest can save and later retrieve only that guest's onboarding profile, shortlist, and shortlist plans during the seven-day trial.
    - Account and guest requests cannot select another actor's onboarding or shortlist storage key, and malformed or excessive payloads make no persistence change.
    - After same-device sign-in, the account can retrieve the eligible guest onboarding and shortlist activity that Plan 03 transferred, while the guest credential no longer grants access.
  artifacts:
    - apps/web/app/api/account/onboarding/route.ts
    - apps/web/app/api/account/shortlist/route.ts
    - apps/web/__tests__/api/account-guest-routes.test.ts
  key_links:
    - Account onboarding and shortlist routes resolve `actor.storageKey` through Plan 03 before every data-store read/write.
    - Plan 03's allowlisted onboarding/shortlist migration makes same-device guest activity visible through the account actor only.
---

<objective>
Make the existing onboarding and shortlist account APIs usable by the opaque guest trial and its same-device migration.

Purpose: D-01--D-03 require a broad, private guest trial whose eligible onboarding and shortlist activity has a real actor-scoped route path before Plan 03 transfers it; the current routes accept sessions only.
Output: Tested actor-scoped, byte-bounded onboarding and shortlist route contracts.
</objective>

<execution_context>@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/02-authentication-api-ai-and-webhook-controls/02-CONTEXT.md
@.planning/phases/02-authentication-api-ai-and-webhook-controls/02-03-SUMMARY.md
@apps/web/app/api/account/onboarding/route.ts
@apps/web/app/api/account/shortlist/route.ts
@apps/web/lib/server/student-actor.ts
@apps/web/lib/api-request.ts
<interfaces>`resolveStudentActor` yields only a server-derived account or guest `storageKey`; the data-store onboarding, shortlist, and shortlist-plan helpers accept that key. Plan 02 provides byte-bounded exact JSON parsing, and Plan 03 owns guest lifecycle, migration, and cookie invalidation.</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Route onboarding profiles through the resolved student actor</name>
  <files>apps/web/app/api/account/onboarding/route.ts, apps/web/__tests__/api/account-guest-routes.test.ts</files>
  <behavior>- A permitted guest receives an opaque actor and can GET/POST only its own onboarding profile during the seven-day trial (D-01, D-02).
- Supplying an account ID, guest ID, or other ownership field cannot read or write another actor's profile, and two guests remain isolated (D-01).
- POST rejects oversized, malformed, unknown-field, or out-of-domain onboarding data before `saveOnboardingProfile`; after migration, the signed-in account reads the transferred profile while the invalidated guest cannot (D-03).</behavior>
  <action>Write route-contract tests first. Replace the session-only GET/POST gate with Plan 03's server-only `resolveStudentActor` policy that may issue/recognize a guest for this student feature, then pass only `actor.storageKey` to onboarding store helpers. For POST, use Plan 02's byte-bounded exact parser and a route validator that accepts the complete established onboarding shape with finite enum values and bounded strings/arrays; validate the completed profile with the existing onboarding rules before writing. Return explicit 400/401/503-style expected failures without a write. Test two opaque guests, an account, browser identity substitution, malformed/oversized input, and the Plan-03 migration followed by cookie invalidation. Do not reintroduce a shared guest key, expose a guest credential in JSON, or add community access (D-01, D-04).</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test --runInBand --runTestsByPath __tests__/api/account-guest-routes.test.ts</automated></verify>
  <done>Onboarding API ownership comes exclusively from the resolved account/guest actor, with bounded validated writes and a verifiable guest-to-account migration path.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Route shortlists and decision plans through the resolved student actor</name>
  <files>apps/web/app/api/account/shortlist/route.ts, apps/web/__tests__/api/account-guest-routes.test.ts</files>
  <behavior>- A permitted guest can GET/POST only its own shortlist IDs and plans; two guests and an account have isolated collections (D-01, D-02).
- POST accepts only bounded programme IDs and finite decision-plan statuses/notes associated with the submitted shortlist; no body field selects a storage owner (D-01).
- After Plan-03 same-device migration, the account GET returns the guest's eligible shortlist IDs and plans once, while the invalidated guest cannot retrieve them (D-03).</behavior>
  <action>Extend the route-contract tests first, then replace the session-only shortlist GET/POST gate with the same permitted guest/account actor resolution and pass only `actor.storageKey` to shortlist helpers. Use Plan 02's byte-bounded exact parser for the existing `programmeIds`/`plans` object; reject unknown fields, non-string or excessive IDs, an excessive collection, unknown plan statuses, oversized notes, and plan entries outside the submitted shortlist before either write. Preserve the current response shape and atomically avoid partial writes on validation failure. Test guest/account isolation, attempted caller identity substitution, malformed and excessive bodies, and Plan-03 transfer of both shortlist collections. Do not store guest identity in the browser body or expand Phase-5 relationship/community behavior (D-01, D-03, D-04).</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test --runInBand --runTestsByPath __tests__/api/account-guest-routes.test.ts</automated></verify>
  <done>Shortlist and shortlist-plan state is actor-scoped, bounded, and available to the migrated account only through the existing safe transfer contract.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser to account onboarding/shortlist routes | Browser payloads and cookies must not choose private storage ownership or cause unbounded writes. |
| guest to signed-in account | Plan 03 transfers eligible private records across actor namespaces after authentication. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-27 | Spoofing / Information disclosure | account onboarding route | high | mitigate | Server actor resolution, exact bounded profile validation, cross-guest/account isolation tests, and post-migration cookie-invalidity coverage. |
| T-02-28 | Spoofing / Tampering | account shortlist route | high | mitigate | Server actor resolution, bounded exact shortlist/plan schema, all-or-nothing validation, and migration visibility tests. |
</threat_model>

<verification>Run the focused account guest-route suite after each task, then web typecheck and the full web Jest suite after the wave merges.</verification>
<success_criteria>Guests have a seven-day opaque, private onboarding/shortlist trial that supplies real eligible activity to the signed-in migration without creating a browser-selected or shared identity path.</success_criteria>
<output>Create `.planning/phases/02-authentication-api-ai-and-webhook-controls/02-12-SUMMARY.md` when done</output>
