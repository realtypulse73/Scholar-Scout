---
phase: 02-authentication-api-ai-and-webhook-controls
plan: 10
type: tdd
wave: 5
depends_on: [02-02, 02-03, 02-06]
files_modified: [apps/web/lib/server/data-store.ts, apps/web/auth.ts, apps/web/app/api/auth/credentials/route.ts, apps/web/components/auth/AuthSessionProvider.tsx, apps/web/__tests__/api/auth-controls.test.ts, apps/web/__tests__/lib/data-store.test.ts, apps/web/__tests__/components/auth/AuthSessionProvider.test.tsx]
autonomous: true
requirements: [SEC-05, SEC-01]
must_haves:
  truths:
    - The credentials exchange limit rejects the sixth request in its locked window with HTTP 429 and reset metadata before account lookup/KDF work.
    - D-19: Password hash/verification uses non-blocking asynchronous work and does not synchronously occupy the Node request event loop.
    - The NextAuth Credentials provider consumes only a short-lived server grant, while OAuth-returned sessions trigger same-device guest migration safely.
  artifacts: [apps/web/__tests__/api/auth-controls.test.ts]
  key_links: ["rate-limit-aware credentials boundary -> atomic limiter -> async scrypt -> one-use NextAuth grant; authenticated OAuth session -> guest migration endpoint"]
---
<objective>
Apply the supported HTTP credential-rate boundary and asynchronous credential work, then trigger guest migration for OAuth-returned sessions.

Purpose: Implement D-03 and D-17--D-20 while retaining local credentials and the current NextAuth foundation.
Output: Rate-bounded credentials exchange, async scrypt, one-use NextAuth grant, and OAuth migration-trigger tests.
</objective>
<execution_context>@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/02-authentication-api-ai-and-webhook-controls/02-02-SUMMARY.md
@.planning/phases/02-authentication-api-ai-and-webhook-controls/02-03-SUMMARY.md
@apps/web/auth.ts
@apps/web/components/auth/AuthForm.tsx
<interfaces>`verifyUserCredentials(email, password)` currently returns a user/null and uses `scryptSync`; Plan 02 supplies typed reservation outcomes plus the Vercel trusted-IP helper, and Plan 03 supplies POST `/api/account/guest-migration`.</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add the rate-limit-aware credentials exchange and one-use NextAuth grant</name>
  <files>apps/web/lib/server/data-store.ts, apps/web/auth.ts, apps/web/app/api/auth/credentials/route.ts, apps/web/__tests__/lib/data-store.test.ts, apps/web/__tests__/api/auth-controls.test.ts</files>
  <behavior>- Five attempts per normalized email/trusted-Vercel-IP pair in 15 minutes are allowed; the sixth POST to `/api/auth/credentials` returns HTTP 429 with `Retry-After` and reset metadata before account lookup or async KDF (D-17).
- The credentials boundary returns 503 for an unavailable limiter or missing/malformed trusted IP; an injected/spoofed `x-forwarded-for` cannot change the limiter key (D-17).
- Password creation/verification awaits async Node scrypt while retaining salt/hash and timing-safe comparison (D-19).
- Unknown-account and incorrect-password outcomes are two fixed allowlisted 401 codes from the credentials boundary; the NextAuth Credentials callback accepts only an opaque, single-use, short-lived server grant and never reflects raw errors (D-18).
- Direct credential callback requests without a valid grant cannot reach lookup/KDF, and rolling expiry restores access without account lockout (D-17, D-20).</behavior>
  <action>Write route and store tests first. Replace sync crypto calls with promisified async `scrypt` while preserving the existing stored salt/hash format and timing-safe comparison. Add POST `/api/auth/credentials` as the supported sign-in HTTP boundary: exact-parse email/password, derive the address only through Plan 02's Vercel `x-vercel-forwarded-for` contract, atomically reserve the email/IP window before lookup/KDF, and deliver 429 plus `Retry-After`/reset metadata or 503 before expensive work. On a verified credential, issue an opaque, one-use, short-lived server grant bound to the normalized identity and trusted address; modify `auth.ts` so NextAuth Credentials accepts and atomically consumes only that grant. Return only the two fixed D-18 failure codes from the custom boundary, and deny direct email/password callback attempts without running lookup/KDF. Never reflect raw exceptions, accept a browser-selected IP, or permanently lock an account (D-17--D-20).</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test --runInBand --runTestsByPath __tests__/api/auth-controls.test.ts __tests__/lib/data-store.test.ts</automated></verify>
  <done>The actual credentials HTTP boundary returns reset-aware 429/503 before lookup or KDF, and NextAuth creates a session only from its consumed one-use server grant.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Trigger guest migration once for an OAuth-returned session</name>
  <files>apps/web/components/auth/AuthSessionProvider.tsx, apps/web/__tests__/components/auth/AuthSessionProvider.test.tsx</files>
  <behavior>- An OAuth-returned authenticated session triggers the session-bound migration endpoint once before profile navigation-dependent UI uses the account (D-03).
- A no-guest/idempotent migration is safe success; a genuine failure is surfaced recoverably without reading guest credential material or choosing staff authority (D-03).</behavior>
  <action>Add a small authenticated-session migration trigger beneath the existing SessionProvider so it handles an OAuth callback session once. Treat a no-guest/idempotent migration as safe success and retain a user-visible recoverable error for a genuine migration failure. Test the one-time request and error state with mocked session/fetch; do not expose guest cookies, add a browser-owned role decision, or add a Phase-5 community flow (D-03, D-04).</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test --runInBand --runTestsByPath __tests__/components/auth/AuthSessionProvider.test.tsx</automated></verify>
  <done>OAuth session availability triggers only the server-side migration action once and handles its safe outcomes without client authority control.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser/proxy to credential endpoints | Burst requests and claimed IPs can consume KDF capacity. |
| NextAuth result to UI | Error detail must not expose raw server exception content. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-02-21 | Denial of service | credentials exchange | high | mitigate | Atomic pre-KDF reservation, HTTP 429/reset boundary, and async scrypt. |
| T-02-22 | Information disclosure | credential errors | medium | accept | D-18 explicitly accepts account enumeration; throttle and allowlist public messages. |
| T-02-23 | Tampering | migration trigger | medium | mitigate | Server-only guest lookup, authenticated route, idempotency tests, no client guest ID. |
</threat_model>
<verification>Run focused auth/store tests, full web Jest suite, lint, and typecheck under Node 20/Corepack pnpm.</verification>
<success_criteria>Repeated auth traffic gets reset-aware safe limits without synchronous KDF blocking, and successful sign-in migrates eligible same-device guest activity without bypassing server controls.</success_criteria>
<output>Create `.planning/phases/02-authentication-api-ai-and-webhook-controls/02-10-SUMMARY.md` when done</output>
