---
phase: 02-authentication-api-ai-and-webhook-controls
plan: 07
type: tdd
wave: 5
depends_on: [02-06]
files_modified: [apps/web/app/api/admin/data/backups/route.ts, apps/web/app/api/admin/data/import/validate/route.ts, apps/web/app/api/admin/data/import/restore/route.ts, apps/web/app/api/admin/data/status/route.ts, 'apps/web/app/api/admin/data/backups/[id]/plan/route.ts', 'apps/web/app/api/admin/data/backups/[id]/restore/route.ts', apps/web/__tests__/api/admin-data-routes.test.ts]
autonomous: true
requirements: [SEC-02]
must_haves:
  truths:
    - Every staff-only data-operation route performs a fresh active-staff check and minimal audit.
  artifacts: [apps/web/__tests__/api/admin-data-routes.test.ts]
  key_links: ["All staff data routes use `requireActiveStaff`; health-token monitoring remains separate."]
---
<objective>
Apply the fresh staff guard to the existing administrative data-operation routes.

Purpose: Complete D-05--D-08 for privileged server routes without changing the later Phase-3 data-operation product scope.
Output: Migrated data-operation guards and route regression coverage.
</objective>
<execution_context>@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/02-authentication-api-ai-and-webhook-controls/02-06-SUMMARY.md
@apps/web/app/api/admin/data/health/route.ts</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Guard backup listing and import validation/restore routes</name>
  <files>apps/web/app/api/admin/data/backups/route.ts, apps/web/app/api/admin/data/import/validate/route.ts, apps/web/app/api/admin/data/import/restore/route.ts, apps/web/__tests__/api/admin-data-routes.test.ts</files>
  <behavior>- Current allowlist removal causes 403 for backup and import operations (D-05, D-06).
- Allowed/denied attempts emit minimal audit records; validation and restore contracts remain intact (D-07).</behavior>
  <action>Replace stale role checks with the Plan-06 guard before request parsing or data access. Use its actor ID for restore attribution, preserve established validation/confirmation/error responses, and extend admin data tests with session emails plus allowed, removed, missing/malformed configuration, and audit assertions. Do not add new restore/product features assigned to Phase 3.</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test --runInBand --runTestsByPath __tests__/api/admin-data-routes.test.ts</automated></verify>
  <done>Backup/import data operations are live-authorized and audited without contract regressions.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Guard backup planning, backup restore, and data status</name>
  <files>apps/web/app/api/admin/data/status/route.ts, apps/web/app/api/admin/data/backups/[id]/plan/route.ts, apps/web/app/api/admin/data/backups/[id]/restore/route.ts, apps/web/__tests__/api/admin-data-routes.test.ts</files>
  <behavior>- Removed/misconfigured staff cannot plan, restore, or inspect privileged data status (D-05, D-06, D-08).
- Each privilege decision is auditable without sensitive input capture (D-07).</behavior>
  <action>Apply the same early guard to these route handlers and update tests using literal-path-safe imports. Preserve the separate bearer-token `/health` monitoring endpoint as a non-staff integration boundary; do not turn it into a staff route or weaken its configured-token behavior. Add audit and safe-denial assertions for every protected endpoint.</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test --runInBand --runTestsByPath __tests__/api/admin-data-routes.test.ts</automated></verify>
  <done>All privileged data-operation endpoints perform current authorization, while service-token health monitoring remains independently protected.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| staff session to data-operation routes | Data backup/restore operations are high-impact privileged actions. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-02-13 | Elevation of privilege | admin data routes | high | mitigate | Reuse fresh allowlist guard before all data operations and regression-test revocation. |
| T-02-14 | Repudiation | data operations | medium | mitigate | Minimal allowed/denied audit records. |
</threat_model>
<verification>Run focused admin-data routes test then the full web Jest suite.</verification>
<success_criteria>All staff-only data operations are server-revocable and minimally auditable without prematurely expanding Phase 3 functionality.</success_criteria>
<output>Create `.planning/phases/02-authentication-api-ai-and-webhook-controls/02-07-SUMMARY.md` when done</output>
