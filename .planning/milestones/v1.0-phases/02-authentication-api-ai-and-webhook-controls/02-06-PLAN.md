---
phase: 02-authentication-api-ai-and-webhook-controls
plan: 06
type: tdd
wave: 4
depends_on: [02-03]
files_modified: [apps/web/lib/server/data-store.ts, apps/web/lib/server/active-staff.ts, apps/web/app/api/admin/programmes/route.ts, apps/web/__tests__/api/active-staff.test.ts]
autonomous: true
requirements: [SEC-02]
must_haves:
  truths:
    - Privileged access depends on the current strict staff allowlist, not a JWT role claim.
    - D-06: A removed staff member receives an immediate 403 from privileged routes while retaining normal student access.
    - Every permitted or denied privileged programme operation records only minimal audit metadata.
  artifacts: [apps/web/lib/server/active-staff.ts]
  key_links: ["Admin route calls the active-staff guard before parse/store work."]
---
<objective>
Establish fresh, fail-closed staff authorization and audit it on the programme administration boundary.

Purpose: Implement D-05 through D-08 and create the reusable guard for all privileged routes.
Output: Active-staff service, minimal audit persistence, migrated programme route, and tests.
</objective>
<execution_context>@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/02-authentication-api-ai-and-webhook-controls/02-CONTEXT.md
@apps/web/app/api/admin/programmes/route.ts
@apps/web/lib/server/data-store.ts</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Create active-staff authorization and minimal audit primitives</name>
  <files>apps/web/lib/server/data-store.ts, apps/web/lib/server/active-staff.ts, apps/web/__tests__/api/active-staff.test.ts</files>
  <behavior>- A session with an old staff JWT is denied after its normalized email is removed (D-05, D-06).
- Missing, blank, duplicate-invalid, or malformed allowlist configuration denies safely (D-08).
- Allowed and denied results persist actor pseudonym/identifier, action, route/outcome, and timestamp only (D-07).</behavior>
  <action>Write tests first. Add a server-only `requireActiveStaff` helper that gets the current session, requires ID/email, strictly parses the current comma-separated allowlist each request, normalizes email, and returns a safe 403 result on any invalid configuration or non-match. Add a dedicated minimal privileged-operation audit shape/append helper, normalized on read, excluding bodies, emails, cookies, tokens, secrets, and programme content. Do not treat `session.user.role` or stored account role as authority (D-05, D-07, D-08).</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test --runInBand --runTestsByPath __tests__/api/active-staff.test.ts</automated></verify>
  <done>Active authorization is revocable on the next request, fail closed, and produces privacy-minimal allowed/denied audit evidence.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Apply active authorization to programme administration</name>
  <files>apps/web/app/api/admin/programmes/route.ts, apps/web/__tests__/api/active-staff.test.ts</files>
  <behavior>- GET, POST, and DELETE return 403 to removed staff while ordinary student routes remain available (D-06).
- Allowed and denied attempts are audited without changing valid 400/409 contracts (D-07).</behavior>
  <action>Replace each programme route JWT-role branch with one early `requireActiveStaff` call before parsing or store work; use the returned current actor ID for programme persistence. Preserve draft validation, conflict handling, and unexpected-error propagation. Add allowed, removed, missing-config, and denied-audit regression cases, including proof the same session can still use non-privileged student access (D-05--D-08).</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test --runInBand --runTestsByPath __tests__/api/active-staff.test.ts</automated></verify>
  <done>Programme administration performs live authorization and records safe audit metadata for every privileged outcome.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| JWT session to admin route | A stale role claim must not grant privileged operations. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-02-11 | Elevation of privilege | admin programmes | high | mitigate | Current allowlist check per request, strict configuration parsing, 403 denial. |
| T-02-12 | Repudiation / Information disclosure | privileged audit | medium | mitigate | Minimal outcome audit with explicit forbidden sensitive fields. |
</threat_model>
<verification>Run focused active-staff tests, lint, and typecheck.</verification>
<success_criteria>Revoking an allowlisted address takes effect immediately at programme administration routes without blocking ordinary student access.</success_criteria>
<output>Create `.planning/phases/02-authentication-api-ai-and-webhook-controls/02-06-SUMMARY.md` when done</output>
