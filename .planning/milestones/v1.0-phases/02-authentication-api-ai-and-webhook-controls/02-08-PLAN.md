---
phase: 02-authentication-api-ai-and-webhook-controls
plan: 08
type: tdd
wave: 4
depends_on: [02-02, 02-03]
files_modified: [apps/web/lib/advisor-contract.ts, apps/web/lib/server/advisor-context.ts, apps/web/app/api/advisor-chat/route.ts, apps/web/components/advisor/AdvisorChat.tsx, apps/web/__tests__/lib/advisor-guardrails.test.ts, apps/web/__tests__/fixtures/advisor-eval-cases.ts, apps/web/__tests__/api/advisor-chat.test.ts, apps/web/__tests__/components/advisor/AdvisorChat.test.tsx]
autonomous: true
requirements: [SEC-03]
must_haves:
  truths:
    - D-10: Advisor requests accept at most 3,000 characters for the resolved actor; only the server selects additional profile, memory, or recommendation context before provider work.
    - D-11: Replies use direct non-streaming Responses API controls, strict JSON validation, a 1,000-token cap, and safe fallbacks.
    - The checked-in synthetic evaluation fixture covers all locked privacy, cost, and student-safety cases.
    - The browser sends only the student message and renders the bounded route's safe success, validation, quota, unavailable, fallback, and crisis outcomes.
  artifacts: [apps/web/lib/advisor-contract.ts, apps/web/lib/server/advisor-context.ts, apps/web/__tests__/fixtures/advisor-eval-cases.ts]
  key_links: ["AdvisorChat -> exact { message } request -> advisor route -> actor -> atomic reservation -> actor-scoped context -> bounded Responses fetch"]
---
<objective>
Harden the direct OpenAI Responses advisor without adding an agent framework.

Purpose: Implement D-09--D-12 and the locked AI-SPEC online guardrails, evaluation cases, and direct-fetch contract.
Output: Bounded advisor route, server-owned context, unit/route/evaluation fixtures.
</objective>
<execution_context>@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/02-authentication-api-ai-and-webhook-controls/02-AI-SPEC.md
@.planning/phases/02-authentication-api-ai-and-webhook-controls/02-RESEARCH.md
@apps/web/app/api/advisor-chat/route.ts
<interfaces>Use the Plan-02 reservation result and Plan-03 Actor. The only browser schema is exact `{ message: string }`; OpenAI is called by native `fetch` only.</interfaces>
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define advisor contracts, context selection, and synthetic evaluation data</name>
  <files>apps/web/lib/advisor-contract.ts, apps/web/lib/server/advisor-context.ts, apps/web/__tests__/lib/advisor-guardrails.test.ts, apps/web/__tests__/fixtures/advisor-eval-cases.ts</files>
  <behavior>- Request accepts one trimmed message of 1--3000 characters and no browser context/history/identity (D-10).
- Reply JSON is exactly a nonempty message of at most 8000 characters.
- Server context is actor-keyed, bounded, and selects at most three recommendations.
- An acute-crisis classifier selects the deterministic qualified-support handoff before any actor-context or OpenAI access.
- Fourteen synthetic cases cover privacy resistance, 10/11 and 25/26 quota edges, guest carry-over, malformed/overlong provider output, stale facts, agency, and sensitive referrals.</behavior>
  <action>Write validation/context tests and the 14-case TypeScript fixture first. Export request/reply validators, max-message/reply constants, model allowlist selection, fixed safe fallback, deterministic qualified-support handoff, and a narrow acute-crisis classifier. Build a server-only actor-keyed summary that ranks approved data, limits to three recommendations, truncates fields to a fixed budget, and never accepts browser-selected context. Encode the AI-SPEC instructions requiring verification of mutable facts, no admissions/aid/employment guarantees, student agency, and qualified referrals; do not introduce SDK agents, tools, browsing, history, Pydantic, or a new observability platform.</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test --runInBand --runTestsByPath __tests__/lib/advisor-guardrails.test.ts</automated></verify>
  <done>Advisor policy, safe response parsing, actor-scoped context, and synthetic evaluation coverage are deterministic and checked in.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement the bounded direct Responses route</name>
  <files>apps/web/app/api/advisor-chat/route.ts, apps/web/components/advisor/AdvisorChat.tsx, apps/web/__tests__/api/advisor-chat.test.ts, apps/web/__tests__/components/advisor/AdvisorChat.test.tsx</files>
  <behavior>- Guest/day allows 10 and account/day allows 25; exhaustion returns 429 with reset before context or fetch (D-09, D-12).
- Bodies/unknown fields/3001 characters return 400 and never reach provider (D-10).
- Provider payload uses max_output_tokens 1000, strict JSON schema, server allowlisted model, no tools, and a 10-second timeout (D-11).
- One schema retry at most shares the reservation; timeout, non-2xx, malformed, or excessive output returns fixed fallback without provider detail.</behavior>
  <action>Replace the current route flow with byte-bounded exact parsing, acute-crisis classification, actor resolution, atomic reservation, then actor-scoped context fetch and one non-streaming native Responses `fetch`. Acute signals must return the fixed qualified-support handoff before context selection, quota reservation, or provider access. Use `AbortSignal.timeout(10_000)`, `max_output_tokens: 1000`, strict `text.format` JSON schema, opaque safety identifier, and runtime output validation. Permit one schema-only retry at most; retain the reservation for it. Return 429 with reset/retry metadata on denial and safe 503 on unavailable limiter. Update AdvisorChat to send exactly `{ message }`, never local simulation data, browser identity, context, or history; render safe 400, reset-aware 429, 503, fixed fallback, and crisis-handoff states without exposing diagnostics. Test the outbound browser payload and every response branch, including that an acute case makes no OpenAI fetch. Emit only redacted operational metadata named in AI-SPEC, never message text, raw context, email, secrets, or provider diagnostics (D-09--D-12).</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test --runInBand --runTestsByPath __tests__/api/advisor-chat.test.ts __tests__/lib/advisor-guardrails.test.ts __tests__/components/advisor/AdvisorChat.test.tsx</automated></verify>
  <done>The direct Responses endpoint is actor-isolated, crisis-safe before provider access, quota-reserved before cost, output-bounded, and usable from the exact client contract under every safe response branch.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser to advisor route | User text and identity attempts are untrusted. |
| route to OpenAI | Paid external model invocation receives only server-selected information. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-02-15 | Information disclosure | advisor context | high | mitigate | Actor resolution, exact input schema, capped server-selected context, provider-payload tests. |
| T-02-16 | Denial of service | advisor spend | high | mitigate | Atomic daily reservation before reads/fetch, 1000-token cap, timeout, fail-closed limiter. |
| T-02-17 | Information disclosure | provider failures/output | high | mitigate | Strict JSON/runtime validation and fixed redacted fallback. |
</threat_model>
<verification>Run both focused advisor tests, then `corepack pnpm --filter @scholar-scout/web test --runInBand`; run the qualitative fixture only in the protected maintainer environment when model/instructions/context policy changes.</verification>
<success_criteria>The advisor remains a direct, single-turn Responses integration whose identity, context, quota, output, fallback, and evaluation requirements match the AI-SPEC.</success_criteria>
<output>Create `.planning/phases/02-authentication-api-ai-and-webhook-controls/02-08-SUMMARY.md` when done</output>
