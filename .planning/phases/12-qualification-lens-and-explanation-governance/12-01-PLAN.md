---
phase: 12
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/lib/qualification-record.ts
  - apps/web/lib/server/data-store.ts
  - apps/web/lib/server/student-records.ts
  - apps/web/app/api/account/qualifications/route.ts
  - apps/web/components/qualifications/QualificationRecordForm.tsx
  - apps/web/components/profile/ProfileDashboard.tsx
  - apps/web/__tests__/lib/qualification-record.test.ts
  - apps/web/__tests__/api/account-qualification-routes.test.ts
  - apps/web/__tests__/lib/student-records.test.ts
  - apps/web/__tests__/components/QualificationRecordForm.test.tsx
autonomous: true
requirements: [MATCH-01, MATCH-04]
must_haves:
  truths:
    - "A signed-in student can save, load, edit, and deliberately clear one private qualification record from Account; the same bounded editor can be invoked from Programmes in Plan 12-03. (D-01, D-02)"
    - "Only diploma/credits, degree, licence, prior work, voluntary military history, a bounded private note, and student-confirmed keywords can enter the record; browser input never supplies the owner. (D-01, D-02, D-03)"
    - "Qualification data is account-only, absent-compatible in legacy documents, excluded from guest migration, and never copied to onboarding/local storage/URLs. (D-01, D-02)"
    - "GPA, tests, prestige, ZIP/residence, passive behavior, sensitive/referral fields, scores, and arbitrary extra fields are rejected before persistence or rank use. (MATCH-04)"
  artifacts:
    - "apps/web/lib/qualification-record.ts exports QualificationRecord, QualificationKind, bounded parsing and normalization helpers."
    - "apps/web/app/api/account/qualifications/route.ts provides account-only GET, POST, and clear behavior."
    - "apps/web/components/qualifications/QualificationRecordForm.tsx renders the private native-control editor."
  key_links:
    - "QualificationRecordForm -> /api/account/qualifications -> resolveStudentActor({ allowGuest: false }) -> account:<id> qualificationProfiles conditional write."
    - "Legacy ScholarScoutData -> normalizeQualificationProfiles -> empty collection without a migration rewrite."
---

<objective>
Deliver the account-only, purpose-separated qualification record through a working signed-in Account UI, route, and conditional persistence seam.

Purpose: Establish the private, bounded input contract that later catalogue explanation code can safely consume without depending on onboarding or browser storage.
Output: Typed record/parser, compatibility-safe store collection, account route, reusable form, and red-to-green account/privacy regression tests.
</objective>

<execution_context>
@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-qualification-lens-and-explanation-governance/12-CONTEXT.md
@.planning/phases/12-qualification-lens-and-explanation-governance/12-RESEARCH.md
@.planning/phases/12-qualification-lens-and-explanation-governance/12-PATTERNS.md
@.planning/phases/12-qualification-lens-and-explanation-governance/12-VALIDATION.md
@.planning/phases/12-qualification-lens-and-explanation-governance/12-UI-SPEC.md
@docs/product-recommendation-governance.md

<interfaces>
From `apps/web/lib/server/student-actor.ts`: `StudentActor` has an account branch with `accountId` and server-created `storageKey`; qualification routes must resolve this actor with guests disabled and require the account branch.

From `apps/web/lib/server/data-store.ts`: `ScholarScoutData`, `readVersionedScholarScoutData`, conditional writes, `PersistenceConflictError`, and `createAuditEvent` are the compatibility and conflict seams. Extend the document with a normalized `qualificationProfiles` object instead of changing `onboardingProfiles`.

From `apps/web/app/api/account/onboarding/route.ts`: retain its bounded-JSON, exact-object, explicit 400/413/409 response convention, while the new route owns a different narrow payload.
</interfaces>
</context>

## Artifacts this phase produces

- `QualificationKind`, `QualificationRecord`, exact parser/normalizer, and account-record get/replace helpers.
- `qualificationProfiles` legacy-compatible account collection and `/api/account/qualifications` GET/POST/clear route.
- `QualificationRecordForm`, Account entry point, and account/API/component regression tests.

## Task ownership boundaries

Task 1 owns the initial end-to-end account contract: parser, compatibility collection, server helper, route, reusable form, Account entry, and the minimum unit/API coverage needed to prove the signed-in path. Task 2 owns only persistence/API compatibility, isolation, and boundary regressions in the store and tests; it does not add form behavior. Task 3 owns only Account form interaction and accessibility states plus its component test. This sequential split keeps the ten listed files traceable to distinct persistence, API, and UI responsibilities without separating the signed-in tracer from the contract it must prove.

## Execution scope guard

The private-account vertical slice is intentionally cohesive: Task 1 owns only the minimum cross-layer persistence, API, and Account UI wiring for one signed-in save/reload; Task 2 owns persistence/API compatibility and isolation coverage only; Task 3 owns UI-state and accessibility completion only. No task opens a new discovery surface or changes the governed catalogue contract. If Task 1 requires any additional production file, an additional UI entry point, or a layer beyond its eight listed tracer files to establish that single path, stop after recording the working tracer and split the remaining persistence/API or UI work into a later plan rather than expanding this plan's scope.

**Accepted planning exception (2026-09-24):** The user approved retaining this bounded ten-file vertical slice as one plan. Its three task boundaries and split trigger above remain mandatory during execution; this approval does not authorize adding files or discovery surfaces beyond the stated scope.

<assumption_delta_decision>
Create `QualificationRecord` as a purpose-separated, account-only collection beside onboarding. This implements D-01/D-02/D-03 because the existing onboarding contract accepts GPA, permits guest flows, and has browser-storage behavior that the qualification record must never inherit. Preserve regression invariants: no onboarding type import, no guest migration, no local-storage or URL serialization, no client owner/storage-key field, and no note or keyword contents in audit events.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Save one private structured qualification through the signed-in Account path</name>
  <reversibility rating="costly">D-01 and D-03 add an account persistence contract; later shape changes require compatibility normalization and coordinated callers.</reversibility>
  <files>apps/web/lib/qualification-record.ts, apps/web/lib/server/data-store.ts, apps/web/lib/server/student-records.ts, apps/web/app/api/account/qualifications/route.ts, apps/web/components/qualifications/QualificationRecordForm.tsx, apps/web/components/profile/ProfileDashboard.tsx, apps/web/__tests__/lib/qualification-record.test.ts, apps/web/__tests__/api/account-qualification-routes.test.ts</files>
  <produces>QualificationKind and QualificationRecord parser; legacy-safe qualificationProfiles collection; account-only qualification route; reusable Account form; parser and authenticated-route tests.</produces>
  <read_first>apps/web/lib/onboarding-types.ts; apps/web/lib/onboarding-validation.ts; apps/web/lib/server/data-store.ts; apps/web/lib/server/student-records.ts; apps/web/lib/server/student-actor.ts; apps/web/app/api/account/onboarding/route.ts; apps/web/components/profile/ProfileDashboard.tsx; apps/web/__tests__/api/account-guest-routes.test.ts</read_first>
  <behavior>
    - A valid exact payload containing only finite structured choices, a trimmed note of at most 500 characters, and at most 12 normalized keywords of at most 40 characters persists only at the session-derived account key.
    - A legacy document without `qualificationProfiles` reads as an empty collection; a qualification record does not enter guest migration or onboarding storage.
    - Anonymous and guest callers receive the route's safe authorization denial; a client owner/key cannot select another account; replacement conflict returns 409 without overwriting the visible draft.
  </behavior>
  <action>Start by adding failing unit and route tests from VALIDATION Wave 0. Define `QualificationKind` solely for diploma/credits, degree, licence, prior work, and voluntary military history, then implement a narrow `QualificationRecord` parser that exact-validates the payload, normalizes finite values and literal keywords deterministically, and applies the UI-contract bounds. Add `qualificationProfiles` beside onboarding profiles with default/normalizer/import-export cloning that preserves legacy documents without a rewrite. Implement student-record helpers with the established single-attempt versioned write and generic audit metadata. Add the thin account-only route using the trusted resolved actor and bounded JSON convention. Build the reusable signed-in Account form with native fieldsets, labelled controls, private-note counter/helper copy, explicit add/remove keyword actions, a polite save status, field errors, and `Save qualifications`; retain client drafts on 409. Keep the note and keywords inside component state/request payload only; D-01/D-02/D-03 prohibit onboarding, URL, browser-storage, staff/provider, and audit-content paths.</action>
  <verify>
    <automated>corepack pnpm --filter @scholar-scout/web test -- --runInBand qualification-record account-qualification-routes</automated>
    <automated>corepack pnpm --filter @scholar-scout/web run typecheck</automated>
  </verify>
  <acceptance_criteria>A signed-in student can save and reload one ordinary structured selection, a private bounded note, and an explicit keyword; an absent legacy collection remains compatible; the route preserves actor ownership and conflict semantics; no test permits an unapproved request key.</acceptance_criteria>
  <done>The account tracer works end to end and its unit/API tests demonstrate the D-01/D-02/D-03 and MATCH-04 boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close private-record persistence and input-boundary regressions</name>
  <files>apps/web/lib/server/data-store.ts, apps/web/lib/server/student-records.ts, apps/web/__tests__/lib/qualification-record.test.ts, apps/web/__tests__/lib/student-records.test.ts, apps/web/__tests__/api/account-qualification-routes.test.ts</files>
  <produces>Compatibility, cross-account isolation, payload-boundary, conditional-conflict, and no-guest-migration regression coverage for the private record.</produces>
  <read_first>apps/web/lib/qualification-record.ts; apps/web/lib/server/data-store.ts; apps/web/lib/server/student-records.ts; apps/web/__tests__/lib/student-records.test.ts; apps/web/__tests__/api/account-qualification-routes.test.ts</read_first>
  <behavior>
    - Parser tests reject duplicate/overlong keyword forms, malformed arrays, prohibited fields, and account selectors while allowing an empty ordinary record.
    - Two account keys remain isolated; stale conditional writes surface the established conflict path; generic audit rows contain no student-entered note or keyword values.
    - Guest-to-account migration leaves the new collection untouched and legacy normalization is stable across read/write/backup validation.
  </behavior>
  <action>Extend the Wave 0 record, store, and route tests to cover the exact-key boundary, all permitted empty/partial states, normalization determinism, request-size/error categories, account isolation, stale writes, and legacy backup/import normalization. Verify compatibility in the actual store cloning/validation paths rather than with a type cast. Preserve `migrateGuestOwnedRecords` behavior; do not add the qualification collection to its allowlist. Keep audit events generic so privacy is maintained even in operational evidence.</action>
  <verify>
    <automated>corepack pnpm --filter @scholar-scout/web test -- --runInBand qualification-record account-qualification-routes student-records</automated>
    <automated>corepack pnpm --filter @scholar-scout/web run lint</automated>
  </verify>
  <acceptance_criteria>Regression tests prove that persistence accepts only the purpose-limited contract, preserves per-account ownership and legacy documents, and leaves guest migration/onboarding behavior separate.</acceptance_criteria>
  <done>The account record has explicit compatibility and privacy regression coverage before catalogue code consumes it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Finish the accessible private-form states and destructive clear flow</name>
  <files>apps/web/components/qualifications/QualificationRecordForm.tsx, apps/web/components/profile/ProfileDashboard.tsx, apps/web/__tests__/components/QualificationRecordForm.test.tsx</files>
  <produces>Accessible Account form loading, error, keyword, conflict, and confirmed-clear interactions plus their component regression suite.</produces>
  <read_first>apps/web/components/qualifications/QualificationRecordForm.tsx; apps/web/components/onboarding/OnboardingWizard.tsx; apps/web/components/profile/ProfileDashboard.tsx; apps/web/__tests__/components/OnboardingWizard.test.tsx; .planning/phases/12-qualification-lens-and-explanation-governance/12-UI-SPEC.md</read_first>
  <behavior>
    - Loading, empty, partial, invalid, save, conflict, failed-clear, and saved states keep the account form usable with visible labels, focus, error association, and polite status text.
    - Keyword entry occurs only after the explicit add action; removal has a precise accessible name and 44px target.
    - Clear requires its in-context confirmation, sends an empty approved record only after confirmation, and never alters shortlist, filters, or comparison choices.
  </behavior>
  <action>Complete the UI-SPEC covered form states and its component backstops: fixed-height non-animated loading skeleton, documented empty/keyword-empty text, long-value wrapping, explicit errors, conflict reload affordance with unsaved draft retained, cancellable clear confirmation, and save/clear status behavior. Keep primary copy short and factual per D-08; use existing Tailwind primitives, visible focus, `min-w-0`, `break-words`, and touch targets. Test labels, live status, limits, manual keyword boundary, privacy copy, clear behavior, and keyboard-accessible controls.</action>
  <verify>
    <automated>corepack pnpm --filter @scholar-scout/web test -- --runInBand QualificationRecordForm</automated>
    <automated>corepack pnpm --filter @scholar-scout/web run typecheck</automated>
  </verify>
  <acceptance_criteria>The Account editor fulfils every covered form-state item in the UI contract and tests retain a student’s draft through a recoverable save conflict.</acceptance_criteria>
  <done>The private qualification input is usable, bounded, and purpose-separated before it is made available on discovery surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| browser to account route | Untrusted qualification payload crosses into account persistence. |
| session actor to document slice | Server-derived identity selects an account-owned record. |
| legacy document to normalizer | Stored historical state enters the new collection contract. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-12-01 | E | qualifications route | high | mitigate | Resolve account actor server-side with guests disabled; accept no owner/storage key; route and isolation tests prove cross-account writes fail. |
| T-12-02 | I | form, audit, persistence | high | mitigate | Keep note/keywords inside the account record and generic audit metadata; exclude browser storage, URL, staff/provider surfaces, and guest migration; component/store tests enforce the boundary. |
| T-12-03 | T | payload parser | high | mitigate | Exact-key parser with bounded finite fields rejects proxy, sensitive/referral, and arbitrary keys before store access. |
| T-12-04 | R | conditional replacement | medium | mitigate | Reuse versioned single-attempt write and 409 reload contract; retain the draft instead of silently overwriting it. |
| T-12-SC | Tampering | dependency supply chain | low | accept | No package installation is planned; existing lockfile and workspace dependencies remain unchanged. |
</threat_model>

<verification>
Run the focused Wave 0 suite after each task, then `corepack pnpm --filter @scholar-scout/web run lint`, `typecheck`, and `corepack pnpm --filter @scholar-scout/web test -- --runInBand` at wave end.
</verification>

<success_criteria>
The private account qualification record can be managed without importing legacy preference matching/onboarding behavior, and all account, guest, compatibility, and payload-boundary tests pass.
</success_criteria>

<output>
Create `.planning/phases/12-qualification-lens-and-explanation-governance/12-01-SUMMARY.md` when done.
</output>
