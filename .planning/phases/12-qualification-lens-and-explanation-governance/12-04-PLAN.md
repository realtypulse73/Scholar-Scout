---
phase: 12
plan: 04
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - apps/web/lib/catalogue-contract.ts
  - apps/web/lib/catalogue-publication.ts
  - apps/web/lib/server/catalogue-publications.ts
  - apps/web/components/admin/CataloguePublicationManager.tsx
  - apps/web/__tests__/lib/catalogue-publication.test.ts
  - apps/web/__tests__/lib/server/catalogue-publications.test.ts
  - apps/web/__tests__/components/CataloguePublicationManager.test.tsx
  - docs/curated-catalogue-publication-runbook.md
autonomous: true
requirements: [MATCH-01, MATCH-03, MATCH-04]
must_haves:
  truths:
    - "Staff can author source-backed requirements, reviewed description, and documented support inside the existing governed candidate import workflow, submit it for independent review, and publish it to the learner snapshot. (D-05, D-06, D-09)"
    - "A published requirement has an exact controlled qualification key and source evidence; unsupported text cannot create a qualification claim. (D-03, D-05, D-09)"
    - "Existing private candidate intake, review, release, and audit visibility boundaries remain the sole staff workflow. (D-02)"
  artifacts:
    - "Candidate and published record contracts carry ordered publishedRequirements, optional reviewedDescription, and documentedSupport evidence."
    - "The existing import manager and runbook document the extended candidate JSON shape without creating a new staff authoring system."
    - "Import, review, publication, and manager regression fixtures prove reviewed evidence reaches a public snapshot."
  key_links:
    - "existing manager JSON import -> importCatalogueCandidates -> private candidate -> independent review -> published snapshot."
    - "published exact qualification key plus FactEvidence -> Plan 12-02 lens input."
---

<objective>
Resolve the governed staff evidence seam by extending the existing candidate JSON import and publication workflow.

Purpose: Give the qualification lens reviewed facts to consume while retaining Phase 10's private candidate, independent review, and snapshot-only learner boundaries.
Output: Evidence-aware candidate/published contracts, existing-manager import guidance, reviewed publication fixtures, and a documented operator example.
</objective>

<execution_context>
@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-curated-import-and-governed-staff-publication/10-CONTEXT.md
@.planning/phases/12-qualification-lens-and-explanation-governance/12-CONTEXT.md
@.planning/phases/12-qualification-lens-and-explanation-governance/12-RESEARCH.md
@.planning/phases/12-qualification-lens-and-explanation-governance/12-PATTERNS.md
@docs/curated-catalogue-publication-runbook.md

<interfaces>
Reuse CatalogueCandidateInput, importCatalogueCandidates, stageCatalogueCandidate, reviewCatalogueCandidate, publishWeeklyCatalogueSnapshot, the existing structured JSON textarea in CataloguePublicationManager, and the existing active-staff import route. Do not introduce a separate staff page, route, role, provider call, or learner-time fetch.

Requirements use only the resolved eight key literals from Plan 12-01. Every requirement, reviewed description, and documented support statement carries FactEvidence; the existing review/publish gates remain authoritative.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Stage, review, and publish one source-backed qualification requirement through existing candidate intake</name>
  <reversibility rating="costly">The reviewed candidate and snapshot fields form the source contract shared with learner surfaces; incompatibly changing them later requires snapshot compatibility work.</reversibility>
  <files>apps/web/lib/catalogue-contract.ts, apps/web/lib/catalogue-publication.ts, apps/web/lib/server/catalogue-publications.ts, apps/web/__tests__/lib/catalogue-publication.test.ts, apps/web/__tests__/lib/server/catalogue-publications.test.ts</files>
  <produces>PublishedRequirement, reviewedDescription, and documentedSupport contracts; candidate/import decoding; candidate-to-review-to-publication test fixture with source evidence and an exact qualification key.</produces>
  <read_first>apps/web/lib/catalogue-contract.ts; apps/web/lib/catalogue-publication.ts; apps/web/lib/server/catalogue-publications.ts; apps/web/__tests__/lib/server/catalogue-publications.test.ts; apps/web/app/api/admin/catalogue-publications/import/route.ts</read_first>
  <behavior>
    - An active staff import stages a private candidate containing one current sourced requirement with one resolved exact key, then the existing independent review and weekly publication path preserves it in the public record.
    - Each optional reviewed description and documented support statement has its own valid FactEvidence; incomplete evidence fails the existing candidate validation rather than becoming an asserted learner fact.
    - Candidate history and learner access retain their existing separation.
  </behavior>
  <action>Write the failing candidate-to-published-snapshot test first. Extend the shared contract and candidate parser with ordered publishedRequirements, optional reviewedDescription, and optional documentedSupport fields that reuse FactEvidence validation. Each requirement has exact text and one or more resolved controlled keys; validate these keys against Plan 12-01 rather than accepting authored free-form categories. Thread the fields through import decoding, checklist evaluation, reviewed candidate staging, independent review, published-record guards, digesting, cloning, and public snapshot serialization. Reuse the current staff import route and candidate lifecycle exactly; D-05/D-06/D-09 require source/date/action evidence, not a new authoring channel.</action>
  <verify>
    <automated>corepack pnpm --filter @scholar-scout/web test -- --runInBand --runTestsByPath __tests__/lib/catalogue-publication.test.ts __tests__/lib/server/catalogue-publications.test.ts</automated>
    <automated>corepack pnpm --filter @scholar-scout/web run typecheck</automated>
  </verify>
  <done>A reviewed candidate imported through the existing staff workflow publishes one exact-key requirement whose source evidence survives into the learner-readable snapshot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the existing JSON import seam operable and test reviewed-evidence rejection paths</name>
  <files>apps/web/components/admin/CataloguePublicationManager.tsx, apps/web/__tests__/components/CataloguePublicationManager.test.tsx, docs/curated-catalogue-publication-runbook.md, apps/web/__tests__/lib/catalogue-publication.test.ts, apps/web/__tests__/lib/server/catalogue-publications.test.ts</files>
  <produces>Existing-manager source-field guidance, runbook candidate JSON example, and regression cases for malformed evidence, unresolved keys, failed review, and unchanged public snapshots.</produces>
  <read_first>apps/web/components/admin/CataloguePublicationManager.tsx; apps/web/__tests__/components/CataloguePublicationManager.test.tsx; docs/curated-catalogue-publication-runbook.md; apps/web/lib/catalogue-publication.ts</read_first>
  <behavior>
    - The established structured candidate-import interface tells staff where source-backed requirement, description, and support fields belong and continues to use the private import/review/release controls.
    - Malformed evidence or an unapproved key is rejected atomically; an existing candidate and active public snapshot remain unchanged.
    - A published fixture can supply checked-requirement, keyword-text, and documented-support evidence to the lens without a live provider request.
  </behavior>
  <action>Update the existing manager helper text and curated-publication runbook with one bounded schema-versioned candidate JSON example that includes the new source-backed fields and explicitly points staff to the existing Stage private import, submit, independent review, and release actions. Add manager and server tests that retain the staff authorization, batch size, atomic import, conflict, lifecycle, and private-history rules while covering malformed requirement text, invalid key, missing evidence, and non-current facts. Add a reviewed published fixture containing one requirement, attributable description, and optional support so Plan 12-02 can consume a real governed record. Do not add a new form, route, role, or staff-visible student-note path.</action>
  <verify>
    <automated>corepack pnpm --filter @scholar-scout/web test -- --runInBand --runTestsByPath __tests__/components/CataloguePublicationManager.test.tsx __tests__/lib/catalogue-publication.test.ts __tests__/lib/server/catalogue-publications.test.ts</automated>
    <automated>corepack pnpm --filter @scholar-scout/web run lint</automated>
  </verify>
  <done>Existing governed import is the documented and testable authoring path for the exact reviewed evidence required by every Phase 12 explanation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| staff candidate JSON to private intake | Authorized staff supply untrusted structured evidence for validation. |
| reviewed candidate to public snapshot | Editorial approval turns validated evidence into learner-readable facts. |
| public snapshot to lens | Learner code may only consume released reviewed evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-12-13 | T | candidate import | high | mitigate | Exact controlled keys and FactEvidence validation reject unsourced, malformed, or arbitrary qualification claims before staging. |
| T-12-14 | S | staff lifecycle | high | mitigate | Retain existing active-staff authorization, private candidate staging, independent review, and release gates. |
| T-12-15 | I | learner snapshot | high | mitigate | Publish only approved snapshot fields and preserve the Phase 10 candidate/audit separation. |
| T-12-SC | Tampering | dependency supply chain | low | accept | No package installation is planned. |
</threat_model>

<verification>
Run the named import/publication test paths after each task; run lint, typecheck, and the full web suite at wave completion.
</verification>

<success_criteria>
Reviewed qualification evidence can be authored and published through the established governed candidate/import path, with no new staff system and no learner-time provider dependency.
</success_criteria>

<output>
Create .planning/phases/12-qualification-lens-and-explanation-governance/12-04-SUMMARY.md when done.
</output>
