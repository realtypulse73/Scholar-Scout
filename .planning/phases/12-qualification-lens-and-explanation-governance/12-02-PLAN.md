---
phase: 12
plan: 02
type: tdd
wave: 3
depends_on: [12-01, 12-04]
files_modified:
  - apps/web/lib/catalogue-discovery.ts
  - apps/web/lib/qualification-lens.ts
  - apps/web/__tests__/lib/qualification-lens.test.ts
  - apps/web/__tests__/lib/catalogue-discovery.test.ts
autonomous: true
requirements: [MATCH-01, MATCH-02, MATCH-03, MATCH-04]
must_haves:
  truths:
    - "Only reviewed, attributable published requirement, programme-description, and documented-support evidence can produce a qualification explanation or rank connection. (D-05, D-06, D-09)"
    - "Qualifications-first sorts the already-filtered reviewed records by current checked requirement count, then explicit literal keyword connection, then deterministic public ID; it retains exactly the same item IDs as normal order. (D-04, D-06)"
    - "Missing, stale, unknown, and conflicting requirement facts produce source/date/action verification rows and cannot lower an opportunity or be counted as an unfulfilled qualification. (D-07)"
    - "The explanation DTO has no note, onboarding, proxy, verdict, or prediction input and provides source-based verification/support data for every rendered surface. (D-02, D-05, MATCH-04)"
  artifacts:
    - "Plan 12-04's reviewed `publishedRequirements`, reviewed description, and documented support snapshot contract is read through the discovery item without re-authoring or modifying candidate/publication contracts."
    - "apps/web/lib/qualification-lens.ts exports pure explanation and ordering functions plus serializable DTO types."
    - "Focused qualification-lens/discovery Jest tests prove source-only, all-visible deterministic behavior using the Plan 12-04 published snapshot contract."
  key_links:
    - "Plan 12-04 governed published snapshot -> buildCatalogueDiscoveryModel -> qualification-lens DTO."
    - "Current structured key + current requirement evidence -> checked requirement; explicit keyword + attributable reviewed text -> keyword connection."
---

<objective>
Create the pure qualification-lens contract that consumes reviewed snapshot evidence and connects a private record to factual catalogue explanations without creating an eligibility model.

Purpose: Ensure ranking and explanations are inspectable, snapshot-only, source-backed, and incapable of suppressing a reviewed opportunity while leaving candidate authoring and publication to Plan 12-04.
Output: Discovery mapping, pure DTO/order functions, and red-green safety tests that consume the Plan 12-04 governed snapshot contract.
</objective>

<execution_context>
@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-qualification-lens-and-explanation-governance/12-CONTEXT.md
@.planning/phases/12-qualification-lens-and-explanation-governance/12-RESEARCH.md
@.planning/phases/12-qualification-lens-and-explanation-governance/12-PATTERNS.md
@.planning/phases/12-qualification-lens-and-explanation-governance/12-VALIDATION.md
@.planning/phases/12-qualification-lens-and-explanation-governance/12-UI-SPEC.md
@docs/product-recommendation-governance.md

<interfaces>
From Plan 12-04's governed snapshot contract: reviewed `publishedRequirements`, reviewed description, documented support, and their `FactEvidence` have already passed candidate parsing, independent review, and publication validation. Plan 12-02 reads that contract only; it must not modify candidate, import, review, publication, or snapshot-authoring behavior.

From `apps/web/lib/catalogue-discovery.ts`: `buildCatalogueDiscoveryModel` already filters the reviewed snapshot and produces stable public-ID normal order. The lens receives its resulting `CatalogueDiscoveryItem[]`, never raw provider data or query-state personal data.

Plan 12-01 will export `QualificationRecord`; this plan treats its `structured` and explicitly selected `keywords` fields as the only student values and does not accept its `note` field.
</interfaces>
</context>

## Artifacts this phase produces

- Discovery mapping for Plan 12-04's published evidence contract.
- `QualificationExplanation`, checked/keyword/needs-verification row types, `buildQualificationLensModel`, and `orderQualificationsFirst`.
- Source-first lens/discovery regression tests, including stale/unknown/conflicting and all-visible ordering invariants.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Consume one governed snapshot requirement in the explanation and ordering lens</name>
  <reversibility rating="costly">The D-04/D-05/D-06 explanation DTO is shared by cards, detail, and comparison, so its contract must remain serializable and source-preserving across those consumers.</reversibility>
  <files>apps/web/lib/catalogue-discovery.ts, apps/web/lib/qualification-lens.ts, apps/web/__tests__/lib/qualification-lens.test.ts, apps/web/__tests__/lib/catalogue-discovery.test.ts</files>
  <produces>Discovery mapping that reads Plan 12-04 published evidence; a source-preserving single-item QualificationExplanation DTO; snapshot-to-discovery-to-lens regression fixtures.</produces>
  <read_first>apps/web/lib/catalogue-contract.ts; apps/web/lib/catalogue-publication.ts; apps/web/lib/catalogue-discovery.ts; apps/web/__tests__/lib/catalogue-discovery.test.ts; apps/web/__tests__/lib/catalogue-publication.test.ts; .planning/phases/12-qualification-lens-and-explanation-governance/12-04-PLAN.md; .planning/phases/12-qualification-lens-and-explanation-governance/12-RESEARCH.md</read_first>
  <behavior>
    - A current reviewed requirement with an explicit controlled qualification key becomes a named checked row with its evidence and verification action when the record contains that key.
    - A literal student-confirmed keyword can create a keyword row only when it appears as a whole token in attributable reviewed requirement or description text; it is distinct from a checked row.
    - Discovery mapping reads the Plan 12-04 reviewed snapshot requirement/description/support fields without altering their candidate-to-publication contract; titles, skills, costs, and unreviewed prose cannot create the connection.
  </behavior>
  <action>Begin with failing discovery and lens tests that use the governed published-snapshot fixture supplied by Plan 12-04. Read that reviewed requirement/description/documented-support evidence through `buildCatalogueDiscoveryModel` without changing candidate, import, review, publication, or snapshot contracts. Implement a named pure `qualification-lens` module that takes a `CatalogueDiscoveryItem` and the Plan 12-01 record's structured choices/explicit keywords only, returning a serializable DTO that lists checked current requirements, keyword connections, and verification/support rows. Require full reviewed evidence before any connection; do not derive one from a title, skills, or unreviewed prose. D-05/D-06/D-09 require factual source/date/action data to remain adjacent to every conclusion.</action>
  <verify>
    <automated>corepack pnpm --filter @scholar-scout/web test -- --runInBand qualification-lens catalogue-discovery</automated>
    <automated>corepack pnpm --filter @scholar-scout/web run typecheck</automated>
  </verify>
  <acceptance_criteria>One Plan 12-04 reviewed snapshot requirement travels through discovery to an explanation with its evidence intact, and a structured/explicit-keyword record produces only its permitted, named connection type.</acceptance_criteria>
  <done>The first read-only snapshot-to-lens path is test-proven and ready for the account/UI integrations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce the all-visible three-bucket ordering and uncertainty semantics</name>
  <files>apps/web/lib/qualification-lens.ts, apps/web/__tests__/lib/qualification-lens.test.ts, apps/web/__tests__/lib/catalogue-discovery.test.ts</files>
  <produces>Pure all-visible three-bucket ordering and source-backed uncertainty DTO rows with deterministic-order and ID-set invariance tests.</produces>
  <read_first>apps/web/lib/qualification-lens.ts; apps/web/lib/catalogue-discovery.ts; apps/web/lib/catalogue-contract.ts; apps/web/__tests__/lib/qualification-lens.test.ts; .planning/phases/12-qualification-lens-and-explanation-governance/12-UI-SPEC.md</read_first>
  <behavior>
    - Qualification order has current checked count descending, then keyword-presence, then public ID, while normal order remains the discovery model's stable order.
    - Both orders preserve count and exact set of already-filtered IDs; a keyword-only result remains behind every result with a checked current requirement.
    - Needs-confirmation, unknown, conflicting, missing, or dated requirements yield a `Needs verification` source/date/action row and neither increment checked count nor move an item into an adverse bucket.
    - The lens API has no parameter for note, GPA/tests/prestige/ZIP, onboarding data, score, passive behavior, or referral information.
  </behavior>
  <action>Write the remaining Wave 0 tests first, then implement order on a copied array of the existing filtered items. Use descending current checked-requirement count, explicit-keyword presence, and `id.localeCompare` as the complete comparator; return normal order unchanged when the record is empty. Map every non-current, missing, stale, unknown, or conflicting requirement into the textual verification-row DTO with its source-date availability and direct action. Keep unavailable evidence informational rather than a lower connection score. Add invariance tests for duplicate IDs, stable ties, ID-set equality, literal keyword boundaries, no note leakage in serialized DTOs, and rejected proxy-shaped inputs. This directly implements D-04/D-06/D-07 and MATCH-02/MATCH-04.</action>
  <verify>
    <automated>corepack pnpm --filter @scholar-scout/web test -- --runInBand qualification-lens catalogue-discovery</automated>
    <automated>corepack pnpm --filter @scholar-scout/web run lint</automated>
  </verify>
  <acceptance_criteria>Tests prove all filtered opportunities survive either order; only current reviewed requirements affect checked counts; the three deterministic levels are inspectable; uncertain evidence remains a source-backed verification prompt.</acceptance_criteria>
  <done>The shared ranking/explanation model cannot become a hidden-option or negative-inference mechanism.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Complete documented-support and explanation-governance regressions</name>
  <files>apps/web/lib/qualification-lens.ts, apps/web/__tests__/lib/qualification-lens.test.ts</files>
  <produces>Documented-support-only DTO fields, non-predictive explanation-copy guards, and absence/support lens regressions against the Plan 12-04 snapshot contract.</produces>
  <read_first>apps/web/lib/catalogue-contract.ts; apps/web/lib/catalogue-publication.ts; apps/web/lib/qualification-lens.ts; apps/web/__tests__/lib/qualification-lens.test.ts; .planning/phases/12-qualification-lens-and-explanation-governance/12-04-PLAN.md; docs/product-recommendation-governance.md</read_first>
  <behavior>
    - A support statement exists only when an attributable reviewed source documents it and is attached to an unchecked or verification row as a provider-confirmation prompt.
    - Missing support evidence creates no synthetic support claim and no negative outcome text.
    - Explanation labels describe checked published requirements, keyword connections, or verification work; they expose no eligibility, admission, enlistment, funding, placement, salary, safety, or outcome conclusion.
  </behavior>
  <action>Add lens tests and DTO fields for the documented-support rule from D-09 using the Plan 12-04 published snapshot contract: carry cited support evidence where present, render the support prompt as conditional factual data, and otherwise leave support absent. Add copy-token/domain tests that keep the lens descriptive and non-predictive across checked, keyword, empty, and uncertain states. Maintain the reviewed-snapshot-only boundary and source action on every explanation path so the UI can render a material verification step without inventing facts; do not change the evidence-authoring or publishing seam.</action>
  <verify>
    <automated>corepack pnpm --filter @scholar-scout/web test -- --runInBand qualification-lens catalogue-discovery</automated>
    <automated>corepack pnpm --filter @scholar-scout/web run typecheck</automated>
  </verify>
  <acceptance_criteria>The DTO can state a documented support only with its source and always offers a provider-verification action; tests cover the absence case and all non-predictive explanation states.</acceptance_criteria>
  <done>Plan 12-03 receives one complete, source-driven explanation contract for cards, detail, and comparison.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| governed published snapshot to lens | Plan 12-04-reviewed evidence becomes learner-readable explanation data. |
| snapshot to lens mapper | Reviewed facts and private bounded fields become a serializable explanation/order DTO. |
| lens order to result collection | A sorting decision can change prominence but must not change visibility. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-12-05 | T | governed snapshot evidence | high | mitigate | Consume Plan 12-04's FactEvidence-validated requirements, descriptions, and documented supports without a parallel authoring path; lens/discovery tests reject unreviewed paths. |
| T-12-06 | I | explanation DTO | high | mitigate | Lens accepts only controlled structured choices and explicit keywords, omits note fields, and tests serialized DTO/privacy invariants. |
| T-12-07 | T | qualification comparator | high | mitigate | Pure comparator operates on already-filtered records and tests exact ID-set/count preservation, deterministic ties, and three documented buckets. |
| T-12-08 | S | source/verification row | medium | mitigate | Preserve source URL/date/state/action from reviewed facts and map uncertainty to verification, preventing a stale assertion from impersonating a current fact. |
| T-12-SC | Tampering | dependency supply chain | low | accept | No package installation is planned; existing lockfile and workspace dependencies remain unchanged. |
</threat_model>

<verification>
Run the focused lens/discovery Jest tests after every task. At wave end, run `corepack pnpm --filter @scholar-scout/web run lint`, `typecheck`, and the full `corepack pnpm --filter @scholar-scout/web test -- --runInBand` suite.
</verification>

<success_criteria>
The lens consumes only the Plan 12-04 reviewed, attributable snapshot; qualification ordering is deterministic, all-visible, and non-predictive; uncertainty and documented support remain factual verification guidance.
</success_criteria>

<output>
Create `.planning/phases/12-qualification-lens-and-explanation-governance/12-02-SUMMARY.md` when done.
</output>
