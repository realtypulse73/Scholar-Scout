---
phase: 06-end-to-end-hardening-and-release-readiness
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/prelaunch-rehearsal.mjs
  - scripts/test-production-tooling.mjs
  - .github/workflows/prelaunch-rehearsal.yml
autonomous: true
gap_closure: true
gap_ids: [G-06-02]
requirements: [OPS-04, PROD-04]
must_haves:
  truths:
    - "G-06-02 / D-09: Candidate quality records the ordered immutable install, root pnpm test, lint, typecheck, and build commands before high-risk, local-browser, or Preview lanes can run."
    - "G-06-02 / D-05 / D-09: The manually dispatched rehearsal checks out and verifies the immutable candidate commit used in every lane record."
  artifacts:
    - path: scripts/prelaunch-rehearsal.mjs
      provides: Fail-closed candidate-quality command contract using root pnpm test.
    - path: scripts/test-production-tooling.mjs
      provides: Subprocess proof that ordering, lane isolation, and missing-record failure remain enforced.
    - path: .github/workflows/prelaunch-rehearsal.yml
      provides: Candidate-pinned checkout before the release-gate runner.
  key_links:
    - from: .github/workflows/prelaunch-rehearsal.yml
      to: scripts/prelaunch-rehearsal.mjs
      via: verified candidate checkout and --candidate-commit input
      pattern: "candidate_commit|rehearse:prelaunch"
---

<objective>
Correct the candidate-quality gate so its evidence contains the prescribed root test command and remains bound to the checked-out candidate.

Purpose: G-06-02 blocks the release gate because filtered workspace commands substitute for required root `pnpm test`.
Output: Tested release-gate command ordering and a candidate-pinned manually dispatched workflow.
</objective>

<execution_context>
@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-end-to-end-hardening-and-release-readiness/06-VERIFICATION.md
@.planning/phases/06-end-to-end-hardening-and-release-readiness/06-07-PLAN.md
@scripts/prelaunch-rehearsal.mjs
@scripts/test-production-tooling.mjs
@.github/workflows/prelaunch-rehearsal.yml
</context>

<interfaces>
From `scripts/prelaunch-rehearsal.mjs`:
- `CANDIDATE_QUALITY_COMMANDS` defines the exact ordered commands recorded in `candidate-quality.json`.
- `runReleaseLane()` records an independent lane and stops at its first nonzero command result.
- `aggregateLocalReleaseRecords()` and `aggregateReleaseRecords()` reject absent, failed, or candidate-mismatched records.
</interfaces>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Fail closed on a candidate-quality record without root pnpm test</name>
  <files>scripts/prelaunch-rehearsal.mjs, scripts/test-production-tooling.mjs, .github/workflows/prelaunch-rehearsal.yml</files>
  <behavior>
    - G-06-02 / D-09: the candidate-quality record contains exactly five ordered commands: immutable install, root `pnpm test`, root lint, root typecheck, and root build.
    - G-06-02 / D-09: a failed root test produces a failed candidate-quality record and prevents high-risk, local-browser, protected Preview-browser, and Preview-outage lanes.
    - G-06-02 / D-05: workflow checkout uses the required candidate commit and verifies `HEAD` equals that input before invoking the local release gate; a mismatch ends the job before a record can pass.
    - High-risk route/service commands remain their own record and are not substituted into candidate-quality evidence.
  </behavior>
  <action>Write Node subprocess assertions before changing the release runner. Replace the filtered workspace test and service-test substitutions in `CANDIDATE_QUALITY_COMMANDS` with the required root `pnpm test`, retaining the immutable install, root lint, root typecheck, and root build in that order. Update failure and record-shape assertions so the test proves no root-test substitution can pass candidate quality and a failed candidate-quality lane stops subsequent local and Preview work. Keep `HIGH_RISK_COMMANDS` as the distinct route/webhook/data-service lane defined by Plan 06-04. Pin `actions/checkout@v4` to the manual `candidate_commit` input and add a fail-closed read-only `git rev-parse HEAD` comparison before the release-gate command; extend the tooling test to assert the pin and comparison. Do not alter production deployment settings, introduce a waiver, accept a shared record for two lanes, or print credentials, fixture values, cookies, or environment values. This implements G-06-02 and D-05/D-06/D-09.</action>
  <verify>
    <automated>pnpm test:production-tooling</automated>
  </verify>
  <done>The candidate-quality record proves the prescribed five commands against the actual selected candidate, and every later lane remains unreachable after a missing or failed root test.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| workflow dispatch input -> checkout | The candidate identifier must select the code actually tested and rehearsed. |
| candidate-quality runner -> release evidence | A successful record must represent the required commands rather than a weaker command set. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-23 | Tampering | candidate checkout | high | mitigate | Pin checkout to the candidate input and compare resolved HEAD before release-gate execution. |
| T-06-24 | Repudiation | candidate-quality evidence | high | mitigate | Assert the exact ordered root commands and candidate-bound record fields in operational-script tests. |
| T-06-25 | Elevation of Privilege | release progression | medium | mitigate | Preserve fail-closed record aggregation so later Preview work cannot run after candidate-quality failure. |
</threat_model>

<verification>Run the operational-tooling suite; inspect the generated fake-run records in its temporary directory only through the assertions, then proceed to the separate authorized Preview evidence plan.</verification>
<success_criteria>G-06-02 is closed by a root-level candidate-quality test command and a workflow that cannot rehearse a different revision from the record it claims to validate.</success_criteria>
<output>Create `.planning/phases/06-end-to-end-hardening-and-release-readiness/06-09-SUMMARY.md` when done.</output>
