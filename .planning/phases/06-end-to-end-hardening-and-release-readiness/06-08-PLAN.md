---
phase: 06-end-to-end-hardening-and-release-readiness
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/lib/server/e2e-fixture-config.ts
  - apps/web/lib/server/e2e-programme-fixture.ts
  - apps/web/lib/server/programme-records.ts
  - apps/web/__tests__/lib/server/e2e-programme-fixture.test.ts
autonomous: true
gap_closure: true
gap_ids: [G-06-01]
requirements: [OPS-04, PROD-04]
must_haves:
  truths:
    - "G-06-01 / D-02: Generated programme records are created and deleted through saveProgrammeRecord and deleteProgrammeRecord, never through a lower-level persistence mutation."
    - "G-06-01 / D-02 / D-03: When the isolated fixture mode is enabled, governed catalogue reads expose exactly the declared generated records and exclude ordinary seed records."
  artifacts:
    - path: apps/web/lib/server/e2e-fixture-config.ts
      provides: Shared server-only fixture-mode configuration used without an initialization cycle.
    - path: apps/web/lib/server/e2e-programme-fixture.ts
      provides: Governed create, verification, and exact cleanup lifecycle for declared records.
    - path: apps/web/lib/server/programme-records.ts
      provides: Fixture-only governed read boundary with no ordinary-seed fallback.
  key_links:
    - from: apps/web/lib/server/e2e-programme-fixture.ts
      to: apps/web/lib/server/programme-records.ts
      via: saveProgrammeRecord, getGovernedProgrammes, and deleteProgrammeRecord
      pattern: "saveProgrammeRecord|deleteProgrammeRecord|getGovernedProgrammes"
---

<objective>
Close the governed-fixture boundary that invalidates the browser release evidence.

Purpose: G-06-01 blocks OPS-04 and PROD-04 because the existing lifecycle bypasses governed programme operations and fixture reads retain ordinary seeds.
Output: A tested server-only fixture configuration seam plus a governed create/read/delete lifecycle whose fixture-mode catalogue contains only its declared generated programmes.
</objective>

<execution_context>
@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-end-to-end-hardening-and-release-readiness/06-VERIFICATION.md
@.planning/phases/06-end-to-end-hardening-and-release-readiness/06-02-SUMMARY.md
@.planning/phases/06-end-to-end-hardening-and-release-readiness/06-05-SUMMARY.md
@apps/web/lib/server/e2e-programme-fixture.ts
@apps/web/lib/server/programme-records.ts
@apps/web/__tests__/lib/server/e2e-programme-fixture.test.ts
</context>

<interfaces>
From `apps/web/lib/server/programme-records.ts`:
- `saveProgrammeRecord(userId: string, programme: Programme)` persists one audited governed programme revision.
- `deleteProgrammeRecord(userId: string, programmeId: string)` removes one audited governed programme record.
- `getGovernedProgrammes()` is the catalogue consumed by discovery and recommendations.

From `apps/web/lib/server/e2e-programme-fixture.ts`:
- `createE2eProgrammeFixture(fixtureId: string): Programme[]` is the server-derived declared fixture set.
- `createAndVerifyE2eFixture()`, `verifyE2eFixture()`, and `cleanupE2eFixture()` are the fixed lifecycle operations used by the protected supervisor.
</interfaces>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Route one generated fixture set through the governed catalogue end to end</name>
  <files>apps/web/lib/server/e2e-fixture-config.ts, apps/web/lib/server/e2e-programme-fixture.ts, apps/web/lib/server/programme-records.ts, apps/web/__tests__/lib/server/e2e-programme-fixture.test.ts</files>
  <behavior>
    - G-06-01 / D-02: with a valid non-production fixture ID, create creates each declared record serially with `saveProgrammeRecord`, verification reads it with `getGovernedProgrammes`, and cleanup removes only those declared IDs with `deleteProgrammeRecord`.
    - G-06-01 / D-02: fixture-mode governed reads return the published declared generated records only; an ordinary seed programme and an unrelated persisted programme cannot appear before or after verification.
    - G-06-01 / D-03: after cleanup, the fixture-mode governed catalogue is empty rather than falling through to the seed catalogue, while ordinary non-fixture catalogue behavior remains unchanged.
    - Production and invalid fixture configuration continue to deny lifecycle activation before programme persistence calls.
  </behavior>
  <action>Start with failing Jest cases in the existing in-memory-store suite for governed create/read/delete invocation and seed exclusion. Extract the enabled fixture ID predicate into `e2e-fixture-config.ts` so `e2e-programme-fixture.ts` and `programme-records.ts` can share it without a module-initialization cycle. Replace the lifecycle's direct conditional-mutation helpers with sequential `saveProgrammeRecord` and `deleteProgrammeRecord` calls using its server-derived fixture actor and generated records. In the fixture branch of `getGovernedProgrammes`, derive the allowed IDs from the server-owned declared fixture set and return only matching published persisted records; keep the normal merge with seeds for non-fixture operation. Use lazy imports only where needed to preserve the established server-only dependency direction. Assert exact declared IDs, audited cleanup, exclusion of both seed and unrelated records, and empty fixture-mode results after cleanup. Do not add a browser-controlled catalogue selector, raw adapter/Blob/JSON access, production enablement, personal data, or any change to normal non-fixture discovery. This implements G-06-01 and D-02/D-03.</action>
  <verify>
    <automated>pnpm --filter @scholar-scout/web test --runInBand __tests__/lib/server/e2e-programme-fixture.test.ts</automated>
  </verify>
  <done>The lifecycle reaches governed save/read/delete operations only, and its enabled fixture catalogue cannot include ordinary seed or unrelated persisted programmes.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| fixture lifecycle -> governed programme operations | Only server-derived fixture records may cross into programme persistence. |
| governed catalogue -> browser journey | Fixture mode must not leak ordinary catalogue or unrelated persisted records into release evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-20 | Tampering | fixture record lifecycle | high | mitigate | Replace direct persistence mutation with audited governed save/delete operations and assert the calls in a stateful test. |
| T-06-21 | Information Disclosure | fixture catalogue | high | mitigate | Fixture-mode read filtering permits only declared generated published IDs and tests reject seed/unrelated records. |
| T-06-22 | Elevation of Privilege | fixture configuration | high | mitigate | Preserve server-only non-production configuration validation before lifecycle persistence. |
</threat_model>

<verification>Run the focused fixture suite, then rerun the lifecycle/route focused suites before attempting any Preview rehearsal.</verification>
<success_criteria>G-06-01 is closed by an executable governed programme boundary, not merely by generated labels or local fixture state.</success_criteria>
<output>Create `.planning/phases/06-end-to-end-hardening-and-release-readiness/06-08-SUMMARY.md` when done.</output>
