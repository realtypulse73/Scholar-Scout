---
phase: 06-end-to-end-hardening-and-release-readiness
plan: 11
type: execute
wave: 3
depends_on: [06-10]
files_modified:
  - .planning/phases/06-end-to-end-hardening-and-release-readiness/06-11-REHEARSAL-EVIDENCE.md
  - .planning/phases/06-end-to-end-hardening-and-release-readiness/06-VERIFICATION.md
autonomous: true
gap_closure: true
gap_ids: [G-06-02, G-06-03, G-06-04, G-06-05]
requirements: [OPS-04, PROD-04]
must_haves:
  truths:
    - "The governed fixture persists and removes generated programmes through the catalogue boundary, and fixture-mode discovery exposes no seed fallback."
    - "The candidate-quality lane proves immutable install, root pnpm test, lint, typecheck, and build before the later lanes."
    - "One immutable candidate has independently validated passed local-browser, protected Preview-browser, and Preview-outage/restoration records, with a five-lane aggregate."
    - "The Phase 6 verification report is based on current source checks plus retained workflow evidence, not on superseded failure evidence."
  artifacts:
    - path: .planning/phases/06-end-to-end-hardening-and-release-readiness/06-11-REHEARSAL-EVIDENCE.md
      provides: Scrubbed, candidate-bound verification record for the successful authorized rehearsal.
    - path: .planning/phases/06-end-to-end-hardening-and-release-readiness/06-VERIFICATION.md
      provides: Re-verified Phase 6 gap status and requirement coverage.
  key_links:
    - from: GitHub Actions run 35355815777 artifact prelaunch-rehearsal
      to: scripts/prelaunch-rehearsal.mjs
      via: Existing aggregate-only record schema validation for commit 089d969368e597c368fe7fa50856a092937fa457.
      pattern: aggregateReleaseRecords
    - from: apps/web/lib/server/e2e-programme-fixture.ts
      to: apps/web/lib/server/programme-records.ts
      via: saveProgrammeRecord, getGovernedProgrammes, and deleteProgrammeRecord.
      pattern: "saveProgrammeRecord|deleteProgrammeRecord|getGovernedProgrammes"
---

<objective>
Close only the remaining Phase 6 verification-record gap by independently validating the successful candidate rehearsal artifact, then re-running the Phase verifier against a clean detached checkout of the corrected source and retained evidence.

Purpose: Plan 06-08 corrected the governed fixture boundary and Plan 06-09 corrected candidate-quality command parity. Authorized GitHub Actions run 35355815777 subsequently passed all five rehearsal lanes for candidate 089d969368e597c368fe7fa50856a092937fa457, but the prior verification report predates that proof. This plan records and re-evaluates the evidence without redeploying, changing secrets, modifying application behavior, or touching Production.
Output: One scrubbed rehearsal-evidence note and a current Phase 6 verification report that either closes the real gaps or names a newly observed, specific remaining gap.
</objective>

<execution_context>
@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-end-to-end-hardening-and-release-readiness/06-CONTEXT.md
@.planning/phases/06-end-to-end-hardening-and-release-readiness/06-VERIFICATION.md
@.planning/phases/06-end-to-end-hardening-and-release-readiness/06-08-SUMMARY.md
@.planning/phases/06-end-to-end-hardening-and-release-readiness/06-09-SUMMARY.md
@.planning/phases/06-end-to-end-hardening-and-release-readiness/06-10-PLAN.md
@apps/web/lib/server/e2e-programme-fixture.ts
@apps/web/lib/server/programme-records.ts
@scripts/prelaunch-rehearsal.mjs
@scripts/test-production-tooling.mjs
@.github/workflows/prelaunch-rehearsal.yml
@docs/production-release-runbook.md
@docs/prelaunch-evidence-template.md
</context>

<interfaces>
From GitHub Actions run `35355815777`:
- Workflow: `ScholarScout Prelaunch Rehearsal`; status: completed; conclusion: success.
- Candidate branch: `codex/phase6-phase5-integration`; candidate SHA: `089d969368e597c368fe7fa50856a092937fa457`.
- Artifact name: `prelaunch-rehearsal`; the passed job includes local proof, protected Preview browser proof, separate Preview outage/restoration proof, and aggregate steps.

From `scripts/prelaunch-rehearsal.mjs`:
- `--release-gate --aggregate-only --candidate-commit <sha> --output-dir <dir> --preview-browser-record <path> --preview-outage-record <path>` validates the five candidate-bound record schemas and produces an aggregate only when all lanes pass.
- Record fields are constrained to safe candidate, target/artifact, UTC, command, outcome, and safe failure-detail metadata.
</interfaces>

## Source Audit

SOURCE | ID | Feature/requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | — | Production-like release readiness with high-risk and end-to-end proof | 06-11 | COVERED | Re-verifies the actual candidate proof after source and external-lane corrections.
REQ | OPS-04 | Automated high-risk behavior and a minimal end-to-end release check | 06-11 | COVERED | Re-runs governed-fixture, lifecycle, tooling, and aggregate-record checks.
REQ | PROD-04 | Discovery, onboarding, and recommendation journey remains functional | 06-11 | COVERED | Validates the successful owned local and protected Preview student-journey evidence for the same candidate.
RESEARCH | — | Generated isolated data, Preview-only proof, scrubbed distinct evidence | 06-11 | COVERED | Uses retained Preview-only workflow artifact; no new deployment, configuration, or production action.
CONTEXT | D-01 to D-04 | Student tracer and observable transition contract | 06-11 | COVERED | Confirms the local and protected Preview journey records were produced by the existing student-only tracer.
CONTEXT | D-05 to D-06 | Deterministic Preview proof and safe evidence | 06-11 | COVERED | Validates candidate binding and records only allowed metadata from the artifact.
CONTEXT | D-07 to D-08 | Failure-path coverage and isolated restored outage proof | 06-11 | COVERED | Re-runs lower-level coverage and validates the passed distinct outage/restoration record.
CONTEXT | D-09 to D-10 | Fail-closed release gate and distinct evidence classes | 06-11 | COVERED | Validates ordered quality commands and five separate same-candidate records before verification is revised.

<tasks>

<task type="tracer">
  <name>Task 1: Validate and retain the passed five-lane rehearsal evidence</name>
  <precondition>Read-only GitHub access can retrieve run 35355815777 and its `prelaunch-rehearsal` artifact, and the executor can create and later remove a new OS-temporary Git worktree. All external workflow and artifact evidence is bound to SHA `089d969368e597c368fe7fa50856a092937fa457`, not to the documentation worktree HEAD.</precondition>
  <files>.planning/phases/06-end-to-end-hardening-and-release-readiness/06-11-REHEARSAL-EVIDENCE.md</files>
  <action>Create one unique OS-temporary parent directory with separate `candidate-worktree` and `artifact` children. Use `git worktree add --detach &lt;candidate-worktree&gt; 089d969368e597c368fe7fa50856a092937fa457`, immediately require `git -C &lt;candidate-worktree&gt; rev-parse HEAD` to equal that SHA and `git -C &lt;candidate-worktree&gt; status --porcelain` to be empty, then run every source read and runtime command in this task exclusively from that detached candidate directory. Before the test commands, run `corepack enable` and `pnpm install --frozen-lockfile --ignore-scripts` in that detached candidate directory; retain only their command names and pass/fail results in the scrubbed note. Never execute a source read or runtime check from the existing phase worktree, and do not alter, reset, clean, stash, or otherwise modify its user changes. Use `gh run view 35355815777 --repo realtypulse73/Scholar-Scout` with JSON fields for conclusion, head SHA, timestamps, URL, and job steps. Require completed success, the stated immutable SHA, and passed steps for candidate checkout verification, local proof, protected Preview browser proof, separate Preview outage/restoration proof, and aggregation. Download only the named `prelaunch-rehearsal` artifact to the new temporary `artifact` directory with `gh run download 35355815777 --repo realtypulse73/Scholar-Scout --name prelaunch-rehearsal --dir &lt;artifact&gt;`, never to a tracked report path. From the detached candidate worktree, run the existing aggregate-only release-gate command against that temporary artifact directory with candidate `089d969368e597c368fe7fa50856a092937fa457` and its two Preview-record paths; it must validate candidate-quality, high-risk, local-browser, preview-browser, and preview-outage as independently passed records for that one candidate. Create the durable scrubbed evidence note only in the active documentation worktree and make every source-derived conclusion name candidate SHA `089d969368e597c368fe7fa50856a092937fa457`. The note explicitly lists each of those five lane names with its passed result, the shared candidate SHA, workflow UTC start and completion bounds, GitHub run link and each relevant job link, artifact name `prelaunch-rehearsal`, the aggregate-validation command, and its passed result. Do not copy deployment configuration, fixture identifiers, capabilities, storage paths, request material, cookies, tokens, student content, raw artifact files, or temporary paths. Use Windows PowerShell `try`/`finally` cleanup so that the exact temporary candidate worktree is removed with `git worktree remove --force`, verify it is absent from `git worktree list`, and, only when `Test-Path -LiteralPath &lt;exact-temporary-parent&gt;` is true, remove that exact parent using `Remove-Item -LiteralPath &lt;exact-temporary-parent&gt; -Recurse -Force`. Commit this documentation-only task separately as `docs(06-11): retain verified rehearsal evidence`. This implements D-01 through D-06 and D-08 through D-10.</action>
  <verify>
    <automated>$tempParent = Join-Path ([System.IO.Path]::GetTempPath()) ("scholarscout-06-11-" + [guid]::NewGuid().ToString('N')); $candidateWorktree = Join-Path $tempParent 'candidate-worktree'; $artifactDirectory = Join-Path $tempParent 'artifact'; $exitCode = 1; [System.IO.Directory]::CreateDirectory($tempParent) | Out-Null; try { git worktree add --detach $candidateWorktree 089d969368e597c368fe7fa50856a092937fa457; if ($LASTEXITCODE -ne 0) { throw 'candidate worktree creation failed' }; if ((git -C $candidateWorktree rev-parse HEAD).Trim() -ne '089d969368e597c368fe7fa50856a092937fa457') { throw 'candidate SHA mismatch' }; if ((git -C $candidateWorktree status --porcelain)) { throw 'candidate worktree is not clean' }; Push-Location $candidateWorktree; try { corepack enable; if ($LASTEXITCODE -ne 0) { throw 'corepack enable failed' }; pnpm install --frozen-lockfile --ignore-scripts; if ($LASTEXITCODE -ne 0) { throw 'immutable install failed' }; $run = gh run view 35355815777 --repo realtypulse73/Scholar-Scout --json conclusion,headSha,status,url,jobs | ConvertFrom-Json; if ($LASTEXITCODE -ne 0 -or $run.status -ne 'completed' -or $run.conclusion -ne 'success' -or $run.headSha -ne '089d969368e597c368fe7fa50856a092937fa457') { throw 'workflow metadata validation failed' }; $requiredSteps = @('Verify candidate checkout', 'Run candidate quality, high-risk, and local browser proof', 'Protected Preview browser proof (maintainer-owned runner)', 'Separate Preview outage and restoration proof (maintainer-owned runner)', 'Aggregate candidate release rehearsal'); foreach ($requiredStep in $requiredSteps) { if (-not @($run.jobs.steps | Where-Object { $_.name -eq $requiredStep -and $_.conclusion -eq 'success' }).Count) { throw "missing passed workflow step: $requiredStep" } }; gh run download 35355815777 --repo realtypulse73/Scholar-Scout --name prelaunch-rehearsal --dir $artifactDirectory; if ($LASTEXITCODE -ne 0) { throw 'artifact download failed' }; pnpm run rehearse:prelaunch -- --release-gate --aggregate-only --candidate-commit 089d969368e597c368fe7fa50856a092937fa457 --output-dir $artifactDirectory --preview-browser-record (Join-Path $artifactDirectory 'preview-browser.json') --preview-outage-record (Join-Path $artifactDirectory 'preview-outage.json'); if ($LASTEXITCODE -ne 0) { throw 'aggregate validation failed' }; $exitCode = 0 } finally { Pop-Location } } finally { if (Test-Path -LiteralPath $candidateWorktree) { git worktree remove --force $candidateWorktree; if ($LASTEXITCODE -ne 0) { cmd /d /c rd /s /q "\\?\$candidateWorktree"; git worktree prune } }; if (git worktree list | Select-String -SimpleMatch $candidateWorktree) { throw 'candidate worktree remains registered' }; if (Test-Path -LiteralPath $tempParent) { cmd /d /c rd /s /q "\\?\$tempParent" } }; exit $exitCode</automated>
  </verify>
  <done>The retained note durably ties the successful workflow to candidate `089d969368e597c368fe7fa50856a092937fa457` using only scrubbed, allowed fields: UTC workflow bounds, run/job links, artifact name, each of candidate-quality, high-risk, local-browser, preview-browser, and preview-outage marked passed, plus the passed aggregate-validation result. That runtime validation runs only in a clean detached checkout at the exact SHA, and its exact temporary worktree and parent directory are removed afterward without changing the existing phase worktree.</done>
</task>

<task type="auto">
  <name>Task 2: Re-run Phase 6 source checks and refresh formal verification</name>
  <precondition>The scrubbed 06-11 rehearsal-evidence note exists, records a successful five-lane aggregate for candidate 089d969368e597c368fe7fa50856a092937fa457, and Task 1 removed its temporary worktree. A new clean detached temporary worktree at that exact SHA can be created for this task's source inspections and runtime commands.</precondition>
  <files>.planning/phases/06-end-to-end-hardening-and-release-readiness/06-VERIFICATION.md</files>
  <action>Create a new unique OS-temporary parent directory and detached `candidate-worktree` with `git worktree add --detach &lt;candidate-worktree&gt; 089d969368e597c368fe7fa50856a092937fa457`. Before every source inspection or runtime command, require that worktree's `rev-parse HEAD` to equal the exact candidate SHA and its porcelain status to be empty. From that detached candidate directory only, run `corepack enable`, then `pnpm install --frozen-lockfile --ignore-scripts`, retaining only safe command/result metadata in the active-worktree evidence. Run the focused governed-fixture and internal lifecycle route tests, including `scripts/run-preview-outage-rehearsal.test.mjs`, the Node lifecycle/protection/Preview tracer tests, and `pnpm test:production-tooling` from the detached candidate directory only. Invoke the Phase 6 verifier so its source reads and every source/runtime check use that same detached candidate directory; write only the resulting documentation report in the active phase worktree. The active documentation worktree may create the 06-11 evidence note and may update only `06-VERIFICATION.md`; it must not be used for source reads or runtime commands. Replace the obsolete conclusions in `06-VERIFICATION.md` with a dated re-verification, and identify candidate SHA `089d969368e597c368fe7fa50856a092937fa457` in every source-derived conclusion. The new report must inspect the corrected fixture source rather than relying on the original direct-mutation finding: fixture persistence must be through `saveProgrammeRecord` and `deleteProgrammeRecord`, while fixture-mode governed reads must return only declared published generated records. It must cite the recorded artifact validation for the local, protected Preview, separate outage/restoration, and aggregate lanes, and independently confirm the root `pnpm test` candidate-quality command. Preserve the earlier report as historical context only through its revision history; do not rewrite requirements, acceptance criteria, or evidence to make a failure disappear. If any source check, artifact validation, candidate binding, or verifier truth still fails, retain `gaps_found` with the exact new reason and stop; do not run another rehearsal or modify app source in this plan. Use a PowerShell `try`/`finally` procedure: remove the exact detached worktree with `git worktree remove --force`, verify it is absent from `git worktree list`, and only when `Test-Path -LiteralPath &lt;exact-temporary-parent&gt;` is true, use `Remove-Item -LiteralPath &lt;exact-temporary-parent&gt; -Recurse -Force`. Do not alter, reset, clean, stash, or otherwise modify existing worktree/user changes. Commit the verification-only task separately as `docs(06-11): reverify phase six release evidence`. This implements D-01 through D-10 and closes OPS-04/PROD-04 only when the verifier finds all current required truths supported.</action>
  <verify>
    <automated>$tempParent = Join-Path ([System.IO.Path]::GetTempPath()) ("scholarscout-06-11-" + [guid]::NewGuid().ToString('N')); $candidateWorktree = Join-Path $tempParent 'candidate-worktree'; $exitCode = 1; New-Item -ItemType Directory -LiteralPath $tempParent | Out-Null; try { git worktree add --detach $candidateWorktree 089d969368e597c368fe7fa50856a092937fa457; if ($LASTEXITCODE -ne 0) { throw 'candidate worktree creation failed' }; if ((git -C $candidateWorktree rev-parse HEAD).Trim() -ne '089d969368e597c368fe7fa50856a092937fa457') { throw 'candidate SHA mismatch' }; if ((git -C $candidateWorktree status --porcelain)) { throw 'candidate worktree is not clean' }; Push-Location $candidateWorktree; try { corepack enable; if ($LASTEXITCODE -ne 0) { throw 'corepack enable failed' }; pnpm install --frozen-lockfile --ignore-scripts; if ($LASTEXITCODE -ne 0) { throw 'immutable install failed' }; pnpm --filter @scholar-scout/web test --runInBand __tests__/lib/server/e2e-programme-fixture.test.ts __tests__/app/api/internal/e2e-fixture/route.test.ts; if ($LASTEXITCODE -ne 0) { throw 'fixture route tests failed' }; node --test scripts/e2e-fixture-lifecycle.test.mjs scripts/preview-deployment-protection.test.mjs scripts/run-preview-release-tracer.test.mjs scripts/run-preview-outage-rehearsal.test.mjs; if ($LASTEXITCODE -ne 0) { throw 'lifecycle and Preview tracer tests failed' }; pnpm test:production-tooling; if ($LASTEXITCODE -ne 0) { throw 'production tooling tests failed' }; $exitCode = 0 } finally { Pop-Location } } finally { if (Test-Path -LiteralPath $candidateWorktree) { git worktree remove --force $candidateWorktree; if ($LASTEXITCODE -ne 0) { throw 'candidate worktree cleanup failed' } }; if (git worktree list | Select-String -SimpleMatch $candidateWorktree) { throw 'candidate worktree remains registered' }; if (Test-Path -LiteralPath $tempParent) { Remove-Item -LiteralPath $tempParent -Recurse -Force } }; exit $exitCode</automated>
  </verify>
  <done>The formal verification report is current and evidence-backed: it records the corrected governed fixture boundary, root-test quality sequence, and the successful candidate-bound five-lane rehearsal, or it names the one exact remaining blocker without claiming Phase 6 is ready. Every source inspection and runtime test ran from a clean detached checkout at `089d969368e597c368fe7fa50856a092937fa457`, which was removed afterward without changing the existing phase worktree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub Actions artifact -> local verifier | External artifact must be tied to the intended workflow, job, and immutable candidate before it can support a release conclusion. |
| temporary artifact directory -> planning evidence | Raw artifact material may contain broader diagnostics and must not be copied into tracked evidence. |
| prior verification report -> revised report | Historical failures must not be silently erased; current conclusions need fresh source and artifact proof. |
| Preview rehearsal -> Production release decision | Preview evidence is a release prerequisite but is not a Production deployment, promotion, alias, or smoke result. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-31 | Tampering | workflow artifact provenance | high | mitigate | Check workflow conclusion, candidate SHA, required passed job steps, and record schemas before evidence is retained. |
| T-06-32 | Information Disclosure | downloaded rehearsal artifact | high | mitigate | Download to one temporary directory, create only a scrubbed note, and remove the exact temporary directory after validation. |
| T-06-33 | Repudiation | phase verification status | medium | mitigate | Include run/job links, UTC metadata, command outcomes, and candidate SHA in the safe evidence note and re-verification. |
| T-06-34 | Elevation of Privilege | external deployment controls | high | accept | This plan is read-only with respect to GitHub/Vercel configuration and performs no secret, environment, deployment, promotion, or alias mutation. |
| T-06-35 | Tampering | governed fixture data boundary | high | mitigate | Re-run fixture, lifecycle, and route tests and inspect the current governed persistence/read links before closing OPS-04 or PROD-04. |
</threat_model>

<verification>
Validate the successful workflow metadata and downloaded artifact with the existing aggregate checker, run the focused source/tooling commands, then require a fresh Phase 6 verifier report. A failed check is a new evidence-backed blocker and ends this plan without a new rehearsal.
</verification>

<success_criteria>
The former Phase 6 gaps are closed only when one immutable candidate has independently validated safe five-lane rehearsal evidence and fresh source checks prove the governed fixture and candidate-quality contracts. The revised verifier either marks OPS-04 and PROD-04 supported or records the exact remaining discrepancy; no Production system is changed.
</success_criteria>

<output>
Create `.planning/phases/06-end-to-end-hardening-and-release-readiness/06-11-SUMMARY.md` when done.
</output>
