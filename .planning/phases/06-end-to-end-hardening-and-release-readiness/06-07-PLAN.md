---
phase: 06-end-to-end-hardening-and-release-readiness
plan: 07
type: execute
wave: 5
depends_on: [06-06]
files_modified: [package.json, scripts/prelaunch-rehearsal.mjs, scripts/test-production-tooling.mjs, .github/workflows/prelaunch-rehearsal.yml, docs/prelaunch-evidence-template.md, docs/production-release-runbook.md]
autonomous: false
requirements: [OPS-04, PROD-04]
user_setup:
  - service: Vercel Preview and deployment protection
    why: D-05/D-08 require an authorized maintainer to configure Preview-only adapter/lifecycle variables and runner-only bypass material, then dispatch the rehearsal.
must_haves:
  truths:
    - "D-08: A separately deployed one-off Preview outage proof safely returns 503 before writes or disclosure, cleans its own fixture, and restores the base Preview configuration without replacing the Preview tracer."
    - "D-09: A candidate-quality lane records a successful immutable `pnpm install --frozen-lockfile --ignore-scripts`, then successful `pnpm test`, `pnpm run lint`, `pnpm run typecheck`, and `pnpm run build`; the rehearsal then executes Plan 06-04's high-risk suite and Plan 06-03's owned local browser tracer, each creating its own candidate-bound scrubbed record before either Preview lane. Missing or failed candidate-quality, high-risk, local-browser, protected Preview-browser, or Preview-outage proof fails the release gate."
    - "D-10: Release evidence records distinct scrubbed local-browser, protected Preview-browser, and Preview-outage outcomes plus their approved artifact/deployment identifiers or links; real production evidence remains a separate production-runbook category."
  artifacts:
    - path: scripts/prelaunch-rehearsal.mjs
      provides: Candidate-bound execution and fail-closed aggregation of independently recorded candidate-quality, Plan 06-04 high-risk, Plan 06-03 local-browser, protected Preview-browser, and Preview-outage records.
    - path: .github/workflows/prelaunch-rehearsal.yml
      provides: Manually dispatched candidate Preview tracer and isolated outage rehearsal orchestration.
    - path: docs/prelaunch-evidence-template.md
      provides: Maintainer-facing distinct browser and deployment/artifact evidence fields.
    - path: docs/production-release-runbook.md
      provides: Preview rehearsal/outage/restoration procedure separate from production evidence.
  key_links:
    - from: .github/workflows/prelaunch-rehearsal.yml
      to: scripts/run-preview-release-tracer.mjs
      via: candidate-bound protected Preview dispatch with runner-only configuration
      pattern: "run-preview-release-tracer"
    - from: scripts/prelaunch-rehearsal.mjs
      to: docs/prelaunch-evidence-template.md
      via: schema-aligned scrubbed required-lane records and approved identifiers/links
      pattern: "candidate-quality|high-risk|local-browser|preview-browser|preview-outage"
---
<objective>
Turn the proven protected Preview tracer into a release rehearsal with a distinct restored outage proof and an evidence gate that does not conflate Preview results with production evidence.

Purpose: D-08 through D-10 require independently failing proof lanes and a maintainer record that identifies approved artifacts/deployments safely.
Output: Rehearsal workflow, release-gate aggregation, outage/restoration automation, evidence template/runbook updates, and maintainer checkpoint.
</objective>
<execution_context>
@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/06-end-to-end-hardening-and-release-readiness/06-CONTEXT.md
@.planning/phases/06-end-to-end-hardening-and-release-readiness/06-RESEARCH.md
@.planning/phases/06-end-to-end-hardening-and-release-readiness/06-06-SUMMARY.md
@docs/phase-5-preview-outage-uat.md
@docs/prelaunch-evidence-template.md
@docs/production-release-runbook.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Run and aggregate the immutable candidate-quality lane with distinct release evidence</name>
  <reversibility rating="costly">D-09 makes each proof lane a release condition, and D-10 fixes its distinct evidence classification.</reversibility>
  <files>package.json, scripts/prelaunch-rehearsal.mjs, scripts/test-production-tooling.mjs, docs/prelaunch-evidence-template.md, docs/production-release-runbook.md</files>
  <behavior>
    - D-09: The candidate-quality lane runs and records, in order, `pnpm install --frozen-lockfile --ignore-scripts`, `pnpm test`, `pnpm run lint`, `pnpm run typecheck`, and `pnpm run build` against the candidate; a missing, failed, or out-of-order command exits nonzero before later release lanes can pass.
    - D-09: After candidate quality succeeds, the rehearsal executes all four Plan 06-04 high-risk commands and emits one distinct scrubbed candidate-bound high-risk record containing the candidate commit, UTC, exact suite command list, pass/fail result, and safe error category.
    - D-09: After the high-risk suite succeeds, the rehearsal executes Plan 06-03's owned local command `node scripts/run-e2e-fixture.mjs --spec apps/web/e2e/student-release-journey.spec.ts --project chromium` and emits one distinct scrubbed candidate-bound local-browser record containing the candidate commit, UTC, command, pass/fail result, and safe error category.
    - Missing/failed candidate-quality, high-risk, local-browser, protected Preview-browser, or Preview-outage record exits nonzero; one record cannot satisfy another lane.
    - D-06/D-10: Each browser record permits only commit, target URL/ID, UTC, command, pass/fail, safe category, and approved artifact/deployment identifier or link; no secret, cookie, environment value, export, fixture identifier, storage detail, or student content is accepted.
    - The evidence template/runbook contain explicit local-browser, protected Preview-browser, and Preview-outage/restoration outcome plus artifact/deployment ID/link fields, and say these supplement rather than replace real production protected-main/deployment/post-deploy-smoke evidence.
  </behavior>
  <action>Extend production-tooling tests and prelaunch aggregation with five independently required records: candidate-quality, high-risk, local-browser, protected Preview-browser, and Preview-outage. Implement the candidate-quality lane as its own candidate-bound runner: execute and record the immutable install command `pnpm install --frozen-lockfile --ignore-scripts`, then `pnpm test`, `pnpm run lint`, `pnpm run typecheck`, and `pnpm run build` in that order. On its success, explicitly execute Plan 06-04's four high-risk commands — the two focused web route-test commands, the webhook-runner test command, and the HTTP data-service test command recorded in 06-04 — and create a separate high-risk record. Then explicitly execute Plan 06-03's owned local Chromium command `node scripts/run-e2e-fixture.mjs --spec apps/web/e2e/student-release-journey.spec.ts --project chromium` and create a separate local-browser record. Each record must bind to the same candidate commit and retain UTC, exact command or ordered suite-command list, pass/fail result, and safe failure category; fail before any protected Preview or outage lane when either execution fails, is absent, or belongs to another candidate. Fail aggregation closed when any required record is absent, failed, or not tied to the candidate; candidate-quality, high-risk, and local-browser evidence cannot satisfy each other or either Preview lane. Preserve restricted raw local diagnostics but permit only the D-06 safe fields in releasable records; no local result can satisfy protected Preview/outage proof. Update `docs/prelaunch-evidence-template.md` with an explicit Candidate quality row containing the five command outcomes and a browser-validation table having separate Local browser, Protected Preview browser, and Preview outage/restoration rows, each with outcome and approved artifact/deployment ID/link fields. Update `docs/production-release-runbook.md` with the matching candidate-quality, high-risk, and local-browser execution procedure, scrubbed field contract, and explicit separation from existing real-production protected-main, production deployment/build-log, post-deploy smoke, and incident evidence. Do not add secrets, cookies, fixture values, student data, production mutation, or a waiver path. This implements D-06, D-09, and D-10.</action>
  <verify><automated>pnpm test:production-tooling</automated></verify>
  <done>The gate fails closed when any immutable install, test, lint, typecheck, build, or required-lane record is missing or failed; docs and generated records separately identify candidate quality, local browser, protected Preview browser, Preview outage/restoration, and real production evidence with only approved scrubbed identifiers/links.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Orchestrate the candidate Preview tracer and distinct restored outage rehearsal</name>
  <files>.github/workflows/prelaunch-rehearsal.yml, scripts/prelaunch-rehearsal.mjs, scripts/test-production-tooling.mjs</files>
  <behavior>
    - D-09: The manually dispatched workflow requires a successful candidate-quality record, then executes and records Plan 06-04's high-risk suite, then executes and records Plan 06-03's owned local browser tracer, before it dispatches or aggregates either Preview lane; Preview proof remains independent from these local records.
    - D-05: A manually dispatched workflow deploys the candidate as Preview only, invokes Plan 06-06's supervisor, and records its protected Preview-browser result independently.
    - D-08: A separate one-off outage Preview uses a fresh fixture/capability, proves safe 503/no-write/no-disclosure, requests exact server cleanup, restores base Preview configuration, and records an independent Preview-outage result.
    - Neither deployment is production, promoted, aliased, or a persistent/project-level Preview configuration change; metadata and cleanup are confirmed before aggregation.
  </behavior>
  <action>Create a manually dispatched `prelaunch-rehearsal` workflow that first invokes the Task 1 candidate-quality runner, then explicitly executes Plan 06-04's full high-risk API/webhook/persistence suite, then explicitly executes Plan 06-03's owned local Chromium tracer, producing and passing the three distinct candidate-bound records to fail-closed aggregation before the workflow dispatches any Preview work. Stop before deployment when any immutable quality command, Plan 06-04 suite command, or Plan 06-03 local tracer has no successful record for the candidate. Only then consume the proven Plan 06-06 supervisor instead of recreating browser protection or fixture lifecycle. Deploy the candidate only as protected Preview with existing server-selected durable-adapter configuration plus deployment-scoped fixture enablement/ID/capability; validate candidate commit/Preview metadata. Run one protected Preview tracer, then deploy a separate one-off outage Preview with fresh lifecycle scope and the existing D-08 failure injection. The outage proof must assert safe 503 before writes/disclosure, exact server cleanup, and base Preview configuration restoration. Reject a production target, promotion, alias, project-level setting mutation, failed cleanup/restoration, a candidate-quality/high-risk/local-browser record mismatch, or either missing Preview result. Test fake workflow/deployment clients for execution order, candidate-bound record fields, and independent result classification; do not print/persist bypass, capability, fixture, student, or storage values. This implements D-05, D-08, D-09, and D-10.</action>
  <verify><automated>pnpm test:production-tooling</automated></verify>
  <done>A candidate passes rehearsal only after candidate quality, executed Plan 06-04 high-risk proof, executed Plan 06-03 local-browser proof, protected Preview-tracer, and Preview-outage/restoration results are present, clean, scrubbed, candidate-bound, and distinct from production evidence.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Authorize and record the Preview release rehearsal</name>
  <what-built>D-05/D-08 protected Preview orchestration, separate outage/restoration proof, and D-09/D-10 evidence gate.</what-built>
  <how-to-verify>1. As an authorized Vercel maintainer, confirm Preview-only durable-adapter/lifecycle configuration and runner-only bypass material are available solely through authorized deployment controls; do not reveal values. 2. Dispatch the rehearsal for the candidate commit and confirm local-browser, protected Preview-browser, and Preview-outage/restoration results are separately recorded. 3. Confirm the Preview tracer runs the Plan 06-03 student journey after Plan 06-05 lifecycle provisioning and Plan 06-06 supervisor cleanup; confirm the separate outage Preview reports safe 503/no-write/no-disclosure, cleans its fixture, and restores base Preview configuration. 4. Confirm neither deployment was production, promoted, or aliased. 5. Populate the template with only commit, Preview URL/ID, UTC, command, result, safe category, and approved artifact/deployment IDs or links; retain protected-main/deployment/post-deploy-smoke evidence in its separate production section.</how-to-verify>
  <resume-signal>Type "approved" with scrubbed identifiers/links, or describe the failed assertion.</resume-signal>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| workflow -> protected Preview | Maintainer-owned orchestration may dispatch Preview-only work but cannot promote or mutate production. |
| outage Preview -> base Preview | Temporary fault configuration must be isolated, cleaned, and restored. |
| evidence -> release decision | Only scrubbed lane-specific identifiers/links are releasable; production proof remains a different authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-12 | Information Disclosure | release evidence | high | mitigate | Schema tests/template fields restrict outcomes to D-06 safe fields and approved artifact/deployment identifiers/links. |
| T-06-13 | Tampering | Preview outage/restoration | high | mitigate | Separate Preview, fresh lifecycle scope, exact cleanup, restoration assertion, and no alias/promotion/project-setting mutation. |
| T-06-14 | Repudiation | release gate | medium | mitigate | Candidate-quality, high-risk, local-browser, protected Preview-browser, and Preview-outage records fail closed on missing/failed proof; candidate-quality records require immutable install, test, lint, typecheck, and build outcomes. |
| T-06-19 | Tampering | workflow composition | high | mitigate | Workflow invokes Plan 06-06 supervisor and rejects target/commit mismatch or persistent configuration mutation. |
</threat_model>
<verification>Run `pnpm test:production-tooling`, then complete the blocking authorized rehearsal. Confirm template/runbook distinguish three scrubbed browser/rehearsal outcomes and artifact/deployment identifiers/links from real production evidence.</verification>
<success_criteria>A release candidate is blocked until immutable clean-install quality, high-risk coverage, local browser proof, protected Preview tracer, and distinct restored Preview outage proof pass; maintainers can record outcomes safely without treating Preview evidence as production evidence.</success_criteria>
<output>Create `.planning/phases/06-end-to-end-hardening-and-release-readiness/06-07-SUMMARY.md` when done.</output>
