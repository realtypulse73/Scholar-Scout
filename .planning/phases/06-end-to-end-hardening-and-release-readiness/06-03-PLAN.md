---
phase: 06-end-to-end-hardening-and-release-readiness
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified: [package.json, pnpm-lock.yaml, playwright.config.ts, apps/web/e2e/student-release-journey.spec.ts, .github/workflows/ci.yml]
autonomous: true
requirements: [OPS-04, PROD-04]
must_haves:
  truths:
    - "D-01/D-03/D-04: One student completes discovery, onboarding, local shortlist persistence, recommendation, and one interactive simulation in Chromium; staff/community and Phase 999.1 remain deferred."
    - "D-02: The browser establishes its guest actor only through the context cookie jar by GET `/api/account/onboarding`, with no user ID or credential."
    - "D-05: Chromium runs serially through the owned HTTPS launcher and publishes bounded diagnostics."
  artifacts:
    - path: playwright.config.ts
      provides: Single-worker Chromium configuration for the launcher-owned HTTPS server.
    - path: apps/web/e2e/student-release-journey.spec.ts
      provides: Student-only cookie-jar release tracer.
  key_links:
    - from: apps/web/e2e/student-release-journey.spec.ts
      to: apps/web/app/api/account/onboarding/route.ts
      via: page.request GET shares the page context cookie jar
      pattern: "page\.request\.get\('/api/account/onboarding'"
    - from: .github/workflows/ci.yml
      to: scripts/run-e2e-fixture.mjs
      via: browser job owns the server/store rather than a reusable web server
      pattern: run-e2e-fixture
---
<objective>
Add the minimal Chromium release tracer and a distinct protected-main CI signal using only the owned local fixture and browser-context guest session.

Purpose: D-01 through D-05 require observable connected student behavior after stabilization without duplicating staff, community, failure matrix, or alternative simulation coverage.
Output: One deterministic Playwright journey and CI artifact lane.
</objective>
<execution_context>
@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/06-end-to-end-hardening-and-release-readiness/06-CONTEXT.md
@.planning/phases/06-end-to-end-hardening-and-release-readiness/06-RESEARCH.md
@.planning/phases/06-end-to-end-hardening-and-release-readiness/06-02-SUMMARY.md
@scripts/run-e2e-fixture.mjs
@apps/web/app/api/account/onboarding/route.ts
@apps/web/lib/server/student-actor.ts
@apps/web/components/onboarding/OnboardingWizard.tsx
@apps/web/components/shortlist/ShortlistButton.tsx
@apps/web/components/recommendations/RecommendationDashboard.tsx
@apps/web/components/simulation/SimulationPlayer.tsx
</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Run one cookie-jar-authenticated student release journey in Chromium</name>
  <precondition>Plan 06-01 records approval for the exact `@playwright/test` release, and Plan 06-02's launcher suite passes.</precondition>
  <reversibility rating="costly">D-01 fixes the student-only browser release contract; later role expansion changes fixture, authentication, and evidence contracts.</reversibility>
  <files>package.json, pnpm-lock.yaml, playwright.config.ts, apps/web/e2e/student-release-journey.spec.ts</files>
  <behavior>
    - First call `page.request.get('/api/account/onboarding')`; this context request receives the HttpOnly guest cookie and sends no identity/credential fields.
    - The same context completes `/onboarding`, then a second request observes generated profile data without reading/logging the cookie value.
    - Governed discovery renders; onboarding changes recommendations; existing shortlist UI survives reload; one `SimulationPlayer` route renders its result.
  </behavior>
  <action>Install only the Plan 06-01 approved Playwright release with pnpm. Configure one Chromium worker, retry traces, HTML reporting, bounded timeouts, and `ignoreHTTPSErrors` only for the launcher self-signed certificate. Build exactly one D-01 tracer: context-cookie bootstrap, normal discovery/onboarding UI, same-context profile observation, normal shortlist/reload proof, then one visible `SimulationPlayer` result. Assert transitions, never full ranking/catalogue snapshots. Do not add E2E headers, manually set cookies, browser IDs/credentials, staff/community steps, Phase 999.1 search, alternative simulation entry, external base URLs, or sensitive diagnostic output. This implements D-01 through D-05.</action>
  <verify><automated>node scripts/run-e2e-fixture.mjs --spec apps/web/e2e/student-release-journey.spec.ts --project chromium</automated></verify>
  <done>The shared context cookie jar, not caller-selected identity, carries one generated student through the complete release path.</done>
</task>
<task type="auto">
  <name>Task 2: Publish the owned browser release signal separately in CI</name>
  <files>.github/workflows/ci.yml</files>
  <action>Add a distinct Chromium job after frozen Corepack-pnpm install. Install Chromium, run only the tracer via `scripts/run-e2e-fixture.mjs`, and upload HTML/retry artifacts on every outcome with bounded retention. Fail if launcher, journey, or artifact fails; retain existing Jest/data-service/tooling jobs. Do not configure external base URL, data file, HTTP/Vercel adapter, or credentials. This implements D-05/D-09.</action>
  <verify><automated>node scripts/run-e2e-fixture.mjs --spec apps/web/e2e/student-release-journey.spec.ts --project chromium</automated></verify>
  <done>CI owns a failing, serialized browser gate and its private server/store.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Playwright context -> student UI/API | Only normal server guest-session logic may establish the actor. |
| CI artifacts -> maintainers | Traces/reports are restricted diagnostics, not release-note content. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-05 | Elevation of Privilege | journey scope | high | mitigate | Student-only guest context; no client identity/credential or role routes. |
| T-06-06 | Tampering | fixture state | high | mitigate | Single worker invokes Plan 06-02 launcher each run. |
| T-06-07 | Information Disclosure | reports/traces | medium | mitigate | Never print cookie values; raw diagnostics remain restricted CI artifacts. |
</threat_model>
<verification>Run the launcher command and verify CI invokes it with no external target/storage settings.</verification>
<success_criteria>The student-only journey is deterministic and cookie-context-authenticated; deferred scope remains outside it.</success_criteria>
<output>Create `.planning/phases/06-end-to-end-hardening-and-release-readiness/06-03-SUMMARY.md` when done.</output>
