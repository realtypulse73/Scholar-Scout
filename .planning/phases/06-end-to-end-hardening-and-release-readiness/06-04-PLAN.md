---
phase: 06-end-to-end-hardening-and-release-readiness
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/__tests__/api/advisor-chat.test.ts
  - apps/web/__tests__/api/account-guest-routes.test.ts
  - apps/web/__tests__/api/campus-notes.test.ts
  - apps/web/__tests__/api/peer-connections.test.ts
  - services/codex-webhook-runner/test/server.test.mjs
  - services/http-data-service/test/server.test.mjs
autonomous: true
requirements: [OPS-04]
must_haves:
  truths:
    - "D-07: High-risk API, webhook, and persistence failure contracts are proven in deterministic route/service integration tests."
    - "D-08: Provider and persistence failure paths fail closed without writes or provider-detail disclosure."
  artifacts:
    - path: apps/web/__tests__/api/advisor-chat.test.ts
      provides: Advisor unavailable/fallback boundary coverage.
    - path: apps/web/__tests__/api/campus-notes.test.ts
      provides: Preview-only community outage 503-before-write coverage.
    - path: services/codex-webhook-runner/test/server.test.mjs
      provides: Signed-webhook failure/no-side-effect integration coverage.
    - path: services/http-data-service/test/server.test.mjs
      provides: Real HTTP persistence failure/no-write coverage.
  key_links:
    - from: route and service tests
      to: API/webhook/data handlers
      via: real Request or ephemeral HTTP interactions and asserted persisted state
      pattern: "status|fetch|write"
---

<objective>
Close the high-risk lower-level failure coverage that the browser tracer intentionally does not duplicate.

Purpose: D-07/D-08 require deterministic proof that external-boundary failures are safe, non-writing, and non-disclosing.
Output: Focused route and real-service integration tests for failure contracts.
</objective>

<execution_context>
@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-end-to-end-hardening-and-release-readiness/06-CONTEXT.md
@.planning/phases/06-end-to-end-hardening-and-release-readiness/06-RESEARCH.md
@apps/web/__tests__/api/advisor-chat.test.ts
@apps/web/__tests__/api/account-guest-routes.test.ts
@services/codex-webhook-runner/test/server.test.mjs
@services/http-data-service/test/server.test.mjs
@docs/phase-5-preview-outage-uat.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Lock down student-route and provider failure outcomes</name>
  <files>apps/web/__tests__/api/advisor-chat.test.ts, apps/web/__tests__/api/account-guest-routes.test.ts</files>
  <behavior>
    - Advisor limiter/actor unavailability returns its safe unavailable category before provider access and records no account mutation.
    - Advisor provider fallback remains the documented safe response and does not disclose upstream/provider details.
    - Onboarding and shortlist persistence conflicts/failures expose only the stable reload category, preserve server-derived ownership, and do not return stored student state.
  </behavior>
  <action>Extend the established Jest route suites with stateful mocks/spies that assert call ordering and no-write behavior around unavailable external boundaries. Cover the representative user-facing recovery state required by D-07, but do not move provider/persistence failure testing into Playwright or broaden the student browser scope. Assert HTTP status and stable public JSON only; reject raw upstream error strings, provider identifiers, foreign records, and browser-supplied owner fields. This implements D-07 and D-08.</action>
  <verify><automated>pnpm --filter @scholar-scout/web test --runInBand __tests__/api/advisor-chat.test.ts __tests__/api/account-guest-routes.test.ts</automated></verify>
  <done>Route failures are deterministic, safe, and proven not to report a failed transition as successful.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the Preview-only community outage branches fail before writes</name>
  <files>apps/web/__tests__/api/campus-notes.test.ts, apps/web/__tests__/api/peer-connections.test.ts</files>
  <behavior>
    - D-08: with `VERCEL_ENV=preview` and `SCHOLARSCOUT_PREVIEW_COMMUNITY_RATE_LIMIT_OUTAGE=1`, campus-note and peer-inbox submission return their established safe 503 unavailable response before create calls.
    - The response discloses neither limiter/provider detail nor remaining count, and the created-note/inbox collections remain unchanged.
    - The same outage switch is ineffective outside Preview.
  </behavior>
  <action>Extend the established route tests using controlled environment and write spies to prove `reserveCommunitySubmission()` causes the exact existing Preview-only unavailable branch before the routes' create operations. Assert the safe response category and no record creation; reset environment/mocks after every case. Do not perform this failure injection through the student browser tracer or broaden D-01 scope. This implements D-07 and D-08.</action>
  <verify><automated>pnpm --filter @scholar-scout/web test --runInBand __tests__/api/campus-notes.test.ts __tests__/api/peer-connections.test.ts</automated></verify>
  <done>Repository coverage proves the exact Preview-only outage setting produces a safe 503/no-write/no-disclosure response before the external boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove webhook and HTTP persistence failures have no external side effects</name>
  <files>services/codex-webhook-runner/test/server.test.mjs, services/http-data-service/test/server.test.mjs</files>
  <behavior>
    - Missing/invalid webhook signature, oversized/malformed payload, unavailable outbound provider, and absent dispatch credential cause no outbound dispatch or record mutation.
    - Malformed/unavailable HTTP persistence reads/writes return the safe documented error and do not replace or corrupt the winning document.
    - Integration tests run through real ephemeral HTTP servers/files and restore temporary state in teardown.
  </behavior>
  <action>Add only gap-focused `node:test` cases using the existing in-process server, ephemeral port, temporary-directory, fetch-recorder, and cleanup patterns. Make each injected failure prove the response category and absence of external calls/writes; retain the existing signed-request, conditional-write, and cleanup behavior. Do not emit provider internals, tokens, raw documents, or logs into test output. This implements OPS-04 and D-07/D-08.</action>
  <verify><automated>pnpm --filter @scholar-scout/codex-webhook-runner test && pnpm --filter @scholar-scout/http-data-service test</automated></verify>
  <done>Webhook and persistence integration suites prove representative rejected/unavailable paths fail closed without a dispatch, unsafe write, or provider disclosure.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| untrusted HTTP/webhook request -> handlers | Invalid input must stop before writes and outbound work. |
| application -> provider/data service | Provider failures must neither leak internals nor cause misleading persistence. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-08 | Spoofing | webhook ingress | high | mitigate | Integration tests retain signature/credential denial before dispatch. |
| T-06-09 | Tampering | persistence failure paths | high | mitigate | Tests assert conditional/no-write behavior and preservation of the winning document. |
| T-06-10 | Information Disclosure | provider failures | medium | mitigate | Assert stable safe categories/responses and absence of upstream detail. |
</threat_model>

<verification>Run focused web route tests, the explicit Preview-only outage tests, and both service integration suites; full `pnpm test` remains the phase gate in Plan 06-05.</verification>
<success_criteria>OPS-04's high-risk failure proof is reliable at the API/webhook/data-service layer instead of being duplicated in browser automation.</success_criteria>
<output>Create `.planning/phases/06-end-to-end-hardening-and-release-readiness/06-04-SUMMARY.md` when done.</output>
