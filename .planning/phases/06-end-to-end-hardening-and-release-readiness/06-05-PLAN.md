---
phase: 06-end-to-end-hardening-and-release-readiness
plan: 05
type: execute
wave: 3
depends_on: [06-03, 06-04]
files_modified: [scripts/e2e-fixture-lifecycle.mjs, scripts/e2e-fixture-lifecycle.test.mjs, apps/web/app/api/internal/e2e-fixture/route.ts, apps/web/__tests__/app/api/internal/e2e-fixture/route.test.ts, apps/web/lib/server/e2e-programme-fixture.ts, apps/web/lib/server/programme-records.ts, apps/web/__tests__/lib/server/e2e-programme-fixture.test.ts]
autonomous: true
requirements: [OPS-04, PROD-04]
must_haves:
  truths:
    - "D-02/D-03/D-05: A runner invokes the fixed server lifecycle protocol against a fresh, isolated production-like Preview fixture/session with deterministic generated non-personal programme records through the governed catalogue boundary."
    - "D-05/D-06: Preview fixture lifecycle transport fails closed before adapter work when its capability or request shape is absent or invalid; no capability reaches browser configuration, diagnostics, or evidence."
  artifacts:
    - path: apps/web/app/api/internal/e2e-fixture/route.ts
      provides: Production-denied fixed lifecycle operations owned by the server adapter runtime.
  key_links:
    - from: scripts/e2e-fixture-lifecycle.mjs
      to: apps/web/app/api/internal/e2e-fixture/route.ts
      via: runner-only capability and fixed no-body POST/GET/DELETE lifecycle protocol
      pattern: "x-scholarscout-e2e-fixture-protocol"
---
<objective>
Prove the server-owned guarded Preview fixture lifecycle before the protected browser supervisor or release-evidence aggregation.

Purpose: D-02, D-03, D-05, and D-06 require a candidate-bound production-like fixture that is isolated, governed, and fail-closed without production access or capability exposure.
Output: Tested Preview lifecycle helper, server route, governed-programme operations, and lifecycle route tests.
</objective>
<execution_context>
@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/phases/06-end-to-end-hardening-and-release-readiness/06-CONTEXT.md
@.planning/phases/06-end-to-end-hardening-and-release-readiness/06-RESEARCH.md
@.planning/phases/06-end-to-end-hardening-and-release-readiness/06-03-SUMMARY.md
@.planning/phases/06-end-to-end-hardening-and-release-readiness/06-04-SUMMARY.md
@scripts/e2e-fixture-lifecycle.mjs
@apps/web/e2e/student-release-journey.spec.ts
</context>
<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Gate the governed Preview fixture lifecycle and exact cleanup</name>
  <files>scripts/e2e-fixture-lifecycle.mjs, scripts/e2e-fixture-lifecycle.test.mjs, scripts/run-preview-release-tracer.mjs, scripts/run-preview-release-tracer.test.mjs, apps/web/app/api/internal/e2e-fixture/route.ts, apps/web/__tests__/app/api/internal/e2e-fixture/route.test.ts, apps/web/lib/server/e2e-programme-fixture.ts, apps/web/lib/server/programme-records.ts, apps/web/__tests__/lib/server/e2e-programme-fixture.test.ts</files>
  <behavior>
    - D-02/D-05: Before browser navigation, a runner-only capability makes no-body POST create-and-verify then GET verify-only calls; after every terminal path it makes one awaited DELETE cleanup-only call to `/api/internal/e2e-fixture`.
    - The fixed transport carries the bearer capability and `x-scholarscout-e2e-fixture-protocol: lifecycle-v1`, accepts no body/query/caller-selected source/catalogue/record/storage input, returns only the created/verified/cleaned phase result, and is denied in production and to browser-shaped requests before adapter work.
    - D-03: Server-derived generated programmes appear in discovery and one post-onboarding recommendation without catalogue/rank snapshots; exact declared programme and bounded generated-student cleanup occurs after success, test failure, child crash, `SIGINT`, and `SIGTERM`.
  </behavior>
  <action>Extend the existing Plan 06-02 lifecycle seam rather than adding raw JSON/Blob operations. The server-only route derives deterministic non-personal descriptors from deployment configuration and invokes `saveProgrammeRecord`, `getGovernedProgrammes`, and `deleteProgrammeRecord` inside the adapter-owning runtime. Reject production, missing or incorrect capability, bodies, queries, browser-origin headers/cookies/fetch metadata, and caller-selected fixture/source/catalogue/record/storage fields before adapter access; add no CORS or browser control surface. The runner calls the exact lifecycle protocol, asserts declared generated programme use in discovery and a post-onboarding recommendation without rank-order coupling, and awaits one exact cleanup after every terminal path. Use fake clients and server tests to prove ordering, no seed fallback/raw persistence/browser fixture control, and cleanup after success, failure, crash, and signal. This implements D-02, D-03, D-05, and D-06.</action>
  <verify><automated>node --test scripts/e2e-fixture-lifecycle.test.mjs &amp;&amp; pnpm --filter @scholar-scout/web test --runInBand __tests__/app/api/internal/e2e-fixture/route.test.ts __tests__/lib/server/e2e-programme-fixture.test.ts</automated></verify>
  <done>The fixed lifecycle creates, verifies, exposes through the governed catalogue, and exactly cleans server-governed generated records without raw persistence access, production access, or a browser fixture-control surface.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| runner -> fixture lifecycle route | An opaque per-deployment capability reaches a fixed server lifecycle protocol; browser receives neither capability nor control surface. |
| lifecycle route -> governed catalogue | Server-owned descriptors are persisted and removed only through the governed catalogue boundary. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-13 | Tampering | Preview fixture | high | mitigate | Candidate-bound server-only configuration, fixed capability transport, and exact terminal-path cleanup. |
| T-06-17 | Tampering | governed fixture | high | mitigate | Route invokes governed catalogue operations only; tests deny seed fallback and raw persistence operations. |
| T-06-18 | Spoofing | lifecycle transport | high | mitigate | Production-denied fixed no-body protocol rejects unauthorized/browser-shaped requests before adapter work. |
</threat_model>
<verification>Run lifecycle and route tests. The later Preview supervisor invokes this lifecycle instead of recreating governed record operations.</verification>
<success_criteria>One isolated production-like fixture lifecycle safely creates and removes generated records through governed programme operations, preserving production, route-safety, no-capability, and no-raw-persistence boundaries.</success_criteria>
<output>Create `.planning/phases/06-end-to-end-hardening-and-release-readiness/06-05-SUMMARY.md` when done.</output>
