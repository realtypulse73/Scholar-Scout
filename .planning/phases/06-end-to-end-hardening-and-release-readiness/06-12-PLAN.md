---
phase: 06-end-to-end-hardening-and-release-readiness
plan: 12
type: execute
wave: 4
depends_on: [06-11]
files_modified:
  - .planning/phases/06-end-to-end-hardening-and-release-readiness/06-12-VERIFICATION-PROVENANCE.md
  - .planning/phases/06-end-to-end-hardening-and-release-readiness/06-VERIFICATION.md
autonomous: true
gap_closure: true
gap_ids: [G-06-02, G-06-03, G-06-04, G-06-05]
requirements: [OPS-04, PROD-04]
must_haves:
  truths:
    - "The five required release lanes are proven against one immutable candidate by the completed successful GitHub Actions execution, whose clean Linux environment did not depend on this workstation's pnpm cache, TLS connection, or file handles."
    - "The corrected governed fixture and the release-gate command contract are inspected at candidate 089d969368e597c368fe7fa50856a092937fa457, not inferred from the active documentation branch."
    - "The Phase 6 verifier distinguishes authoritative candidate evidence from Windows-local corroboration, names any missing retained local output precisely, and does not lower OPS-04 or PROD-04 release requirements."
    - "The exact disposable C:\\s6r candidate worktree is either removed under the existing user authorization or documented as a bounded Windows cleanup blocker without touching another path."
  artifacts:
    - path: .planning/phases/06-end-to-end-hardening-and-release-readiness/06-12-VERIFICATION-PROVENANCE.md
      provides: Scrubbed provenance matrix binding source inspection, official workflow evidence, and any independently retained local output to the same candidate SHA.
    - path: .planning/phases/06-end-to-end-hardening-and-release-readiness/06-VERIFICATION.md
      provides: Fresh, evidence-backed Phase 6 verdict that neither suppresses a remaining release discrepancy nor confuses local Windows infrastructure failure with candidate behavior.
  key_links:
    - from: GitHub Actions run 35355815777
      to: prelaunch-rehearsal artifact and five release lanes
      via: completed success, exact head SHA, named successful job steps, and aggregate record validation
      pattern: "Verify candidate checkout|Aggregate candidate release rehearsal"
    - from: git object 089d969368e597c368fe7fa50856a092937fa457
      to: governed fixture and release rehearsal source
      via: git show at the immutable candidate rather than active-worktree source reads
      pattern: "saveProgrammeRecord|deleteProgrammeRecord|getGovernedProgrammes|pnpm test"
---

<objective>
Close the Phase 6 evidence-review blocker without pretending that transient Windows dependency/cache failures are product failures or rerunning a successful external rehearsal.

Purpose: The candidate already passed the complete clean GitHub Actions rehearsal at immutable SHA `089d969368e597c368fe7fa50856a092937fa457`. Focused local fixture and lifecycle checks also passed in clean exact checkouts, while a combined Windows rerun became non-deterministic only when downloading dependencies or removing a disposable pnpm store. This plan independently binds the authoritative CI/artifact proof and exact source to the candidate, records whether safe local outputs remain independently inspectable, then asks a fresh verifier for a truthful current verdict.
Output: A scrubbed provenance record, a current Phase 6 verification report, and bounded cleanup of only the already-authorized `C:\\s6r` disposable candidate if Windows releases it. No application source change, new deployment, new rehearsal, secret/configuration change, GitHub/Vercel mutation, or Production action.
</objective>

<execution_context>
@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-end-to-end-hardening-and-release-readiness/06-CONTEXT.md
@.planning/phases/06-end-to-end-hardening-and-release-readiness/06-11-REHEARSAL-EVIDENCE.md
@.planning/phases/06-end-to-end-hardening-and-release-readiness/06-VERIFICATION.md
@.planning/phases/06-end-to-end-hardening-and-release-readiness/06-11-PLAN.md
@apps/web/lib/server/e2e-programme-fixture.ts
@apps/web/lib/server/programme-records.ts
@scripts/prelaunch-rehearsal.mjs
@scripts/test-production-tooling.mjs
@.github/workflows/prelaunch-rehearsal.yml
@docs/production-release-runbook.md
@docs/prelaunch-evidence-template.md
</context>

<interfaces>
Candidate source: `089d969368e597c368fe7fa50856a092937fa457`.

Authoritative completed external execution:
- GitHub Actions run `35355815777` (`ScholarScout Prelaunch Rehearsal`) has `status=completed`, `conclusion=success`, and the same `headSha`.
- Its successful job includes these exact steps: `Verify candidate checkout`; `Run candidate quality, high-risk, and local browser proof`; `Protected Preview browser proof (maintainer-owned runner)`; `Separate Preview outage and restoration proof (maintainer-owned runner)`; `Aggregate candidate release rehearsal`.
- Artifact name: `prelaunch-rehearsal`; Plan 06-11's committed scrubbed note records a successful aggregate of candidate-quality, high-risk, local-browser, preview-browser, and preview-outage for that candidate.

Known local corroboration:
- Focused fixture/internal-route tests completed 16/16, and Node lifecycle/protection/Preview tracer/outage tests completed 27/27 in clean exact candidate checkouts.
- `pnpm test:production-tooling` completed 23/23 in a separate clean exact checkout using the project-local Node 20.20.2 and cached pnpm 10.34.5.
- A later combined rerun is not valid counter-evidence: it is blocked only by Windows TLS resets during dependency download and Windows locks in the disposable pnpm store.
</interfaces>

## Source Audit

SOURCE | ID | Feature/requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | — | Production-like release confidence with automated, isolated, end-to-end proof | 06-12 | COVERED | Uses the completed clean external rehearsal and exact-source inspection; does not substitute a local Windows failure for the release gate.
REQ | OPS-04 | High-risk behavior and a minimal end-to-end release check | 06-12 | COVERED | Binds candidate-quality, high-risk, local-browser, Preview-browser, outage, and aggregate evidence to one SHA and rechecks fixture/gate source.
REQ | PROD-04 | Discovery, onboarding, and recommendation journey remains functional | 06-12 | COVERED | Retains the candidate-bound local and protected Preview student-journey results from the successful workflow.
RESEARCH | — | Isolated generated data, Preview-only proof, and scrubbed evidence | 06-12 | COVERED | Evidence contains allowed metadata only; no fixture data, configuration, or credentials are retrieved or copied.
CONTEXT | D-01 to D-04 | Student-only tracer and observable transitions | 06-12 | COVERED | Confirms the passed local/Preview journey evidence is the existing student tracer, without adding deferred staff/community scope.
CONTEXT | D-05 to D-06 | Deterministic Preview proof and non-sensitive evidence | 06-12 | COVERED | Validates completed candidate-bound Preview proof and records only SHA, UTC bounds, links, lane status, and command/result metadata.
CONTEXT | D-07 to D-08 | Failure-path coverage and isolated restored outage proof | 06-12 | COVERED | Requires the separately passed outage/restoration lane from the authoritative aggregate; no new failure injection is run.
CONTEXT | D-09 to D-10 | Fail-closed release gate and distinct evidence | 06-12 | COVERED | A fresh verifier may pass only on complete same-SHA proof or must name a concrete missing/mismatched item.

<tasks>

<task type="tracer">
  <name>Task 1: Build a candidate-bound provenance record without a Windows dependency rerun</name>
  <precondition>Read-only GitHub access can retrieve workflow run 35355815777 and the existing tracked Plan 06-11 evidence note is present. The executor can read immutable Git objects locally. Neither condition authorizes a GitHub/Vercel mutation, a deployment, or a secret read.</precondition>
  <files>.planning/phases/06-end-to-end-hardening-and-release-readiness/06-12-VERIFICATION-PROVENANCE.md</files>
  <action>Using only read-only GitHub commands, retrieve run `35355815777` metadata and step outcomes. Require `completed`, `success`, exact head SHA `089d969368e597c368fe7fa50856a092937fa457`, and a successful result for all five exact named workflow steps in this plan's interfaces. Download no artifact unless the existing Plan 06-11 note cannot be cross-checked from the metadata; if a download is necessary, use one new OS-temporary directory, validate only the known artifact name and aggregate schema from a disposable exact detached checkout, then remove that exact temporary directory. Inspect source strictly with `git show 089d969368e597c368fe7fa50856a092937fa457:<path>` for the governed fixture, governed programme boundary, rehearsal script, tooling tests, and workflow. Confirm source uses `saveProgrammeRecord` and `deleteProgrammeRecord` for fixture writes/deletes, fixture-mode governed reads exclude ordinary seeds, and candidate quality includes root `pnpm test`; record only the resulting source assertions and candidate SHA. Inspect retained, safe local command output only if it actually remains in a known untracked disposable location or a committed scrubbed record. If it can be independently tied to the exact SHA and one of the stated command/result counts, record the command, candidate SHA, and pass count. If no such retained safe output exists, write the exact limited statement `local corroboration output was not retained for independent inspection; the authoritative clean GitHub Actions candidate-quality and five-lane aggregate evidence remains available` and do not invent a local rerun or a pass result. Create the provenance record in the active documentation worktree with only candidate SHA, Git object paths, permitted source conclusions, workflow UTC bounds/links/job link, artifact name, five lane statuses, and the retained-local-output finding. Do not copy raw logs, temporary paths, fixture IDs, storage paths, cookies, tokens, capabilities, request content, student content, dependency-cache locations, or Windows error text. Commit documentation only as `docs(06-12): record candidate verification provenance`. This implements D-01 through D-10.</action>
  <verify>
    <automated>$sha = '089d969368e597c368fe7fa50856a092937fa457'; $run = gh run view 35355815777 --repo realtypulse73/Scholar-Scout --json status,conclusion,headSha,url,createdAt,updatedAt,jobs | ConvertFrom-Json; if ($LASTEXITCODE -ne 0 -or $run.status -ne 'completed' -or $run.conclusion -ne 'success' -or $run.headSha -ne $sha) { throw 'workflow identity is not the passed immutable candidate' }; $requiredSteps = @('Verify candidate checkout', 'Run candidate quality, high-risk, and local browser proof', 'Protected Preview browser proof (maintainer-owned runner)', 'Separate Preview outage and restoration proof (maintainer-owned runner)', 'Aggregate candidate release rehearsal'); foreach ($name in $requiredSteps) { if (-not @($run.jobs.steps | Where-Object { $_.name -eq $name -and $_.conclusion -eq 'success' }).Count) { throw "required passed workflow step missing: $name" } }; $fixture = git show "$sha`:apps/web/lib/server/e2e-programme-fixture.ts"; $records = git show "$sha`:apps/web/lib/server/programme-records.ts"; $rehearsal = git show "$sha`:scripts/prelaunch-rehearsal.mjs"; if (($fixture -notmatch 'saveProgrammeRecord') -or ($fixture -notmatch 'deleteProgrammeRecord') -or ($records -notmatch 'getGovernedProgrammes') -or ($rehearsal -notmatch 'pnpm test')) { throw 'candidate source no longer matches required fixture or quality-gate contract' }; Test-Path '.planning/phases/06-end-to-end-hardening-and-release-readiness/06-12-VERIFICATION-PROVENANCE.md'</automated>
  </verify>
  <done>The provenance record proves exactly one candidate through immutable source inspection and the completed official five-lane workflow. It does not misrepresent an unavailable Windows rerun as a release failure or a local historical result as independently retained evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Freshly verify Phase 6 and clean only the approved disposable candidate</name>
  <precondition>Task 1's provenance record exists and identifies an exact candidate SHA, complete passed workflow metadata, and candidate-source contract result. The user has already authorized deleting only `C:\\s6r`; no other temporary directory is in scope.</precondition>
  <files>.planning/phases/06-end-to-end-hardening-and-release-readiness/06-VERIFICATION.md</files>
  <action>Invoke a fresh `gsd-verifier` after Task 1. The verifier must use `git show` at `089d969368e597c368fe7fa50856a092937fa457` for all candidate-source inspection and read only the new provenance note plus the committed Plan 06-11 evidence note for external execution evidence. It must not run package installation, contact Vercel, trigger GitHub workflows, read secrets, use the dirty active worktree as candidate source, or require a combined Windows rerun. In the formal report, separate three evidence classes: exact Git source, authoritative clean GitHub Actions execution, and optional independently retained local corroboration. Mark OPS-04 and PROD-04 supported only if the immutable-source contract and all five named GitHub workflow lanes are present, candidate-bound, and passed; otherwise retain `gaps_found` with the exact missing/mismatched lane or source condition. The absence of separately retained local output must be reported as limited corroboration only when authoritative completed candidate-quality and aggregate evidence is still complete; it must never be silently hidden or replace a missing required lane. After the verifier completes, clean exactly the already-authorized `C:\\s6r` candidate directory: first require `git -C C:\\s6r\\candidate rev-parse HEAD` to equal the exact SHA and verify `C:\\s6r\\candidate` is the registered worktree path. Then use `git worktree remove --force C:\\s6r\\candidate`, verify that exact path is absent from `git worktree list`, and remove only `C:\\s6r` if it exists. Make at most three bounded cleanup attempts that target this literal path. Do not use a wildcard, drive root, parent enumeration, reset, clean, stash, process kill, or deletion of any other directory. If Windows still denies the exact removal, record `C:\\s6r cleanup blocked by a Windows handle after bounded retries` in the plan summary, leave the formal release verdict based on the completed evidence, and stop without destructive escalation. Commit only the verification report as `docs(06-12): reverify phase six from immutable evidence`. This implements D-01 through D-10 while preserving the D-09 fail-closed release boundary.</action>
  <verify>
    <automated>$sha = '089d969368e597c368fe7fa50856a092937fa457'; $verification = Get-Content -Raw '.planning/phases/06-end-to-end-hardening-and-release-readiness/06-VERIFICATION.md'; if ($verification -notmatch $sha) { throw 'verification does not identify immutable candidate' }; if (($verification -notmatch '35355815777') -or ($verification -notmatch 'candidate-quality') -or ($verification -notmatch 'high-risk') -or ($verification -notmatch 'local-browser') -or ($verification -notmatch 'preview-browser') -or ($verification -notmatch 'preview-outage')) { throw 'verification does not account for the five required evidence lanes' }; $candidatePath = 'C:\s6r\candidate'; if (Test-Path -LiteralPath $candidatePath) { if ((git -C $candidatePath rev-parse HEAD).Trim() -ne $sha) { throw 'approved cleanup target SHA mismatch' }; if (-not (git worktree list --porcelain | Select-String -SimpleMatch 'worktree C:/s6r/candidate')) { throw 'approved cleanup target is not the registered disposable worktree' }; $removed = $false; for ($attempt = 1; $attempt -le 3 -and -not $removed; $attempt++) { git worktree remove --force $candidatePath; if (-not (Test-Path -LiteralPath $candidatePath)) { $removed = $true } }; if ($removed) { if (git worktree list --porcelain | Select-String -SimpleMatch 'worktree C:/s6r/candidate') { throw 'removed candidate remains registered' }; if (Test-Path -LiteralPath 'C:\s6r') { Remove-Item -LiteralPath 'C:\s6r' -Recurse -Force } } }; $true</automated>
  </verify>
  <done>The refreshed verification report tells the truth about the immutable candidate and required release lanes, preserves the fail-closed gate, and clearly limits any Windows-local evidence gap. Only the explicitly approved disposable `C:\\s6r` target is cleaned when possible; cleanup failure is bounded, named, and never expanded into deletion of another path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| immutable Git object -> verifier | Candidate conclusions must be read from the exact SHA, not a dirty documentation checkout. |
| GitHub Actions -> release evidence | External evidence is acceptable only when workflow identity, job steps, lanes, and candidate SHA agree. |
| local retained output -> corroboration | Historical local output cannot be treated as proof unless safely retained and candidate-bound. |
| `C:\\s6r` -> filesystem cleanup | The user authorized one literal temporary target, not drive-wide or parent-directory deletion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-31 | Tampering | candidate evidence | high | mitigate | Require the exact SHA in Git object reads, workflow metadata, every passed lane, and formal verification. |
| T-06-32 | Repudiation | workflow evidence | high | mitigate | Record run/job links, UTC bounds, named artifact, and each required passed step in a scrubbed provenance record. |
| T-06-33 | Information Disclosure | artifact and local logs | high | mitigate | Retain only allowed command/result metadata; do not download/copy raw content unless aggregate validation is necessary and disposable. |
| T-06-34 | Denial of Service | Windows pnpm store and TLS | medium | accept | Treat transient download/lock faults as local infrastructure evidence limits, use completed clean CI proof, and do not retry downloads as a release decision shortcut. |
| T-06-35 | Elevation of Privilege | cleanup target | high | mitigate | Assert exact registered candidate path and SHA, restrict to three literal-path attempts, and stop on any mismatch or handle denial. |
</threat_model>

<verification>
Run the Task 1 read-only provenance verification, then have a fresh gsd-verifier issue the Phase 6 report from immutable source plus completed candidate evidence. Run the literal-path cleanup assertion only after verification and never treat cleanup as a substitute for the release gate.
</verification>

<success_criteria>
Phase 6 is assessed from complete candidate-bound proof rather than a flaky Windows package install. The report either supports OPS-04 and PROD-04 with all five authoritative lanes and corrected source contract, or records a single exact remaining discrepancy. No Production state, deployment, secret, configuration, app source, or user-owned dirty file changes.
</success_criteria>

<output>
Create `.planning/phases/06-end-to-end-hardening-and-release-readiness/06-12-SUMMARY.md` when done.
</output>
