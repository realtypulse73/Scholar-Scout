---
phase: 10-curated-import-and-governed-staff-publication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/lib/catalogue-publication.ts
  - apps/web/lib/server/data-store.ts
  - apps/web/lib/server/active-staff.ts
  - apps/web/lib/server/catalogue-publications.ts
  - apps/web/app/api/admin/catalogue-publications/route.ts
  - apps/web/__tests__/api/admin-catalogue-publications.test.ts
  - apps/web/__tests__/lib/catalogue-publication.test.ts
  - apps/web/__tests__/lib/data-store.test.ts
autonomous: true
requirements: [EVID-03, PUB-01, PUB-02]
must_haves:
  truths:
    - "An active editor can stage one source-backed catalogue candidate through a staff-only API, while a non-staff or incorrectly capable actor cannot."
    - "An active staff identity can hold a nonempty set of editorial capabilities, so an administrator who also has editor capability can stage their own candidate without weakening per-command authorization."
    - "Every staged candidate receives the same automated editorial checklist and remains private with ordered correction codes when it is incomplete."
    - "Adding catalogue publication state preserves existing ScholarScout document data and recovery normalization."
  artifacts:
    - path: "apps/web/lib/catalogue-publication.ts"
      provides: "Typed catalogue candidate, checklist, lifecycle, rights, and audit contracts"
      exports: ["evaluateCatalogueChecklist", "CatalogueCandidate", "CataloguePublicationState"]
    - path: "apps/web/lib/server/catalogue-publications.ts"
      provides: "CAS-backed private candidate staging command"
      exports: ["stageCatalogueCandidate"]
    - path: "apps/web/app/api/admin/catalogue-publications/route.ts"
      provides: "Authorized staff staging entry point"
      exports: ["POST"]
  key_links:
    - from: "apps/web/app/api/admin/catalogue-publications/route.ts"
      to: "apps/web/lib/server/active-staff.ts"
      via: "authorization before request body parsing"
      pattern: "requireActiveStaff"
    - from: "apps/web/lib/server/catalogue-publications.ts"
      to: "apps/web/lib/server/data-store.ts"
      via: "single conditional mutation of cataloguePublicationState"
      pattern: "commitConditionalMutation"
---

<objective>
Prove the governed-publication architecture with one real editor-to-private-draft path, including active-staff capability enforcement, checklist feedback, and additive persistence.

Purpose: establish the safe path that every later import, review, release, and restore command builds on.
Output: typed publication state, capability-aware authorization, staging route, and route-contract coverage.
</objective>

<execution_context>
@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-curated-import-and-governed-staff-publication/10-CONTEXT.md
@.planning/phases/10-curated-import-and-governed-staff-publication/10-RESEARCH.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-CONTEXT.md
@apps/web/lib/catalogue-contract.ts
@apps/web/lib/server/active-staff.ts
@apps/web/lib/server/data-store.ts
@apps/web/lib/server/persistence-operations.ts
</context>

## Source Coverage Audit

| Source | ID | Required outcome | Plan | Status |
|---|---|---|---|---|
| GOAL | — | bounded reviewed publication that protects the current catalogue | 01–05 | COVERED |
| REQ | EVID-03 | staff source/evidence/freshness/claim review before public release | 01–03 | COVERED |
| REQ | EVID-04 | immutable deterministic learner snapshot without live-provider dependency | 03, 05 | COVERED |
| REQ | PUB-01 | active authorized create/revise/publish/retire/restore actions with audit | 01–05 | COVERED |
| REQ | PUB-02 | recoverable correction and protected public record on validation failure | 01–04 | COVERED |
| REQ | PUB-03 | capped batch, conflict recovery, and explicit publication | 02–05 | COVERED |
| RESEARCH | — | additive whole-document normalization and one-shot CAS commands | 01–05 | COVERED |
| RESEARCH | — | schema-versioned JSON staging with depth/size/count bounds | 02 | COVERED |
| RESEARCH | — | stable ID ordering and non-secret digest in release manifest | 03 | COVERED |
| CONTEXT | D-01–D-04 | role separation, rechecked checklist, draft correction and re-review | 01–02 | COVERED |
| CONTEXT | D-05–D-08 | bounded individual/import intake, weekly manifest, retained snapshots | 02–03 | COVERED |
| CONTEXT | D-09–D-11 | deliberate merge, emergency correction, quarantine | 04 | COVERED |
| CONTEXT | D-12–D-14 | stored rights proof, current rights evaluation, text fallback | 01, 03 | COVERED |
| CONTEXT | D-15 | protected concise record/snapshot audit history | 02, 04 | COVERED |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Stage one checked editor candidate end to end</name>
  <reversibility rating="costly">The additive publication-state shape and server-only capability contract will be read by every Phase 10 command and later discovery work.</reversibility>
  <files>apps/web/lib/catalogue-publication.ts, apps/web/lib/server/data-store.ts, apps/web/lib/server/active-staff.ts, apps/web/lib/server/catalogue-publications.ts, apps/web/app/api/admin/catalogue-publications/route.ts, apps/web/__tests__/api/admin-catalogue-publications.test.ts</files>
  <behavior>
    - A currently allowlisted actor configured as an editor stages a valid single candidate and receives its private draft/checklist result.
    - Capability parsing accepts a strict normalized-email-to-nonempty-capability-array mapping; editor-only, reviewer-plus-editor, and administrator-plus-editor actors receive only the actions their individual requested capability permits.
    - An actor absent from the active allowlist, or active without the requested catalogue capability, receives the existing safe forbidden response before request JSON is read.
    - A malformed candidate remains a private draft with stable ordered correction codes and does not change any public snapshot pointer.
  </behavior>
  <action>Implement the D-01/D-03/D-04 tracer before broadening the workflow. Add `catalogue-publication.ts` beside the Phase 9 contract with the distinct `CatalogueCandidate`, lifecycle, record/media-rights, checklist-result, and concise-audit types; evaluate sources, material card facts/evidence, source freshness, claim-boundary wording, declared regional boundary relationship, and rights metadata against an injected clock. A checklist pass means editorial completeness only. Add an optional/defaulted `cataloguePublicationState` to `ScholarScoutData` and every data-store normalization, clone, validation, and full-recovery import path so legacy documents stay valid and publication state cannot be dropped. Extend `ActiveStaffActor` to return the already allowlist-validated normalized email and parse the server-only `SCHOLARSCOUT_CATALOGUE_STAFF_CAPABILITIES` JSON object as an exact normalized-email-to-nonempty-array of `editor`, `reviewer`, and `administrator` capabilities, then normalize it to a set. Reject malformed JSON, duplicate normalized-email keys, empty arrays, invalid/duplicate capability values, or a missing active-email entry; every mutation must request and verify its own required capability against that set after current-email authorization. An administrator who also holds `editor` must be able to stage their own candidate, but no browser role or JWT role is trusted. Add one CAS-backed `stageCatalogueCandidate` command and POST `action: 'stage'` route that require `editor` before `request.json()`, write only the candidate slice plus concise audit metadata, and expose no configuration values or version tokens. Start with the API test red, then prove the working route using the injected memory store and mocked current session, including editor-only, reviewer-plus-editor, and administrator-plus-editor capability combinations.</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test -- --runInBand __tests__/api/admin-catalogue-publications.test.ts</automated></verify>
  <done>An allowlisted editor-capable actor can stage one complete candidate regardless of their additional reviewer/administrator capabilities; malformed work is preserved as a private correction-ready draft; denied calls parse neither body nor storage; legacy ScholarScout data survives a publication-state round trip.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete the reusable checklist and privacy-minimal state invariants</name>
  <files>apps/web/lib/catalogue-publication.ts, apps/web/__tests__/lib/catalogue-publication.test.ts, apps/web/lib/server/data-store.ts, apps/web/__tests__/lib/data-store.test.ts</files>
  <behavior>
    - The checklist returns deterministic codes for source, fact evidence, freshness, claim boundary, region, and media conditions, including correct fact-only fallback for absent/expired/revoked/uncertain media proof.
    - The reusable checklist contract exposes a safe, ordered six-category status summary for source, material evidence, freshness, claim boundary, regional boundary, and media-rights status, with a fixed statement that a pass means editorial completeness rather than real-world provider truth.
    - Candidate records have immutable creator/revision/checklist context, and changes clear an earlier approval state.
    - Persisted audit data contains only actor, capability, action, timestamp, outcome, version, required reason, and correction/review status.
  </behavior>
  <action>Expand the tracer contract into the full D-03/D-12/D-13/D-14 invariant suite. Reuse the controlled unions and validators from `catalogue-contract.ts`; do not introduce provider fetching, truth scoring, eligibility/outcome language, or learner data into candidate structures. Make the checklist emit a safe ordered summary with exactly source, material evidence, freshness, claim boundary, regional boundary, and media-rights statuses plus the fixed plain-language disclosure that a pass means editorial completeness, not verified real-world provider truth. Make rights records accept documented Scholar Scout ownership, licence, provider approval, or approved embed together with a source and optional expiry. Ensure media outcomes are calculated fresh from the injected clock and yield a text-and-official-source representation whenever rights are not usable. Normalize missing publication state into an empty valid state, deep-clone it with the rest of the document, and reject malformed stored publication state as invalid data rather than silently repairing it. Add focused pure and store regression coverage for legacy document preservation, stable correction ordering, all six safe checklist-summary categories and pass disclosure, stale/future dates, invalid regional provenance, and audit redaction.</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test -- --runInBand __tests__/lib/catalogue-publication.test.ts __tests__/lib/data-store.test.ts</automated></verify>
  <done>The reusable checklist gives exact repeatable correction codes, rights uncertainty produces factual fallback data, and all publication state remains additive, validated, and free of secrets or learner information.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| staff browser → catalogue route | Untrusted request bodies and stale UI state reach a privileged mutation endpoint. |
| session/allowlist → capability resolver | A current staff identity is translated into a limited editorial capability. |
| mutation command → whole-document store | One bounded catalogue change crosses the conditional-write boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-01 | E | staff capability resolver | high | mitigate | Require current allowlist plus server-only capability on every request before parsing input. |
| T-10-02 | T | candidate staging | high | mitigate | Typed checklist, injected freshness clock, bounded input, and single CAS write retain invalid work privately. |
| T-10-03 | I | audit/state responses | medium | mitigate | Return concise typed audit DTOs and omit configuration, tokens, headers, learner values, and raw import bodies. |
| T-10-SC | Tampering | package installation | high | mitigate | No package installation is in scope; use committed dependencies and focused tests. |
</threat_model>

<verification>
Run the focused API and pure contract suites, then `corepack pnpm --filter @scholar-scout/web run typecheck`.
</verification>

<success_criteria>
One active editor can create a private checked candidate through the real staff route; invalid or unauthorized work cannot affect a public catalogue record.
</success_criteria>

<output>
Create `.planning/phases/10-curated-import-and-governed-staff-publication/10-01-SUMMARY.md` when done.
</output>
