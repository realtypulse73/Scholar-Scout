---
phase: 10-curated-import-and-governed-staff-publication
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - apps/web/lib/catalogue-publication.ts
  - apps/web/lib/server/catalogue-publications.ts
  - apps/web/app/api/admin/catalogue-publications/route.ts
  - apps/web/app/api/admin/catalogue-publications/import/route.ts
  - apps/web/__tests__/lib/catalogue-publication.test.ts
  - apps/web/__tests__/lib/server/catalogue-publications.test.ts
  - apps/web/__tests__/api/admin-catalogue-publications.test.ts
  - docs/curated-catalogue-publication-runbook.md
autonomous: true
requirements: [EVID-03, PUB-01, PUB-02, PUB-03]
must_haves:
  truths:
    - "Editors can use either one-record correction or a versioned small JSON intake to create private candidates."
    - "A creator holding editor capability needs a different reviewer-capable actor for ordinary review; an editor-plus-administrator may self-approve only after the same server checklist passes."
    - "Editing a reviewed candidate removes its approval and shows the creator exact resubmission corrections."
  artifacts:
    - path: "apps/web/app/api/admin/catalogue-publications/import/route.ts"
      provides: "Bounded candidate-import staging endpoint"
      exports: ["POST"]
    - path: "apps/web/lib/server/catalogue-publications.ts"
      provides: "Candidate review, correction, import, and concise audit commands"
      exports: ["reviewCatalogueCandidate", "importCatalogueCandidates"]
    - path: "docs/curated-catalogue-publication-runbook.md"
      provides: "Maintainer capability configuration and safe staff intake operation"
  key_links:
    - from: "apps/web/app/api/admin/catalogue-publications/import/route.ts"
      to: "apps/web/lib/server/catalogue-publications.ts"
      via: "capability-authorized schema-versioned staged import"
      pattern: "importCatalogueCandidates"
    - from: "apps/web/lib/server/catalogue-publications.ts"
      to: "apps/web/lib/catalogue-publication.ts"
      via: "reused checklist on stage, review, and correction"
      pattern: "evaluateCatalogueChecklist"
---

<objective>
Deliver the complete staff intake and independent-review workflow while keeping every candidate private until an explicit later release.

Purpose: let staff correct individual records and add small curated batches without risking a full-document recovery import or automatic publication.
Output: versioned intake parser, review transitions, correction/resubmission state, safe audit read, and runbook.
</objective>

<execution_context>
@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-curated-import-and-governed-staff-publication/10-CONTEXT.md
@.planning/phases/10-curated-import-and-governed-staff-publication/10-RESEARCH.md
@.planning/phases/10-curated-import-and-governed-staff-publication/10-01-SUMMARY.md
@apps/web/lib/catalogue-publication.ts
@apps/web/lib/server/catalogue-publications.ts
@apps/web/lib/server/data-recovery.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bounded schema-versioned staff intake and correction commands</name>
  <reversibility rating="costly">The versioned candidate-import envelope is a staff-facing compatibility boundary; changing its record shape later requires compatible tooling and fixture handling.</reversibility>
  <files>apps/web/lib/catalogue-publication.ts, apps/web/lib/server/catalogue-publications.ts, apps/web/app/api/admin/catalogue-publications/import/route.ts, apps/web/__tests__/lib/catalogue-publication.test.ts, apps/web/__tests__/lib/server/catalogue-publications.test.ts</files>
  <behavior>
    - A `{ schemaVersion: 1, changes: [...] }` body with one through 25 bounded `upsert` or `retire` changes stages private candidates only; a retire request is an approval-ready retirement intent and cannot alter an active public snapshot at intake time.
    - The twenty-sixth change, unsupported schema/action, oversized/deep value, duplicate stable ID, stale candidate revision, or malformed record produces safe correction feedback with no mutation.
    - Editing a candidate creates its next revision, invalidates prior approval, preserves prior valid data, and makes it eligible for a fresh review.
  </behavior>
  <action>Implement D-04/D-05/D-06 with a focused JSON candidate intake parser, never with `restoreScholarScoutDataFromImport` or the signed recovery package. Require `editor` capability for the import mutation before request parsing. Define explicit byte, nesting, string, array, and 25-change limits; accept only `schemaVersion: 1`, stable unique record IDs, exact `upsert|retire` actions, expected candidate revision for existing candidates, and complete candidate payloads. Stage every accepted change as a private draft through the same command/checklist boundary used for individual correction. Model `retire` as a private candidate action against an existing stable ID and preserve the target record/candidate/audit lineage; it is not a public deletion until an approved retirement is included by the immutable snapshot command. Apply a single conditional write for a wholly valid import; report structural problems without partially applying a malformed envelope. Preserve the original candidate body and ordered correction codes during corrections, and clear approval/reviewer data whenever its record revision changes. Add tests covering the max batch, all parser guardrails, stage-only result, retirement-intent preservation, capability denial before parsing, and a concurrent replacement conflict with no automatic retry.</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test -- --runInBand __tests__/lib/catalogue-publication.test.ts __tests__/lib/server/catalogue-publications.test.ts</automated></verify>
  <done>Individual corrections and valid one-to-twenty-five JSON changes produce reviewable private candidates; malformed envelopes and stale writes leave both candidate/public state intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce reviewer independence and expose safe staff history</name>
  <files>apps/web/lib/server/catalogue-publications.ts, apps/web/app/api/admin/catalogue-publications/route.ts, apps/web/__tests__/api/admin-catalogue-publications.test.ts, apps/web/__tests__/lib/server/catalogue-publications.test.ts, docs/curated-catalogue-publication-runbook.md</files>
  <behavior>
    - A creator with editor capability cannot use only editor capability to approve their own candidate; a different reviewer-capable actor can approve only a currently passing revision.
    - An actor holding both editor and administrator capabilities can self-approve their own candidate only after the server checklist passes; a reviewer-plus-editor actor can stage and independently review another creator's candidate.
    - A reviewer candidate DTO contains only a safe six-category automated checklist summary for source, material evidence, freshness, claim boundary, regional boundary, and media-rights status, plus a fixed statement that passing means editorial completeness rather than verified provider truth.
    - An authorized caller can read concise candidate audit history, while an unauthorized caller cannot obtain candidate, audit, capability, or configuration information.
  </behavior>
  <action>Implement D-01/D-02/D-03/D-15 candidate-review transitions as server commands and explicit POST actions, using the current active-staff guard before parsing any request. Require `reviewer` plus a different creator identity for ordinary approval; allow self-approval only when the creator holds both `editor` for candidate ownership and `administrator` for the exceptional approval; require `editor` for candidate stage/edit/submit and verify the exact requested capability for every mutation. Rerun the automated checklist in the review command; retain exact correction codes for a failed review and write concise lifecycle audit events for stage, edit, submit, approval, and failure. Add a protected GET response with a per-record lifecycle/audit DTO containing actor, capability, action, timestamp, outcome, required reason when present, correction/review status, candidate revision, and no raw candidate/import content. Its reviewer-candidate portion must contain only the ordered safe six-category summary — source, material evidence, freshness, claim boundary, regional boundary, media-rights — and a fixed `passMeaning` disclosure that passing means editorial completeness, not verified real-world provider truth. Document the normalized-email-to-nonempty-capability-array configuration, intake JSON format, 25-record safety cap, and the same disclosure. Do not place capability configuration, secrets, raw import, learner data, or provider-private data in a response or audit entry; snapshot-history DTOs are added after snapshot records exist in Plan 03. Add explicit route/server tests for the six-category summary and disclosure serialization as well as self-review rejection, administrator exception, recheck-after-edit, denied-before-parsing behavior, and audit DTO redaction.</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test -- --runInBand __tests__/api/admin-catalogue-publications.test.ts __tests__/lib/server/catalogue-publications.test.ts</automated></verify>
  <done>Ordinary review is independent, editor-plus-administrator self-approval remains checklist-gated, rejected work stays private and repairable, and authorized candidate history contains only concise operational evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| imported JSON → candidate parser | Staff-provided file content may be malformed, oversized, or try to alter unrelated data. |
| editor/reviewer capability → lifecycle transition | The caller requests a state change whose legality depends on current role and candidate ownership. |
| audit store → staff history response | Operational history must remain useful without leaking private source or account material. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-04 | T/D | JSON intake parser | high | mitigate | Exact schema version, byte/depth/count limits, duplicate detection, and one focused CAS mutation. |
| T-10-05 | E | review transition | high | mitigate | Enforce current allowlist, capability, creator/reviewer distinction, and server checklist. |
| T-10-06 | I | audit history API | medium | mitigate | Use an allowlisted DTO with no credentials, raw import bodies, learner data, or private provider notes. |
| T-10-SC | Tampering | package installation | high | mitigate | No dependency changes; validation uses committed runtime and Jest. |
</threat_model>

<verification>
Run focused parser/server/API tests, then typecheck and lint the web workspace.
</verification>

<success_criteria>
Staff can stage bounded curated work, correct it, and obtain an independent checklist-based review without an import changing the learner catalogue.
</success_criteria>

<output>
Create `.planning/phases/10-curated-import-and-governed-staff-publication/10-02-SUMMARY.md` when done.
</output>
