---
phase: 10-curated-import-and-governed-staff-publication
plan: 03
type: execute
wave: 3
depends_on: [10-02]
files_modified:
  - apps/web/lib/catalogue-publication.ts
  - apps/web/lib/server/catalogue-publications.ts
  - apps/web/lib/server/programme-records.ts
  - apps/web/app/api/admin/catalogue-publications/route.ts
  - apps/web/__tests__/lib/catalogue-publication.test.ts
  - apps/web/__tests__/lib/server/catalogue-publications.test.ts
  - apps/web/__tests__/api/admin-catalogue-publications.test.ts
autonomous: true
requirements: [EVID-03, EVID-04, PUB-02, PUB-03]
must_haves:
  truths:
    - "An administrator can explicitly publish at most one normal, ordered weekly snapshot per stable `YYYY-Www` ISO-week period from at most 25 approved candidates, only during Monday 09:00–17:00 America/New_York."
    - "An administrator can preview the next weekly release without writing state and see the same selected, quarantined, and media-fallback outcomes that publication will recheck."
    - "The final release rechecks every selected candidate, quarantines only failed records with correction codes, and keeps the prior active snapshot safe."
    - "Authorized staff can retrieve concise snapshot history and manifest lineage without receiving secrets, tokens, learner data, or provider-private material."
    - "The future learner read seam returns only a cloned stored active snapshot and does not make provider calls."
  artifacts:
    - path: "apps/web/lib/server/catalogue-publications.ts"
      provides: "Weekly preview/release, deterministic snapshot, and authorized snapshot-history commands"
      exports: ["previewWeeklyCatalogueRelease", "publishWeeklyCatalogueSnapshot", "getCatalogueSnapshotHistory", "getPublishedCatalogueSnapshot"]
    - path: "apps/web/lib/catalogue-publication.ts"
      provides: "Manifest, snapshot, quarantine, canonical digest, and deterministic normal-release scheduling contract"
      exports: ["CatalogueSnapshot", "CatalogueSnapshotManifest", "getWeeklyReleasePeriodKey", "isWithinNormalWeeklyReleaseWindow"]
    - path: "apps/web/lib/server/programme-records.ts"
      provides: "Governed future catalogue public-read seam"
      exports: ["getPublishedCatalogueSnapshot"]
  key_links:
    - from: "apps/web/lib/server/catalogue-publications.ts"
      to: "apps/web/lib/catalogue-publication.ts"
      via: "canonical stable-ID manifest and release-time checklist"
      pattern: "evaluateCatalogueChecklist"
    - from: "apps/web/lib/server/programme-records.ts"
      to: "apps/web/lib/server/catalogue-publications.ts"
      via: "server-only active snapshot reader"
      pattern: "getPublishedCatalogueSnapshot"
---

<objective>
Turn approved candidates into an administrator-controlled weekly snapshot that is deterministic, retained, and suitable for later learner discovery without live provider dependencies.

Purpose: create the publication boundary required before student-facing discovery is built.
Output: release manifest, weekly command, quarantine behavior, and server-only public snapshot seam.
</objective>

<execution_context>
@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/10-curated-import-and-governed-staff-publication/10-CONTEXT.md
@.planning/phases/10-curated-import-and-governed-staff-publication/10-RESEARCH.md
@.planning/phases/10-curated-import-and-governed-staff-publication/10-02-SUMMARY.md
@apps/web/lib/server/programme-records.ts
@apps/web/lib/server/persistence-operations.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the deterministic weekly snapshot, retirement, and manifest command</name>
  <files>apps/web/lib/catalogue-publication.ts, apps/web/lib/server/catalogue-publications.ts, apps/web/__tests__/lib/catalogue-publication.test.ts, apps/web/__tests__/lib/server/catalogue-publications.test.ts</files>
  <behavior>
    - An administrator-selected approved batch containing one through 25 current candidate revisions produces one new normal `weekly` snapshot with stable-ID order, manifest sequence, prior snapshot reference, deterministic non-secret content digest, and a stable America/New_York `YYYY-Www` ISO-week period key.
    - A normal `weekly` release accepts an injected instant only from Monday 09:00 inclusive until 17:00 exclusive in the fixed `America/New_York` time zone, rejects any other time, and rejects a second normal release carrying the same `YYYY-Www` ISO-week period key; the next ISO week is independently eligible.
    - An emergency release is a separately authorized `emergency` manifest/audit type, can run outside the normal release window, and neither consumes nor bypasses the one-normal-release-per-period rule.
    - An approved retirement removes its stable ID from the next active public snapshot while preserving the private retirement candidate, prior snapshots, audit history, and a manifest entry containing the retired stable ID and candidate revision.
    - Final validation reevaluates source/evidence/freshness/claim/region/rights; record blockers quarantine only the failed candidate while media blockers omit media and preserve factual text/source fallback.
    - A release conflict does not retry or overwrite another release; it returns a recoverable conflict result.
  </behavior>
  <action>Implement D-03/D-06/D-07/D-08/D-11/D-12/D-13/D-14 as a single CAS-backed weekly-release command. Define append-only snapshot and manifest records with snapshot ID, monotonic sequence/version, release kind, timestamp, prior snapshot ID, stable ordered included IDs/revisions, retired stable IDs/revisions, excluded/quarantined IDs and correction codes, and a SHA-256 digest over an explicitly canonical, non-secret snapshot payload. For normal `weekly` releases, use a server-owned injectable clock and the fixed `America/New_York` zone to derive the canonical `YYYY-Www` ISO-week period key from zoned calendar date parts; do not accept a client period, time zone, or timestamp. Permit the release only on Monday from 09:00 inclusive through 17:00 exclusive in that zone, and under the same conditional mutation reject a manifest whose normal-release period key already exists. Emergency snapshots remain a separately authorized `emergency` release kind and audit/manifest type, are exempt from the normal window and duplicate-period guard, and never consume a normal weekly slot. Select only currently approved candidate revisions, cap selected changes at 25, recompute the checklist immediately before constructing the snapshot, and write candidate quarantine state plus the new snapshot/manifest in one conditional mutation. Treat an approved `retire` candidate as a snapshot operation: remove its stable ID from the newly active public snapshot, retain its candidate/review/audit history and all earlier snapshots, and record its stable ID and revision in the new manifest. Keep prior snapshots internally; never mutate an old snapshot in place. A candidate whose media proof is no longer usable must contribute a factual record without media, while source, claim, boundary, or material evidence failures remain outside the release. Require administrator capability for the state-changing command and return conflicts without retrying. Add tests with injected clock/time-zone seams proving Monday boundary acceptance, Sunday/weekday and 17:00 rejection, stable `YYYY-Www` ISO-period calculation across calendar-year/DST boundaries, a duplicate normal-period rejection, next-period acceptance, emergency exemption with distinct manifest/audit kind, stable equivalent manifests independent of input order, final recheck behavior, retirement public invisibility, retained prior snapshots, retirement conflict recovery, restoration from a prior snapshot that contains a retired record, cap enforcement, and conditional-write conflict recovery.</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test -- --runInBand __tests__/lib/catalogue-publication.test.ts __tests__/lib/server/catalogue-publications.test.ts</automated></verify>
  <done>A successful normal weekly release is one append-only, deterministic, bounded snapshot in its Monday 09:00–17:00 America/New_York window, with one normal manifest per stable ISO-week period; emergency snapshots remain separately authorized and auditable; approved retirement removes only the retired ID from the new public version while preserving history and manifest lineage; invalid candidates are repairable and excluded while unaffected approved candidates can release.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire administrator preview/release, snapshot history, and the snapshot-only learner read boundary</name>
  <files>apps/web/lib/server/catalogue-publications.ts, apps/web/lib/server/programme-records.ts, apps/web/app/api/admin/catalogue-publications/route.ts, apps/web/__tests__/api/admin-catalogue-publications.test.ts, apps/web/__tests__/lib/server/catalogue-publications.test.ts</files>
  <behavior>
    - Only a currently allowlisted administrator can invoke the explicit weekly publication action.
    - Only a currently allowlisted administrator can invoke a non-mutating weekly-release preview that applies the cap, canonical ordering, current checklist, and server-derived normal-release window/period eligibility and returns selected, quarantined, and media-fallback summaries without changing state.
    - An authorized staff caller can query a redacted snapshot-history DTO containing actor, capability, action, timestamp, outcome, version, required reason, correction/review status, and manifest lineage.
    - The read seam returns an immutable copy of the active stored snapshot or an explicit empty catalogue state, in stable record order.
    - Read-path tests prove the catalogue module does not call network/provider code and does not expose private candidate/reviewer/audit information.
  </behavior>
  <action>Expose administrator-only `preview-weekly` and `publish-weekly` actions that authorize before parsing candidate selection. `preview-weekly` is a non-mutating server command/API: it applies the same 25-change cap, stable ordering, current checklist, and server-owned injected clock/time-zone policy as release, then returns only selected revisions, quarantine corrections, factual media-fallback summaries, and a safe current normal-release eligibility/ISO-period summary without a conditional write, version increment, or audit mutation. `publish-weekly` must derive schedule policy from the same server clock, rerun the checklist, and CAS write rather than trust preview output; reject an outside-window or duplicate-period normal request and return only safe manifest/quarantine/schedule summary data. Add an authorized snapshot-history query/DTO that returns only actor, capability, action, timestamp, outcome, version, required reason, correction/review status, and manifest lineage; redact secrets, tokens, learner data, raw imports/candidates, and provider-private material. Add the D-07 `getPublishedCatalogueSnapshot()` server-only seam at the governed catalogue boundary, preserving `getGovernedProgrammes()` for legacy surfaces until Phase 11 explicitly adopts the six-area snapshot contract. Return a cloned public DTO derived solely from the active stored snapshot, including factual media fallback results but excluding candidates, rights-review internals, checklist diagnostics, capability settings, and audit history. Do not add a public route/page or provider integration in this phase. Test administrator authorization for preview/release, preview non-mutation and server-derived Monday 09:00–17:00 America/New_York eligibility, outside-window and duplicate-period normal-release rejection, next-period acceptance, emergency exemption, fresh release recheck, cap/order/quarantine/media-fallback parity, snapshot-history redaction/lineage, empty state, clone isolation, deterministic ordering, and source-level/runtime regression that the read seam has no network dependency.</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test -- --runInBand __tests__/api/admin-catalogue-publications.test.ts __tests__/lib/server/catalogue-publications.test.ts</automated></verify>
  <done>Administrators can inspect an exact non-mutating weekly preview with server-derived schedule eligibility before choosing an explicit, in-window, once-per-ISO-week normal release that rechecks current state; authorized staff can read redacted snapshot history; later learner-facing code has one deterministic stored-snapshot read seam with no live provider dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| approved candidates → release builder | Earlier review state must not replace release-time evidence/rights validation. |
| release builder → active snapshot | A bounded conditional mutation makes a public catalogue version active. |
| stored snapshot → future learner reader | Only reviewed public DTO fields may cross from staff state to a learner-facing seam. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-07 | T | weekly release | high | mitigate | Recheck selected revisions, sort canonically, cap the batch, and perform one CAS write. |
| T-10-08 | I | public snapshot read | high | mitigate | Clone an allowlisted stored snapshot DTO and exclude candidate, review, audit, and configuration fields. |
| T-10-09 | R | manifest/audit | medium | mitigate | Store release actor, kind, sequence, prior ID, content digest, and quarantine outcome in concise append-only records. |
| T-10-SC | Tampering | package installation | high | mitigate | Use Node built-in hashing and existing dependencies; no package installation occurs. |
</threat_model>

<verification>
Run focused publication API/server/pure tests, then web typecheck, lint, and full Jest suite at the wave boundary.
</verification>

<success_criteria>
The public catalogue state advances only by an explicit, bounded, rechecked administrator release that preserves prior state and produces a deterministic manifest.
</success_criteria>

<output>
Create `.planning/phases/10-curated-import-and-governed-staff-publication/10-03-SUMMARY.md` when done.
</output>
