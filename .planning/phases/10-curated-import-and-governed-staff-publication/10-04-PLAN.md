---
phase: 10-curated-import-and-governed-staff-publication
plan: 04
type: execute
wave: 4
depends_on: [10-03]
files_modified:
  - apps/web/lib/catalogue-publication.ts
  - apps/web/lib/server/catalogue-publications.ts
  - apps/web/app/api/admin/catalogue-publications/route.ts
  - apps/web/app/admin/catalogue-publications/page.tsx
  - apps/web/components/admin/CataloguePublicationManager.tsx
  - apps/web/__tests__/lib/server/catalogue-publications.test.ts
  - apps/web/__tests__/api/admin-catalogue-publications.test.ts
  - apps/web/__tests__/components/CataloguePublicationManager.test.tsx
autonomous: true
requirements: [EVID-03, EVID-04, PUB-01, PUB-02, PUB-03]
must_haves:
  truths:
    - "Staff can compare a stale attempted candidate against the current revision and must supply a reason when retaining an older value."
    - "A reviewer can create a checked emergency snapshot, and an administrator can create a new restorable snapshot from a prior one."
    - "Authorized staff can use a focused intake/review screen to inspect private candidates, correction lists, independent-review state, and safe conflict choices."
  artifacts:
    - path: "apps/web/components/admin/CataloguePublicationManager.tsx"
      provides: "Focused staff UI for candidate intake, review, correction, and conflict resolution"
      exports: ["default"]
    - path: "apps/web/app/admin/catalogue-publications/page.tsx"
      provides: "Staff-gated catalogue publication route"
      exports: ["default"]
    - path: "apps/web/lib/server/catalogue-publications.ts"
      provides: "Conflict merge, emergency-release, and restore commands"
      exports: ["resolveCatalogueCandidateConflict", "publishEmergencyCatalogueSnapshot", "restoreCatalogueSnapshot"]
  key_links:
    - from: "apps/web/components/admin/CataloguePublicationManager.tsx"
      to: "apps/web/app/api/admin/catalogue-publications/route.ts"
      via: "staff actions and safe conflict/audit DTOs"
      pattern: "fetch\('/api/admin/catalogue-publications"
    - from: "apps/web/lib/server/catalogue-publications.ts"
      to: "apps/web/lib/catalogue-publication.ts"
      via: "merge comparison, checklist, and snapshot/manifest contract"
      pattern: "evaluateCatalogueChecklist"
---

<objective>
Complete recoverable governance and the first focused staff-console slice: deliberate conflict reconciliation, reviewer emergency repairs, administrator restore, and candidate intake/review operation.

Purpose: make the editorial workflow usable without expanding the existing oversized programme-management component or weakening snapshot history.
Output: conflict/emergency/restore commands, safe APIs, focused intake/review staff route/component, and interaction coverage.
</objective>

<execution_context>
@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-curated-import-and-governed-staff-publication/10-CONTEXT.md
@.planning/phases/10-curated-import-and-governed-staff-publication/10-RESEARCH.md
@.planning/phases/10-curated-import-and-governed-staff-publication/10-03-SUMMARY.md
@apps/web/components/admin/ProgrammeAdminManager.tsx
@apps/web/components/auth/StaffGate.tsx
@apps/web/lib/admin-programmes.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add conflict reconciliation, emergency release, and restore commands</name>
  <files>apps/web/lib/catalogue-publication.ts, apps/web/lib/server/catalogue-publications.ts, apps/web/app/api/admin/catalogue-publications/route.ts, apps/web/__tests__/lib/server/catalogue-publications.test.ts, apps/web/__tests__/api/admin-catalogue-publications.test.ts</files>
  <behavior>
    - A stale candidate write returns current and attempted safe comparable values; a resolution that keeps an older value rejects an absent or oversized reason and records an accepted reason in the audit.
    - A reviewer can make a reasoned emergency correction only after the checklist passes, creating a new `emergency` snapshot with prior snapshot reference.
    - An administrator restore creates a new `restore` snapshot that references the retained target snapshot; it does not retarget the active pointer to historic state in place.
  </behavior>
  <action>Implement D-09/D-10/D-15 using the existing single-attempt conditional-write style. Return a structured conflict DTO containing only current/attempted candidate fields approved for staff comparison, revisions, and merge choices. Require a short bounded reason whenever an explicit choice retains an older attempted field over the newer stored field; append that reason only to the catalogue audit event. Require `editor` for a candidate conflict resolution initiated by its creator, `reviewer` for emergency correction, and `administrator` for restore; verify each exact capability server-side before request parsing. Add reviewer-only emergency correction that requires a bounded reason, reruns the same checklist immediately, applies media fallback when needed, and appends an `emergency` snapshot/manifest. Add administrator-only restore by copying a retained snapshot into a fresh `restore` snapshot and manifest with a prior/target reference, keeping the former active snapshot retained. Every command must return a safe conflict rather than retrying, and every route must authorize before request parsing.</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test -- --runInBand __tests__/lib/server/catalogue-publications.test.ts __tests__/api/admin-catalogue-publications.test.ts</automated></verify>
  <done>Staff can reconcile a stale candidate deliberately, reviewers can create fully audited emergency snapshots, and administrators can restore through a new append-only snapshot without deleting history.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Provide a focused, accessible intake, review, and conflict console</name>
  <files>apps/web/app/admin/catalogue-publications/page.tsx, apps/web/components/admin/CataloguePublicationManager.tsx, apps/web/__tests__/components/CataloguePublicationManager.test.tsx, apps/web/app/api/admin/catalogue-publications/route.ts, apps/web/__tests__/api/admin-catalogue-publications.test.ts</files>
  <behavior>
    - A staff member sees private candidate lifecycle state, ordered corrections, creator/reviewer separation, safe per-record audit evidence, and the reviewer DTO's source, material-evidence, freshness, claim-boundary, regional-boundary, and media-rights statuses without seeing configuration secrets or learner data.
    - The console visibly explains that a passing six-category checklist means editorial completeness, not verified real-world provider truth.
    - A stale-write response renders two values side by side and requires a reason field before submitting an older-value choice.
    - Intake, correction/resubmission, reviewer approval, and conflict resolution controls are rendered only for the corresponding capability and show server correction/conflict feedback accessibly.
  </behavior>
  <action>Create a new staff-gated `/admin/catalogue-publications` page and a focused client manager rather than extending `ProgrammeAdminManager`. Fetch only the safe candidate-intake/review/conflict DTOs from the Phase 10 route, use labeled forms and keyboard-operable controls, keep errors/status text available to assistive technology, and render a clear distinction between private draft, awaiting review, approved, and quarantined states. Render the reviewer candidate DTO's six ordered automated checklist categories — source, material evidence, freshness, claim boundary, regional boundary, and media-rights status — beside corrections, with a visible fixed explanation that pass means editorial completeness, not verified real-world provider truth. Include structured import paste/upload interaction, individual candidate correction/resubmission, independent reviewer approval, and side-by-side conflict resolution with conditional older-value reason input. Do not add weekly preview/publish, emergency, restore, snapshot-history, or manifest controls in this task; Plan 05 adds that release/recovery/audit console slice against its dedicated DTOs. Let the server remain authoritative: hiding a button is never a substitute for a route capability check. Add component/API tests for capability-sensitive intake/review controls, all six safe checklist summaries and the pass disclosure, correction messages, conflict reason requirement, and safe candidate audit rendering.</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test -- --runInBand __tests__/components/CataloguePublicationManager.test.tsx __tests__/api/admin-catalogue-publications.test.ts</automated></verify>
  <done>Authorized staff can operate intake, correction, independent review, and deliberate conflict reconciliation in an accessible management surface; unauthorized or unqualified actors cannot use the server commands even if they construct requests directly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| stale client revision → conflict resolver | A user-selected merge must not overwrite newer stored information silently. |
| reviewer/admin request → emergency or restore command | A high-impact public snapshot operation needs current capability, checklist, reason, and immutable history. |
| staff DTO → browser manager | Operations data becomes visible in a browser but must exclude secrets and learner data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-10 | T/R | conflict resolver | high | mitigate | Expected revision, compare DTO, required older-value reason, concise audit, and no automatic retry. |
| T-10-11 | E/T | emergency/restore commands | high | mitigate | Current reviewer/admin authorization, bounded reasons, release-time checklist, append-only snapshot history, and CAS. |
| T-10-12 | I | staff console | medium | mitigate | Use a safe operational DTO and omit secrets, raw input, tokens, learner fields, and provider-private data. |
| T-10-SC | Tampering | package installation | high | mitigate | Keep implementation inside current Next.js/React dependencies; no new package is installed. |
</threat_model>

<verification>
Run focused server/API/component tests, then web lint, typecheck, and full Jest tests at the wave boundary. Include the configured human end-of-phase check rather than an in-plan checkpoint.
</verification>

<success_criteria>
Concurrent staff work, urgent catalogue corrections, and snapshot recovery all remain deliberate, role-controlled, auditable, and recoverable from a separate staff surface.
</success_criteria>

<output>
Create `.planning/phases/10-curated-import-and-governed-staff-publication/10-04-SUMMARY.md` when done.
</output>
