---
phase: 10-curated-import-and-governed-staff-publication
plan: 06
type: execute
wave: 6
depends_on: [10-05]
files_modified:
  - apps/web/lib/catalogue-publication.ts
  - apps/web/lib/server/catalogue-publications.ts
  - apps/web/lib/server/data-store.ts
  - apps/web/app/api/admin/catalogue-publications/import/route.ts
  - apps/web/components/admin/CataloguePublicationManager.tsx
  - apps/web/__tests__/lib/catalogue-publication.test.ts
  - apps/web/__tests__/lib/server/catalogue-publications.test.ts
  - apps/web/__tests__/lib/data-store.test.ts
  - apps/web/__tests__/api/admin-catalogue-publications.test.ts
  - apps/web/__tests__/components/CataloguePublicationManager.test.tsx
autonomous: true
gap_closure: true
requirements: [EVID-04, PUB-01, PUB-03]
must_haves:
  truths:
    - "A full-document recovery rejects semantically malformed or incoherent catalogue publication state before it can replace the stored public snapshot."
    - "An active editor can receive a bounded current-versus-attempted conflict DTO from a real stale private import and resolve its title and claim-boundary choices in the staff console."
    - "Keeping an older attempted title or claim boundary remains impossible without the server-validated audit reason, and the stale import performs no automatic retry or partial state write."
  artifacts:
    - path: "apps/web/lib/catalogue-publication.ts"
      provides: "Deep, coherent candidate, snapshot, manifest, lineage, and canonical-digest validation"
      exports: ["isCataloguePublicationState"]
    - path: "apps/web/app/api/admin/catalogue-publications/import/route.ts"
      provides: "Editor-authorized stale-import response with bounded comparison data"
      exports: ["POST"]
    - path: "apps/web/components/admin/CataloguePublicationManager.tsx"
      provides: "Staff conflict screen populated by a real stale-import response"
      exports: ["default"]
  key_links:
    - from: "apps/web/lib/server/data-store.ts"
      to: "apps/web/lib/catalogue-publication.ts"
      via: "validateScholarScoutDataImport calls deep isCataloguePublicationState before normalization or recovery write"
      pattern: "isCataloguePublicationState"
    - from: "apps/web/app/api/admin/catalogue-publications/import/route.ts"
      to: "apps/web/lib/server/catalogue-publications.ts"
      via: "editor-authorized import returns the server-created safe conflict DTO on a stale upsert"
      pattern: "importCatalogueCandidates"
    - from: "apps/web/components/admin/CataloguePublicationManager.tsx"
      to: "apps/web/app/api/admin/catalogue-publications/import/route.ts"
      via: "409 stale-import response initializes the comparison UI and conflict-resolution request"
      pattern: "catalogue-publications/import"
---

<objective>
Close the two verification-proven Phase 10 safety gaps: full-recovery publication-state coherence and real stale-import conflict recovery.

Purpose: prevent a malformed recovered state from reaching learner reads, while making the already-designed safe conflict controls reachable from staff intake rather than a synthetic UI scenario.
Output: deep recovery validator with negative restore coverage, a bounded stale-import DTO path, and staff-console end-to-end conflict coverage.
</objective>

<execution_context>
@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/10-curated-import-and-governed-staff-publication/10-CONTEXT.md
@.planning/phases/10-curated-import-and-governed-staff-publication/10-VERIFICATION.md
@.planning/phases/10-curated-import-and-governed-staff-publication/10-01-SUMMARY.md
@.planning/phases/10-curated-import-and-governed-staff-publication/10-02-SUMMARY.md
@.planning/phases/10-curated-import-and-governed-staff-publication/10-03-SUMMARY.md
@.planning/phases/10-curated-import-and-governed-staff-publication/10-04-SUMMARY.md
@.planning/phases/10-curated-import-and-governed-staff-publication/10-05-SUMMARY.md
@apps/web/lib/catalogue-publication.ts
@apps/web/lib/server/catalogue-publications.ts
@apps/web/lib/server/data-store.ts
@apps/web/app/api/admin/catalogue-publications/import/route.ts
@apps/web/components/admin/CataloguePublicationManager.tsx
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry one stale private import into the real staff conflict screen</name>
  <files>apps/web/lib/server/catalogue-publications.ts, apps/web/app/api/admin/catalogue-publications/import/route.ts, apps/web/components/admin/CataloguePublicationManager.tsx, apps/web/__tests__/lib/server/catalogue-publications.test.ts, apps/web/__tests__/api/admin-catalogue-publications.test.ts, apps/web/__tests__/components/CataloguePublicationManager.test.tsx</files>
  <behavior>
    - An editor stages a valid private candidate, another permitted edit advances its revision, and the original one-record upsert import receives HTTP 409 with only candidate ID, current/attempted revisions, title, claim boundary, region ID, and the two supported merge choices.
    - The stale import does not modify the candidate, retry automatically, or disclose the raw import, facts, sources, media, staff configuration, or learner data.
    - The manager consumes that actual 409 DTO, displays current and attempted title and claim-boundary values, lets the editor choose each field, and sends the returned candidate identity/revision plus the chosen values to the existing resolution action.
    - Selecting an older attempted value requires an 8-to-500-character reason in the browser and remains enforced by the server command; selecting current values does not fabricate an audit reason.
  </behavior>
  <action>Implement the verification gap for D-05, D-09, and D-15 as one production-quality import-to-console path. Preserve the import route's existing `requireActiveStaff` editor authorization before JSON parsing. Extend the server import seam so a stale upsert can surface the existing safe comparison contract without performing a second mutation: derive the comparison from the stored candidate and the already parsed attempted candidate, return only the D-09 allowlisted fields, and leave a multi-record batch atomic. The import route must translate that typed stale result into a safe 409 body, while malformed import, ownership, and persistence-conflict responses retain their existing safe behavior. Do not introduce provider fetching, accept client-supplied current values, expose raw candidate/import objects, or retry a stale mutation.

Replace the manager's locally fabricated conflict initialization with handling for the import endpoint's returned conflict. Keep the import editor as the genuine source of the attempted candidate; populate the conflict state exclusively from the returned DTO. Render both title and claim-boundary comparisons with explicit current/attempted choices, keep comparable values read-only, make both choice states controllable, and send the existing conflict-resolution endpoint the returned attempted values and selected choices. Retain the accessible live status feedback and let the server remain the final reason validator. Add red-first server, route, and component regressions covering the complete stale import sequence, DTO field redaction, no-write result, title/claim choice rendering, required older-value reason, and a successful reasoned resolution.</action>
  <verify>
    <automated>corepack pnpm --filter @scholar-scout/web test -- --runInBand __tests__/lib/server/catalogue-publications.test.ts __tests__/api/admin-catalogue-publications.test.ts __tests__/components/CataloguePublicationManager.test.tsx</automated>
  </verify>
  <done>A real stale private import opens the staff comparison screen with server-derived safe values; staff can resolve both permitted fields deliberately, and stale intake has no write or disclosure escape hatch.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject incoherent recovered catalogue-publication state before public recovery</name>
  <files>apps/web/lib/catalogue-publication.ts, apps/web/lib/server/data-store.ts, apps/web/__tests__/lib/catalogue-publication.test.ts, apps/web/__tests__/lib/data-store.test.ts, apps/web/__tests__/lib/server/catalogue-publications.test.ts</files>
  <behavior>
    - Legacy Scholar Scout documents with no catalogue-publication property still normalize to the empty private state.
    - A recovery candidate must have coherent identity, revision, ownership, timestamp, lifecycle, approval, media, and checklist data; incomplete private drafts remain representable only when their calculated correction-state checklist is coherent, while approved candidates require a passing coherent checklist and matching approval revision.
    - Every recovered snapshot has a unique ID and sequence, valid public records and canonical digest, an exact one-to-one manifest with matching immutable metadata/digest, and valid non-cyclic references to retained earlier snapshots; an active ID resolves to an existing coherent snapshot.
    - A malformed candidate, altered snapshot record/digest, orphan or mismatched manifest, invalid lineage, or dangling active ID makes both validation and full recovery fail before the stored learner read model changes.
  </behavior>
  <action>Implement the first verification gap for D-03, D-07, D-08, D-11, D-12, D-13, and D-14 by strengthening `isCataloguePublicationState` from shallow shape checking into deterministic semantic/coherence validation. Reuse the Phase 9 catalogue validators, the Phase 10 checklist evaluator, exact finite unions, ISO date parsing, stable IDs, canonical public-record digest helper, and existing manifest/snapshot types; do not relax a checklist into a provider-truth, eligibility, or outcome claim. Distinguish an intentionally incomplete private draft or quarantined candidate from malformed recovered data by validating its core lifecycle fields and requiring the stored checklist's ordered categories, correction codes, disclosure, fallback status, and approval state to agree with its recoverable candidate input. Require approved state to be complete, passing, and revision-consistent. Validate public records independently because snapshots are the learner boundary.

Require snapshot IDs/sequences and manifest IDs/snapshot IDs to be unique; require each snapshot to have exactly one manifest whose kind, release timestamp, period/restore/prior metadata, sequence, digest, and included/retired/quarantine entry contracts agree with that snapshot and its valid lineage. Ensure references point only to an earlier retained snapshot, do not form a self-reference/cycle, and make a non-null active snapshot ID resolve to a validated stored snapshot. Keep `validateScholarScoutDataImport` as the recovery gate before `normalizeImportData`, `restoreScholarScoutDataFromImport`, and backup recovery can preserve an incoming state; legacy omission must still take the existing empty-state branch. Add negative full-recovery tests that seed valid state, attempt each incoherent recovery shape, assert the stable validation failure and rejected restore, then prove the prior active public snapshot remains the only readable learner model. Include pure validator tests for coherent normal, emergency, and restore lineage plus each malformed semantic case.</action>
  <verify>
    <automated>corepack pnpm --filter @scholar-scout/web test -- --runInBand __tests__/lib/catalogue-publication.test.ts __tests__/lib/data-store.test.ts __tests__/lib/server/catalogue-publications.test.ts; corepack pnpm --filter @scholar-scout/web run typecheck; corepack pnpm --filter @scholar-scout/web run lint</automated>
  </verify>
  <done>Only coherent versioned catalogue-publication states survive full document recovery, legacy data remains compatible, and failed recovery cannot alter the stored active public snapshot.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| recovered whole-document input → data store | A recovery artifact may try to make malformed private or public catalogue state authoritative. |
| editor import JSON → staff conflict DTO | An untrusted stale attempt crosses from a bounded private import into a browser-visible comparison. |
| staff conflict choice → conditional mutation | A deliberate merge can replace a newer stored field only with server-side concurrency and reason checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-10-16 | T/I | recovery validation and snapshot reader | high | mitigate | Validate candidates, snapshots, manifests, digest, lineage, and active reference before normalization/recovery writes; prove failed restore preserves the prior read model. |
| T-10-17 | T/R | stale import and conflict resolver | high | mitigate | Authorize before parsing, return the bounded D-09 comparison, keep import atomic/no-retry, and require the server-validated reason for an older selected value. |
| T-10-18 | I | staff conflict DTO | medium | mitigate | Restrict the DTO to ID, revisions, title, claim boundary, region, and choices; test it contains no raw import or private operational data. |
| T-10-SC | Tampering | package installation | high | mitigate | Use the existing TypeScript, Next.js, React, Jest, and Node facilities; no package is installed. |
</threat_model>

<verification>
Run the focused stale-import and recovery suites, then web typecheck and lint. At the phase boundary, run the full web Jest suite and re-run the Phase 10 verification report so EVID-04, PUB-01, and PUB-03 are evidenced by the repaired recovery and staff-conflict paths.
</verification>

<success_criteria>
The public catalogue can be restored only from coherent, independently verified stored state, and staff can reach an auditable conflict comparison through a real private import without losing work or exposing private data.
</success_criteria>

## Source Coverage Audit

| Source | Required item | Coverage |
|---|---|---|
| GOAL | Deterministic reviewed snapshot safe through recovery | Task 2 closes the verifier's recovered-state gap. |
| GOAL | Active authorized staff have recoverable stale conflict handling | Task 1 closes the disconnected real-intake gap. |
| REQ | EVID-04 provider-independent versioned snapshot | Task 2 validates the recovered snapshot, manifest, digest, and learner boundary. |
| REQ | PUB-01 authorized lifecycle with audit evidence | Task 1 preserves authorization-before-parsing and reasoned conflict audit. |
| REQ | PUB-03 bounded validated intake and conflict-safe recovery | Tasks 1 and 2 cover the import conflict and whole-document recovery boundaries. |
| CONTEXT | D-01 through D-04 review/checklist lifecycle | Preserved by existing 10-01 through 10-05; Task 2 adds checklist coherence validation. |
| CONTEXT | D-05 through D-08 bounded intake and immutable snapshots | Task 1 completes a bounded intake conflict; Task 2 validates immutable recovery state. |
| CONTEXT | D-09 through D-11 conflict, emergency, quarantine controls | Task 1 wires D-09; Task 2 validates their recovered state. |
| CONTEXT | D-12 through D-15 media and redacted audit boundaries | Task 2 validates media/checklist coherence; Task 1 redacts conflict data and preserves reason audit. |

<output>
Create `.planning/phases/10-curated-import-and-governed-staff-publication/10-06-SUMMARY.md` when done.
</output>
