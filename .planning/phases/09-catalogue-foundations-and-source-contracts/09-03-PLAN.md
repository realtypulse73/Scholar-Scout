---
phase: 09-catalogue-foundations-and-source-contracts
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - apps/web/lib/catalogue-contract.ts
  - apps/web/__tests__/lib/catalogue-contract.test.ts
autonomous: true
requirements:
  - EVID-01
  - EVID-02
  - EVID-05
must_haves:
  truths:
    - "Every material fact has required authority, source label/URL, structured source date, review date, explicit factual state, and direct verification action."
    - "Current, needs-confirmation, unknown, and conflicting evidence states remain distinguishable at the six-month and two-year boundaries."
    - "Employer-linked training keeps taught skill, employer payer, trainee pay evidence, and employment-commitment disclosure structurally distinct."
    - "Wage context is dated occupation-and-area information, structurally separate from provider offers and any individual forecast."
    - "A non-sensitive opportunity-card contract carries location, pathway type, skill taught, training payer, cost or tuition, verification status, duration, and delivery as sourced facts or visibly unresolved facts."
  artifacts:
    - path: apps/web/lib/catalogue-contract.ts
      provides: Evidence, freshness, employer-training, and wage-context contracts plus pure validators.
      exports:
        - FactEvidence
        - getFreshnessStatus
        - validateFactEvidence
        - validateEmployerTrainingFacts
        - OccupationAreaWageContext
        - CatalogueOpportunityCardFacts
        - validateCatalogueOpportunityCardFacts
        - getOpportunityCardVerificationStatus
    - path: apps/web/__tests__/lib/catalogue-contract.test.ts
      provides: Evidence state, freshness threshold, paid-training, and wage-context regression coverage.
  key_links:
    - from: apps/web/lib/catalogue-contract.ts
      to: later Phase 10 snapshot validation
      via: Field-level evidence validator and freshness classifier are reusable pure domain exports.
      pattern: "validateFactEvidence|getFreshnessStatus"
  prohibitions:
    - "No live learner-facing provider aggregation, runtime scraping, or automatic publication."
    - "No provider-quality, eligibility, job, salary, placement, or outcome claim is derived by this foundation."
    - "No sensitive re-entry information, sensitive ranking, or sensitive query field is present."
    - "No sensitive support/reentry data, eligibility/ranking field, provider inventory, runtime source retrieval, or user-facing discovery is represented by the card-fact contract."
    - "Missing, stale, or conflicting facts are never silently upgraded to current."
---

<objective>
Add field-level evidence, freshness, a non-sensitive first-view opportunity-card contract, paid-training disclosure, and wage-context contracts to the catalogue foundation.

Purpose: Prevent stale source data or a paid-training statement from being mistaken for a current provider, job, or personal-pay promise.
Output: Pure evidence, card-fact, and disclosure validators with deterministic threshold tests.
</objective>

<execution_context>
@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-CONTEXT.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-RESEARCH.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-VALIDATION.md
@apps/web/lib/catalogue-contract.ts

<interfaces>
Extend the separate Phase 9 `catalogue-contract.ts` module from Plan 09-01. Reuse its required `SourceDate` and `SourceMetadata` types plus injected-clock `CATALOGUE_FRESHNESS_POLICY` / `getFreshnessStatus`; do not make `sourceDate` optional anywhere. A tagged unavailable source date stays structurally present and makes the affected fact explicitly non-current/unresolved. Its controlled `employer-linked-training` pathway ID is the only route allowed to use `EmployerTrainingFacts`. No existing `Programme`, publication route, persistence store, or ranking helper changes in this plan.

Use the research-defined policy constants: 183 days for operational facts and 731 days for official boundary records. All classifier functions accept an injected `now: Date` so test results never depend on wall-clock time.

The Phase 9 card contract is a domain-only record boundary, not a provider inventory, route payload, live-source client, or rendered discovery surface. Its required first-view facts are location, pathway type, skill taught, training payer, cost or tuition, verification status, duration, and delivery. The allowed delivery vocabulary is `in-person`, `online`, or `hybrid`; use the controlled pathway vocabulary from Plan 09-01. A material value cannot be carried as a bare string/number: it is a `SourcedFact<Value>` with valid evidence or an explicit unresolved value with a visible `unknown`, `conflicting`, or `needs-confirmation` state and a verification action. Verification status is a deterministic aggregate derived from the individual card-fact states, not a raw provider-quality or outcome field.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce field-level evidence and deterministic freshness</name>
  <files>apps/web/lib/catalogue-contract.ts, apps/web/__tests__/lib/catalogue-contract.test.ts</files>
  <read_first>
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-CONTEXT.md (D-04, D-06, D-08)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-RESEARCH.md (Patterns 3 and freshness classifier)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-PATTERNS.md (`outcome-profiles.ts` / `outcome-profiles.test.ts` are the pure-validator, fixed-clock, and unit-test analogs)
    - apps/web/lib/catalogue-contract.ts
    - apps/web/lib/outcome-profiles.ts
    - apps/web/__tests__/lib/outcome-profiles.test.ts
  </read_first>
  <behavior>
    - A usable material fact requires authority, valid HTTP(S) source URL/label, required structured source date, review date, factual state, and an actionable direct-verification instruction.
    - An explicit `conflicting` or `unknown` assertion remains that state; malformed, future-dated, incomplete, or explicitly unavailable source-date evidence cannot become current.
    - Current evidence becomes needs-confirmation immediately beyond 183 operational days; the injected clock makes exact boundary behavior repeatable.
  </behavior>
  <action>Extend `catalogue-contract.ts` per D-04, D-06, and D-08 with `FactStatus`, `FactAuthority`, `FactEvidence`, and `SourcedFact<Value>` on top of the Plan 09-01 source metadata and injected-clock classifier exports. Every `FactEvidence` instance must require the structured `SourceDate` field; accept a documented source date only when it validates as an ISO calendar date, and accept its tagged unavailable variant only when the fact is an explicit non-current unresolved state with a direct verification action. Preserve `unknown` and `conflicting` states rather than deriving them away, keep `needs-confirmation` distinct from `current`, and reject missing source-date structure, invalid/future source dates, missing/invalid/future review dates, or incomplete evidence from current classification. Write red tests first for each required field, both source-date variants, invalid URL/date, exact operational and boundary thresholds, one day beyond each threshold, explicit conflicting evidence, and legacy-state mapping. The neutral confirmation action should support D-04’s `Confirm details` wording without rating programme quality. Keep re-entry support and participation-restriction fields outside this Phase 9 contract per D-07; Phase 14 owns that separately sourced referral/policy surface.</action>
  <verify>
    <automated>pnpm --filter @scholar-scout/web run test -- catalogue-contract &amp;&amp; pnpm --filter @scholar-scout/web run typecheck</automated>
  </verify>
  <acceptance_criteria>Evidence validation and the injected-clock classifier preserve four public states and reliably downgrade incomplete or stale operational facts.</acceptance_criteria>
  <done>Every future catalogue material fact has a reusable, test-proven evidence contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Separate paid training and wage context from outcome claims</name>
  <files>apps/web/lib/catalogue-contract.ts, apps/web/__tests__/lib/catalogue-contract.test.ts</files>
  <read_first>
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-CONTEXT.md (D-02 and D-08)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-RESEARCH.md (Pattern 4 and Pitfall 5)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-PATTERNS.md (`outcome-profiles.ts` / `outcome-profiles.test.ts` are the fact-validator/test analogs)
    - apps/web/lib/catalogue-contract.ts
    - apps/web/lib/outcome-profiles.ts
    - apps/web/__tests__/lib/outcome-profiles.test.ts
  </read_first>
  <behavior>
    - Employer-linked training requires a taught skill, `trainingPayer: 'employer'`, trainee-pay evidence with required structured source date, employment-commitment state, and commitment evidence with required structured source date.
    - A record without a published commitment uses the controlled `no-published-guarantee` disclosure; ambiguous source material remains `unknown` with a verification action.
    - Occupation-and-area wage context requires its own structured source date/review date/evidence and a clear informational-not-offer label, with no provider, learner, offer, placement, or forecast field.
  </behavior>
  <action>Add `EmployerTrainingFacts` and its validator for D-02: scope it exclusively to employer-linked training, require a factual taught-skill field, employer payer, separately validated trainee-pay evidence, controlled employment-commitment state, and separately validated commitment evidence. The taught-skill, trainee-pay, and commitment material facts must each carry Plan 09-01's required structured source date and review date through `FactEvidence`; a tagged unavailable source date is permitted only with an explicit non-current unresolved fact and direct verification action. Do not infer a commitment from narrative text. Add `OccupationAreaWageContext` as a distinct occupation-plus-area informational record with its own evidence including required structured source/review dates and a fixed explanatory label that makes it context rather than an offer or forecast. Write tests that reject missing source-date structure, trainee-pay/commitment evidence, preserve `no-published-guarantee` and `unknown` disclosures, reject a wage context without independent dated evidence, and assert the contract exposes no outcome/ranking fields. Record the EVID-02 and EVID-05 source-content limitation explicitly: automated tests validate the shape and policy, while Phase 10 staff review must approve the real-world source and wording before any such record is published.</action>
  <verify>
    <automated>pnpm --filter @scholar-scout/web run test -- catalogue-contract &amp;&amp; pnpm --filter @scholar-scout/web run lint</automated>
  </verify>
  <acceptance_criteria>Paid-training and wage-context types make their evidence and uncertainty visible while preventing the contract from representing a personal outcome or provider employment promise.</acceptance_criteria>
  <done>Employer-linked training and wage context can be carried forward as factual, source-backed information without becoming a promise.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make complete first-view opportunity facts source-safe</name>
  <files>apps/web/lib/catalogue-contract.ts, apps/web/__tests__/lib/catalogue-contract.test.ts</files>
  <read_first>
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-CONTEXT.md (D-08 and D-07 boundary)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-RESEARCH.md (field-level evidence and no-live-aggregation boundary)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-VALIDATION.md (09-03-03)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-PATTERNS.md (`outcome-profiles.ts` / `outcome-profiles.test.ts` are the typed baseline-builder and validator-test analogs)
    - apps/web/lib/catalogue-contract.ts
    - apps/web/lib/outcome-profiles.ts
    - apps/web/__tests__/lib/outcome-profiles.test.ts
  </read_first>
  <behavior>
    - `CatalogueOpportunityCardFacts` requires location, pathway type, skill taught, training payer, cost-or-tuition, duration, and delivery card facts; each accepts only a valid `SourcedFact` with required structured source/review dates or an explicit unresolved fact with a visible non-current state, required structured source/review dates, and direct verification action.
    - The card contract derives a deterministic verification status from its constituent fact states and keeps `current`, `needs-confirmation`, `unknown`, and `conflicting` distinguishable.
    - Delivery accepts only `in-person`, `online`, or `hybrid`; pathway type accepts only the controlled Plan 09-01 vocabulary.
    - The validator rejects a bare material value, missing first-view field, malformed evidence, unsupported delivery/pathway, and a purported unresolved fact without a visible state/action.
  </behavior>
  <action>Implement D-08 with named `CatalogueOpportunityCardFacts`, `CatalogueCardFact<Value>`, an explicit unresolved-card-fact union, `validateCatalogueOpportunityCardFacts`, and `getOpportunityCardVerificationStatus`. Require typed card facts for location, controlled pathway type, skill taught, training payer, cost or tuition, duration, and controlled delivery. Each independently material value must be a validated `SourcedFact<Value>` with required structured source/review dates or an explicit unresolved card fact with a non-current visible state, required structured source/review dates, and direct verification action; reject raw scalar fact values and missing source-date structure. Calculate the card verification status only from validated constituent fact states so it is visible without becoming a provider-quality, eligibility, ranking, or outcome assertion. Keep the contract domain-only and non-sensitive: do not add data fields for support/referral circumstances, reentry, learner identity, qualifications, eligibility, ranking, provider inventory, source fetching, routes, or UI. Write dedicated regression tests independent of the employer-training suite: one fully sourced card; one card with an explicit needs-confirmation cost; each permitted delivery value; current/needs-confirmation/unknown/conflicting aggregate outcomes; and rejections for each required field, bare values, unsupported controlled value, malformed evidence, missing source-date structure, and unresolved facts missing state/action.</action>
  <verify>
    <automated>pnpm --filter @scholar-scout/web run test -- catalogue-contract &amp;&amp; pnpm --filter @scholar-scout/web run typecheck</automated>
  </verify>
  <acceptance_criteria>The D-08 first-view contract can only represent every required card fact as attributable evidence or visible unresolved information, with deterministic verification state and no pathway to a sensitive, provider-inventory, or discovery feature.</acceptance_criteria>
  <done>A later reader can use the complete, source-safe opportunity-card record without interpreting raw facts or collecting student-sensitive information.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source assertion → factual evidence | Source data can be stale, incomplete, conflicting, or misrepresented as current. |
| Employer / wage source → catalogue contract | Pay and training statements can be misread as employment, salary, or placement commitments. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-07 | Tampering | `FactEvidence` and freshness classifier | high | mitigate | Required field-level provenance, injected-clock thresholds, and non-current handling for incomplete/stale/future evidence. |
| T-09-08 | Spoofing | employer training disclosure | high | mitigate | Controlled payer/commitment unions and separate validated pay/commitment evidence; no prose inference. |
| T-09-09 | Information disclosure | evidence / wage contract | medium | mitigate | Public catalogue record types omit re-entry, personal qualification, identity, and ranking fields. |
| T-09-10 | Repudiation | wage context | medium | mitigate | Require independent dated authority/source/verification action and an informational-context label. |
| T-09-11 | Tampering | `CatalogueOpportunityCardFacts` | high | mitigate | Limit the contract to non-sensitive first-view factual fields, require sourced-or-explicitly-unresolved material values, and reject raw facts or unsupported controlled values in dedicated regression tests. |
| T-09-SC | Tampering | package installation | high | mitigate | No packages are installed in Phase 9; existing TypeScript/Jest tooling only. |
</threat_model>

## Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Truthful facts and visible uncertainty | All tasks make provenance, age, factual state, verification action, and complete first-view facts executable domain rules. |
| REQ | EVID-01, EVID-02, EVID-05 | Task 1 handles required structured source dates and evidence/freshness; Task 2 handles paid training and separate wage context; Task 3 applies the same source-date/evidence rule to every D-08 first-view fact. |
| RESEARCH | Per-fact authority, source/review dates, four states, 183/731 thresholds, explicit paid-training disclosure, no-live-aggregation boundary | Tests cover documented and unavailable source-date variants, each controlled state and threshold; field shapes keep claims separate and card facts remain domain-only. |
| CONTEXT | D-02, D-04, D-06, D-07, D-08 | Task 1 implements neutral uncertainty and dated facts while excluding D-07’s separately sourced policy surface; Task 2 implements factual paid-training representation; Task 3 implements D-08’s complete non-sensitive first-view contract. |

<verification>
Run `pnpm --filter @scholar-scout/web run test -- catalogue-contract`, `pnpm --filter @scholar-scout/web run typecheck`, and `pnpm --filter @scholar-scout/web run lint`. Before Phase 10 publication work, manually inspect a proposed real source against the fixture/contract fields; this plan intentionally has no real provider, wage, policy, or payment claim to approve.
</verification>

<success_criteria>
- Field-level evidence contains attribution, required structured source/review dates, status, and a direct verification action; a source date is never omitted.
- Exact six-month/two-year transitions and all four factual states are regression-tested.
- The employer-training and wage-context shapes cannot communicate a provider-quality or individual outcome claim.
- Every D-08 first-view fact is a sourced value or explicitly unresolved with a visible verification action; the domain contract remains non-sensitive and non-discovery-facing.
</success_criteria>

## Artifacts this phase produces

- Extended `apps/web/lib/catalogue-contract.ts` — field-evidence, freshness, paid-training, and wage-context vocabulary.
- Extended `apps/web/__tests__/lib/catalogue-contract.test.ts` — deterministic claim-safety and freshness tests.

<output>
Create `.planning/phases/09-catalogue-foundations-and-source-contracts/09-03-SUMMARY.md` when done.
</output>
