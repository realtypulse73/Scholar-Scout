---
phase: 09-catalogue-foundations-and-source-contracts
plan: 04
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
  - 09-03
files_modified:
  - apps/web/lib/catalogue-contract.ts
  - apps/web/lib/catalogue-fixtures.ts
  - apps/web/__tests__/lib/catalogue-contract.test.ts
  - apps/web/__tests__/lib/catalogue-fixtures.test.ts
autonomous: true
requirements:
  - REG-01
  - REG-02
  - REG-03
  - EVID-01
  - EVID-02
  - EVID-05
must_haves:
  truths:
    - "A region's official authority and boundary identifier are accepted only when they are the exact declared pair for that region; a Kingston record cannot carry a U.S. CBSA identifier."
    - "Every documented fact source is no later than the injected clock, its review is no earlier than its source, and unavailable source dates remain explicitly non-current."
    - "A verified coverage cell has current attributable evidence and a direct verification action; an unverified cell cannot be mistaken for a local offering."
    - "Malformed runtime values for sourced text facts produce deterministic validation errors rather than exceptions."
    - "Unresolved first-view facts have a null value and a valid non-current state, and employment-commitment states match their evidence state."
    - "Frozen regional and coverage fixtures are deeply immutable, with independent date objects for every boundary and local-focus source."
  artifacts:
    - path: apps/web/lib/catalogue-contract.ts
      provides: Exact regional provenance, chronology-safe evidence, discriminated coverage, resilient text validation, and claim-safe card/employment contracts.
      exports:
        - validateCatalogueRegion
        - validateFactEvidence
        - validateCoverageMatrix
        - validateEmployerTrainingFacts
        - validateCatalogueOpportunityCardFacts
    - path: apps/web/lib/catalogue-fixtures.ts
      provides: Deeply immutable six-region source fixtures and the complete explicit 36-cell coverage baseline.
    - path: apps/web/__tests__/lib/catalogue-contract.test.ts
      provides: Targeted regression tests for CR-01 through CR-04 and WR-01 through WR-02.
    - path: apps/web/__tests__/lib/catalogue-fixtures.test.ts
      provides: Targeted deep-immutability and independent-source-date regression tests for WR-03.
  key_links:
    - from: apps/web/lib/catalogue-fixtures.ts
      to: apps/web/lib/catalogue-contract.ts
      via: Deeply readonly region and coverage records pass the contract validators without losing controlled region, pathway, or provenance semantics.
      pattern: "validate(CatalogueRegion|CoverageMatrix)|DeepReadonly"
    - from: apps/web/lib/catalogue-contract.ts
      to: Phase 10 reviewed import validation
      via: Pure validators reject forged regional provenance, impossible evidence chronology, unsupported verified coverage, malformed values, and claim-state mismatches before a later publication boundary consumes them.
      pattern: "validate(CatalogueRegion|FactEvidence|CoverageMatrix|EmployerTrainingFacts)"
  prohibitions:
    - "No provider inventory, runtime source retrieval, provider-site scraping, automatic publication, persistence, routes, or UI."
    - "No ranking, qualification verdict, sensitive/referral field, recruitment pressure, employment promise, salary forecast, or outcome claim."
    - "No change to the frozen source roster, exact six regions, controlled six pathway classes, D-08 first-view field list, 183-day operational policy, or 731-day boundary policy."
---

<objective>
Repair every Phase 09 code-review finding through a test-first pass while preserving the frozen, domain-only six-area catalogue foundation.

Purpose: Make the Phase 10 import/publication boundary safe to build upon by preventing forged regional provenance, impossible dates, unsupported coverage claims, malformed runtime data, unsupported card assertions, inconsistent employment claims, and mutable shared fixtures.
Output: Targeted regression tests followed by hardened pure catalogue contracts and deeply immutable fixtures.
</objective>

<execution_context>
@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-CONTEXT.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-RESEARCH.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-VALIDATION.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-PATTERNS.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-REVIEW.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-01-SUMMARY.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-02-SUMMARY.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-03-SUMMARY.md
@apps/web/lib/catalogue-contract.ts
@apps/web/lib/catalogue-fixtures.ts
@apps/web/__tests__/lib/catalogue-contract.test.ts
@apps/web/__tests__/lib/catalogue-fixtures.test.ts

<interfaces>
Keep the Phase 09 module pure and separate from legacy `Programme` records. The frozen region roster remains exactly five U.S. Census/OMB CBSA records with `26420`, `16980`, `15380`, `12060`, and `35380`, plus Greater Kingston, Jamaica with the STATIN KMA authority and a null boundary identifier. Do not alter the source URLs, boundary versions, check date, local-focus anchors, exact ten-mile rule, or 36 explicit `not-yet-verified` baseline rows.

The repair must preserve D-01's exact six-region/ten-mile foundation, D-02's separate employer-linked training disclosures, D-03's six browseable controlled pathways, D-04's neutral actionable confirmation state, D-05's explicit coverage state, D-06's existing fixed-clock freshness thresholds, D-07's exclusion of re-entry and participation-policy data, and D-08's source-safe non-sensitive first-view facts. The Deferred Ideas in CONTEXT.md remain out of scope.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Write failing cross-contract provenance and immutability regressions</name>
  <files>apps/web/__tests__/lib/catalogue-contract.test.ts, apps/web/__tests__/lib/catalogue-fixtures.test.ts</files>
  <precondition>The reviewed Phase 09 baseline is present and the new regressions fail before any production-module edit.</precondition>
  <read_first>
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-REVIEW.md (all CR and WR findings are the test matrix)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-CONTEXT.md (D-01 through D-08 scope and deferred ideas)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-VALIDATION.md (focused-command and task-map conventions)
    - apps/web/lib/catalogue-contract.ts (current public validator signatures and controlled values)
    - apps/web/lib/catalogue-fixtures.ts (frozen source roster and coverage construction)
    - apps/web/__tests__/lib/catalogue-contract.test.ts (typed baseline builders)
    - apps/web/__tests__/lib/catalogue-fixtures.test.ts (fixture assertion style)
  </read_first>
  <behavior>
    - CR-01: A U.S. region using STATIN/null provenance and Kingston using a U.S. CBSA authority or identifier are rejected, while the exact declared authority/ID pairs remain valid per D-01.
    - CR-02: At a fixed clock, documented source dates after the clock are rejected for both current and non-current evidence; a review before its documented source is rejected; an unavailable date is accepted only with an explicit non-current evidence status and action per D-04 and D-06.
    - CR-03: A runtime-shaped verified coverage row without evidence/action is rejected; a verified row with current attributable evidence/action is accepted; the complete not-yet-verified 36-cell baseline remains accepted and keeps all D-03 pathway classes explicit.
    - CR-04: Number, object, and array values in taught-skill, trainee-pay, and wage text inputs return validation errors without throwing.
    - WR-01 and WR-02: An unresolved card fact with a non-null value or a missing/invalid state is rejected; each employment-commitment state is rejected when paired with an incompatible evidence status and accepted only with its controlled compatible status.
    - WR-03: Nested fixture records and the exported arrays are frozen; all boundary/local-focus source-date instances are distinct; a mutation attempt cannot alter a second region's provenance.
  </behavior>
  <action>Before changing either production module, add focused Jest regressions for CR-01 through CR-04 and WR-01 through WR-03. Use existing valid typed baselines, override one field per case, and pass fixed dates into every freshness/evidence validator. Add runtime-shaped bad-input cases through narrow test-only casts and assert both that each validator does not throw and that it returns the expected recoverable error category. Cover commitment compatibility for every controlled commitment state, including an explicit conflicting state if the repaired union adds it. In fixture tests, assert public immutability via `Object.isFrozen`, test a nested mutation through a narrow test-only cast, then prove another region's source metadata remains unchanged and every boundary/local-focus source-date object has unique identity. Keep tests contract-facing; do not mock internals or make network requests. Run this task's two-file Jest command and stop if it unexpectedly passes before implementation.</action>
  <verify>
    <automated>pnpm --filter @scholar-scout/web run test -- __tests__/lib/catalogue-contract.test.ts __tests__/lib/catalogue-fixtures.test.ts --runInBand</automated>
  </verify>
  <done>The new regressions fail against the reviewed baseline for every listed finding, with failure output proving the tests exercise the public validator/fixture contract rather than test-only helpers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden regional, evidence, coverage, text, card, and commitment validation</name>
  <files>apps/web/lib/catalogue-contract.ts</files>
  <read_first>
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-REVIEW.md (CR-01, CR-02, CR-03, CR-04, WR-01, WR-02)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-CONTEXT.md (D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-RESEARCH.md (two-layer geography, source-date, evidence, coverage, and employer-training patterns)
    - apps/web/lib/catalogue-contract.ts
    - apps/web/__tests__/lib/catalogue-contract.test.ts
  </read_first>
  <behavior>
    - Exact regional provenance is bound by a private map keyed by `CatalogueRegionId`, including each U.S. CBSA ID and the null Kingston condition.
    - Documented fact dates are never future relative to the injected clock, reviews do not predate their documented source, and an unavailable date cannot support a current fact.
    - Coverage is discriminated by state: verified coverage holds current `FactEvidence`, including direct source/action provenance, while not-yet-verified coverage retains only the explicit non-availability state required by D-05.
    - Every malformed runtime text input returns an error array; unresolved card facts use null plus a supported non-current state; commitment/evidence pairs express only the evidence their state supports.
  </behavior>
  <action>Implement the contract repair in one pure-module pass. Add a private authoritative requirement map keyed by `CatalogueRegionId` and make `validateCatalogueRegion` require its exact boundary authority and boundary identifier: each U.S. region must use its declared Census/OMB authority and CBSA ID, while Greater Kingston must use STATIN KMA and a null identifier, per D-01. Preserve the existing separate local-focus shape and ten-mile rule. In `validateFactEvidence`, when a documented source date is valid, reject it if it is later than injected `now`, and reject a valid review date that precedes it; keep unavailable source dates limited to explicit non-current states with their required verification action, per D-04 and D-06.

Replace the optional-source coverage shape with a state-discriminated union. A `verified` cell must carry current `FactEvidence`, including authority, source date, review date, and direct verification action; `not-yet-verified` remains the explicit D-03/D-05 baseline without an availability assertion. Make coverage validation receive an injected clock for evidence validation, update readonly input signatures as needed for immutable fixtures, and retain controlled six-pathway/matrix validation. Guard sourced text values with an explicit string check before trimming so malformed runtime values return existing-style errors. Require unresolved D-08 card facts to have `value === null` and a supported non-current state that matches evidence. Extend the commitment union with a distinct conflicting state when needed, then enforce: definitive states accept only current or needs-confirmation evidence, unknown requires unknown evidence, and conflicting requires conflicting evidence. Do not add source fetching, provider inventory, persistence, UI, ranking, sensitive fields, or any decision/outcome logic.</action>
  <verify>
    <automated>pnpm --filter @scholar-scout/web run test -- __tests__/lib/catalogue-contract.test.ts --runInBand</automated>
  </verify>
  <done>All CR-01 through CR-04 and WR-01 through WR-02 contract regressions pass, while existing exact controlled region/pathway, D-08 field, freshness-policy, and domain-only tests remain green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make frozen regional and coverage fixtures deeply immutable</name>
  <files>apps/web/lib/catalogue-fixtures.ts</files>
  <read_first>
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-REVIEW.md (WR-03)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-CONTEXT.md (D-01, D-03, D-06)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-RESEARCH.md (frozen geographic source roster and no-provider-inventory boundary)
    - apps/web/lib/catalogue-contract.ts (readonly validator inputs and controlled fixture types after Task 2)
    - apps/web/lib/catalogue-fixtures.ts
    - apps/web/__tests__/lib/catalogue-fixtures.test.ts
  </read_first>
  <behavior>
    - The six frozen regional records and 36 coverage rows expose deeply readonly types and cannot be mutated through nested source, boundary, local-focus, or coverage properties.
    - Each boundary and local-focus source date is a separate immutable object, so a consumer cannot affect another record through aliasing.
    - The exact source roster, date-unavailable representation, controlled ordering, and all explicit not-yet-verified cells still validate under the repaired contract.
  </behavior>
  <action>Change only `catalogue-fixtures.ts` to export deeply readonly fixture data and deep-freeze every exported record, nested metadata object, source-date object, coverage row, and outer array. Build a fresh unavailable-date object for every official-boundary and local-focus metadata record instead of sharing one reference. If a small local `DeepReadonly` utility or freeze helper is necessary, keep it pure, local to this fixture boundary unless the Task 2 contract interface needs the type for readonly validator inputs. Preserve every frozen source string, date, coordinate, boundary version/identifier, exact 36-row controlled order, and D-03 `not-yet-verified` state. Do not introduce provider facts or alter any source claim. Run the two-file regression command, then complete the plan quality gate with TypeScript, lint, and the full Jest suite.</action>
  <verify>
    <automated>pnpm --filter @scholar-scout/web run test -- __tests__/lib/catalogue-contract.test.ts __tests__/lib/catalogue-fixtures.test.ts --runInBand &amp;&amp; pnpm --filter @scholar-scout/web run typecheck &amp;&amp; pnpm --filter @scholar-scout/web run lint &amp;&amp; pnpm --filter @scholar-scout/web run test --runInBand</automated>
  </verify>
  <done>WR-03 regressions pass; all repaired unit tests, web typecheck, lint, and full Jest are green without modifying frozen sources or crossing the Phase 09 domain-only boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frozen regional source record → catalogue validator | A forged authority/identifier relationship could misrepresent official regional scope. |
| Imported source/evidence assertion → catalogue contract | Untrusted or stale chronology, malformed values, and unsupported facts could falsely look public-ready or cause validation failure. |
| Coverage assertion → later catalogue reader | A source-less verified state could present an unproven area/pathway offering as attributable. |
| Shared static fixture → later consumer | Mutation or date-object aliasing could silently corrupt frozen provenance for another record. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-12 | Spoofing | `validateCatalogueRegion` | high | mitigate | Bind each controlled region ID to its exact authority and boundary-ID rule; test cross-region forged pairs. |
| T-09-13 | Tampering | `validateFactEvidence` | high | mitigate | Use injected-clock source/review chronology checks and non-current unavailable-date semantics. |
| T-09-14 | Spoofing | `validateCoverageMatrix` | high | mitigate | Require current attributable evidence and a verification action for verified coverage through a discriminated union. |
| T-09-15 | Denial of service | sourced text validators | high | mitigate | Type-check text before trimming and return deterministic errors for malformed runtime payloads. |
| T-09-16 | Tampering | unresolved card facts | high | mitigate | Require a null unresolved value, a supported non-current state, matching evidence, and direct action per D-08. |
| T-09-17 | Integrity | employer commitment disclosure | high | mitigate | Enforce a controlled commitment-to-evidence-state mapping so unknown/conflicting evidence cannot imply a definitive statement. |
| T-09-18 | Tampering | frozen fixture objects | medium | mitigate | Deep-freeze exported values, make types deeply readonly, and allocate independent source-date objects. |
| T-09-SC | Tampering | package installation | high | mitigate | No package install occurs; repair uses the existing TypeScript and Jest stack only. |
</threat_model>

## Source Coverage Audit

| Source | ID | Feature / constraint | Plan coverage | Status |
|--------|----|----------------------|---------------|--------|
| GOAL | — | Deterministic six-area source-first foundation with visible uncertainty and no invented availability | All tasks repair the contract/fixtures that enforce provenance, chronology, explicit coverage, and immutable frozen sources. | COVERED |
| REQ | REG-01 | Exact official boundary, authority, source, and release/check relationship | Tasks 1–2 test then bind each region to its authority and exact ID/null rule. | COVERED |
| REQ | REG-02 | Six controlled, browseable pathway classes in every area | Task 1 retains an explicit complete-matrix regression while Task 2 preserves controlled vocabulary/matrix validation. | COVERED |
| REQ | REG-03 | Honest state per metro/pathway | Tasks 1–2 require discriminated verified evidence and preserve all 36 explicit not-yet-verified cells. | COVERED |
| REQ | EVID-01 | Per-fact attribution, source/review dates, state, and action | Tasks 1–2 add chronology and verified-coverage/action regressions, then harden evidence validation. | COVERED |
| REQ | EVID-02 | Current, needs-confirmation, unknown, and conflicting remain distinct | Tasks 1–2 test and enforce source/date/commitment/card-state consistency at a fixed clock. | COVERED |
| REQ | EVID-05 | Wage context is sourced informational context, not a promise | Tasks 1–2 prove malformed wage input is recoverable while retaining the existing separate wage evidence contract. | COVERED |
| RESEARCH | — | Separate official boundary/local focus, fixed clock, explicit 36-cell coverage, pure validation, static fixtures, no live aggregation | Tasks 1–3 preserve those architecture patterns while closing provenance, date, coverage, input, and immutability gaps. | COVERED |
| CONTEXT | D-01 | Exact six-region official boundary plus separate ten-mile local focus | Tasks 1–2 bind official provenance by exact region and preserve distinct local focus. | COVERED |
| CONTEXT | D-02 | Factual employer-paid training with no implied employment promise | Tasks 1–2 enforce commitment/evidence compatibility. | COVERED |
| CONTEXT | D-03 | All approved pathway classes stay browseable and non-decisional | Tasks 1–2 retain the six-class explicit coverage matrix without ranking or suppression. | COVERED |
| CONTEXT | D-04 | Neutral uncertainty with source, date, and confirmation action | Tasks 1–2 require non-current availability semantics and direct actions. | COVERED |
| CONTEXT | D-05 | Explicit verified/unverified presentation foundation | Tasks 1–2 make coverage state discriminated and evidence-backed; later UI ordering remains outside this domain-only phase. | COVERED |
| CONTEXT | D-06 | 183-day operational and 731-day boundary freshness policy | Tasks 1–2 retain injected-clock policies and add missing chronology checks without changing thresholds. | COVERED |
| CONTEXT | D-07 | Re-entry/participation policy needs official evidence and neutral fallback | Tasks 1–2 keep those policy fields outside the Phase 09 contract. | COVERED |
| CONTEXT | D-08 | Source-safe, non-sensitive first-view facts | Tasks 1–2 ensure unresolved facts cannot carry unsupported material values. | COVERED |

<verification>
Repair order is mandatory: first run the two-file RED regression suite in Task 1 before any production edit; then run the contract-only suite after Task 2; then run the two-file suite, typecheck, lint, and full Jest suite after Task 3. The final plan gate is `pnpm --filter @scholar-scout/web run test -- __tests__/lib/catalogue-contract.test.ts __tests__/lib/catalogue-fixtures.test.ts --runInBand &amp;&amp; pnpm --filter @scholar-scout/web run typecheck &amp;&amp; pnpm --filter @scholar-scout/web run lint &amp;&amp; pnpm --filter @scholar-scout/web run test --runInBand`.
</verification>

<success_criteria>
- All seven findings in `09-REVIEW.md` are closed with an explicit regression test and a passing production behavior.
- The repair preserves frozen sources, the complete six-area/six-pathway matrix, D-08 field boundary, and existing freshness thresholds.
- Region, coverage, fact, card, employer-training, wage-context, and fixture validation remain pure, deterministic, non-sensitive, and disconnected from provider inventory, scraping, persistence, UI, and ranking.
- Targeted tests, TypeScript typecheck, lint, and the full Jest suite pass.
</success_criteria>

<output>
Create `.planning/phases/09-catalogue-foundations-and-source-contracts/09-04-SUMMARY.md` when done.
</output>
