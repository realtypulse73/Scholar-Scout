---
phase: 09-catalogue-foundations-and-source-contracts
plan: 06
type: execute
wave: 5
depends_on:
  - 09-05
files_modified:
  - apps/web/lib/catalogue-contract.ts
  - apps/web/__tests__/lib/catalogue-contract.test.ts
  - .planning/phases/09-catalogue-foundations-and-source-contracts/09-VALIDATION.md
autonomous: true
requirements:
  - REG-01
  - REG-02
  - REG-03
  - EVID-01
  - EVID-02
must_haves:
  truths:
    - "Every public source and region validator turns null, primitive, array, and missing nested runtime shapes into deterministic validation errors instead of exceptions."
    - "A not-yet-verified coverage row contains only its explicit unknown-coverage assertion; an import cannot attach evidence or a source URL to it."
    - "A verified coverage row still requires current attributable evidence and the frozen six-region, 36-cell not-yet-verified fixture baseline remains valid."
  artifacts:
    - path: apps/web/lib/catalogue-contract.ts
      provides: Pure import-boundary guards for source metadata, catalogue regions, and discriminated coverage rows.
      exports:
        - validateSourceMetadata
        - validateCatalogueRegion
        - validateCoverageMatrix
    - path: apps/web/__tests__/lib/catalogue-contract.test.ts
      provides: RED-first CR-01 and CR-02 runtime-shape and coverage-state regressions.
    - path: .planning/phases/09-catalogue-foundations-and-source-contracts/09-VALIDATION.md
      provides: Task-map coverage for the final Phase 09 repair and its quality gate.
  key_links:
    - from: apps/web/__tests__/lib/catalogue-contract.test.ts
      to: apps/web/lib/catalogue-contract.ts
      via: Public validator calls with unknown-shaped parsed import values and fixed-clock coverage matrices.
      pattern: "validate(SourceMetadata|CatalogueRegion|CoverageMatrix)"
    - from: apps/web/lib/catalogue-contract.ts
      to: apps/web/lib/catalogue-fixtures.ts
      via: The existing frozen six-region, 36-cell baseline continues to satisfy the stricter coverage discriminator.
      pattern: "state: 'not-yet-verified'"
---

<objective>
Close the two remaining Phase 09 import-boundary blockers with RED-first regressions and narrow pure-validator hardening.

Purpose: Phase 10 must be able to submit parsed source snapshots to public catalogue validators without malformed nested values throwing or unverified coverage carrying unsupported provenance.
Output: Passing CR-01/CR-02 regressions, hardened `catalogue-contract.ts`, and the final Phase 09 validation-map entries.
</objective>

<execution_context>
@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-CONTEXT.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-REVIEW.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-VALIDATION.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-05-SUMMARY.md
@apps/web/lib/catalogue-contract.ts
@apps/web/lib/catalogue-fixtures.ts
@apps/web/__tests__/lib/catalogue-contract.test.ts
@apps/web/__tests__/lib/catalogue-fixtures.test.ts

<interfaces>
`validateSourceMetadata` and `validateCatalogueRegion` are public parsed-import boundaries. Their runtime parameters must accept `unknown`; invalid root values and invalid `officialBoundary`/`localFocus` nested values must return stable string arrays before any member access.

`validateCoverageMatrix(regions, coverage, now)` already takes unknown-shaped array entries and an injected clock. A `verified` row requires current `FactEvidence`, while a `not-yet-verified` row may retain only `regionId`, `pathway`, `state`, and `reviewedAt`; own `evidence` and `sourceUrl` properties are invalid even if their values are `undefined`.

Keep `catalogueRegions` and `catalogueCoverage` unchanged. They are deeply frozen D-01 fixtures with the exact six approved regions and D-03's ordered 36-cell `not-yet-verified` baseline. The D-08 first-view fact boundary is outside this repair.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Add RED public-import regressions for nested shapes and unverified-row evidence</name>
  <files>apps/web/__tests__/lib/catalogue-contract.test.ts</files>
  <precondition>The current focused contract suite passes and `09-REVIEW.md` continues to list only CR-01 and CR-02 before the new assertions are written.</precondition>
  <read_first>
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-REVIEW.md (CR-01 and CR-02 define the complete repair scope)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-CONTEXT.md (D-01, D-03, D-06, and D-08 are locked)
    - apps/web/lib/catalogue-contract.ts (public validator signatures, `isRecord`, coverage discriminator, and stable error conventions)
    - apps/web/__tests__/lib/catalogue-contract.test.ts (existing `region`, `coverage`, `factEvidence`, `FACT_NOW`, and complete-matrix builders)
    - apps/web/__tests__/lib/catalogue-fixtures.test.ts (frozen six-region/36-cell invariant that this repair must preserve)
    - apps/web/lib/outcome-profiles.ts (pure named-export validator returning deterministic `string[]` errors is the domain-library pattern analog from `09-PATTERNS.md`)
    - apps/web/__tests__/lib/outcome-profiles.test.ts (local fully valid baseline helper plus direct contract assertions is the test pattern analog from `09-PATTERNS.md`)
  </read_first>
  <behavior>
    - `validateSourceMetadata` called with each of undefined, null, a primitive, and an array never throws and returns exactly `Source metadata must be an object.`.
    - `validateCatalogueRegion` called with each of undefined, null, a primitive, and an array returns exactly `Catalogue region must be an object.`; omitting or setting undefined on either `officialBoundary` or `localFocus` of an otherwise valid region, and replacing either nested value with null, a primitive, or an array, returns its corresponding stable nested-object error without throwing.
    - A complete valid matrix whose one `not-yet-verified` row owns `evidence`, including an own property with an undefined value, is rejected with the stable key-qualified evidence error; the analogous own `sourceUrl` row, including an own `sourceUrl: undefined`, is rejected with its exact stable key-qualified source-URL error.
    - A complete verified matrix with current, dated `FactEvidence` remains accepted, and existing fixture tests continue to establish that all 36 frozen rows are valid not-yet-verified rows.
  </behavior>
  <action>Before production edits, extend only the existing catalogue-contract Jest suite with targeted fixed-clock public-boundary cases for CR-01 and CR-02. Follow the `outcome-profiles.ts` / `outcome-profiles.test.ts` pattern analog from `09-PATTERNS.md`: retain a local fully valid baseline and assert direct deterministic validator results rather than broad fixture snapshots. Use one parameter table containing undefined, null, a primitive, and an array for direct `validateSourceMetadata` calls; assert no throw and the exact root-shape error. Use the same table for direct `validateCatalogueRegion` calls, then independently omit and set undefined on `officialBoundary` and `localFocus` of a valid region, and replace each nested value with null, a primitive, and an array, so every case returns exactly `Official boundary must be an object.` or `Local focus must be an object.` while preserving the valid sibling. Build complete one-region/six-pathway matrices from the existing typed helpers. For `not-yet-verified`, add separate runtime-cast rows with own `evidence` (including an undefined own field) and own `sourceUrl`, including `sourceUrl: undefined`; assert the planned exact key-qualified errors. Add a complete `verified` matrix with the existing current `factEvidence` and assert it remains valid. Exercise exported functions only and fixed `FACT_NOW`; do not alter frozen fixtures, source roster, vocabulary, dates, D-01 geography, D-03 browseable pathways, D-06 freshness thresholds, or D-08 card facts. Run the focused Jest command and confirm the new cases fail for the intended CR-01/CR-02 behavior before changing production code.</action>
  <verify>
    <automated>pnpm --filter @scholar-scout/web run test -- __tests__/lib/catalogue-contract.test.ts --runInBand</automated>
  </verify>
  <done>The newly added assertions demonstrably fail only because the reviewed public validators dereference malformed source/region structures or allow evidence-bearing not-yet-verified rows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guard public source/region inputs and enforce the coverage discriminator at runtime</name>
  <files>apps/web/lib/catalogue-contract.ts</files>
  <read_first>
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-REVIEW.md (exact CR-01 and CR-02 findings)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-CONTEXT.md (D-01, D-03, D-06, and D-08 constraints)
    - apps/web/lib/catalogue-contract.ts (current exported types, `isRecord`, validators, and coverage chronology behavior)
    - apps/web/__tests__/lib/catalogue-contract.test.ts (Task 1 RED cases)
    - apps/web/lib/outcome-profiles.ts (pure validator and deterministic `string[]` return pattern analog from `09-PATTERNS.md`)
    - apps/web/__tests__/lib/outcome-profiles.test.ts (local valid-record builder and direct assertion pattern analog from `09-PATTERNS.md`)
  </read_first>
  <behavior>
    - Public source and region validation handles malformed root and nested shapes before any property access, preserving deterministic root and nested-object errors.
    - A not-yet-verified row that owns `evidence` or `sourceUrl` is invalid at runtime; an own `sourceUrl: undefined` returns exactly `Not-yet-verified coverage cannot include a source URL for {key}.`, whereas a verified row still validates its complete evidence, current status, dates, and chronology.
    - Valid controlled rows continue through duplicate, unsupported-key, and missing-pair evaluation in declaration order, and the frozen fixture matrix remains valid.
  </behavior>
  <action>Modify only `catalogue-contract.ts` to make Task 1 green. Follow the pure named-export, deterministic `string[]` validation-return style in `outcome-profiles.ts`, the domain-library pattern analog identified by `09-PATTERNS.md`; the Task 1 regressions should use the companion `outcome-profiles.test.ts` local-valid-baseline style. Widen the public `validateSourceMetadata` and `validateCatalogueRegion` input parameters to `unknown`. At each boundary, use the existing non-array `isRecord` guard before member access: return exactly `Source metadata must be an object.` for an invalid source root; return exactly `Catalogue region must be an object.` for an invalid region root; and for an otherwise usable region emit exactly `Official boundary must be an object.` or `Local focus must be an object.` when that nested value fails the guard. Validate the valid nested sibling normally, retaining existing source-date, authority, boundary-ID, coordinate, and source-metadata errors. In the `not-yet-verified` branch of `validateCoverageMatrix`, use an own-property check for `evidence` and `sourceUrl` and add deterministic key-qualified errors `Not-yet-verified coverage cannot include evidence for {key}.` and `Not-yet-verified coverage cannot include a source URL for {key}.`; reject an own field regardless of its value, including an own `sourceUrl: undefined`. Leave the verified branch and its current attributable `FactEvidence`, injected-clock date, and chronology checks intact. Preserve D-01's frozen official-boundary/local-focus split, D-03's six controlled browseable pathways, D-06's 183/731-day clock policies, and D-08's pure non-sensitive card contract. Do not edit fixtures or introduce sources, provider inventory, publication, persistence, routes, UI, ranking, eligibility, sensitive inputs, or outcome logic.</action>
  <reversibility rating="reversible">This narrows pure import validation behind existing exported validators without changing persisted records, published data, or the frozen fixture source roster.</reversibility>
  <verify>
    <automated>pnpm --filter @scholar-scout/web run test -- __tests__/lib/catalogue-contract.test.ts --runInBand</automated>
  </verify>
  <done>All CR-01 shape variants and CR-02 evidence/source-field variants return their deterministic errors without exceptions; valid verified coverage and the existing contract regressions pass.</done>
</task>

<task type="auto">
  <name>Task 3: Record final repair coverage and run the complete Phase 09 quality gate</name>
  <files>.planning/phases/09-catalogue-foundations-and-source-contracts/09-VALIDATION.md</files>
  <read_first>
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-VALIDATION.md (per-task map and test-creation ordering)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-06-PLAN.md (Task IDs and final verification obligations)
    - apps/web/__tests__/lib/catalogue-contract.test.ts (completed CR-01/CR-02 regressions)
    - apps/web/__tests__/lib/catalogue-fixtures.test.ts (frozen source/36-cell preservation coverage)
  </read_first>
  <action>Update `09-VALIDATION.md` only to add pending Task 09-06-01 and 09-06-02 entries plus test-creation ordering for this final repair: the first maps CR-01/CR-02 RED cases to the focused contract command, and the second maps the hardened public validators and runtime coverage discriminator to focused contract, fixture-preservation, typecheck, lint, and full Jest checks. Preserve every existing task row and status; do not change scope, requirements, fixtures, source facts, or validation results for prior plans. Then run the focused contract suite, the fixture suite, web typecheck, web lint, and full in-band web Jest suite in that order. Record no invented pass counts; the generated execution summary will record real outcomes.</action>
  <verify>
    <automated>pnpm --filter @scholar-scout/web run test -- __tests__/lib/catalogue-contract.test.ts --runInBand &amp;&amp; pnpm --filter @scholar-scout/web run test -- __tests__/lib/catalogue-fixtures.test.ts --runInBand &amp;&amp; pnpm --filter @scholar-scout/web run typecheck &amp;&amp; pnpm --filter @scholar-scout/web run lint &amp;&amp; pnpm --filter @scholar-scout/web run test --runInBand</automated>
  </verify>
  <done>The validation map names the final repair tests, and targeted contract/fixture tests, TypeScript, ESLint, and the complete web Jest suite all pass while frozen source facts and all 36 coverage cells remain unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parsed reviewed snapshot → public source/region validators | Imported JSON may contain null, primitives, arrays, or missing nested metadata instead of the declared TypeScript shape. |
| Parsed coverage row → coverage discriminator | A typed-only exclusion can be bypassed by runtime JSON and attach evidence to an explicitly unknown cell. |
| Frozen Phase 09 fixture → stricter validator | A stricter runtime discriminator must not reinterpret source-free, explicit unknown coverage as an availability assertion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-22 | Denial of service | `validateSourceMetadata`, `validateCatalogueRegion` | high | mitigate | Public unknown-boundary guards reject root and nested non-record shapes as stable errors before property reads; parameterized Jest cases prove no throw. |
| T-09-23 | Tampering | `validateCoverageMatrix` | high | mitigate | Runtime own-property checks reject evidence and source URL fields on not-yet-verified rows while the verified branch retains full fact-evidence validation. |
| T-09-24 | Spoofing | frozen catalogue coverage fixture | medium | mitigate | Fixture-focused Jest and complete quality gate confirm the exact six-region/36-cell source-free unknown baseline remains accepted. |
| T-09-SC | Tampering | package installation | high | mitigate | No package installation is part of this repair; existing TypeScript and Jest tooling only. |
</threat_model>

## Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Deterministic, source-first catalogue foundation that Phase 10 can validate safely | Tasks 1–2 harden only the pure parsed-import boundary; Task 3 proves the completed baseline still passes. |
| REQ | REG-01, REG-02, REG-03, EVID-01, EVID-02 | Tasks 1–2 preserve six-region vocabulary and explicit coverage while requiring recoverable source/region validation and verified-only evidence. |
| RESEARCH | Pure validators, injected-clock evidence, frozen reviewed snapshots, no live aggregation | Task 2 preserves current/date behavior and adds no runtime source retrieval; Task 3 runs the contract and fixture gates. |
| CONTEXT | D-01, D-03, D-06, D-08 | Task 1 cites and protects the frozen geography/pathway/card boundaries; Task 2 retains the official/local split, all pathways, clock policies, and D-08 scope. |

<verification>
Run the focused contract and fixture suites, then `pnpm --filter @scholar-scout/web run typecheck`, `pnpm --filter @scholar-scout/web run lint`, and `pnpm --filter @scholar-scout/web run test --runInBand`.
</verification>

<success_criteria>
- Public source and region validation has a stable, non-throwing result for null, primitive, array, and malformed nested boundary/local-focus imports.
- Runtime coverage validation rejects own evidence or source URL fields on every not-yet-verified row, while complete verified rows still require current source evidence.
- No fixture source, coverage row, controlled vocabulary, geographic policy, card fact, or Phase 09 scope boundary changes; all 36 explicit cells remain valid.
</success_criteria>

<output>
Create `.planning/phases/09-catalogue-foundations-and-source-contracts/09-06-SUMMARY.md` when done.
</output>
