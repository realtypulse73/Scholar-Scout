---
phase: 09-catalogue-foundations-and-source-contracts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/lib/catalogue-contract.ts
  - apps/web/__tests__/lib/catalogue-contract.test.ts
autonomous: true
requirements:
  - REG-01
  - REG-02
  - REG-03
  - EVID-01
  - EVID-02
must_haves:
  truths:
    - "A deterministic catalogue contract distinguishes every official regional boundary from its documented ten-mile downtown local-focus anchor, with required structured source-date metadata."
    - "The six approved region identifiers and six approved pathway classes are controlled, stable vocabulary; coverage has an explicit state for every valid pair."
    - "A great-circle inclusion check and injected-clock 731-day boundary classifier are reproducible without claiming travel time or commute distance."
    - "Coverage validation reports empty input, duplicates, unsupported keys, and missing pairs instead of inferring availability."
  artifacts:
    - path: apps/web/lib/catalogue-contract.ts
      provides: Pure regional, pathway, local-focus, coverage, and validation contract.
      exports:
        - CATALOGUE_REGION_IDS
        - CATALOGUE_PATHWAYS
        - SourceDate
        - SourceMetadata
        - CATALOGUE_FRESHNESS_POLICY
        - getFreshnessStatus
        - validateSourceMetadata
        - validateCatalogueRegion
        - calculateGreatCircleMiles
        - isWithinLocalFocus
        - validateCoverageMatrix
    - path: apps/web/__tests__/lib/catalogue-contract.test.ts
      provides: Contract and edge-case regression coverage.
  key_links:
    - from: apps/web/lib/catalogue-contract.ts
      to: apps/web/lib/catalogue-fixtures.ts
      via: Stable region/pathway vocabulary and coverage validator consumed by the Phase 9 fixture module.
      pattern: "CATALOGUE_(REGION_IDS|PATHWAYS)|validateCoverageMatrix"
  prohibitions:
    - "No live learner-facing provider aggregation, runtime scraping, or automatic publication."
    - "No provider-quality, eligibility, job, salary, placement, or outcome claim is derived by this foundation."
    - "No sensitive re-entry information, sensitive ranking, or sensitive query field is present."
    - "Missing, stale, or conflicting facts are never silently upgraded to current."
---

<objective>
Create the pure, deterministic catalogue foundation that later publication and discovery work can trust.

Purpose: Establish the six-region vocabulary, two-layer geography, complete coverage contract, and testable distance behavior before adding any source snapshots.
Output: A strict TypeScript contract and its focused Jest suite.
</objective>

<execution_context>
@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-CONTEXT.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-RESEARCH.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-VALIDATION.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-PATTERNS.md
@apps/web/lib/programmes.ts
@apps/web/lib/outcome-profiles.ts
@apps/web/__tests__/lib/outcome-profiles.test.ts

<interfaces>
From `apps/web/lib/programmes.ts`, preserve the legacy `Programme` module unchanged. This plan creates a separate named-export module; it must not import persistence, change `Programme`, or reuse its `matchScore`, `acceptanceRate`, or broad source-confidence fields as catalogue evidence.

The new module must export six exact region IDs: `greater-houston`, `greater-chicago`, `greater-buffalo`, `greater-atlanta`, `greater-new-orleans`, and `greater-kingston-jamaica`; and six exact pathway IDs: `university`, `community-college`, `trade-career-school`, `registered-apprenticeship`, `employer-linked-training`, and `military-information`.

Before Plan 09-02 begins, this plan exports the source metadata contract it consumes: `SourceDate` is a required tagged value with either a validated ISO calendar date or an explicit `unavailable` state; it is never an optional property. `SourceMetadata` requires source label, HTTP(S) URL, `sourceDate`, and `checkedAt`. `OfficialBoundary` contains `boundaryVersion` and `boundaryId: string | null` so every region has the same inspectable shape and Kingston can explicitly record that it has no CBSA identifier. Export `CATALOGUE_FRESHNESS_POLICY` with `operationalFactMaxAgeDays: 183` and `boundaryMaxAgeDays: 731`, `getFreshnessStatus(metadata, kind, now)`, `validateSourceMetadata`, and `validateCatalogueRegion`. The classifier receives `now: Date`; it must not read wall-clock time. A metadata record with an unavailable source date is structured non-current and cannot classify as `current`.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one source-dated region-to-coverage path end to end</name>
  <files>apps/web/lib/catalogue-contract.ts, apps/web/__tests__/lib/catalogue-contract.test.ts</files>
  <read_first>
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-CONTEXT.md (D-01 and D-03)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-RESEARCH.md (Patterns 1 and 2)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-PATTERNS.md (pure-validator and deterministic-date conventions; `outcome-profiles.ts` / `outcome-profiles.test.ts` are the contract/test analogs)
    - apps/web/lib/programmes.ts (existing type/export style only)
    - apps/web/lib/outcome-profiles.ts (named pure validator and injected-date analog)
    - apps/web/__tests__/lib/outcome-profiles.test.ts (fixed-date unit-test analog)
  </read_first>
  <behavior>
    - A region has distinct `officialBoundary` and `localFocus` records, each with required authority, source label/URL, structured source date, and check date fields.
    - An official boundary exposes `boundaryVersion` and `boundaryId`; a Kingston-style no-CBSA case uses `boundaryId: null`, while an unavailable source-date state is non-current under an injected clock.
    - A valid region/pathway coverage row produces a stable pair key and accepts only `verified` or `not-yet-verified` coverage states.
    - A single valid in-radius location is accepted by the fixed local-focus rule.
  </behavior>
  <action>Create `catalogue-contract.ts` as a pure named-export TypeScript module, separate from legacy programme records, implementing D-01, D-03, D-06, EVID-01, and EVID-02. Define the exact six-region and six-pathway controlled constants/types; `SourceDate` as a required discriminated value that validates a full ISO calendar date when documented and records an explicit unavailable state otherwise; `SourceMetadata`; `CatalogueRegion`; `OfficialBoundary`; `LocalFocus`; `CatalogueCoverage`; and the coverage-state union. `OfficialBoundary` must carry `boundaryVersion` and `boundaryId: string | null`; source label/URL, `sourceDate`, and `checkedAt` belong in the mandatory metadata boundary used by both official boundaries and local-focus anchors. Export `CATALOGUE_FRESHNESS_POLICY` with 183 operational and 731 boundary days, plus `getFreshnessStatus(metadata, kind, now)`, `validateSourceMetadata`, `validateCatalogueRegion`, stable `createCoverageKey`, and `validateCoverageMatrix`. The injected-clock classifier must validate dates and return a non-current state when the tagged source date is unavailable, malformed, future-dated, incomplete, or stale. Implement great-circle miles with a documented `EARTH_RADIUS_MILES = 3958.7613`, finite coordinate validation, and inclusive membership at the radius boundary; preserve full precision for the membership decision. Start the test file red, then prove one real object path from a source-dated region through one coverage row and the local-focus inclusion helper. Do not add routes, UI, storage, packages, ranking, or provider calls.</action>
  <verify>
    <automated>pnpm --filter @scholar-scout/web run test -- catalogue-contract</automated>
  </verify>
  <acceptance_criteria>A strict, isolated catalogue contract compiles; a test constructs one source-shaped region and coverage row, validates it, and confirms the ten-mile helper works without touching the legacy catalogue.</acceptance_criteria>
  <done>The first end-to-end domain slice passes and is ready for six-area fixtures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make boundary freshness and coverage failures explicit</name>
  <files>apps/web/lib/catalogue-contract.ts, apps/web/__tests__/lib/catalogue-contract.test.ts</files>
  <read_first>
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-RESEARCH.md (great-circle and matrix-validation examples)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-VALIDATION.md (REG-01 through REG-03 test obligations)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-PATTERNS.md (validator-return convention; `outcome-profiles.ts` / `outcome-profiles.test.ts` are the analogs)
    - apps/web/lib/outcome-profiles.ts
    - apps/web/__tests__/lib/outcome-profiles.test.ts
  </read_first>
  <behavior>
    - At exactly ten miles, a finite point is inside; a point beyond the unrounded ten-mile comparison is outside; invalid latitude/longitude values are rejected deterministically.
    - Empty region or coverage input, duplicate pairs, unknown region/pathway values, and missing expected pairs produce stable validation errors.
    - The expected pair sequence follows exported region then pathway declaration order, with no accidental input-order-dependent result.
    - The 731-day boundary policy is exact under an injected clock: a valid boundary metadata record at the threshold remains current, one day later requires confirmation, and a required unavailable/malformed/future source date cannot classify as current.
  </behavior>
  <action>Expand the pure contract and tests to cover the unresolved REG-01/REG-02/REG-03/EVID-01/EVID-02 probes. Make `validateCatalogueRegion` require the exact boundary metadata fields from the cross-plan interface and use `boundaryId: null` only for the explicit no-CBSA case. Make `validateCoverageMatrix` return deterministic, deduplicated error messages for absent arrays, duplicate keys, unsupported IDs, and every missing expected pair; derive expected pair order from the exported controlled constants rather than caller ordering. Test the injected-clock 731-day boundary policy at the exact threshold and one day beyond; test documented, unavailable, malformed, and future `SourceDate` variants so no unavailable date can be treated as current. Add coordinate-domain guards and precise boundary tests using precomputed coordinates against the declared earth-radius constant. Document in exported JSDoc that local focus is straight-line geographic scope, not a commute estimate or regional-boundary replacement. Keep all six pathway classes in the controlled vocabulary; no function may sort, hide, rank, or infer a coverage state.</action>
  <verify>
    <automated>pnpm --filter @scholar-scout/web run test -- catalogue-contract &amp;&amp; pnpm --filter @scholar-scout/web run typecheck</automated>
  </verify>
  <acceptance_criteria>Tests lock the inclusive distance threshold, invalid-coordinate handling, deterministic complete-matrix errors, explicit coverage-state representation, empty inputs, and controlled ordering.</acceptance_criteria>
  <done>Every contract-level edge in the Phase 9 validation probe has an automated, deterministic outcome.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Curated source data → domain contract | Static records can be malformed or falsely suggest regional availability. |
| Domain contract → later catalogue readers | Later layers could mistake a local-focus coordinate for an official regional boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-01 | Tampering | `validateCoverageMatrix` | high | mitigate | Strict controlled unions, duplicate/unknown/missing-pair validation, and focused Jest cases. |
| T-09-02 | Spoofing | `CatalogueRegion` geography | medium | mitigate | Separate authority-bearing official boundary and documented downtown anchor types; no source-less geometry. |
| T-09-03 | Information disclosure | catalogue contract | medium | accept | The module contains public catalogue vocabulary only; sensitive circumstances are deliberately absent and the test suite protects the public-only contract surface. |
| T-09-SC | Tampering | package installation | high | mitigate | No packages are installed in Phase 9; existing TypeScript/Jest tooling only. |
</threat_model>

## Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Deterministic six-area, source-first foundation | This plan supplies the controlled domain seam. |
| REQ | REG-01, REG-02, REG-03, EVID-01, EVID-02 | Tasks 1–2 provide boundary/local-focus metadata, source-date validation, injected-clock boundary freshness, six-pathway, and explicit-cell validation. |
| RESEARCH | Two-layer geography, 36-cell matrix, fixed great-circle scope, 731-day boundary freshness | Tasks 1–2 implement and test each rule. |
| CONTEXT | D-01, D-03, D-06 | Task 1 creates the geography/source seam; Task 2 locks the costly boundary-freshness behavior; deferred ranking and sensitive-referral work remain excluded. |

<verification>
Run `pnpm --filter @scholar-scout/web run test -- catalogue-contract`, then `pnpm --filter @scholar-scout/web run typecheck` after this plan.
</verification>

<success_criteria>
- A pure TypeScript contract exposes the six approved region and pathway identifiers plus the mandatory metadata and injected-clock freshness interfaces consumed by Plan 09-02.
- Region scope and ten-mile local focus are structurally distinct and reproducible.
- Matrix validation gives deterministic feedback for all missing/invalid cells.
</success_criteria>

## Artifacts this phase produces

- `apps/web/lib/catalogue-contract.ts` — reusable catalogue vocabulary, geometry, and coverage validation.
- `apps/web/__tests__/lib/catalogue-contract.test.ts` — contract regression suite.

<output>
Create `.planning/phases/09-catalogue-foundations-and-source-contracts/09-01-SUMMARY.md` when done.
</output>
