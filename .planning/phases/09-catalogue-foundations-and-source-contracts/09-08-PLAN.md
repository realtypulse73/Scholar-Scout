---
phase: 09-catalogue-foundations-and-source-contracts
plan: 08
type: execute
wave: 7
depends_on:
  - 09-07
files_modified:
  - apps/web/lib/catalogue-contract.ts
  - apps/web/__tests__/lib/catalogue-contract.test.ts
autonomous: true
requirements:
  - REG-01
  - REG-03
  - EVID-01
must_haves:
  truths:
    - "A controlled region ID authorizes coverage only when its supplied regional record passes the official-boundary and local-focus provenance contract."
    - "A forged official boundary or malformed local focus paired with the same controlled region ID rejects complete not-yet-verified and verified coverage matrices."
    - "All earlier Phase 09 validation guards, the frozen six-region roster, its ordered 36 explicit coverage cells, and D-08 first-view facts continue to validate without change."
  artifacts:
    - path: apps/web/lib/catalogue-contract.ts
      provides: Pure coverage-matrix validation that forwards regional provenance errors and admits only fully valid supplied regions to declared-region membership.
      exports:
        - validateCatalogueRegion
        - validateCoverageMatrix
    - path: apps/web/__tests__/lib/catalogue-contract.test.ts
      provides: RED-first public-validator regressions for forged and malformed supplied regional provenance.
  key_links:
    - from: apps/web/lib/catalogue-contract.ts
      to: apps/web/__tests__/lib/catalogue-contract.test.ts
      via: validateCoverageMatrix delegates each object-shaped supplied region to validateCatalogueRegion before forming its regionIds membership set.
      pattern: "validateCatalogueRegion\\(candidate\\)|Coverage region is not declared"
---

<objective>
Close the final Phase 09 CR-01 import-integrity blocker with a strict RED-first validator repair.

Purpose: A reviewed snapshot must never treat a controlled regional ID as declared merely because an object has that ID; authoritative boundary and local-focus provenance must validate before coverage can attach to it.
Output: Focused regression coverage and a minimal repair in `catalogue-contract.ts`; no fixture, source-roster, UI, persistence, import-route, ranking, provider-inventory, or documentation change.
</objective>

<execution_context>
@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-CONTEXT.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-RESEARCH.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-REVIEW.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-07-SUMMARY.md
@apps/web/lib/catalogue-contract.ts
@apps/web/lib/catalogue-fixtures.ts
@apps/web/__tests__/lib/catalogue-contract.test.ts
@apps/web/__tests__/lib/catalogue-fixtures.test.ts

<interfaces>
`validateCoverageMatrix(regions, coverage, now)` is a public pure validator over readonly unknown-shaped list entries. Its supplied-region declaration set controls which region/pathway pairs must exist and which controlled coverage rows may be accepted. Each object-shaped supplied record now must be validated through `validateCatalogueRegion` before it is admitted to this declaration set; the region-validation errors remain part of the matrix error result.

`validateCatalogueRegion(region)` validates controlled ID, label, official boundary authority/ID/version/source metadata, and the independent local-focus provenance/coordinates/ten-mile radius. `validateCoverageMatrix` retains its injected clock, exact controlled ordering, duplicate and unsupported-key checks, coverage evidence chronology, and own-property discriminator behavior.

`catalogueRegions` and `catalogueCoverage` remain deeply frozen preservation inputs: six source-bearing regional records and 36 ordered explicit `not-yet-verified` cells. D-08's non-sensitive opportunity-card facts and all established Phase 09 contract types are preservation constraints.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Add RED regressions for forged and malformed supplied regional provenance</name>
  <files>apps/web/__tests__/lib/catalogue-contract.test.ts</files>
  <precondition>The focused catalogue-contract suite passes before the new public-validator cases are added.</precondition>
  <read_first>
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-REVIEW.md (CR-01 is the complete repair scope)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-CONTEXT.md (D-01 through D-08 are locked constraints)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-07-SUMMARY.md (existing supplied-ID and chronology guards)
    - apps/web/lib/catalogue-contract.ts (validateCatalogueRegion, validateCoverageMatrix, controlled constants, and stable errors)
    - apps/web/__tests__/lib/catalogue-contract.test.ts (valid region/coverage/evidence/fixed-clock builders and review regressions)
    - apps/web/__tests__/lib/catalogue-fixtures.test.ts (six-region and 36-cell preservation invariants)
  </read_first>
  <behavior>
    - A same-ID Greater Houston record whose official boundary authority and boundary ID are forged returns the existing regional-provenance error and `Coverage region is not declared: greater-houston.` for a complete six-pathway `not-yet-verified` matrix.
    - The same forged-boundary record returns both errors for an otherwise complete six-pathway `verified` matrix with valid current evidence.
    - A same-ID Greater Houston record with malformed local-focus provenance returns its existing local-focus error and `Coverage region is not declared: greater-houston.` for complete matrices in each of those two coverage states.
    - The valid supplied-region path and all prior coverage errors retain their established results.
  </behavior>
  <action>Before production edits, extend only the focused public-validator Jest suite with fixed-clock matrix cases for CR-01. Reuse the local valid region, fact evidence, and controlled-pathway builders to create a complete six-row Greater Houston matrix in each `not-yet-verified` and `verified` state. Use a valid same-ID Greater Houston record as the starting point, then independently make its official boundary contradict that ID's required authority/boundary pair and make its local focus malformed. For every state-and-provenance variant, assert the relevant existing regional error plus the stable `Coverage region is not declared: greater-houston.` error. Retain a valid-evidence payload for verified rows so the observed RED failure isolates provenance admission. Per D-01: retain separate official-boundary and ten-mile local-focus records. Per D-02: retain the employer-training contract. Per D-03: retain six browseable pathway classes. Per D-04 and D-05: retain visible uncertainty states. Per D-06: retain injected-clock thresholds. Per D-07: retain the sensitive-policy exclusion. Per D-08: retain non-sensitive first-view facts. Run the focused command before production edits and confirm these cases are RED only because object-shaped invalid regions currently enter the declared-region set.</action>
  <verify>
    <automated>pnpm --filter @scholar-scout/web run test -- __tests__/lib/catalogue-contract.test.ts --runInBand</automated>
  </verify>
  <done>The new forged-boundary and malformed-local-focus complete-matrix regressions fail against the reviewed baseline for the intended declaration-set defect.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Admit only provenance-valid supplied regions to coverage membership</name>
  <files>apps/web/lib/catalogue-contract.ts</files>
  <read_first>
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-REVIEW.md (CR-01 required repair semantics)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-CONTEXT.md (D-01 through D-08 locked boundaries)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-07-SUMMARY.md (prior guards that remain intact)
    - apps/web/lib/catalogue-contract.ts (region-list admission, regionIds construction, row validation, and stable errors)
    - apps/web/__tests__/lib/catalogue-contract.test.ts (Task 1 RED cases)
    - apps/web/lib/catalogue-fixtures.ts (frozen valid roster that must continue validating)
  </read_first>
  <behavior>
    - Every object-shaped supplied region forwards its `validateCatalogueRegion` errors through `validateCoverageMatrix`.
    - Only supplied regions with zero provenance-validation errors contribute their controlled ID to `regionIds`; coverage rows for invalid records therefore receive the existing undeclared-region error in verified and not-yet-verified states.
    - Non-object region entries, duplicate/unsupported controlled IDs, controlled coverage ordering, missing-pair reporting, evidence and chronology checks, and coverage own-property rules retain their existing behavior.
    - The valid frozen six-region roster and its ordered 36 explicit not-yet-verified coverage cells remain accepted.
  </behavior>
  <action>Modify only `catalogue-contract.ts` to make Task 1 green. Replace object-shape-only supplied-region admission with an ordered loop: retain the stable root error for a non-object candidate; for an object candidate, run `validateCatalogueRegion`, append every returned error to the matrix error list, and add that candidate to the valid-region collection only when its error list is empty. Build duplicate and controlled-ID membership checks from this valid collection, leaving the existing coverage-row validation sequence intact so controlled rows tied to invalid provenance produce the established undeclared-region error regardless of coverage state. Preserve all earlier guards, controlled declaration order, fixed-clock behavior, no-live-source boundary, frozen source roster, and 36-cell baseline. Per D-01: retain the two geography layers. Per D-02: retain the distinct paid-training facts. Per D-03: retain the controlled six-pathway vocabulary. Per D-04 and D-05: retain uncertainty-state semantics. Per D-06: retain the freshness thresholds. Per D-07: retain the sensitive-policy boundary. Per D-08: retain the first-view fact contract. Avoid changes outside this pure validator and its regression suite.</action>
  <reversibility rating="reversible">This narrows a pure import validator without changing persisted snapshots, public fixtures, or a transport contract.</reversibility>
  <verify>
    <automated>pnpm --filter @scholar-scout/web run test -- __tests__/lib/catalogue-contract.test.ts --runInBand &amp;&amp; pnpm --filter @scholar-scout/web run test -- __tests__/lib/catalogue-fixtures.test.ts --runInBand &amp;&amp; pnpm --filter @scholar-scout/web run typecheck &amp;&amp; pnpm --filter @scholar-scout/web run lint &amp;&amp; pnpm --filter @scholar-scout/web run test --runInBand</automated>
  </verify>
  <done>The CR-01 regressions pass; invalid regional provenance cannot authorize coverage; and targeted tests, fixture invariants, TypeScript typecheck, ESLint, and the complete web Jest suite pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parsed reviewed-snapshot regions → coverage declaration set | Untrusted imported objects can carry a controlled ID while omitting or forging the authoritative regional provenance required to authorize coverage. |
| Coverage declaration set → learner-facing reviewed snapshot | A controlled coverage state could otherwise appear valid for a region whose boundary and local focus were not established. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-08-01 | Tampering | validateCoverageMatrix supplied-region admission | high | mitigate | Task 2 forwards `validateCatalogueRegion` errors and adds only error-free records to declared-region membership; Task 1 proves forged official boundaries and malformed local focus cannot authorize either coverage state. |
| T-09-08-02 | Integrity | reviewed snapshot coverage matrix | high | mitigate | Retain the controlled-ID check plus the undeclared-region error, ordered missing-pair logic, duplicate checks, and coverage evidence validation; execute the complete quality gate. |
| T-09-08-SC | Tampering | dependency supply chain | low | accept | This repair installs no package and changes no dependency manifest or lockfile. |
</threat_model>

## must_haves

SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | — | Six-area source-first foundation represents trustworthy facts and visible uncertainty without invented coverage. | 09-08 | COVERED | Valid regional provenance becomes mandatory before a controlled coverage state is accepted. |
REQ | REG-01 | Exact approved regional boundary, authority, source, and check date. | 09-08 | COVERED | The declaration set accepts a regional ID only after boundary and local-focus validation. |
REQ | REG-03 | Honest coverage state for every metro/pathway pair. | 09-08 | COVERED | Forged or malformed provenance cannot make complete verified or not-yet-verified coverage valid. |
REQ | EVID-01 | Attributable evidence with source and review metadata. | 09-08 | COVERED | Regional source metadata is enforced through the existing public region validator before matrix admission. |
RESEARCH | — | Explicit complete matrix, distinct official boundary/local focus, frozen source roster, and no live aggregation. | 09-08 | COVERED | Both tasks preserve the roster, 36-cell baseline, controlled ordering, and pure snapshot boundary. |
CONTEXT | D-01 | Official regional boundary and separate ten-mile local focus. | 09-08 | COVERED | Task 1 tests both provenance layers; Task 2 requires their valid contract before membership. |
CONTEXT | D-02 | Employer-paid training remains a distinct factual pathway. | 09-08 | COVERED | Task actions preserve controlled pathway and training contracts. |
CONTEXT | D-03 | Six approved pathway classes remain browseable. | 09-08 | COVERED | Complete test matrices exercise the existing six controlled paths without changing vocabulary. |
CONTEXT | D-04 | Uncertainty remains neutral, source-visible, and dated. | 09-08 | COVERED | Task 2 retains coverage state and evidence semantics. |
CONTEXT | D-05 | Verified and unverified listings remain distinctly presented. | 09-08 | COVERED | Task 1 exercises each coverage state independently. |
CONTEXT | D-06 | Two-year boundary and six-month operational freshness policy. | 09-08 | COVERED | Fixed-clock and existing freshness behavior remain intact. |
CONTEXT | D-07 | Sensitive policy information needs official evidence and cannot be inferred. | 09-08 | COVERED | No sensitive-policy surface is added or changed. |
CONTEXT | D-08 | Preserve non-sensitive first-view location, pathway, skill, payer, cost, verification, duration, and delivery facts. | 09-08 | COVERED | The plan limits changes to regional provenance admission and preserves the card contract. |

<verification>
Sequence is mandatory: add and run the focused RED regressions before any production edit, then execute the full gate after Task 2. The full command is `pnpm --filter @scholar-scout/web run test -- __tests__/lib/catalogue-contract.test.ts --runInBand &amp;&amp; pnpm --filter @scholar-scout/web run test -- __tests__/lib/catalogue-fixtures.test.ts --runInBand &amp;&amp; pnpm --filter @scholar-scout/web run typecheck &amp;&amp; pnpm --filter @scholar-scout/web run lint &amp;&amp; pnpm --filter @scholar-scout/web run test --runInBand`.
</verification>

<success_criteria>
- A forged official boundary and a malformed local focus on a same-ID controlled region each return their regional provenance error plus `Coverage region is not declared: greater-houston.` for complete verified and not-yet-verified matrices.
- `validateCoverageMatrix` forwards object-shaped region validation errors and derives membership solely from fully valid supplied records.
- Existing coverage checks, all prior guards, the frozen six-region source roster, ordered 36-cell baseline, and D-08 card facts remain intact.
- Targeted contract and fixture tests, TypeScript typecheck, ESLint, and the complete web Jest suite pass.
</success_criteria>

<output>
Create `.planning/phases/09-catalogue-foundations-and-source-contracts/09-08-SUMMARY.md` when done.
</output>
