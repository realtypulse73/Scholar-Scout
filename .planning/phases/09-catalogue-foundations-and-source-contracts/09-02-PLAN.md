---
phase: 09-catalogue-foundations-and-source-contracts
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - apps/web/lib/catalogue-fixtures.ts
  - apps/web/__tests__/lib/catalogue-fixtures.test.ts
autonomous: true
requirements:
  - REG-01
  - REG-02
  - REG-03
must_haves:
  truths:
    - "Each approved area exposes the frozen official boundary authority/source/version/boundary ID/required structured source date/check date and its exact cited civic downtown ten-mile anchor."
    - "The five U.S. records use `OMB July 2023 delineation` rather than a moving Census lookup; every boundary and anchor record is checked on `2026-09-22`."
    - "The fixture exports exactly 36 explicit region/pathway coverage cells, including six cells for Greater Kingston."
    - "Greater Kingston uses STATIN KMA authority and has no U.S. CBSA identifier."
    - "An unreviewed local option remains visibly `not-yet-verified`; it is not inferred from a city, national listing, or category name."
  artifacts:
    - path: apps/web/lib/catalogue-fixtures.ts
      provides: Frozen six-area regional and coverage fixtures.
      exports:
        - catalogueRegions
        - catalogueCoverage
    - path: apps/web/__tests__/lib/catalogue-fixtures.test.ts
      provides: Six-region source and 36-cell fixture validation.
  key_links:
    - from: apps/web/lib/catalogue-fixtures.ts
      to: apps/web/lib/catalogue-contract.ts
      via: Imported controlled types, matrix validator, and boundary freshness policy.
      pattern: "from './catalogue-contract'"
  prohibitions:
    - "No live learner-facing provider aggregation, runtime scraping, or automatic publication."
    - "No provider-quality, eligibility, job, salary, placement, or outcome claim is derived by this foundation."
    - "No sensitive re-entry information, sensitive ranking, or sensitive query field is present."
    - "Missing, stale, or conflicting facts are never silently upgraded to current."
---

<objective>
Create the frozen six-area regional fixture and its complete, honest coverage matrix.

Purpose: Make regional scope auditable before reviewed provider inventory exists.
Output: Source-bearing regional records plus an exact 36-cell coverage fixture and regression suite.
</objective>

<execution_context>
@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-CONTEXT.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-RESEARCH.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-VALIDATION.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-PATTERNS.md
@apps/web/lib/catalogue-contract.ts
@apps/web/lib/western-new-york.ts
@apps/web/__tests__/lib/western-new-york.test.ts

<interfaces>
From Plan 09-01, use the exported `CatalogueRegion`, `CatalogueCoverage`, `CATALOGUE_REGION_IDS`, `CATALOGUE_PATHWAYS`, `SourceDate`, `SourceMetadata`, `CATALOGUE_FRESHNESS_POLICY`, `getFreshnessStatus`, `validateSourceMetadata`, `validateCatalogueRegion`, and matrix validation helpers. `catalogue-fixtures.ts` is static source material; it does not import server persistence or request data. Plan 09-02 depends only on completed Plan 09-01; it must not import, wait for, or depend on Plan 09-03.

Official U.S. CBSA identifiers are: Greater Houston `26420`, Greater Chicago `16980`, Greater Buffalo `15380`, Greater Atlanta `12060`, and Greater New Orleans `35380`. Greater Kingston, Jamaica is represented only by the STATIN Kingston Metropolitan Area authority.

The resolved geographic roster is binding; do not select alternate URLs, versions, anchors, or a current Census lookup. For Houston, Chicago, Buffalo, Atlanta, and New Orleans, set `officialBoundary.sourceUrl` to `https://www.census.gov/geographies/reference-maps/2023/geo/cbsa.html`, `officialBoundary.boundaryVersion` to `OMB July 2023 delineation`, and `officialBoundary.checkedAt` to `2026-09-22`. Their exact `boundaryId` values are respectively `26420`, `16980`, `15380`, `12060`, and `35380`. For Kingston set `officialBoundary.sourceUrl` to `https://statinja.gov.jm/maps/kmacommunitiesandpopulation.html`, `officialBoundary.boundaryVersion` to `STATIN KMA communities and population`, `officialBoundary.checkedAt` to `2026-09-22`, and set `boundaryId: null` to make its no-CBSA condition explicit. Every official-boundary and anchor metadata record must include the required `SourceDate` tagged value from Plan 09-01; a documented value must be a validated ISO calendar date, while an unavailable source-date value remains structurally present and validates only as non-current. Do not make `sourceDate` optional.

Use these exact local-focus anchor labels, civic-address anchors, URLs, and `checkedAt: '2026-09-22'`: City of Houston City Hall — `901 Bagby, Houston, TX 77002` — `https://houstontx.gov/contactus/`; Chicago City Hall — `121 N LaSalle Street, Chicago, IL 60602` — `https://311.chicago.gov/`; Buffalo City Hall — `65 Niagara Square, Buffalo, NY 14202` — `https://www.buffalony.gov/m/directory/department?did=114`; Atlanta City Hall Annex — `55 Trinity Avenue SW, Atlanta, GA 30303` — `https://www.atlantaga.gov/residents/city-hall`; New Orleans City Hall — `1300 Perdido Street, New Orleans, LA 70112` — `https://nola.gov/contact-us/`; and Kingston & St. Andrew Municipal Corporation — `24 Church Street, Kingston, Jamaica` — `https://www.ksamc.gov.jm/contact-us`. Set the fixture `anchorLabel` to the exact `label — address` text for each roster row, then normalize that cited reference point once to stored decimal coordinates; retain `radiusMiles: 10` as a straight-line local focus, not a commute claim. Each anchor also supplies the required structured `sourceDate` metadata under the Plan 09-01 validator.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze six source-bearing regional records</name>
  <files>apps/web/lib/catalogue-fixtures.ts, apps/web/__tests__/lib/catalogue-fixtures.test.ts</files>
  <read_first>
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-CONTEXT.md (D-01 and D-06)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-RESEARCH.md (Patterns 1–2 and Kingston source guidance)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-PATTERNS.md (`western-new-york.ts` / `western-new-york.test.ts` are the static-fixture/test analogs)
    - apps/web/lib/catalogue-contract.ts (Plan 09-01 metadata, 731-day policy/classifier, and region validator exports)
    - apps/web/lib/western-new-york.ts
    - apps/web/__tests__/lib/western-new-york.test.ts
  </read_first>
  <behavior>
    - The fixture contains exactly the six approved region IDs, each with independent official-boundary and downtown-anchor provenance.
    - The five U.S. areas carry the frozen `OMB July 2023 delineation` version, the July-2023 CBSA map URL, declared CBSA identifiers, required structured source dates, and `2026-09-22` check dates; Kingston carries the exact STATIN KMA source/version/check date and `boundaryId: null`.
    - Each local focus carries the research-roster civic anchor label/address/source URL, required structured source date, and `2026-09-22` check date, so its stored coordinate is traceable without runtime geocoding.
    - The Plan 09-01 injected-clock classifier treats a boundary record older than its frozen 731-day policy as non-current.
  </behavior>
  <action>Create `catalogue-fixtures.ts` with six readonly `CatalogueRegion` records per D-01 and D-06 using the binding roster in `<interfaces>` verbatim. Use the exact Plan 09-01 `SourceMetadata` shape for every official-boundary and local-focus record: source label, HTTP(S) URL, mandatory tagged `sourceDate`, and checked date. For the five U.S. records use only `https://www.census.gov/geographies/reference-maps/2023/geo/cbsa.html`, `boundaryVersion: 'OMB July 2023 delineation'`, the declared CBSA IDs, and `checkedAt: '2026-09-22'`; use only the declared STATIN record/version/check date for Kingston and set `boundaryId: null`. Normalize each specified civic address once to finite stored decimal coordinates, retain the exact `label — address` in `anchorLabel`, pair it with its exact civic source URL/metadata, and set `radiusMiles: 10`. Retain these as transparent scope metadata, not provider availability. Begin focused Jest tests red, then assert literal expected IDs, authority values, source URLs, versions, boundary IDs, source-date variants, all twelve boundary/anchor check dates, the six full label/address anchors and URLs, Kingston’s explicit no-CBSA field, distinct anchor/boundary records, and the Plan 09-01 injected-clock 731-day classifier behavior. The test inputs must be literal expected fixture metadata, not values derived from the fixture under test.</action>
  <verify>
    <automated>pnpm --filter @scholar-scout/web run test -- catalogue-fixtures</automated>
  </verify>
  <acceptance_criteria>All six regional records are source-bearing, the country-specific authority distinction is preserved, and date validation rejects a boundary record outside the two-year window.</acceptance_criteria>
  <done>The frozen regional fixture makes its geographic basis independently inspectable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish the complete explicit coverage matrix</name>
  <files>apps/web/lib/catalogue-fixtures.ts, apps/web/__tests__/lib/catalogue-fixtures.test.ts</files>
  <read_first>
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-CONTEXT.md (D-05)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-VALIDATION.md (36-cell fixture obligation)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-PATTERNS.md (`western-new-york.ts` / `western-new-york.test.ts` are the static-fixture/test analogs)
    - apps/web/lib/catalogue-contract.ts
    - apps/web/lib/western-new-york.ts
    - apps/web/__tests__/lib/western-new-york.test.ts
  </read_first>
  <behavior>
    - Six regions multiplied by six controlled pathway classes yields exactly 36 rows, each with a stable region/pathway key.
    - Every cell has an explicit coverage state and review date; an absent reviewed source is represented as `not-yet-verified` rather than inferred local availability.
    - The fixture validator detects deleted, duplicated, or unsupported cells and preserves declared region/pathway order.
  </behavior>
  <action>Populate `catalogueCoverage` from the controlled region/pathway constants so every pair exists exactly once, with explicit state and reviewed date, preserving D-05’s visible distinction for later presentation. Phase 9 ships all 36 coverage cells as `not-yet-verified`; verified opportunity imports belong exclusively to Phase 10. Do not add provider names, employer-pay terms, support/restriction claims, local military offices, wage values, or presumed programme availability. Expand the fixture tests to count all 36 keys, run `validateCoverageMatrix`, assert each pair is explicit, ordered by the contract constants, and `not-yet-verified`, and exercise deleted, duplicate, and unknown-key mutations. Include a test that Greater Kingston has six coverage rows without fabricating a Jamaican provider inventory.</action>
  <verify>
    <automated>pnpm --filter @scholar-scout/web run test -- catalogue-fixtures &amp;&amp; pnpm --filter @scholar-scout/web run typecheck</automated>
  </verify>
  <acceptance_criteria>The six-by-six matrix is complete, deterministic, and explicit about unverified coverage; no fixture fact asserts an unreviewed local offering.</acceptance_criteria>
  <done>The six-area contract now has a complete, auditable coverage baseline for Phase 10 reviewed imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Official boundary sources → frozen fixture | A wrong authority, stale boundary, or invalid anchor can misstate local scope. |
| Fixture coverage state → later discovery/publication | A missing or invented cell could be read as real local availability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04 | Spoofing | regional fixture source metadata | high | mitigate | Freeze the resolved July-2023 Census/OMB and STATIN URLs, exact versions, civic anchors, IDs, and `2026-09-22` checked dates; test literal expected fixture metadata and the U.S./STATIN distinction. |
| T-09-05 | Tampering | coverage fixture | high | mitigate | Generate/check all 36 controlled pairs and test deletion, duplication, unknown keys, and order. |
| T-09-06 | Repudiation | fixture review date | medium | mitigate | Require source and check/review dates in all boundary/coverage records and use injected clocks in tests. |
| T-09-SC | Tampering | package installation | high | mitigate | No packages are installed in Phase 9; existing TypeScript/Jest tooling only. |
</threat_model>

## Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Source-first six-area scope with visible uncertainty | The six source records and 36 explicit states establish the auditable baseline. |
| REQ | REG-01, REG-02, REG-03 | Tasks 1–2 supply all regional records with required metadata, all pathway cells, and explicit coverage state. |
| RESEARCH | Resolved July-2023 Census/OMB release, six civic anchors, STATIN KMA distinction, no invented inventory | Task 1 freezes and regression-tests every roster URL/version/ID/source-date/anchor/check date using Plan 09-01's policy; Task 2 keeps every Phase 9 cell explicitly `not-yet-verified`. |
| CONTEXT | D-01, D-05, D-06 | Task 1 applies scope/freshness; Task 2 preserves state separation for later verified-first presentation. |

<verification>
Run `pnpm --filter @scholar-scout/web run test -- catalogue-fixtures` and `pnpm --filter @scholar-scout/web run typecheck`. At the phase gate, also run web lint and the full web Jest suite.
</verification>

<success_criteria>
- Six auditable regional records use the correct U.S. Census/OMB or STATIN authority, exact boundary version/ID convention, and Plan 09-01 source-date/freshness contract.
- The fixture has exactly 36 explicit, ordered `not-yet-verified` coverage cells.
- Coverage truth stays separate from provider inventory and remains honest when a source is unavailable.
</success_criteria>

## Artifacts this phase produces

- `apps/web/lib/catalogue-fixtures.ts` — frozen six-area regional and coverage source snapshot.
- `apps/web/__tests__/lib/catalogue-fixtures.test.ts` — matrix and source-boundary regression suite.

<output>
Create `.planning/phases/09-catalogue-foundations-and-source-contracts/09-02-SUMMARY.md` when done.
</output>
