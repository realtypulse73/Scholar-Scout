---
phase: 09-catalogue-foundations-and-source-contracts
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - apps/web/lib/catalogue-fixtures.ts
  - apps/web/__tests__/lib/catalogue-fixtures.test.ts
autonomous: true
requirements:
  - REG-01
  - REG-02
  - REG-03
must_haves:
  truths:
    - "Each approved area exposes an official boundary authority/source/version/check date and an independent documented downtown ten-mile anchor."
    - "The fixture exports exactly 36 explicit region/pathway coverage cells, including six cells for Greater Kingston."
    - "Greater Kingston uses STATIN KMA authority and has no U.S. CBSA identifier."
    - "An unreviewed local option remains visibly `not-yet-verified`; it is not inferred from a city, national listing, or category name."
  artifacts:
    - path: apps/web/lib/catalogue-fixtures.ts
      provides: Frozen six-area regional and coverage fixtures.
      exports:
        - catalogueRegions
        - catalogueCoverage
    - path: apps/web/__tests__/lib/catalogue-fixtures.test.ts
      provides: Six-region source and 36-cell fixture validation.
  key_links:
    - from: apps/web/lib/catalogue-fixtures.ts
      to: apps/web/lib/catalogue-contract.ts
      via: Imported controlled types, matrix validator, and boundary freshness policy.
      pattern: "from './catalogue-contract'"
  prohibitions:
    - "No live learner-facing provider aggregation, runtime scraping, or automatic publication."
    - "No provider-quality, eligibility, job, salary, placement, or outcome claim is derived by this foundation."
    - "No sensitive re-entry information, sensitive ranking, or sensitive query field is present."
    - "Missing, stale, or conflicting facts are never silently upgraded to current."
---

<objective>
Create the frozen six-area regional fixture and its complete, honest coverage matrix.

Purpose: Make regional scope auditable before reviewed provider inventory exists.
Output: Source-bearing regional records plus an exact 36-cell coverage fixture and regression suite.
</objective>

<execution_context>
@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-CONTEXT.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-RESEARCH.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-VALIDATION.md
@apps/web/lib/catalogue-contract.ts

<interfaces>
From Plan 09-01, use the exported `CatalogueRegion`, `CatalogueCoverage`, `CATALOGUE_REGION_IDS`, `CATALOGUE_PATHWAYS`, and validation helpers. `catalogue-fixtures.ts` is static source material; it does not import server persistence or request data.

Official U.S. CBSA identifiers are: Greater Houston `26420`, Greater Chicago `16980`, Greater Buffalo `15380`, Greater Atlanta `12060`, and Greater New Orleans `35380`. Greater Kingston, Jamaica is represented only by the STATIN Kingston Metropolitan Area authority.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze six source-bearing regional records</name>
  <files>apps/web/lib/catalogue-fixtures.ts, apps/web/__tests__/lib/catalogue-fixtures.test.ts</files>
  <read_first>
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-CONTEXT.md (D-01 and D-06)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-RESEARCH.md (Patterns 1–2 and Kingston source guidance)
    - apps/web/lib/catalogue-contract.ts
  </read_first>
  <behavior>
    - The fixture contains exactly the six approved region IDs, each with independent official-boundary and downtown-anchor provenance.
    - The five U.S. areas carry the declared Census/OMB CBSA identifiers; Kingston carries STATIN KMA authority and omits a CBSA ID.
    - Boundary records older than the frozen 731-day policy cannot pass the fixture validation clock.
  </behavior>
  <action>Create `catalogue-fixtures.ts` with six readonly `CatalogueRegion` records per D-01 and D-06. Freeze a chosen Census/OMB delineation source URL and release/version for the five U.S. regions, plus the STATIN Kingston Metropolitan Area source URL/version for Greater Kingston; include a checked date that the tests evaluate against the 731-day boundary policy. For each area, record a source-backed downtown anchor label, finite latitude/longitude, anchor source URL, checked date, and the fixed ten-mile radius. Retain these as transparent scope metadata, not provider availability. Begin its focused Jest suite with failing tests that assert exact IDs, authority values, U.S. CBSA mapping, Kingston’s absent CBSA field, distinct anchor/boundary records, and two-year boundary freshness.</action>
  <verify>
    <automated>pnpm --filter @scholar-scout/web run test -- catalogue-fixtures</automated>
  </verify>
  <acceptance_criteria>All six regional records are source-bearing, the country-specific authority distinction is preserved, and date validation rejects a boundary record outside the two-year window.</acceptance_criteria>
  <done>The frozen regional fixture makes its geographic basis independently inspectable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish the complete explicit coverage matrix</name>
  <files>apps/web/lib/catalogue-fixtures.ts, apps/web/__tests__/lib/catalogue-fixtures.test.ts</files>
  <read_first>
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-CONTEXT.md (D-05)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-VALIDATION.md (36-cell fixture obligation)
    - apps/web/lib/catalogue-contract.ts
  </read_first>
  <behavior>
    - Six regions multiplied by six controlled pathway classes yields exactly 36 rows, each with a stable region/pathway key.
    - Every cell has an explicit coverage state and review date; an absent reviewed source is represented as `not-yet-verified` rather than inferred local availability.
    - The fixture validator detects deleted, duplicated, or unsupported cells and preserves declared region/pathway order.
  </behavior>
  <action>Populate `catalogueCoverage` from the controlled region/pathway constants so every pair exists exactly once, with explicit state and reviewed date, preserving D-05’s visible distinction for later presentation. Until a later governed import provides reviewed opportunity sources, use the honest `not-yet-verified` state and do not add provider names, employer-pay terms, support/restriction claims, local military offices, wage values, or presumed programme availability. Expand the fixture tests to count all 36 keys, run `validateCoverageMatrix`, assert each pair is explicit and ordered by the contract constants, and exercise deleted, duplicate, and unknown-key mutations. Include a test that Greater Kingston has six coverage rows without fabricating a Jamaican provider inventory.</action>
  <verify>
    <automated>pnpm --filter @scholar-scout/web run test -- catalogue-fixtures &amp;&amp; pnpm --filter @scholar-scout/web run typecheck</automated>
  </verify>
  <acceptance_criteria>The six-by-six matrix is complete, deterministic, and explicit about unverified coverage; no fixture fact asserts an unreviewed local offering.</acceptance_criteria>
  <done>The six-area contract now has a complete, auditable coverage baseline for Phase 10 reviewed imports.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Official boundary sources → frozen fixture | A wrong authority, stale boundary, or invalid anchor can misstate local scope. |
| Fixture coverage state → later discovery/publication | A missing or invented cell could be read as real local availability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-04 | Spoofing | regional fixture source metadata | high | mitigate | Freeze authority-specific URLs, IDs, versions, and checked dates; test the U.S./STATIN distinction. |
| T-09-05 | Tampering | coverage fixture | high | mitigate | Generate/check all 36 controlled pairs and test deletion, duplication, unknown keys, and order. |
| T-09-06 | Repudiation | fixture review date | medium | mitigate | Require source and check/review dates in all boundary/coverage records and use injected clocks in tests. |
| T-09-SC | Tampering | package installation | high | mitigate | No packages are installed in Phase 9; existing TypeScript/Jest tooling only. |
</threat_model>

## Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Source-first six-area scope with visible uncertainty | The six source records and 36 explicit states establish the auditable baseline. |
| REQ | REG-01, REG-02, REG-03 | Tasks 1–2 supply all regional records, all pathway cells, and explicit coverage state. |
| RESEARCH | Census/OMB CBSA records, STATIN KMA distinction, no invented inventory | Task 1 freezes distinct authority records; Task 2 keeps unreviewed cells explicit. |
| CONTEXT | D-01, D-05, D-06 | Task 1 applies scope/freshness; Task 2 preserves state separation for later verified-first presentation. |

<verification>
Run `pnpm --filter @scholar-scout/web run test -- catalogue-fixtures` and `pnpm --filter @scholar-scout/web run typecheck`. At the phase gate, also run web lint and the full web Jest suite.
</verification>

<success_criteria>
- Six auditable regional records use the correct U.S. Census/OMB or STATIN authority.
- The fixture has exactly 36 explicit, ordered coverage cells.
- Coverage truth stays separate from provider inventory and remains honest when a source is unavailable.
</success_criteria>

## Artifacts this phase produces

- `apps/web/lib/catalogue-fixtures.ts` — frozen six-area regional and coverage source snapshot.
- `apps/web/__tests__/lib/catalogue-fixtures.test.ts` — matrix and source-boundary regression suite.

<output>
Create `.planning/phases/09-catalogue-foundations-and-source-contracts/09-02-SUMMARY.md` when done.
</output>
