---
phase: 09-catalogue-foundations-and-source-contracts
plan: 10
type: tdd
wave: 9
depends_on:
  - 09-09
files_modified:
  - apps/web/lib/catalogue-contract.ts
  - apps/web/__tests__/lib/catalogue-contract.test.ts
autonomous: true
gap_closure: true
requirements:
  - REG-01
user_setup: []
must_haves:
  truths:
    - "Every valid latitude/longitude pair, including the reproduced near-antipodal pair, produces a finite deterministic great-circle distance."
    - "The ten-mile inclusive local-focus boundary and invalid-coordinate null/false behavior remain unchanged."
  artifacts:
    - path: apps/web/lib/catalogue-contract.ts
      provides: A bounded Haversine intermediate before both central-angle square roots.
      exports:
        - calculateGreatCircleMiles
        - isWithinLocalFocus
    - path: apps/web/__tests__/lib/catalogue-contract.test.ts
      provides: Public-helper regressions for the near-antipodal coordinate pair and established local-focus behavior.
  key_links:
    - from: apps/web/__tests__/lib/catalogue-contract.test.ts
      to: apps/web/lib/catalogue-contract.ts
      via: Direct public calculateGreatCircleMiles calls exercise the bounded central-angle path.
      pattern: calculateGreatCircleMiles
---

<objective>
Close the Phase 09 numerical-geometry verification gap with a RED-first, local repair.

Purpose: A valid near-antipodal coordinate pair must not make the documented local-focus helper return a non-number because of floating-point rounding.
Output: One public-helper regression and one bounded Haversine calculation; no fixture, source roster, API, persistence, UI, dependency, or documentation change.
</objective>

<execution_context>
@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-CONTEXT.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-RESEARCH.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-REVIEW.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-VERIFICATION.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-09-SUMMARY.md
@apps/web/lib/catalogue-contract.ts
@apps/web/__tests__/lib/catalogue-contract.test.ts

<interfaces>
`calculateGreatCircleMiles(from, to)` accepts validated `GeoCoordinate` values and returns `number | null`: `null` only for invalid coordinates, otherwise a finite great-circle distance using `EARTH_RADIUS_MILES`. `isWithinLocalFocus(localFocus, location)` consumes that helper and retains its inclusive ten-mile threshold.

The review and verification reproduce the failure with `from = { latitude: -11.18571800391112, longitude: 139.73174389513332 }` and `to = { latitude: 11.18571800391112, longitude: -40.26825610486668 }`. The mathematical central angle is π; ordinary floating-point evaluation can put the Haversine intermediate infinitesimally outside its closed range before the square-root calculation.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Add the public near-antipodal distance regression</name>
  <files>apps/web/__tests__/lib/catalogue-contract.test.ts</files>
  <precondition>The focused catalogue-contract Jest suite is green before the regression is added.</precondition>
  <read_first>
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-VERIFICATION.md (the failed truth, exact reproduction, and missing repair)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-REVIEW.md (WR-01 numerical finding)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-CONTEXT.md (D-01 through D-08 locked constraints)
    - apps/web/lib/catalogue-contract.ts (public helper contract and local-focus consumer)
    - apps/web/__tests__/lib/catalogue-contract.test.ts (existing ten-mile, invalid-coordinate, and Earth-radius assertions)
  </read_first>
  <behavior>
    - Calling `calculateGreatCircleMiles` with `(-11.18571800391112, 139.73174389513332)` and `(11.18571800391112, -40.26825610486668)` returns a finite number.
    - That result is within the existing Jest precision convention of `EARTH_RADIUS_MILES * Math.PI`, proving the same deterministic great-circle result expected for the reproduced antipodal geometry.
    - The existing ten-mile inclusive, beyond-ten-mile exclusion, invalid-coordinate `null`, and invalid-location `false` assertions remain passing without altered expectations.
  </behavior>
  <action>Add one focused public-API Jest case beside the existing great-circle boundary case. Use the exact coordinate values from the verification report and assert both finiteness and a close match to `EARTH_RADIUS_MILES * Math.PI`; exercise only `calculateGreatCircleMiles`, not a private expression. Do not modify the established ten-mile or invalid-coordinate assertions: they are the behavior-preservation boundary. Per D-01, retain the documented ten-mile straight-line local focus as distinct from the official regional boundary. Preserve D-02 through D-08 by leaving employer-training facts, controlled pathways, evidence states, freshness rules, sensitive-policy limits, and first-view fact fields untouched. Run the focused test before production edits and confirm this new case is RED because the current helper yields a non-finite value for these valid coordinates.</action>
  <verify>
    <automated>pnpm --filter @scholar-scout/web run test -- __tests__/lib/catalogue-contract.test.ts --runInBand</automated>
  </verify>
  <done>The focused suite demonstrates the one newly added regression fails on the reviewed baseline for the reproduced valid coordinate pair, while existing local-focus behavior remains specified by its unchanged assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bound the Haversine intermediate before the central-angle roots</name>
  <files>apps/web/lib/catalogue-contract.ts</files>
  <read_first>
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-VERIFICATION.md (gap reason and required repair)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-REVIEW.md (WR-01 expected numerical guard)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-CONTEXT.md (D-01 through D-08 locked constraints)
    - apps/web/lib/catalogue-contract.ts (calculateGreatCircleMiles and isWithinLocalFocus)
    - apps/web/__tests__/lib/catalogue-contract.test.ts (Task 1 regression and preserved behavior)
  </read_first>
  <behavior>
    - For valid input, the Haversine intermediate is bounded inclusively to 0 through 1 before either square-root operand is evaluated.
    - The reproduced near-antipodal call returns the finite π-radius distance required by Task 1.
    - Existing non-antipodal distance, inclusive local-focus, and invalid-coordinate results retain their public behavior.
  </behavior>
  <action>In `calculateGreatCircleMiles`, retain the existing coordinate validation, radian conversion, Haversine formula, Earth-radius constant, return type, and `isWithinLocalFocus` consumer. Immediately after calculating the raw Haversine value, derive one local bounded value with the inclusive lower and upper mathematical limits, then use that bounded value for both operands of the central-angle square-root calculation. Make no changes to coordinate validity, local-focus radius comparison, region fixtures, evidence validators, routes, persistence, UI, dependencies, or public signatures. This implements D-01's deterministic ten-mile straight-line focus while preserving D-02 through D-08 unchanged.</action>
  <reversibility rating="reversible">The change is local pure arithmetic with no data migration, transport contract, or dependency impact.</reversibility>
  <verify>
    <automated>pnpm --filter @scholar-scout/web run test -- __tests__/lib/catalogue-contract.test.ts --runInBand &amp;&amp; pnpm --filter @scholar-scout/web run typecheck &amp;&amp; pnpm --filter @scholar-scout/web run lint &amp;&amp; pnpm --filter @scholar-scout/web run test --runInBand</automated>
  </verify>
  <done>The exact near-antipodal regression returns a finite deterministic distance close to `EARTH_RADIUS_MILES * Math.PI`; the focused suite, TypeScript check, ESLint, and complete web Jest suite pass with existing local-focus behavior preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Valid geographic coordinates → local-focus classification | Floating-point arithmetic can exceed the Haversine domain by a rounding unit and make a valid distance non-finite. |
| Great-circle helper → local-focus eligibility check | A non-finite distance can cause incorrect geographic classification for later catalogue consumers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-10-01 | Tampering | `calculateGreatCircleMiles` central-angle calculation | medium | mitigate | Task 2 bounds the arithmetic-domain value before square-root evaluation; Task 1 proves the exact valid-coordinate regression. |
| T-09-10-02 | Integrity | `isWithinLocalFocus` distance consumer | medium | mitigate | Existing public boundary assertions and the full Jest gate ensure finite distances preserve inclusive ten-mile semantics. |
| T-09-10-SC | Tampering | dependency supply chain | low | accept | This repair adds no package, lockfile, or external integration. |
</threat_model>

## must_haves

SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | — | Deterministic, source-first catalogue facts for the approved regions without fabricated coverage. | 09-10 | COVERED | A valid coordinate input now produces a deterministic distance for the documented local-focus rule.
REQ | REG-01 | Exact regional boundary and distinct local focus are reproducible. | 09-10 | COVERED | The repair applies only to deterministic straight-line local-focus arithmetic.
REQ | REG-02 | All six controlled pathway classes remain visible. | 09-01, 09-02 | COVERED | Unchanged, outside this numerical repair.
REQ | REG-03 | Each region/pathway has an honest coverage state. | 09-01, 09-02, 09-09 | COVERED | Unchanged, outside this numerical repair.
REQ | EVID-01 | Material facts carry attributable metadata. | 09-01, 09-03, 09-09 | COVERED | Unchanged, outside this numerical repair.
REQ | EVID-02 | Evidence statuses remain distinct. | 09-01, 09-03, 09-09 | COVERED | Unchanged, outside this numerical repair.
REQ | EVID-05 | Wage context remains dated contextual information. | 09-03 | COVERED | Unchanged, outside this numerical repair.
RESEARCH | — | Use a pure documented great-circle calculation for the ten-mile local focus; do not introduce a GIS service or map UI. | 09-10 | COVERED | The helper and unit suite remain the only changed implementation boundary.
CONTEXT | D-01 | Local ten-mile focus remains distinct from official boundaries. | 09-10 | COVERED | Task 1 and Task 2 preserve and repair only the deterministic local-focus calculation.
CONTEXT | D-02 | Employer-paid training remains a factual distinct pathway. | 09-01, 09-03 | COVERED | No related artifact changes.
CONTEXT | D-03 | All approved pathways stay browseable without determinations. | 09-01, 09-02 | COVERED | No related artifact changes.
CONTEXT | D-04 | Uncertain facts retain neutral sourced guidance. | 09-03 | COVERED | No related artifact changes.
CONTEXT | D-05 | Verified and unverified listings remain distinct. | 09-02, 09-09 | COVERED | No related artifact changes.
CONTEXT | D-06 | Boundary and operational freshness policies remain deterministic. | 09-01, 09-03 | COVERED | No related artifact changes.
CONTEXT | D-07 | Sensitive-policy claims stay official-source-only. | 09-03 | COVERED | No related artifact changes.
CONTEXT | D-08 | First-view cards remain non-sensitive and sourced. | 09-03 | COVERED | No related artifact changes.

<verification>
Execute TDD in order: Task 1 adds and runs the failing exact-coordinate regression before editing production arithmetic; Task 2 makes it pass. Then run the focused contract suite, TypeScript typecheck, ESLint, and the complete web Jest suite using the commands in Task 2. Do not accept a passing test until the new assertion has been observed failing on the unmodified helper.
</verification>

<success_criteria>
- The exact pair `(-11.18571800391112, 139.73174389513332)` and `(11.18571800391112, -40.26825610486668)` returns a finite distance close to `EARTH_RADIUS_MILES * Math.PI` through the public helper.
- The raw Haversine result is inclusively bounded before central-angle roots, preventing floating-point domain overshoot for valid coordinates.
- Existing ten-mile inclusion/exclusion and invalid-coordinate public behavior remains unchanged.
- The focused Jest suite, TypeScript typecheck, ESLint, and complete web Jest suite pass.
</success_criteria>

<output>
Create `.planning/phases/09-catalogue-foundations-and-source-contracts/09-10-SUMMARY.md` when done, recording the RED failure, GREEN repair, and all required gate results.
</output>
