---
phase: 09-catalogue-foundations-and-source-contracts
plan: 09
type: tdd
wave: 8
depends_on:
  - 09-08
files_modified:
  - apps/web/lib/catalogue-contract.ts
  - apps/web/__tests__/lib/catalogue-contract.test.ts
autonomous: true
requirements:
  - REG-03
  - EVID-01
must_haves:
  truths:
    - "An invalid injected Date produces the same deterministic validation-clock error at every public catalogue validator that accepts a clock."
    - "A current fact with future dates and a complete verified coverage matrix with future dates cannot validate when their supplied clock is invalid."
    - "An opportunity-card status derived with an invalid clock is unknown, while valid-clock freshness and all established Phase 09 contracts retain their behavior."
  artifacts:
    - path: apps/web/lib/catalogue-contract.ts
      provides: Fail-closed validation-clock guard at every public validator that accepts the injected clock.
      exports:
        - validateFactEvidence
        - validateEmployerTrainingFacts
        - validateOccupationAreaWageContext
        - validateCatalogueOpportunityCardFacts
        - validateCoverageMatrix
    - path: apps/web/__tests__/lib/catalogue-contract.test.ts
      provides: RED-first invalid-clock regressions for direct evidence, composed facts, cards, status derivation, and complete verified coverage.
  key_links:
    - from: apps/web/__tests__/lib/catalogue-contract.test.ts
      to: apps/web/lib/catalogue-contract.ts
      via: Public validator calls receive new Date('invalid') together with otherwise valid future current evidence and a complete verified coverage matrix.
      pattern: "Validation clock must be a valid Date"
---

<objective>
Close review finding CR-01 with a strict RED-first validation-clock repair.

Purpose: Phase 10 must never accept future material as current because a caller supplied an invalid injected clock to the Phase 09 import boundary.
Output: Focused invalid-clock regressions and a minimal fail-closed repair in the pure catalogue contract; no fixture, source roster, route, persistence, UI, live integration, ranking, provider inventory, or documentation change.
</objective>

<execution_context>
@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-CONTEXT.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-RESEARCH.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-REVIEW.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-08-SUMMARY.md
@apps/web/lib/catalogue-contract.ts
@apps/web/__tests__/lib/catalogue-contract.test.ts
@apps/web/__tests__/lib/catalogue-fixtures.test.ts

<interfaces>
`validateFactEvidence(evidence, now)`, `validateEmployerTrainingFacts(facts, now)`, `validateOccupationAreaWageContext(context, now)`, `validateCatalogueOpportunityCardFacts(facts, now)`, and `validateCoverageMatrix(regions, coverage, now)` are the complete public validation boundaries that receive the injected clock. Each must reject an invalid `Date` with the identical stable error `Validation clock must be a valid Date.` before it can certify current evidence.

`getOpportunityCardVerificationStatus(facts, now)` is the public status consumer: it delegates to card validation and must consequently return `unknown` when the clock is invalid. `getFreshnessStatus` already classifies an invalid clock as `unknown`; retain that behavior and do not widen this repair to non-validator APIs.

The valid frozen six-region roster, ordered 36-cell baseline, controlled pathway vocabulary, source-date semantics, provenance admission, and field-level fact contracts are preservation constraints from Plans 01–08.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Add RED regressions for an invalid injected validation clock</name>
  <files>apps/web/__tests__/lib/catalogue-contract.test.ts</files>
  <precondition>The focused catalogue-contract suite passes before the new clock-regression cases are added.</precondition>
  <read_first>
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-REVIEW.md (CR-01 defines the complete repair scope)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-CONTEXT.md (D-01 through D-08 are locked constraints)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-08-SUMMARY.md (provenance-admission behavior that must remain intact)
    - apps/web/lib/catalogue-contract.ts (all public clock-taking validators and status derivation)
    - apps/web/__tests__/lib/catalogue-contract.test.ts (fixed clock, valid evidence, composed-fact, card, region, and coverage builders)
  </read_first>
  <behavior>
    - `validateFactEvidence` returns exactly `Validation clock must be a valid Date.` for an otherwise valid `current` evidence object with documented and review dates in 2099 when passed `new Date('invalid')`.
    - `validateEmployerTrainingFacts`, `validateOccupationAreaWageContext`, and `validateCatalogueOpportunityCardFacts` each return exactly that same stable error for their otherwise valid fixtures and the invalid clock.
    - `getOpportunityCardVerificationStatus` returns `unknown` for the valid card fixture and the invalid clock.
    - `validateCoverageMatrix` returns exactly that same stable error for one valid supplied region and a complete six-pathway verified matrix whose coverage-review and current-evidence dates are in 2099, when passed the invalid clock.
    - The existing valid fixed-clock assertions continue to distinguish current, needs-confirmation, and unknown source freshness.
  </behavior>
  <action>Before production edits, add one focused invalid-clock regression group to the public Jest suite. Reuse `factEvidence`, `employerTrainingFacts`, `wageContext`, `cardFacts`, `region`, and `CATALOGUE_PATHWAYS`; create a local invalid clock with `new Date('invalid')`, then derive otherwise valid 2099 current evidence and a complete six-row verified matrix whose row review date also falls in 2099. Assert the single stable clock error from each of the five public validators, and assert `unknown` from the public opportunity-card status helper. Keep the direct future-evidence and complete verified-matrix cases independent so the reviewed fail-open paths are observed without unrelated shape failures. Per D-01, preserve official boundaries and separate ten-mile local focus. Per D-02, preserve distinct employer-paid-training disclosures. Per D-03, preserve all six browseable pathway classes. Per D-04 and D-05, preserve neutral, explicit uncertainty states. Per D-06, preserve the fixed-clock freshness policy. Per D-07, preserve the official-evidence-only sensitive-policy boundary. Per D-08, preserve the non-sensitive first-view card contract. Run the focused test command before production edits and confirm the new cases are RED because the invalid clock currently disables future-date and freshness enforcement.</action>
  <verify>
    <automated>pnpm --filter @scholar-scout/web run test -- __tests__/lib/catalogue-contract.test.ts --runInBand</automated>
  </verify>
  <done>The new public-validator regressions fail against the reviewed baseline only because an invalid clock is currently accepted as an optional check.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fail closed at every public injected-clock validation boundary</name>
  <files>apps/web/lib/catalogue-contract.ts</files>
  <read_first>
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-REVIEW.md (CR-01 fail-closed requirement)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-CONTEXT.md (D-01 through D-08 locked boundaries)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-08-SUMMARY.md (existing provenance-first admission pattern)
    - apps/web/lib/catalogue-contract.ts (isValidDate, direct validators, composed validators, coverage matrix, and status helper)
    - apps/web/__tests__/lib/catalogue-contract.test.ts (Task 1 RED cases)
  </read_first>
  <behavior>
    - Each public validator that accepts `now` checks it at its own boundary and returns the stable validation-clock error when `now` is not a valid Date.
    - A bad clock cannot bypass review-date-in-the-future, source-date-in-the-future, or operational-freshness checks for a `current` fact.
    - A bad clock cannot make a complete verified coverage matrix with future coverage-review and evidence dates validate.
    - `getOpportunityCardVerificationStatus` remains a derived consumer and reports `unknown` because invalid-clock card validation has errors.
    - Valid-clock source metadata, source-date availability, provenance, controlled order, duplicate, missing-pair, coverage-evidence chronology, and first-view card behavior retain their existing results.
  </behavior>
  <action>Modify only `catalogue-contract.ts` to make Task 1 green. Introduce one private stable validation-clock error source adjacent to `isValidDate`, then apply an early fail-closed guard at the start of each public validator that receives `now`: fact evidence, employer training, occupation-area wage context, opportunity-card facts, and coverage matrix. The guard must return the same one-element error result for an invalid Date rather than conditionally skipping its date/freshness validation. Leave `getOpportunityCardVerificationStatus` as a delegating consumer so it derives `unknown` from the card validator, and leave `getFreshnessStatus` unchanged because it is a classifier that already returns `unknown` for an invalid clock. Preserve all valid-clock validation order and existing error strings beyond the new root condition. Per D-01, retain authoritative boundaries and local-focus provenance. Per D-02, retain factual trainee-pay and employment-commitment rules. Per D-03, retain the controlled six-pathway contract without eligibility or outcome determination. Per D-04 and D-05, retain source-visible uncertainty semantics. Per D-06, retain the two-year boundary and six-month operational freshness thresholds. Per D-07, retain sensitive-policy evidence limits. Per D-08, retain the complete non-sensitive first-view card boundary. Do not change fixtures, public routes, persistence, source imports, or UI.</action>
  <reversibility rating="reversible">This is a local pure-validator guard with no stored-data migration, dependency, or public transport-contract change.</reversibility>
  <verify>
    <automated>pnpm --filter @scholar-scout/web run test -- __tests__/lib/catalogue-contract.test.ts --runInBand &amp;&amp; pnpm --filter @scholar-scout/web run test -- __tests__/lib/catalogue-fixtures.test.ts --runInBand &amp;&amp; pnpm --filter @scholar-scout/web run typecheck &amp;&amp; pnpm --filter @scholar-scout/web run lint &amp;&amp; pnpm --filter @scholar-scout/web run test --runInBand</automated>
  </verify>
  <done>All five public validators reject an invalid injected Date with the deterministic validation-clock error; future current evidence and a complete future verified matrix cannot pass; the status helper reports unknown; and the focused suites, fixture suite, typecheck, lint, and full web Jest suite pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 10 reviewed-import clock → Phase 09 catalogue validators | A caller-controlled or malformed injected Date can otherwise suppress future-date and freshness rejection at the import boundary. |
| Fact and coverage evidence → public catalogue state | Future records could be represented as current and verified if the validation clock does not fail closed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-09-01 | Tampering | Clock-taking public catalogue validators | high | mitigate | Task 2 rejects invalid injected clocks at every public validation boundary; Task 1 proves direct and composed evidence paths return the stable error. |
| T-09-09-02 | Integrity | validateCoverageMatrix verified coverage admission | high | mitigate | Task 1 uses complete verified rows with future evidence and review dates; Task 2 makes the invalid clock block matrix acceptance before it can certify coverage. |
| T-09-09-03 | Repudiation | Deterministic validation results | medium | mitigate | A single stable validation-clock message gives the import caller a repeatable failure category without exposing source contents. |
| T-09-09-SC | Tampering | dependency supply chain | low | accept | This repair installs no package and changes no dependency manifest or lockfile. |
</threat_model>

## must_haves

SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | — | Deterministic source-first catalogue facts and visible uncertainty never invent current coverage. | 09-09 | COVERED | Invalid clocks cannot certify future evidence or verified coverage.
REQ | REG-01 | Exact approved regional boundary, authority, source, and check date. | 09-01, 09-08 | COVERED | Existing regional provenance artifacts remain unchanged by this clock-only repair.
REQ | REG-02 | Every selected metro retains all six pathway classes. | 09-01, 09-02 | COVERED | Task 1's complete verified matrix retains the controlled six-pathway vocabulary.
REQ | REG-03 | Each metro/pathway has an honest coverage state. | 09-09 | COVERED | Future verified coverage cannot pass through an invalid clock.
REQ | EVID-01 | Attributable evidence includes source and review metadata. | 09-09 | COVERED | Invalid clocks fail deterministically instead of suppressing future and freshness validation.
REQ | EVID-02 | Stale, unavailable, conflicting, and current evidence remain explicit. | 09-03, 09-09 | COVERED | Task 2 preserves valid-clock status behavior and prevents malformed clock input from upgrading future evidence.
REQ | EVID-05 | Wage context stays dated occupation-and-area information. | 09-03, 09-09 | COVERED | The wage validator receives the same fail-closed clock guard without changing its informational contract.
RESEARCH | — | Pure deterministic snapshot validation, explicit freshness, frozen roster, no live aggregation. | 09-09 | COVERED | The repair changes only pure clock-taking validators and their tests.
CONTEXT | D-01 | Official boundary and separate ten-mile local focus. | 09-01, 09-08 | COVERED | Task actions preserve both regional provenance layers.
CONTEXT | D-02 | Employer-paid training is distinct and factual. | 09-09 | COVERED | Task 1 and Task 2 cover its public validator while preserving trainee-pay and commitment semantics.
CONTEXT | D-03 | Six pathway classes stay browseable without eligibility or outcome determinations. | 09-09 | COVERED | Complete matrix regression retains all controlled paths and does not add decision logic.
CONTEXT | D-04 | Government-confirmed uncertainty is neutral, source-visible, and dated. | 09-09 | COVERED | Fail-closed clock handling prevents future evidence from becoming current.
CONTEXT | D-05 | Verified and unverified listings stay unmistakably separate. | 09-09 | COVERED | Verified coverage cannot be falsely accepted from future material.
CONTEXT | D-06 | Boundary freshness is two years and operational facts are reviewed within six months. | 09-09 | COVERED | Invalid clock input becomes a validation error while valid-clock thresholds remain unchanged.
CONTEXT | D-07 | Sensitive-policy claims require official evidence and no inference. | 09-03 | COVERED | The repair leaves sensitive-policy handling untouched.
CONTEXT | D-08 | First-view cards have non-sensitive sourced decision facts. | 09-09 | COVERED | The public card validator and derived status are covered without changing fields.

<verification>
The execution sequence is mandatory: Task 1 writes and runs the focused RED regressions before any production edit; Task 2 makes them green and runs the complete quality gate. Execute `pnpm --filter @scholar-scout/web run test -- __tests__/lib/catalogue-contract.test.ts --runInBand`, then `pnpm --filter @scholar-scout/web run test -- __tests__/lib/catalogue-fixtures.test.ts --runInBand`, `pnpm --filter @scholar-scout/web run typecheck`, `pnpm --filter @scholar-scout/web run lint`, and `pnpm --filter @scholar-scout/web run test --runInBand`.
</verification>

<success_criteria>
- `new Date('invalid')` yields exactly the stable validation-clock error from every public injected-clock validator.
- A direct current fact with 2099 source/review dates and a complete verified six-pathway matrix with 2099 evidence/review dates fail under the invalid clock.
- The public card-status helper returns unknown for an invalid clock; valid-clock behavior, the frozen six-region source roster, ordered 36-cell baseline, and all prior Phase 09 guards remain intact.
- Targeted contract and fixture suites, TypeScript typecheck, ESLint, and the complete web Jest suite pass after the repair.
</success_criteria>

<output>
Create `.planning/phases/09-catalogue-foundations-and-source-contracts/09-09-SUMMARY.md` when done.
</output>
