---
phase: 09-catalogue-foundations-and-source-contracts
plan: 07
type: execute
wave: 6
depends_on:
  - 09-06
files_modified:
  - apps/web/lib/catalogue-contract.ts
  - apps/web/__tests__/lib/catalogue-contract.test.ts
autonomous: true
requirements:
  - REG-01
  - REG-02
  - REG-03
  - EVID-01
  - EVID-02
must_haves:
  truths:
    - "Every coverage row whose controlled region ID is absent from the supplied regional matrix is rejected, whether its state is verified or not-yet-verified."
    - "A documented source date cannot be checked before the source date, while explicitly unavailable source dates retain their existing non-current/unknown semantics."
    - "The frozen six-region source roster, ordered 36-cell explicit not-yet-verified baseline, D-08 first-view-card boundary, and Phase 09 domain-only boundary remain unchanged."
  artifacts:
    - path: apps/web/lib/catalogue-contract.ts
      provides: Pure region-matrix membership and source-metadata chronology validation for reviewed snapshot imports.
      exports:
        - validateCoverageMatrix
        - validateSourceMetadata
    - path: apps/web/__tests__/lib/catalogue-contract.test.ts
      provides: RED-first regressions for controlled-but-undeclared coverage and impossible source/review chronology.
  key_links:
    - from: apps/web/lib/catalogue-contract.ts
      to: Phase 10 reviewed snapshot import boundary
      via: validateCoverageMatrix binds rows to the supplied region records and validateSourceMetadata rejects impossible documented-source chronology before publication work.
      pattern: "validateCoverageMatrix|validateSourceMetadata"
---

<objective>
Close the two final Phase 09 catalogue-contract review findings with a strict RED-first pure-validator repair.

Purpose: Prevent a snapshot from attaching coverage to a controlled region whose declared boundary record is missing, or from claiming that staff checked a source before that documented source existed.
Output: Focused regression coverage and a minimal `catalogue-contract.ts` repair; no fixture, source-roster, UI, persistence, import-route, ranking, or provider-inventory change.
</objective>

<execution_context>
@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-CONTEXT.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-RESEARCH.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-REVIEW.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-06-SUMMARY.md
@apps/web/lib/catalogue-contract.ts
@apps/web/lib/catalogue-fixtures.ts
@apps/web/__tests__/lib/catalogue-contract.test.ts
@apps/web/__tests__/lib/catalogue-fixtures.test.ts

<interfaces>
`validateCoverageMatrix(regions, coverage, now)` is a pure public validator over readonly unknown-shaped list entries. It currently derives `regionIds` from supplied records for missing-pair checks, but a coverage row with a controlled `regionId` can still bypass that supplied-list boundary. Keep its injected clock, controlled declaration order, duplicate/unsupported/missing-pair behavior, verified-evidence chronology checks, and runtime own-property discriminator unchanged except for the new supplied-region membership check.

`validateSourceMetadata(metadata)` is a pure public validator. `SourceDate` is either a documented ISO calendar date or `{ state: 'unavailable', value: null }`. The unavailable representation is required metadata and must remain accepted by source-metadata validation; freshness/classification continues to render it non-current/unknown under the injected clock. Only a parseable documented source date participates in the new comparison with `checkedAt`.

`catalogueRegions` and `catalogueCoverage` are deeply frozen source fixtures: exactly six regions and 36 ordered explicit `not-yet-verified` cells. They are preservation inputs, not edit targets. D-08's non-sensitive opportunity-card facts, D-01 official-boundary/local-focus split, D-02 employer-training contract, D-03 browseable six-pathway vocabulary, D-04/D-05 uncertainty states, D-06 clock thresholds, and D-07's absent sensitive-policy fields are established scope constraints, not repair targets.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Add failing regressions for undeclared controlled coverage and source-date chronology</name>
  <files>apps/web/__tests__/lib/catalogue-contract.test.ts</files>
  <precondition>The current focused catalogue-contract suite passes before these regressions are added.</precondition>
  <read_first>
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-REVIEW.md (CR-01 and WR-01 are the complete repair scope)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-CONTEXT.md (D-01 through D-08 are locked preservation constraints)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-06-SUMMARY.md (current parsed-import guard guarantees)
    - apps/web/lib/catalogue-contract.ts (current `validateSourceMetadata`, `validateCoverageMatrix`, controlled constants, and error conventions)
    - apps/web/__tests__/lib/catalogue-contract.test.ts (existing valid region, coverage, evidence, fixed-clock, and complete-matrix builders)
    - apps/web/__tests__/lib/catalogue-fixtures.test.ts (frozen six-region/36-cell invariants that must remain unchanged)
  </read_first>
  <behavior>
    - An otherwise valid one-region/six-pathway matrix with one additional controlled Greater Houston `not-yet-verified` row returns the deterministic supplied-region error, because Houston is absent from the supplied `regions` list.
    - The analogous additional controlled Greater Houston `verified` row with otherwise valid current `FactEvidence` returns the same deterministic supplied-region error.
    - Documented source metadata with `sourceDate: 2026-09-22` and `checkedAt: 2026-09-21` returns exactly `Checked date cannot precede the documented source date.`.
    - Explicitly unavailable source metadata remains valid when its ISO checked date is present, proving that the new chronology rule does not convert an unavailable date into a fabricated documented date or alter its existing non-current semantics.
  </behavior>
  <action>Before any production edit, extend only the existing contract Jest suite with fixed-clock, public-validator cases for CR-01 and WR-01. Reuse its local valid Kingston region, `coverage`, `factEvidence`, `FACT_NOW`, and complete six-pathway builders. In two independent matrices, retain the complete valid Kingston set and append one controlled Greater Houston row despite omitting Houston from the supplied regions: first a `not-yet-verified` row, then a `verified` row carrying valid current evidence. Assert the exact stable error `Coverage region is not declared: greater-houston.` in each case, while all non-mutated rows remain valid. Add a direct `validateSourceMetadata` case with the documented source/review chronology above and assert the exact error; add the explicit unavailable `SourceDate` counterpart with a valid checked date and assert no chronology error. Preserve D-01 through D-08: do not modify frozen regional sources, the 36-cell fixture, pathway vocabulary, two-layer geography, freshness thresholds, evidence/card/employer/wage types, or the domain-only no-UI/no-persistence boundary. Run the focused command and confirm each new case fails against the reviewed baseline for the intended missing validation behavior, not for setup, type, or fixture failure.</action>
  <verify>
    <automated>pnpm --filter @scholar-scout/web run test -- __tests__/lib/catalogue-contract.test.ts --runInBand</automated>
  </verify>
  <done>The added tests are RED solely because controlled-but-undeclared coverage passes the current matrix validator and documented source metadata permits an earlier checked date.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind coverage rows to supplied regions and reject impossible documented-source chronology</name>
  <files>apps/web/lib/catalogue-contract.ts</files>
  <read_first>
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-REVIEW.md (CR-01 and WR-01 required outcomes)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-CONTEXT.md (D-01 through D-08 locked boundaries)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-06-SUMMARY.md (current runtime-shape and coverage-discriminator guards)
    - apps/web/lib/catalogue-contract.ts (current `regionIds` assembly, row loop, and source-date helpers)
    - apps/web/__tests__/lib/catalogue-contract.test.ts (Task 1 RED cases)
    - apps/web/lib/catalogue-fixtures.ts (frozen fixture imports that must continue validating without modification)
  </read_first>
  <behavior>
    - Each coverage row with a controlled ID absent from the supplied regions is rejected before it can be accepted as verified or explicit unknown coverage.
    - Controlled rows belonging to supplied regions retain all existing coverage-state, evidence, injected-clock chronology, duplicate, unsupported-key, and controlled-order behavior.
    - `validateSourceMetadata` rejects an ISO checked date before a valid documented source date, while an unavailable source date remains structurally valid and keeps its established non-current/unknown behavior.
  </behavior>
  <action>Modify only `catalogue-contract.ts` to make Task 1 green. In the existing coverage-row loop, after a row's `regionId` has passed the controlled vocabulary check, require membership in the `regionIds` set derived from supplied valid region records. Add the exact stable error `Coverage region is not declared: ${regionId}.` when that controlled region is absent; run this rule for every applicable row regardless of whether the state is verified or not-yet-verified, without skipping existing state/evidence/own-property checks. Do not change controlled-region handling, missing-pair declaration order, or the frozen six-region/36-cell fixture behavior. In `validateSourceMetadata`, parse the documented source date and the checked date through existing date helpers only after their existing shape validations; when both parse and the checked date precedes the documented source date, add exactly `Checked date cannot precede the documented source date.`. Do not compare an unavailable source-date variant, do not alter `getFreshnessStatus`, and do not invent a source date. Preserve D-01's official-boundary/local-focus distinction, D-02's factual paid-training boundary, D-03's six browseable pathway classes, D-04/D-05 visible uncertainty, D-06's 183/731-day policy, D-07's absent sensitive-policy surface, and D-08's non-sensitive first-view-card contract. Do not edit fixtures, sources, routes, persistence, UI, ranking, eligibility, sensitive inputs, provider inventory, or outcome logic.</action>
  <reversibility rating="reversible">The repair only tightens pure input validation behind existing exports; it changes no persisted data, public snapshot, fixture fact, or external contract.</reversibility>
  <verify>
    <automated>pnpm --filter @scholar-scout/web run test -- __tests__/lib/catalogue-contract.test.ts --runInBand &amp;&amp; pnpm --filter @scholar-scout/web run test -- __tests__/lib/catalogue-fixtures.test.ts --runInBand &amp;&amp; pnpm --filter @scholar-scout/web run typecheck &amp;&amp; pnpm --filter @scholar-scout/web run lint &amp;&amp; pnpm --filter @scholar-scout/web run test --runInBand</automated>
  </verify>
  <done>CR-01 and WR-01 regressions pass; the complete frozen six-region/36-cell fixture, existing contract behavior, typecheck, lint, and full web Jest suite remain green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parsed reviewed snapshot region list → coverage rows | A controlled identifier can falsely appear covered when its authoritative region record is absent from the submitted matrix. |
| Documented source metadata → review chronology | An impossible review-before-source ordering can be imported as apparently attributable provenance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-25 | Tampering | `validateCoverageMatrix` | high | mitigate | Bind every controlled coverage row to an ID from the supplied validated region list and regression-test verified plus explicit-unknown rows. |
| T-09-26 | Repudiation | `validateSourceMetadata` | medium | mitigate | Compare parsed documented source and checked dates, return a deterministic chronology error, and preserve explicit unavailable-date semantics. |
| T-09-27 | Spoofing | frozen Phase 09 fixtures | medium | mitigate | Run the fixture suite and complete web quality gate without fixture edits, proving the exact source roster and 36-cell baseline remain accepted. |
| T-09-SC | Tampering | package installation | high | mitigate | No packages are installed; use the existing TypeScript and Jest stack. |
</threat_model>

## Source Coverage Audit

| Source | Item | Plan coverage | Status |
|--------|------|---------------|--------|
| GOAL | Deterministic six-area source-first catalogue with no invented availability | Tasks 1–2 reject undeclared matrix coverage and impossible provenance chronology while retaining static reviewed fixtures. | COVERED |
| REQ | REG-01, REG-02, REG-03, EVID-01, EVID-02 | Task 1 proves the two import-integrity defects; Task 2 binds coverage to supplied regional provenance and enforces documented-source chronology. | COVERED |
| RESEARCH | Pure validators, fixed clocks, complete explicit matrix, structured unavailable dates, and no live aggregation | Both tasks use public pure validators and fixed test data; Task 2 retains unavailable-date and no-runtime-source semantics. | COVERED |
| CONTEXT | D-01 through D-08 | Task 1 cites all locked decisions as preservation constraints; Task 2 preserves the established geography, pathway, freshness, uncertainty, policy-boundary, and D-08 card contracts while repairing only CR-01 and WR-01. | COVERED |

<verification>
Strict sequence: first run the new focused RED regressions before production edits; after the pure repair, run the focused contract suite, frozen fixture suite, web typecheck, web lint, and complete in-band web Jest suite. The plan gate is `pnpm --filter @scholar-scout/web run test -- __tests__/lib/catalogue-contract.test.ts --runInBand &amp;&amp; pnpm --filter @scholar-scout/web run test -- __tests__/lib/catalogue-fixtures.test.ts --runInBand &amp;&amp; pnpm --filter @scholar-scout/web run typecheck &amp;&amp; pnpm --filter @scholar-scout/web run lint &amp;&amp; pnpm --filter @scholar-scout/web run test --runInBand`.
</verification>

<success_criteria>
- Every coverage row for a controlled but undeclared regional ID fails validation in both verified and not-yet-verified state tests.
- A checked date cannot precede a documented source date; unknown/unavailable source-date behavior remains explicit and unchanged.
- Frozen sources, all 36 coverage cells, D-08, and Phase 09's domain-only boundary remain unchanged.
- Targeted contract and fixture tests, TypeScript typecheck, ESLint, and the full web Jest suite pass.
</success_criteria>

<output>
Create `.planning/phases/09-catalogue-foundations-and-source-contracts/09-07-SUMMARY.md` when done.
</output>
