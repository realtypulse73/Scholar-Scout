---
phase: 09-catalogue-foundations-and-source-contracts
plan: 05
type: execute
wave: 4
depends_on:
  - 09-04
files_modified:
  - apps/web/lib/catalogue-contract.ts
  - apps/web/__tests__/lib/catalogue-contract.test.ts
  - .planning/phases/09-catalogue-foundations-and-source-contracts/09-VALIDATION.md
autonomous: true
requirements:
  - REG-01
  - REG-02
  - REG-03
  - EVID-01
  - EVID-02
must_haves:
  truths:
    - "Verified coverage cannot present a future review date or a review date that predates its attributable evidence review or documented source date under the injected clock."
    - "Malformed evidence on an unresolved opportunity-card fact returns a deterministic validation error instead of throwing."
    - "Malformed region and coverage-list entries return deterministic shape errors instead of preventing snapshot validation."
    - "The frozen six-region roster, all 36 explicit not-yet-verified cells, evidence contract, D-08 card boundary, and source-free Phase 9 scope remain unchanged except for the three reviewed validation defects."
  artifacts:
    - path: apps/web/lib/catalogue-contract.ts
      provides: Pure chronology and runtime-shape validation for Phase 10 import inputs.
      exports:
        - validateCoverageMatrix
        - validateCatalogueOpportunityCardFacts
    - path: apps/web/__tests__/lib/catalogue-contract.test.ts
      provides: RED-first regressions for the three Phase 09 re-review blockers.
    - path: .planning/phases/09-catalogue-foundations-and-source-contracts/09-VALIDATION.md
      provides: Task-map entries for the repair regressions and final quality gate.
  key_links:
    - from: apps/web/__tests__/lib/catalogue-contract.test.ts
      to: apps/web/lib/catalogue-contract.ts
      via: Fixed-clock public-validator calls exercise coverage chronology plus malformed card/matrix inputs.
      pattern: "validateCoverageMatrix|validateCatalogueOpportunityCardFacts"
    - from: apps/web/lib/catalogue-contract.ts
      to: Phase 10 reviewed imports
      via: Pure validators convert untrusted snapshot values into stable error arrays before publication work is introduced.
      pattern: "Catalogue coverage row must be an object|Catalogue region must be an object"
---

<objective>
Close the three Phase 09 re-review blockers with RED-first contract regressions and narrowly scoped pure-validator repairs.

Purpose: Make the Phase 10 import boundary reject forged coverage chronology and malformed JSON-shaped values recoverably, while retaining the completed six-area source foundation.
Output: Passing chronology and malformed-shape regressions, hardened `catalogue-contract.ts`, and an updated Phase 09 validation task map.
</objective>

<execution_context>
@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-CONTEXT.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-RESEARCH.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-PATTERNS.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-REVIEW.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-VALIDATION.md
@.planning/phases/09-catalogue-foundations-and-source-contracts/09-04-SUMMARY.md
@apps/web/lib/catalogue-contract.ts
@apps/web/lib/catalogue-fixtures.ts
@apps/web/__tests__/lib/catalogue-contract.test.ts
@apps/web/__tests__/lib/catalogue-fixtures.test.ts

<interfaces>
`validateCoverageMatrix(regions, coverage, now)` is a pure exported function with an injected `Date`, controlled region/pathway constants, and string-array error output. Its next public signature must accept readonly lists of unknown-shaped entries so an import can be rejected before member access; all existing typed callers remain valid.

`VerifiedCatalogueCoverage` has `state: 'verified'`, `reviewedAt`, and current `FactEvidence`; `FactEvidence` holds a structured `sourceDate`, `reviewedAt`, and verification action. A verified coverage review date therefore must parse, be no later than injected `now`, and be no earlier than both a valid evidence review date and a documented evidence source date.

`validateCatalogueOpportunityCardFacts(facts, now)` delegates each item to `validateCatalogueCardFact`. An unresolved item has a null value, non-current state, and evidence. The helper must establish that `evidence` is a non-array object before it reads `evidence.status` or passes the object to `validateFactEvidence`.

Keep `catalogueRegions` and `catalogueCoverage` unchanged: they are deeply frozen and contain the exact D-01 six-region roster and D-03 36-cell `not-yet-verified` baseline. No fixture, route, persistence, provider source, UI, ranking, sensitive-data, eligibility, or outcome behavior belongs in this repair.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Add failing public-validator regressions for chronology and malformed import shapes</name>
  <files>apps/web/__tests__/lib/catalogue-contract.test.ts</files>
  <precondition>The reviewed baseline is present and the focused test file passes before these new regressions are added.</precondition>
  <read_first>
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-REVIEW.md (CR-01 through CR-03 are the complete repair scope)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-CONTEXT.md (D-01 through D-08 remain locked)
    - apps/web/lib/catalogue-contract.ts (current validator signatures, date helpers, and controlled error conventions)
    - apps/web/__tests__/lib/catalogue-contract.test.ts (existing valid `region`, `coverage`, `factEvidence`, `cardFacts`, and `FACT_NOW` builders)
  </read_first>
  <behavior>
    - A complete verified matrix with a coverage `reviewedAt` after `FACT_NOW` returns its stable coverage-review future-date error, even when every evidence object is otherwise current.
    - A verified coverage review date before its evidence review date, and a separate date before a documented evidence source date, each return their stable chronology errors under `FACT_NOW`.
    - For each of `null`, a primitive, and an array assigned to an unresolved `duration.evidence`, `validateCatalogueOpportunityCardFacts` does not throw and returns the same duration evidence-shape error.
    - For each of `null`, a primitive, and an array inserted independently into a valid regions list or a complete coverage list, `validateCoverageMatrix` does not throw and returns the stable region or coverage-row shape error while its valid companion data retains a complete controlled matrix.
  </behavior>
  <action>Before editing production code, extend the existing contract suite only. Build full six-pathway verified matrices from the existing typed baselines and fixed `FACT_NOW`; add isolated cases for a coverage review after the clock, before a valid evidence review, and before a valid documented source date. Assert exact public error strings for each coverage chronology case and keep all non-mutated cells valid. Add parameterized runtime-shaped casts for `null`, a primitive, and an array as unresolved `duration.evidence`; each case must assert no throw and the same deterministic `Duration evidence must be an object.` result. Add separate parameterized cases with one malformed region entry and with one malformed coverage row beside otherwise valid controlled data; assert no throw and exact `Catalogue region must be an object.` or `Catalogue coverage row must be an object.` output. Exercise only exported validators and fixed clocks; preserve D-01, D-02, D-03, D-04, D-05, D-06, D-07, and D-08 by leaving fixture data, controlled vocabulary, factual card fields, and all non-review behaviors unchanged. Run the focused Jest command and confirm the new tests fail before the source edit.</action>
  <verify>
    <automated>pnpm --filter @scholar-scout/web run test -- __tests__/lib/catalogue-contract.test.ts --runInBand</automated>
  </verify>
  <done>Each new test fails against the reviewed baseline for the intended public-validator defect, rather than from a type, import, fixture, or clock setup failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden coverage chronology and card/matrix runtime-shape guards</name>
  <files>apps/web/lib/catalogue-contract.ts</files>
  <read_first>
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-REVIEW.md (CR-01, CR-02, and CR-03 required fixes)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-CONTEXT.md (D-01 through D-08 boundary)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-RESEARCH.md (pure validation, injected-clock evidence, and no-live-import patterns)
    - apps/web/lib/catalogue-contract.ts (existing `validateCoverageMatrix`, `validateCatalogueCardFact`, `isRecord`, and date helpers)
    - apps/web/__tests__/lib/catalogue-contract.test.ts (Task 1 RED cases)
  </read_first>
  <behavior>
    - Verified coverage validates its review date against the injected clock and against its evidence review plus documented source dates, while valid current evidence remains accepted.
    - Card evidence that is null, primitive, or array-shaped is rejected before state comparison, with a stable string-array error and no exception.
    - Region and coverage-list elements that are null, primitive, or array-shaped are rejected before property access; valid cells still receive duplicate, unsupported-key, and missing-pair validation in declaration order.
  </behavior>
  <action>Modify only `catalogue-contract.ts` to make the Task 1 public regressions pass. In `validateCoverageMatrix`, accept readonly unknown-shaped region and coverage lists at the runtime boundary, use `isRecord` before reading any region or row property, and add one deterministic shape error per malformed entry without creating an undefined key. Continue processing valid entries so controlled-pair validation remains intact. For verified rows with parseable dates, compare `reviewedAt` to a valid injected `now`, then require it not to predate the evidence review date or a documented evidence source date; emit stable key-qualified coverage chronology errors. Retain `validateFactEvidence` as the owner of fact-evidence freshness and action validation, and do not change the 183-day or 731-day policies. In `validateCatalogueCardFact`, require `fact.evidence` to satisfy `isRecord` before reading its status in either sourced or unresolved branches, returning the stable label-qualified evidence-shape error when it does not. Preserve D-01's frozen source roster and separate local focus, D-02's paid-training disclosure boundary, D-03's six visible pathway classes, D-04's actionable uncertainty, D-05's explicit coverage states, D-06's injected-clock thresholds, D-07's absent sensitive policy fields, and D-08's source-safe first-view fact contract. Do not edit fixtures or add sources, imports, publication, storage, UI, ranking, sensitive inputs, or decision/outcome logic.</action>
  <reversibility rating="reversible">The changes only tighten pure import validation behind existing exported functions; they create no persisted or published data format.</reversibility>
  <verify>
    <automated>pnpm --filter @scholar-scout/web run test -- __tests__/lib/catalogue-contract.test.ts --runInBand</automated>
  </verify>
  <done>The three review blockers are green: forged verified-coverage chronology is rejected, malformed card evidence returns a stable error, and malformed matrix entries cannot throw; all existing catalogue-contract tests still pass.</done>
</task>

<task type="auto">
  <name>Task 3: Record the repair map and run the Phase 09 quality gate</name>
  <files>.planning/phases/09-catalogue-foundations-and-source-contracts/09-VALIDATION.md</files>
  <read_first>
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-VALIDATION.md (current per-task table and test-creation ordering)
    - .planning/phases/09-catalogue-foundations-and-source-contracts/09-05-PLAN.md (Task IDs, threats, and commands)
  </read_first>
  <action>Update the validation task map only because it has no Plan 09-05 entries. Add one row for `09-05-01` mapping the RED regressions to `REG-01`, `REG-02`, `REG-03`, `EVID-01`, and `EVID-02` with threats T-09-19 through T-09-21 and the focused contract Jest command; add one row for `09-05-02` mapping the green validator repair to the same requirements/threats and command. Extend test-creation ordering with Plan 09-05's required RED-before-GREEN sequence. Then run the focused contract test, web typecheck, web lint, and the full in-band web Jest suite. Preserve the existing map rows, manual review, and frozen-source validation coverage.</action>
  <verify>
    <automated>pnpm --filter @scholar-scout/web run test -- __tests__/lib/catalogue-contract.test.ts --runInBand &amp;&amp; pnpm --filter @scholar-scout/web run typecheck &amp;&amp; pnpm --filter @scholar-scout/web run lint &amp;&amp; pnpm --filter @scholar-scout/web run test --runInBand</automated>
  </verify>
  <done>The validation map traces both repair tasks and the complete web quality gate passes without changes beyond the three reviewed validation defects and their regression coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reviewed evidence → verified coverage cell | A forged audit date could make coverage appear reviewed before its supporting evidence existed. |
| Untrusted snapshot card fact → pure card validator | JSON-shaped evidence values can be malformed and must not terminate import validation. |
| Untrusted snapshot matrix → controlled coverage validator | Malformed list items must be reported while valid pairs remain evaluable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-19 | Tampering | `validateCoverageMatrix` verified rows | high | mitigate | Compare coverage review dates against injected now, evidence review dates, and documented source dates; lock the results with fixed-clock regressions. |
| T-09-20 | Denial of service | `validateCatalogueCardFact` | high | mitigate | Record-check card evidence before property access and return a deterministic label-qualified validation error for malformed shapes. |
| T-09-21 | Denial of service | `validateCoverageMatrix` lists | high | mitigate | Record-check every region and coverage item before property access, preserve valid-list evaluation, and test null, primitive, and array entries. |
| T-09-SC | Tampering | package installation | high | mitigate | No package installation is part of this repair; use the existing TypeScript and Jest stack. |
</threat_model>

## Source Coverage Audit

| Source | ID | Feature or constraint | Plan coverage | Status |
|--------|----|-----------------------|---------------|--------|
| GOAL | — | Deterministic source-first six-area foundation with visible uncertainty | Tasks 1–2 reject forged chronology and malformed imports without changing the frozen catalogue facts. | COVERED |
| REQ | REG-01 | Exact regional source boundary remains safely validated | Tasks 1–2 make malformed regional import entries recoverable while retaining the prior exact-provenance rule. | COVERED |
| REQ | REG-02 | Six controlled pathways remain browseable in each valid region | Tasks 1–2 preserve controlled complete-matrix processing when malformed entries accompany valid pairs. | COVERED |
| REQ | REG-03 | Coverage state is honest and explicit | Tasks 1–2 enforce attributable verified-review chronology and retain explicit unverified rows. | COVERED |
| REQ | EVID-01 | Material evidence is attributable and dated | Tasks 1–2 require coverage review chronology and recover from malformed fact evidence. | COVERED |
| REQ | EVID-02 | Current and unresolved states stay distinct | Tasks 1–2 use the injected clock and retain the current/non-current evidence contract. | COVERED |
| REQ | EVID-05 | Wage context remains dated informational context | Preserved by the existing Plan 09-03/09-04 contract; this repair changes no wage behavior because none of the three blockers concerns it. | COVERED (prior plan) |
| RESEARCH | — | Pure validators, fixed clocks, explicit matrix, no runtime provider retrieval | Tasks 1–2 exercise public pure validators only and add no source or runtime integration. | COVERED |
| CONTEXT | D-01 | Six exact official regions plus separate ten-mile local focus | Task 2 preserves the frozen roster and does not alter geography. | COVERED |
| CONTEXT | D-02 | Separate factual employer-paid training disclosure | Task 2 leaves employer-training shapes and claims unchanged. | COVERED |
| CONTEXT | D-03 | All pathway classes stay browseable and non-decisional | Task 2 retains controlled matrix behavior and adds no ranking or suppression. | COVERED |
| CONTEXT | D-04 | Neutral actionable uncertainty | Task 2 preserves non-current evidence states and direct actions. | COVERED |
| CONTEXT | D-05 | Explicit verified versus unverified coverage foundation | Task 2 tightens verified evidence chronology while retaining the explicit unverified baseline. | COVERED |
| CONTEXT | D-06 | 183-day and 731-day freshness policy | Task 2 preserves both policies and adds coverage-review chronology under the supplied clock. | COVERED |
| CONTEXT | D-07 | No invented re-entry or participation-policy fact | Task 2 leaves those fields outside the contract. | COVERED |
| CONTEXT | D-08 | Source-safe non-sensitive first-view facts | Tasks 1–2 make malformed card evidence recoverable without adding or removing first-view facts. | COVERED |

<verification>
Execution order is mandatory: add and run the focused RED regressions before any production edit; make only the pure contract repair; then record the task map and run `pnpm --filter @scholar-scout/web run test -- __tests__/lib/catalogue-contract.test.ts --runInBand`, `pnpm --filter @scholar-scout/web run typecheck`, `pnpm --filter @scholar-scout/web run lint`, and `pnpm --filter @scholar-scout/web run test --runInBand`.
</verification>

<success_criteria>
- Every verified coverage review date is valid relative to the injected clock and cannot predate supporting evidence dates.
- Every malformed unresolved-card evidence value and malformed matrix entry returns a deterministic validation error without an exception.
- The exact six-region roster, 36 unverified baseline cells, source/evidence contract, D-08 record facts, and domain-only Phase 09 boundary remain intact.
- Targeted Jest, TypeScript, ESLint, and the full web Jest suite pass.
</success_criteria>

<output>
Create `.planning/phases/09-catalogue-foundations-and-source-contracts/09-05-SUMMARY.md` when done.
</output>
