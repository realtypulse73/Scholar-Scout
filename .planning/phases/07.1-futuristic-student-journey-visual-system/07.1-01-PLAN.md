---
phase: 07.1-futuristic-student-journey-visual-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/layout.tsx
  - apps/web/app/globals.css
  - apps/web/components/visual/StudentJourneyScene.tsx
  - apps/web/app/page.tsx
  - apps/web/__tests__/components/StudentJourneyScene.test.tsx
  - apps/web/tailwind.config.js
  - apps/web/tailwind.config.ts
  - apps/web/__tests__/lib/design-system-contract.test.ts
  - apps/web/components/ui/Button.tsx
  - apps/web/components/ui/Card.tsx
  - apps/web/components/ui/Badge.tsx
  - apps/web/components/ui/Input.tsx
  - apps/web/__tests__/components/ui-primitives.test.tsx
autonomous: true
requirements: [D-01, D-02, D-03, D-04, D-05, D-06, D-07]
must_haves:
  truths:
    - "Every route receives self-hosted Space Grotesk through the root layout, with no browser-side Google Fonts link."
    - "White and silver surfaces, red primary/selected states, blue-gray readable text, and semantic status colors have one shared token contract."
    - "The reusable lifestyle scene keeps content above non-interactive decoration and provides an unchanged still image under reduced motion."
  artifacts:
    - "apps/web/components/visual/StudentJourneyScene.tsx"
    - "apps/web/__tests__/components/StudentJourneyScene.test.tsx"
    - "apps/web/__tests__/lib/design-system-contract.test.ts"
  key_links:
    - "RootLayout -> --font-space-grotesk -> both Tailwind config files -> font-sans utilities"
    - "StudentJourneyScene -> globals.css scene classes -> prefers-reduced-motion rule"
---

<objective>
Establish the app-wide visual system and prove a single home-page scene end to end.

Purpose: Create the safe shared foundation that every later page migration consumes, per D-01 through D-07.
Output: Root font loading, matching Tailwind token maps, upgraded UI primitives, one reusable CSS-only scene wrapper, and focused regression tests.
</objective>

<execution_context>
@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07.1-futuristic-student-journey-visual-system/07.1-CONTEXT.md
@.planning/phases/07.1-futuristic-student-journey-visual-system/07.1-RESEARCH.md
@.planning/phases/07.1-futuristic-student-journey-visual-system/07.1-PATTERNS.md
@.planning/phases/07.1-futuristic-student-journey-visual-system/07.1-UI-SPEC.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Wire one Space Grotesk home scene from root layout through reduced-motion CSS</name>
  <files>apps/web/app/layout.tsx, apps/web/app/globals.css, apps/web/components/visual/StudentJourneyScene.tsx, apps/web/app/page.tsx, apps/web/__tests__/components/StudentJourneyScene.test.tsx</files>
  <read_first>apps/web/app/layout.tsx; apps/web/app/globals.css; apps/web/app/page.tsx; apps/web/components/simulations/SimulationScene.tsx; apps/web/__tests__/components/ui-primitives.test.tsx; 07.1-CONTEXT.md D-01 and D-05 through D-07; 07.1-UI-SPEC.md Motion and Scene Contract</read_first>
  <behavior>
    - A rendered scene exposes its caller content while its two decorative student-side layers are aria-hidden and cannot receive pointer or keyboard interaction.
    - The root body receives the Space Grotesk variable and retains the existing session-provider boundary and metadata.
    - Scene CSS uses only transform and opacity for the 10–12 second journey loop and 8–10 second low-opacity trail; reduced-motion disables both animations.
  </behavior>
  <action>Write the focused scene test first. Replace the root Geist sans loader with next/font/google Space_Grotesk using display swap, latin subset, and the --font-space-grotesk variable; retain the local mono loader, metadata, and AuthSessionProvider. Add global white/silver fallback scene rules, deterministic keyframes, a clipped right-hand student-side container, 24px-or-less visual travel, and a reduced-motion media rule that disables each decorative animation. Create a server-safe StudentJourneyScene wrapper around the existing transition image with a relative reserved-height section, white-to-silver content overlay, children at a higher stacking layer, and at most two aria-hidden pointer-events-none decorative layers. Apply it to the existing home transition story without changing its copy, links, data, or action semantics. This implements D-01 and D-05 through D-07.</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test --runInBand --runTestsByPath __tests__/components/StudentJourneyScene.test.tsx</automated></verify>
  <acceptance_criteria>Home renders the existing lifestyle still through StudentJourneyScene; only the right/student decorative layers animate; those layers are hidden from assistive technology and cannot intercept actions; reduced-motion has a complete still contract.</acceptance_criteria>
  <done>The first production-quality visual slice works end to end and its focused component test passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define matching red-white-silver tokens in both Tailwind sources</name>
  <files>apps/web/tailwind.config.js, apps/web/tailwind.config.ts, apps/web/__tests__/lib/design-system-contract.test.ts</files>
  <read_first>apps/web/tailwind.config.js; apps/web/tailwind.config.ts; apps/web/app/globals.css; 07.1-UI-SPEC.md Color and Typography; 07.1-RESEARCH.md Pattern 2 and Pattern 4</read_first>
  <behavior>
    - The JavaScript runtime config and TypeScript mirror expose equivalent Space Grotesk sans and semantic color values.
    - Brand is the approved red action scale; danger, success, and warning remain separate semantic scales.
    - Font and token contract assertions fail if either Tailwind configuration drifts from the approved values.
  </behavior>
  <action>Create a deterministic source-contract test before changing both configuration files. Change background/foreground, brand, ink, border, focus, and sans definitions in the runtime JavaScript configuration and its TypeScript mirror together. Use the approved values: white #FFFFFF, silver #F3F5F7, accent #C8102E, pressed #A30E26, blush #FFF1F3, border #D7DEE6, readable deep blue-gray #26364A, and muted copy #516173. Point sans at --font-space-grotesk with a system fallback. Keep content globs, touch targets, radius, plugins, and success/warning/danger meanings intact. This implements D-01 through D-04.</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test --runInBand --runTestsByPath __tests__/lib/design-system-contract.test.ts</automated></verify>
  <acceptance_criteria>The runtime and typed Tailwind configurations use the same approved visual roles and the root font variable; status semantics remain distinct from primary red.</acceptance_criteria>
  <done>Tailwind can compile the new app-wide visual vocabulary without configuration drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make shared controls and surfaces express the visual hierarchy</name>
  <files>apps/web/components/ui/Button.tsx, apps/web/components/ui/Card.tsx, apps/web/components/ui/Badge.tsx, apps/web/components/ui/Input.tsx, apps/web/__tests__/components/ui-primitives.test.tsx</files>
  <read_first>apps/web/components/ui/Button.tsx; apps/web/components/ui/Card.tsx; apps/web/components/ui/Badge.tsx; apps/web/components/ui/Input.tsx; apps/web/__tests__/components/ui-primitives.test.tsx; 07.1-UI-SPEC.md Color; 07.1-PATTERNS.md Shared UI primitives</read_first>
  <behavior>
    - Primary buttons and selected UI consume the brand red scale with white readable text.
    - Secondary cards and inputs are white/silver with a visible focus treatment on both white and silver surfaces.
    - Danger stays destructive and success/warning badges keep their semantic tones.
  </behavior>
  <action>Extend primitive tests before editing. Update shared primitive class contracts to use the new surface and semantic tokens, visible focus-visible ring plus offset, 44px minimum controls, and semibold rather than heavy visual emphasis. Preserve default button type, disabled behavior, aria-invalid handling, prop forwarding, and className overrides. Keep the destructive Button/Badge/Input state on danger rather than brand red, and preserve success and warning badge meaning. This implements D-02 through D-04.</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test --runInBand --runTestsByPath __tests__/components/ui-primitives.test.tsx</automated></verify>
  <acceptance_criteria>Shared actions, cards, badges, and inputs provide white/silver surfaces, red primary state, readable blue-gray copy, and a keyboard-visible focus affordance without conflating status meaning.</acceptance_criteria>
  <done>Every later page migration inherits the approved base hierarchy through existing primitives.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static CSS -> browser interaction | Decorative visual layers must not alter focus, pointer handling, or reading order. |
| build-time font acquisition -> browser | Font files must be self-hosted by Next rather than fetched by each visitor. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07.1-01 | DoS | StudentJourneyScene overlay | high | mitigate | aria-hidden, pointer-events-none, clipped containment, and above-layer content test. |
| T-07.1-02 | DoS | control and focus styling | medium | mitigate | high-contrast ring/offset and primitive test contracts. |
| T-07.1-03 | I/D | root font loading | medium | mitigate | next/font/google build-time self-hosted Space Grotesk, no browser link. |
| T-07.1-SC | Tampering | package installation | low | accept | no package installation occurs; existing Next font capability is used. |
</threat_model>

<verification>
Run the three focused Jest commands after their tasks, then `corepack pnpm --filter @scholar-scout/web typecheck`, `corepack pnpm --filter @scholar-scout/web lint`, and `corepack pnpm --filter @scholar-scout/web test --runInBand` before starting Wave 2.
</verification>

<success_criteria>
Space Grotesk and approved semantic tokens are available across the app, home proves one safe animated student-side scene, and shared controls preserve contrast and status meaning.
</success_criteria>

## Artifacts this phase produces

- `StudentJourneyScene` reusable visual wrapper and focused accessibility test.
- A checked, paired Tailwind visual-token contract.
- Shared primitive hierarchy that later route migrations can reuse.

<output>
Create `.planning/phases/07.1-futuristic-student-journey-visual-system/07.1-01-SUMMARY.md` when done.
</output>
