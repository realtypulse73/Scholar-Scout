---
phase: 03-administrative-and-data-operations-correctness
plan: 05
type: execute
wave: 4
depends_on: [03-04]
files_modified:
  - apps/web/components/admin/ProgrammeAdminManager.tsx
  - apps/web/__tests__/components/ProgrammeAdminManager.test.tsx
autonomous: false
requirements: [OPS-02, OPS-03, DATA-03]
must_haves:
  truths:
    - "D-01/D-02/D-03/D-04: UI renders only server capabilities, refreshes manually, retains last-known read-only state on failure, shows inline Retry, and never claims stale success."
    - "D-05/D-06/D-16: preview is count-only and gates reason/typed confirmation; import validation is visibly non-mutating and shares the restore flow."
    - "D-10–D-12: initial/read failure is an explicit focused alert with safe category/incident ID and every mutation disabled until fresh success."
    - "All 14 UI states are covered and the three visual backstops are manually checked at 320/768/1280 px with long text."
  artifacts:
    - "ProgrammeAdminManager.tsx contains the capability-driven accessible recovery state machine using existing Tailwind primitives."
    - "ProgrammeAdminManager.test.tsx covers state, focus, ARIA, and duplicate-submission contracts."
  key_links:
    - "capability response -> rendered operation cards/mutationsAllowed"
    - "preview token -> reason/phrase -> apply response -> focused result"
---
<objective>Deliver the approved capability-driven, accessible staff recovery interface without adding a second admin namespace or design system (D-01–D-06, D-10–D-12, D-16).</objective>
<execution_context>@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03-administrative-and-data-operations-correctness/03-UI-SPEC.md
@.planning/phases/03-administrative-and-data-operations-correctness/03-04-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Render capability health and backup history from server truth</name>
  <read_first>apps/web/components/admin/ProgrammeAdminManager.tsx, apps/web/components/ui/index.ts, apps/web/__tests__/components/advisor/AdvisorChat.test.tsx, .planning/phases/03-administrative-and-data-operations-correctness/03-UI-SPEC.md</read_first>
  <files>apps/web/components/admin/ProgrammeAdminManager.tsx, apps/web/__tests__/components/ProgrammeAdminManager.test.tsx</files>
  <behavior>
    - Initial loading, verified empty/zero, healthy partial/populated, refreshing last-known, and unavailable states are distinct.
    - Only available+allowed server operation cards render; unsupported export is absent; no polling occurs.
    - Failure focuses role=alert, retains Last verified content read-only, shows full incident ID/Retry, and disables all mutations.
    - Backup zero/one/many render newest-first, with hold metadata and no delete action.
  </behavior>
  <action>Replace nullable/empty fallbacks with the discriminated capability state from the UI-SPEC. Use existing Card/Button/Input/Badge and Tailwind scales; preserve the health summary as primary visual anchor. Manual Refresh/Retry only. Keep status text in addition to color, mono/wrappable IDs, persistent disabled explanations, and no client endpoint inventory.</action>
  <verify><automated>pnpm --filter @scholar-scout/web run test -- --runInBand __tests__/components/ProgrammeAdminManager.test.tsx</automated></verify>
  <acceptance_criteria>All capability/health/backup states render truthfully and accessibly; failure cannot look empty or allow a mutation.</acceptance_criteria>
  <done>All capability/health/backup states render truthfully and accessibly; failure cannot look empty or allow a mutation.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement accessible progressive preview, confirmation, and results</name>
  <read_first>apps/web/components/admin/ProgrammeAdminManager.tsx, apps/web/__tests__/components/ProgrammeAdminManager.test.tsx, .planning/phases/03-administrative-and-data-operations-correctness/03-UI-SPEC.md</read_first>
  <files>apps/web/components/admin/ProgrammeAdminManager.tsx, apps/web/__tests__/components/ProgrammeAdminManager.test.tsx</files>
  <behavior>
    - Validate/preview pending states disable duplicates and use polite live status; destructive red appears only on final Apply.
    - Preview shows four count columns and safe plan metadata; no student rows/content; reason/phrase appear only after valid preview.
    - Focus moves to preview, alert, or result; expiry/conflict clears confirmation, disables Apply, returns focus to preview, and preserves reason for retryable failure.
    - Apply pending cannot duplicate; completion announces complete or unchanged and never retains stale success.
  </behavior>
  <action>Send raw package only to authoritative server validation and apply only the plan token/reason/phrase. Implement UI-SPEC copy, aria-describedby/aria-invalid, operation-specific labels, internal table overflow, and full selectable identifiers. Keep page-level overflow impossible and do not introduce shadcn/icons/background polling/browser-only confirmation.</action>
  <verify><automated>pnpm --filter @scholar-scout/web run test -- --runInBand __tests__/components/ProgrammeAdminManager.test.tsx __tests__/api/admin-data-routes.test.ts</automated></verify>
  <acceptance_criteria>The 14 covered states have DOM assertions, keyboard/focus contracts pass, and duplicate validation/apply is prevented.</acceptance_criteria>
  <done>The 14 covered states have DOM assertions, keyboard/focus contracts pass, and duplicate validation/apply is prevented.</done>
</task>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the three visual and assistive backstops</name>
  <read_first>.planning/phases/03-administrative-and-data-operations-correctness/03-UI-SPEC.md, apps/web/components/admin/ProgrammeAdminManager.tsx</read_first>
  <files>apps/web/components/admin/ProgrammeAdminManager.tsx</files>
  <what-built>Capability overview, backup list, impact table, confirmation, alerts/live status, and result panels in the existing admin manager.</what-built>
  <action>Perform the UI-SPEC's three human-only visual backstops and production-like keyboard/announcement pass after automated component coverage is green; report issues without approving partial conformance.</action>
  <how-to-verify>At 320px, 768px, and 1280px render healthy, unavailable, preview, and confirmation states. Use a 200-character safe reason, maximum operator reason, long incident ID and digest. Confirm no page-level overflow/collision/sole-value ellipsis; table scroll stays internal. Keyboard-only: trigger preview, retry failure, conflict, apply failure, and success; confirm focus and a single operation-specific announcement.</how-to-verify>
  <verify><human_check>All three viewport/long-text backstops and the keyboard/live-announcement flow match 03-UI-SPEC without overflow, collisions, hidden sole identifiers, lost focus, or duplicate announcements.</human_check></verify>
  <acceptance_criteria>The reviewer approves all three visual backstops and assistive interaction flow, or returns concrete issues for correction.</acceptance_criteria>
  <done>The reviewer approves all three visual backstops and assistive interaction flow, or returns concrete issues for correction.</done>
  <resume-signal>Type "approved" or describe visual/accessibility issues.</resume-signal>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| capability JSON -> browser | UI must not infer authority or mutate from stale state. |
| staff input -> routes | Browser state is untrusted; server remains authoritative. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-14 | Elevation | client controls | high | mitigate | Render server capabilities; server reauthorizes. |
| T-03-15 | Information Disclosure | preview/UI | high | mitigate | Counts and safe IDs only; no snapshots/student content. |
| T-03-16 | Repudiation | result state | medium | mitigate | Never retain stale success; show operation/incident IDs. |
</threat_model>
<verification>Focused component/route tests pass, then human backstops approve all three viewport/long-text cases and keyboard/live behavior.</verification>
<success_criteria>The approved 6/6 UI contract is implemented with 14 automated state contracts and 3 accepted visual backstops.</success_criteria>
<output>Create `03-05-SUMMARY.md` when done.</output>
