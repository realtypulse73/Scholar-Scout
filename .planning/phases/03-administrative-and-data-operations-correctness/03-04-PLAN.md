---
phase: 03-administrative-and-data-operations-correctness
plan: 04
type: execute
wave: 3
depends_on: [03-03]
files_modified:
  - apps/web/app/api/admin/data/import/validate/route.ts
  - apps/web/app/api/admin/data/import/restore/route.ts
  - apps/web/__tests__/api/admin-data-routes.test.ts
  - scripts/production-env-check.mjs
  - scripts/test-production-tooling.mjs
  - .env.production.example
autonomous: true
requirements: [OPS-02, OPS-03, DATA-03]
must_haves:
  truths:
    - "D-14: import accepts only a bounded signed versioned envelope and never trusts filename/client metadata."
    - "D-16: validation is non-mutating and stages the same protected apply flow as backup restore."
    - "D-13: missing signing configuration or unhealthy storage hides/disables the capability and fails closed."
  artifacts:
    - "Import validate/apply routes share recovery-service semantics; production readiness checks dedicated signing configuration."
  key_links:
    - "bounded raw package -> authoritative server validation -> plan token -> common apply"
---
<objective>Complete the signed import route flow and fail-closed deployment configuration (D-13, D-14, D-16, D-17).</objective>
<execution_context>@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03-administrative-and-data-operations-correctness/03-03-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Validate bounded import packages without mutation</name>
  <read_first>apps/web/app/api/admin/data/import/validate/route.ts, apps/web/lib/api-request.ts, apps/web/__tests__/api/admin-data-routes.test.ts</read_first>
  <files>apps/web/app/api/admin/data/import/validate/route.ts, apps/web/__tests__/api/admin-data-routes.test.ts</files>
  <behavior>
    - Staff denial occurs before reading package bytes; over 5 MiB is 413; malformed/signature/digest/schema failures are safe 400 with zero writes.
    - Valid package returns a count-only staged plan and performs zero writes.
    - Missing signing secret or failed current read is safe retryable 503.
  </behavior>
  <action>Use bounded request bytes and authoritative envelope verification; do not use filename, browser-parsed JSON, or client source claims. Return the common preview/plan DTO. Assert write count zero for every validation case and redact package/provider content.</action>
  <verify><automated>pnpm --filter @scholar-scout/web run test -- --runInBand __tests__/api/admin-data-routes.test.ts</automated></verify>
  <acceptance_criteria>Validation is bounded, authorized, cryptographically verified, count-only, and non-mutating.</acceptance_criteria>
  <done>Validation is bounded, authorized, cryptographically verified, count-only, and non-mutating.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Apply staged imports and enforce signing readiness</name>
  <read_first>apps/web/app/api/admin/data/import/restore/route.ts, scripts/production-env-check.mjs, scripts/test-production-tooling.mjs, .env.production.example, apps/web/__tests__/api/admin-data-routes.test.ts</read_first>
  <files>apps/web/app/api/admin/data/import/restore/route.ts, scripts/production-env-check.mjs, scripts/test-production-tooling.mjs, .env.production.example, apps/web/__tests__/api/admin-data-routes.test.ts</files>
  <behavior>
    - Import apply accepts only planToken/reason/confirmation and shares 400/409/410/413/503/no-write behavior with backup apply.
    - Production readiness requires the dedicated signing secret; no NEXTAUTH_SECRET fallback exists.
    - Deterministic tooling tests prove missing current signing material fails, valid dedicated current material passes, and NEXTAUTH_SECRET alone fails; optional previous verification material is accepted only as a complete ID/secret pair.
  </behavior>
  <action>Delete raw snapshot apply semantics and call the common plan application service. Add dedicated current signing key ID/secret and optional previous verification key ID/secret variables to the environment example and production check without exposing values. Extend `test-production-tooling.mjs` with sanitized child-process environment cases proving missing current secret fails, a valid dedicated current pair passes, NEXTAUTH_SECRET alone fails, and a half-configured previous pair fails. After staff authorization, every import rejection records privacy-minimal operational evidence through the independent sink and asserts zero application writes plus absence of provider/student/package content. No new external integration is introduced: this phase hardens internal routes/adapters with Node crypto.</action>
  <verify><automated>pnpm --filter @scholar-scout/web run test -- --runInBand __tests__/api/admin-data-routes.test.ts &amp;&amp; node --test scripts/test-production-tooling.mjs</automated></verify>
  <acceptance_criteria>Import apply has restore-grade safeguards, and production cannot advertise it without dedicated signing configuration.</acceptance_criteria>
  <done>Import apply has restore-grade safeguards, and production cannot advertise it without dedicated signing configuration.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| uploaded bytes -> import route | Bounded untrusted package input. |
| environment -> signing service | Secret configuration controls capability availability. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-11 | Tampering/DoS | import validate | high | mitigate | Byte bound, exact envelope, HMAC/digest/schema. |
| T-03-12 | Elevation | import routes | high | mitigate | Staff first; actor-bound plan. |
| T-03-13 | Spoofing | signing config | high | mitigate | Dedicated required secret; no fallback. |
## ASVS Level 1 Control Mapping
| ASVS area | Task/verification mapping | Disposition |
|---|---|---|
| V2/V3 Authentication and Session | Tasks 1–2 authorize staff before package read/apply. | mitigate |
| V4 Access Control | Actor-bound staged plan is the only import apply authority. | mitigate |
| V5 Validation | Exact envelope, byte ceiling, DTO and sanitized env-test cases. | mitigate |
| V6 Cryptography | Dedicated current/previous key configuration; no auth-secret fallback; tooling tests. | mitigate |
| V7 Error/Logging | Route tests assert safe response plus privacy-minimal independent evidence. | mitigate |
| V8 Data Protection | Package/provider/student content exclusion and zero-write rejection assertions. | mitigate |
| V12/V13 Files and API | Bounded upload and authenticated import route tests. | mitigate |
</threat_model>
<verification>Focused route tests and production-env check pass; full web suite/typecheck at wave end.</verification>
<success_criteria>Import validation never writes; apply cannot bypass the bound restore flow; missing configuration fails closed.</success_criteria>
<output>Create `03-04-SUMMARY.md` when done.</output>
