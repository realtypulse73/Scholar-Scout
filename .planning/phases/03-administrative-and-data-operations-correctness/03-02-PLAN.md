---
phase: 03-administrative-and-data-operations-correctness
plan: 02
type: execute
wave: 1
depends_on: [03-01]
files_modified:
  - apps/web/lib/server/data-recovery.ts
  - apps/web/lib/server/data-store.ts
  - apps/web/__tests__/lib/data-recovery.test.ts
autonomous: true
requirements: [OPS-03, DATA-03]
must_haves:
  truths:
    - "D-05/D-06/D-07/D-08: previews are count-only, all-or-nothing server plans bound to actor, exact source, current digest, expiry, and one-use semantics."
    - "D-09/D-17: successful apply composes a pre-change backup and privacy-minimal immutable lifecycle evidence into one store write."
    - "D-14/D-16: only a bounded exact signed versioned envelope can stage a non-mutating import plan."
    - "D-15: retention is deterministic newest-first, maximum 10 and 30 days, with unresolved-incident holds preserved at equality boundaries."
  artifacts:
    - "apps/web/lib/server/data-recovery.ts implements canonical bytes, envelope validation, plan issuance/consumption, apply composition, retention, and audit policy."
  key_links:
    - "validated envelope/backup -> count-only plan -> fresh digest check -> one composed store write"
---

<objective>
Implement the recovery-domain policy that makes backup and import restoration validated, bound, recoverable, and auditable (D-05 through D-09, D-14 through D-17).

Purpose: Make destructive operations impossible without a matching server preview and safe recovery evidence.
Output: Tested recovery service and strengthened persisted recovery metadata.
</objective>

<execution_context>@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/03-administrative-and-data-operations-correctness/03-01-SUMMARY.md
@.planning/phases/03-administrative-and-data-operations-correctness/03-RESEARCH.md
@.planning/phases/03-administrative-and-data-operations-correctness/03-VALIDATION.md</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Validate canonical signed envelopes and issue bound plans</name>
  <read_first>apps/web/lib/server/data-recovery.ts, apps/web/lib/api-request.ts, apps/web/__tests__/lib/data-recovery.test.ts</read_first>
  <files>apps/web/lib/server/data-recovery.ts, apps/web/__tests__/lib/data-recovery.test.ts</files>
  <behavior>
    - Exact envelope keys, supported schema, bounded timestamps/source metadata/collections, 5 MiB ceiling, canonical SHA-256 digest, key ID, HMAC, and timing-safe compare are enforced (D-14).
    - Validation never writes and returns a 10-minute plan with count/category deltas only (D-06/D-16).
    - Plan binds plan ID, actor ID, source ID/digest, current-data digest, expiry, and single-use/idempotent outcome (D-07).
  </behavior>
  <action>Use Node 20 crypto only and a dedicated SCHOLARSCOUT_DATA_PACKAGE_SIGNING_SECRET; capability is unavailable when signing material is absent. Centralize canonical serialization. Reject unknown fields/versions, bad signature/digest, future/invalid timestamps, deep/oversized collections, actor/source/current changes, expiry equality and later, and replay. Do not trust filename or browser metadata. Treat the 10-minute lifetime and 5 MiB ceiling as named bounded policy constants.</action>
  <verify><automated>pnpm --filter @scholar-scout/web run test -- --runInBand __tests__/lib/data-recovery.test.ts</automated></verify>
  <acceptance_criteria>All tampering/staleness cases fail before writes; preview DTOs contain counts and safe identifiers only; valid plan claims are stable and verifiable.</acceptance_criteria>
  <done>All tampering/staleness cases fail before writes; preview DTOs contain counts and safe identifiers only; valid plan claims are stable and verifiable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compose all-or-nothing apply, retention, holds, and lifecycle evidence</name>
  <read_first>apps/web/lib/server/data-recovery.ts, apps/web/lib/server/data-store.ts, apps/web/__tests__/lib/data-recovery.test.ts</read_first>
  <files>apps/web/lib/server/data-recovery.ts, apps/web/lib/server/data-store.ts, apps/web/__tests__/lib/data-recovery.test.ts</files>
  <behavior>
    - Whitespace reason or wrong phrase fails; valid apply freshly reads/verifies, creates a non-nested pre-change backup, restores all categories, appends trusted audit, and calls write exactly once (D-05/D-08/D-09).
    - Retention order is stable newest-first for equal timestamps, keeps exactly 10 within 30 days, excludes the exactly-expired boundary, and preserves incident-held backups (D-15).
    - Audit contains D-17 fields and no snapshot contents, record samples, emails, or student data; pruning/hold lifecycle is auditable.
  </behavior>
  <action>Compose and validate the full target document in memory before one port write; on any validation/read/write error report unchanged/failure and never claim provider-level transactions. Use stable creation-time plus ID ordering for equal timestamps. Preserve held backups regardless of count/age, and reject duplicate IDs. Make repeated valid plan submission idempotently return its recorded outcome without a second write; leave distributed CAS to Phase 4.</action>
  <verify><automated>pnpm --filter @scholar-scout/web run test -- --runInBand __tests__/lib/data-recovery.test.ts __tests__/lib/data-store.test.ts</automated></verify>
  <acceptance_criteria>Successful apply has one write containing restored data, recovery backup, and minimal audit; failed apply has no write; retention boundary and stable ordering tests pass.</acceptance_criteria>
  <done>Successful apply has one write containing restored data, recovery backup, and minimal audit; failed apply has no write; retention boundary and stable ordering tests pass.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| package bytes -> recovery service | Untrusted bulk input enters cryptographic/schema validation. |
| plan token -> apply | Bearer data must bind actor/source/state/time/use. |
| recovery service -> store | One validated document crosses the mutation boundary. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-04 | Tampering | envelope/plan | high | mitigate | Exact schema, HMAC, digest, timing-safe verification. |
| T-03-05 | Spoofing/Elevation | plan apply | high | mitigate | Actor/source/version/expiry/single-use binding. |
| T-03-06 | Information Disclosure | preview/audit | high | mitigate | Counts only; privacy-minimal trusted audit. |
| T-03-07 | DoS | package validation | medium | mitigate | 5 MiB and structural bounds before expensive work. |
</threat_model>

<verification>Focused recovery/store tests pass; full web suite and typecheck pass at wave end.</verification>
<success_criteria>Every D-05–D-09 and D-14–D-17 invariant is mechanically covered without introducing packages or Phase 4 CAS claims.</success_criteria>
<output>Create `.planning/phases/03-administrative-and-data-operations-correctness/03-02-SUMMARY.md` when done.</output>
