---
phase: 03-administrative-and-data-operations-correctness
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/web/lib/server/data-store.ts
  - apps/web/lib/server/data-recovery.ts
  - apps/web/app/api/admin/data/capabilities/route.ts
  - apps/web/__tests__/lib/data-store.test.ts
  - apps/web/__tests__/lib/data-recovery.test.ts
  - apps/web/__tests__/api/admin-data-routes.test.ts
autonomous: true
requirements: [OPS-02, OPS-03]
must_haves:
  truths:
    - "D-10/D-11: verified absence is an empty dataset, while corrupt, unreadable, timed-out, or provider-failed reads are explicit unavailable outcomes and block mutations."
    - "D-01/D-02/D-04: an authorized fresh capability read is the sole source of visible operation availability, allowed action, safe reason, retryability, and health."
    - "D-12/D-13: storage failures expose only a stable safe category and incident ID, fail closed before write, and never leak provider details."
  artifacts:
    - "apps/web/lib/server/data-recovery.ts defines typed health/capability/failure contracts."
    - "apps/web/app/api/admin/data/capabilities/route.ts exposes the staff-authorized tracer path."
  key_links:
    - "capabilities GET -> requireActiveStaff -> fresh validated store read -> safe DTO/503"
---

<objective>
Establish the production-quality tracer from a staff capability request through fresh storage validation to a safe UI-ready response, while creating the Wave 0 fixtures required by later plans (D-01, D-02, D-04, D-10, D-11, D-12, D-13).

Purpose: Prove that an outage cannot masquerade as empty data before any recovery mutation is connected.
Output: Typed storage failures, canonical capability route, and deterministic store/recovery/route test harnesses.
</objective>

<execution_context>
@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-administrative-and-data-operations-correctness/03-CONTEXT.md
@.planning/phases/03-administrative-and-data-operations-correctness/03-RESEARCH.md
@.planning/phases/03-administrative-and-data-operations-correctness/03-VALIDATION.md
</context>

<preconditions>
- Phase 2 Preview credential-limiter UAT is complete and recorded before Phase 3 execution starts.
- Use Node 20.x and Corepack-selected pnpm 10.34.5; repair the local command path before accepting test evidence.
</preconditions>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Drive a staff capability request through a fresh validated read</name>
  <read_first>apps/web/lib/server/data-store.ts, apps/web/lib/server/active-staff.ts, apps/web/app/api/admin/data/status/route.ts, apps/web/__tests__/api/admin-data-routes.test.ts</read_first>
  <files>apps/web/lib/server/data-recovery.ts, apps/web/app/api/admin/data/capabilities/route.ts, apps/web/__tests__/lib/data-recovery.test.ts, apps/web/__tests__/api/admin-data-routes.test.ts</files>
  <behavior>
    - Authorized healthy read returns verified counts, adapter label, last-verified timestamp, and only server-known capabilities per D-01/D-02.
    - Removed/unauthorized staff is denied before parsing or storage access.
    - Throwing read returns 503 with safe category, incident ID, retryable=true, no provider text, and zero writes per D-12/D-13.
  </behavior>
  <action>Write failing tests first, then add a server-only recovery contract and canonical capabilities GET. Reuse requireActiveStaff before any store work. Perform exactly one fresh validated read per explicit request (D-04/D-11); do not poll, infer endpoints client-side, or convert failure to zero counts. Keep OPS-02's unclassified probe resolved as “server capability is authoritative; absence from the capability response means the operation is not rendered,” not as an assumption in client code.</action>
  <verify><automated>pnpm --filter @scholar-scout/web run test -- --runInBand __tests__/lib/data-recovery.test.ts __tests__/api/admin-data-routes.test.ts</automated></verify>
  <acceptance_criteria>The healthy and unavailable paths run end-to-end; authorization precedes storage; responses contain no sensitive/provider detail; no failure path writes.</acceptance_criteria>
  <done>The healthy and unavailable paths run end-to-end; authorization precedes storage; responses contain no sensitive/provider detail; no failure path writes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Separate verified absence from operational read failure across adapters</name>
  <read_first>apps/web/lib/server/data-store.ts, apps/web/__tests__/lib/data-store.test.ts, docs/http-data-adapter-runbook.md, docs/vercel-blob-data-adapter.md</read_first>
  <files>apps/web/lib/server/data-store.ts, apps/web/__tests__/lib/data-store.test.ts</files>
  <behavior>
    - JSON ENOENT, HTTP 404, and confirmed missing Blob return a validated empty document.
    - Permission, malformed JSON, non-2xx, invalid provider body, timeout/stream/provider failures throw typed safe failures.
    - Every successful stored document is fully structurally validated before normalization/use.
  </behavior>
  <action>Replace the JSON blanket catch with verified-not-found handling and typed categorized failures. Apply complete persisted-document validation to JSON, HTTP, and Blob success bodies. Keep the existing adapter port and no fallback/queue. This resolves OPS-03's unclassified probe: the governing distinction is verified absence versus operational failure, tested for empty, null/invalid, and provider-error inputs.</action>
  <verify><automated>pnpm --filter @scholar-scout/web run test -- --runInBand __tests__/lib/data-store.test.ts</automated></verify>
  <acceptance_criteria>Only proven absence yields empty data; every other failed or invalid read is unavailable and cannot reach a mutation as editable state.</acceptance_criteria>
  <done>Only proven absence yields empty data; every other failed or invalid read is unavailable and cannot reach a mutation as editable state.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| staff request -> capability route | Session/allowlist authorization precedes storage. |
| adapter/provider -> server document | Provider results are untrusted until complete validation. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-01 | Tampering/DoS | data-store reads | high | mitigate | Distinguish absence, validate all bodies, fail closed. |
| T-03-02 | Elevation | capabilities route | high | mitigate | requireActiveStaff before storage. |
| T-03-03 | Information Disclosure | safe failures | medium | mitigate | Stable category + random incident ID only. |
</threat_model>

<verification>
Run the two focused commands above, then the full web test suite and typecheck for the Wave 0 gate; record duration and keep focused feedback under 120 seconds.
</verification>

<success_criteria>
The capability tracer is runnable; adapter absence/failure cases are deterministic; no operational failure can be represented as a valid empty dataset.
</success_criteria>

<output>Create `.planning/phases/03-administrative-and-data-operations-correctness/03-01-SUMMARY.md` when done.</output>
