---
phase: 13-provider-detail-transition-stories-and-media-safety
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - apps/web/components/catalogue/CatalogueDiscoveryOverview.tsx
  - apps/web/components/catalogue/DiscoveryPreviewSlot.tsx
  - apps/web/components/catalogue/CatalogueFocusView.tsx
  - apps/web/__tests__/components/CatalogueDiscoveryOverview.test.tsx
  - apps/web/__tests__/components/DiscoveryPreviewSlot.test.tsx
  - apps/web/__tests__/components/CatalogueFocusView.test.tsx
autonomous: true
requirements: [MEDIA-01, MEDIA-02]
must_haves:
  truths:
    - "Visual discovery is optional and every metro/pathway remains browseable without a story, a profile, or residence inference."
    - "At most one currently visible, central provider-card local preview may move; students can pause it and reduced-motion visitors receive still factual cards. A valid-but-unrenderable embed yields no client-rendered media path and may resolve to an eligible labelled illustration; factual fallback remains when no safe projection exists."
    - "Provider detail makes Compare this option and Visit the official provider site equally prominent, and the official source opens a new tab."
  artifacts:
    - path: "apps/web/components/catalogue/CatalogueDiscoveryOverview.tsx"
      provides: "Optional visual provider-card explorer using the reviewed discovery DTO."
    - path: "apps/web/components/catalogue/DiscoveryPreviewSlot.tsx"
      provides: "Shared local/illustration presentation state with factual fallback for an absent safe projection."
    - path: "apps/web/components/catalogue/CatalogueFocusView.tsx"
      provides: "Provider detail actions that retain factual source and comparison access."
  key_links:
    - from: "apps/web/components/catalogue/CatalogueDiscoveryOverview.tsx"
      to: "apps/web/lib/catalogue-discovery.ts"
      via: "cards render only the server-projected renderableMedia and controlled detail href"
      pattern: "renderableMedia"
    - from: "apps/web/components/catalogue/CatalogueFocusView.tsx"
      to: "/shortlist"
      via: "explicit Compare this option link sits beside official-provider action"
      pattern: "Compare this option"
---

<objective>
Expand the proven safe media path into an optional visual opportunity explorer and detail actions without allowing motion or visual presentation to obscure factual browsing.

Purpose: Give learners a calm visual hook while preserving their existing source-first, choice-preserving workflow.
Output: Governed visual cards, strict one-card playback behavior, no embed renderer with D-03 continuation to eligible illustrations, and equal detail actions.
</objective>

<execution_context>
@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-provider-detail-transition-stories-and-media-safety/13-CONTEXT.md
@.planning/phases/13-provider-detail-transition-stories-and-media-safety/13-RESEARCH.md
@.planning/phases/13-provider-detail-transition-stories-and-media-safety/13-01-SUMMARY.md
@apps/web/components/catalogue/CatalogueDiscoveryOverview.tsx
@apps/web/components/catalogue/CatalogueFocusView.tsx
@apps/web/app/globals.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add optional governed visual cards and equal provider-detail next steps</name>
  <reversibility rating="costly">The visual-card entry and paired detail actions establish the public learner navigation contract for provider media.</reversibility>
  <files>apps/web/components/catalogue/CatalogueDiscoveryOverview.tsx, apps/web/components/catalogue/DiscoveryPreviewSlot.tsx, apps/web/components/catalogue/CatalogueFocusView.tsx, apps/web/__tests__/components/CatalogueDiscoveryOverview.test.tsx, apps/web/__tests__/components/CatalogueFocusView.test.tsx</files>
  <behavior>
    - A discovery card with a valid projection presents its labelled visual and controlled Scholar Scout detail link; a card without one remains a complete factual card.
    - The discovery page exposes a clearly optional visual-explorer entry while retaining all six metro controls and all pathway coverage without a personal-data requirement.
    - Detail presents the facts below the visual and offers Compare this option and Visit the official provider site with matched button treatment; the latter uses a new browsing context.
  </behavior>
  <action>Begin with failing component tests, then implement D-05, D-08, and D-09 using only the reviewed `CatalogueDiscoveryItem` and controlled existing discovery/detail URLs. Add visual cards as an additive part of the existing results list; factual cards, filters, coverage, status announcement, shortlist behavior, and selected metro controls stay available whether a projection exists or not. Keep visual-card activation inside Scholar Scout first, not an external provider route. On `CatalogueFocusView`, preserve factual evidence and make the comparison and official-provider controls sibling actions with equivalent visual hierarchy; use the existing controlled source URL, `target="_blank"`, and `rel="noreferrer"` for the official action.</action>
  <verify>
    <automated>corepack pnpm --filter @scholar-scout/web test -- --runInBand --runTestsByPath __tests__/components/CatalogueDiscoveryOverview.test.tsx __tests__/components/CatalogueFocusView.test.tsx</automated>
  </verify>
  <done>The visual explorer is an optional enhancement to ordinary six-area browsing, and provider details visibly pair comparison with the official external verification action.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce the single-card motion and factual-fallback interaction contract</name>
  <files>apps/web/components/catalogue/CatalogueDiscoveryOverview.tsx, apps/web/components/catalogue/DiscoveryPreviewSlot.tsx, apps/web/__tests__/components/CatalogueDiscoveryOverview.test.tsx, apps/web/__tests__/components/DiscoveryPreviewSlot.test.tsx</files>
  <behavior>
    - When several valid local previews are on screen, only the visible card whose midpoint is nearest the viewport center is allowed to play muted; it pauses on scroll-away, when another card becomes nearer, and after its visible Pause control is activated.
    - `prefers-reduced-motion` prevents playback and leaves the label, facts, source actions, and pause-state explanation readable.
    - An absent safe projection leaves the preview slot factual and preserves its sources and provider-detail actions without creating a third-party media surface; a valid-but-unrenderable embed can instead yield the resolver's eligible labelled illustration projection.
  </behavior>
  <action>Use a single overview-owned visibility/center coordinator, refs, and `IntersectionObserver` updates to implement D-06 rather than independent card autoplay. Request `HTMLMediaElement.play()` only for the selected visible local-preview card, catch playback rejection without hiding facts, keep it muted, and call `pause()` for every exit condition. Expose a visible labelled Pause/Play control and a live-readable state without timers or auto-advance. Keep `DiscoveryPreviewSlot` limited to selected local previews, labelled Scholar Scout illustrations, and the factual fallback; it receives no embed URL, iframe contract, or activation control in Phase 13. Test with mocked observers, media methods, and `matchMedia` for center transfer, scroll-away, manual pause, reduced motion, illustration labelling after a valid-but-unrenderable embed, and factual fallback when neither local nor illustration selection passes.</action>
  <verify>
    <automated>corepack pnpm --filter @scholar-scout/web test -- --runInBand --runTestsByPath __tests__/components/CatalogueDiscoveryOverview.test.tsx __tests__/components/DiscoveryPreviewSlot.test.tsx</automated>
  </verify>
  <done>Playback is bounded to one visible central local card and remains user-controllable; valid-but-unrenderable embeds expose no renderer and may continue to a labelled eligible illustration, while any absent safe projection renders the complete factual fallback.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reviewed discovery DTO → client visual cards | Public data controls visual card rendering and client media behavior. |
| selected snapshot projection → client preview | Client presentation must never gain an embed URL or third-party renderer from the reviewed snapshot. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-04 | Denial of Service | visual-card playback | medium | mitigate | Centralize visibility arbitration, pause off-screen media, provide a visible control, and honor reduced motion. |
| T-13-05 | Information Disclosure | approved embed candidate | high | mitigate | The client contract has no external embed renderer; the resolver continues a valid-but-unrenderable embed candidate to an eligible labelled illustration or, if none passes, factual fallback. |
| T-13-06 | Spoofing | provider-detail actions | medium | mitigate | Keep Scholar Scout detail first, label official action, and retain factual sources alongside matched comparison action. |
</threat_model>

<verification>
Run the focused component suite, then `corepack pnpm --filter @scholar-scout/web run typecheck` and `corepack pnpm --filter @scholar-scout/web run lint`.
</verification>

<success_criteria>
Visual discovery is optional, all ordinary browsing controls remain usable, exactly one eligible card can move under the stated conditions, and provider detail preserves equally prominent choice and official-source actions.
</success_criteria>

<output>
Create `.planning/phases/13-provider-detail-transition-stories-and-media-safety/13-02-SUMMARY.md` when done.
</output>
