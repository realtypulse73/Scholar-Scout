---
phase: 11-choice-preserving-six-area-discovery
plan: 05
type: execute
wave: 4
depends_on: [11-01, 11-02, 11-03, 11-04]
files_modified:
  - apps/web/lib/catalogue-discovery.ts
  - apps/web/__tests__/lib/catalogue-discovery.test.ts
  - apps/web/components/catalogue/CatalogueOpportunityCard.tsx
  - apps/web/__tests__/components/CatalogueOpportunityCard.test.tsx
  - apps/web/components/catalogue/CatalogueFocusView.tsx
  - apps/web/__tests__/components/CatalogueFocusView.test.tsx
  - apps/web/components/catalogue/CatalogueComparison.tsx
  - apps/web/__tests__/components/CatalogueComparison.test.tsx
  - apps/web/__tests__/app/programmes/[id]/page.test.tsx
autonomous: true
gap_closure: true
requirements: [DISC-02]
must_haves:
  truths:
    - "Each reviewed discovery item supplies a bounded factual reasons-to-consider collection that retains the source, date, state, and verification guidance of its published facts, with no learner-profile, eligibility, or outcome assertion."
    - "Overview cards, focused detail, and saved-choice comparison visibly render their factual reasons with an official verification path."
    - "Every saved comparison record links to its detail route using that record's controlled metro, so records in all six approved regions resolve against their own reviewed snapshot model."
  artifacts:
    - path: apps/web/lib/catalogue-discovery.ts
      provides: Bounded source-backed factual reasons DTO derived only from public snapshot facts.
    - path: apps/web/components/catalogue/CatalogueComparison.tsx
      provides: Reason-aware saved-choice comparison cards with metro-preserving detail URLs.
  key_links:
    - from: apps/web/lib/catalogue-discovery.ts
      to: apps/web/components/catalogue/CatalogueOpportunityCard.tsx
      via: CatalogueDiscoveryItem reasons-to-consider DTO keeps its fact evidence intact for overview rendering.
      pattern: "reasonsToConsider"
    - from: apps/web/components/catalogue/CatalogueComparison.tsx
      to: apps/web/app/programmes/[id]/page.tsx
      via: Controlled detail URL carries item.regionId through the existing metro query filter.
      pattern: "buildCatalogueDetailHref|metro"
---

<objective>
Close the two verified Phase 11 DISC-02 gaps: make factual reasons-to-consider flow from the reviewed public record into every discovery surface, and keep a saved comparison choice in its own metro when opening details.

Purpose: restore the complete source-first discovery contract without extending discovery into personal matching, provider fetching, publication workflow, or Phase 13 media work.
Output: reason-aware discovery DTO and surfaces, metro-preserving comparison detail links, and focused regression coverage.
</objective>

<execution_context>
@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-choice-preserving-six-area-discovery/11-VERIFICATION.md
@.planning/phases/11-choice-preserving-six-area-discovery/11-01-SUMMARY.md
@.planning/phases/11-choice-preserving-six-area-discovery/11-02-SUMMARY.md
@.planning/phases/11-choice-preserving-six-area-discovery/11-03-SUMMARY.md
@.planning/phases/11-choice-preserving-six-area-discovery/11-04-SUMMARY.md
@apps/web/lib/catalogue-discovery.ts
@apps/web/components/catalogue/CatalogueOpportunityCard.tsx
@apps/web/components/catalogue/CatalogueFocusView.tsx
@apps/web/components/catalogue/CatalogueComparison.tsx
@apps/web/app/programmes/[id]/page.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Derive a bounded, source-backed factual-reasons DTO from the reviewed snapshot</name>
  <files>apps/web/lib/catalogue-discovery.ts, apps/web/__tests__/lib/catalogue-discovery.test.ts</files>
  <behavior>
    - A `CatalogueDiscoveryItem` exposes a bounded `reasonsToConsider` collection derived only from named public snapshot facts, and each entry retains the value, fact state, source label, source date, and verification action of its source fact.
    - The collection contains only factual labels/values that a visitor can verify; it is neither ordered by a learner attribute nor phrased as an eligibility, admission, employment, pay, funding, enlistment, placement, or personal-outcome assertion.
    - A record with unavailable reason-eligible values exposes an honest empty reason collection rather than inventing a reason or reading a provider at request time.
  </behavior>
  <action>Begin with model tests for reviewed snapshot records carrying mixed evidence states. Add a public `CatalogueDiscoveryReason` representation and a `reasonsToConsider` field on `CatalogueDiscoveryItem`. Implement the verification report's approved equivalent to a new publication field: deterministically derive at most three labelled reasons from the existing public record's non-null, material facts (skill taught, delivery, and training payer), preserving the mapped fact's value, state, and complete evidence object. Keep the derivation wholly inside `mapPublishedRecord`; it must receive only `CataloguePublishedRecord` data and must not inspect search parameters beyond the existing filters, a session, shortlist, profile, sensitive data, ranking, or a provider URL request. Give reason labels neutral factual wording and use empty output when no listed public fact has a value. Add tests that prove the bounded count, exact carried evidence, mixed-state visibility, deterministic source-record derivation, and absence of a fabricated reason. This implements D-01, D-02, D-06, D-07, D-08, and D-09 while closing the first missing item in `11-VERIFICATION.md`.</action>
  <verify>
    <automated>corepack pnpm --filter @scholar-scout/web test -- --runInBand __tests__/lib/catalogue-discovery.test.ts; corepack pnpm --filter @scholar-scout/web run typecheck</automated>
  </verify>
  <done>Every public discovery item has a finite, source-backed factual-reasons contract or an honest empty collection, and the model still accepts only the existing controlled discovery inputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render source-first factual reasons on overview cards and focused detail</name>
  <files>apps/web/components/catalogue/CatalogueOpportunityCard.tsx, apps/web/__tests__/components/CatalogueOpportunityCard.test.tsx, apps/web/components/catalogue/CatalogueFocusView.tsx, apps/web/__tests__/components/CatalogueFocusView.test.tsx</files>
  <behavior>
    - A scan-friendly overview card shows only the first factual reason when one is available, including its label/value, readable fact state, named evidence source, source date, fact-specific verification guidance, and a native direct link to that reason fact's stored source URL. The separate `See all facts` detail path remains available for the complete record and is not a substitute for the reason's direct source link.
    - Focused detail shows every bounded reason in a labelled factual-reasons section, with each reason's state, source/date, verification guidance, and source action available through native links.
    - Empty reason collections are stated honestly and do not alter save, compare, alternate-route, navigation, non-media preview, or factual evidence behavior.
  </behavior>
  <action>Write component regressions before rendering changes. In `CatalogueOpportunityCard`, add a concise, source-first factual-reason block using only the first `item.reasonsToConsider` entry to preserve overview-card density. Render that reason's label/value, readable fact state, evidence source label, source date, and its fact-specific verification guidance, plus a native anchor whose `href` is that reason fact's stored direct source URL. Retain a separately named `See all facts` link to the complete record; it must not replace or obscure the direct fact-source action. In `CatalogueFocusView`, add a labelled `Reasons to consider` section that maps all `item.reasonsToConsider` entries with the same source/date/state/verification-link treatment used by material facts. When the collection is empty, render plain availability text rather than a claim about student fit or a request for more information. Preserve the existing responsive, native-control, reduced-motion, and non-media contracts. Tests must assert the overview renders exactly one reason, its source date, fact-specific verification wording, and a direct source-URL `href`, alongside the `See all facts` path; assert source-backed reason text and verification links on focused detail, mixed evidence states remain readable, and the empty state stays factual. Apply D-01, D-02, D-03, D-04, D-07, D-08, and D-09 while closing the source-first rendering portion of `11-VERIFICATION.md`.</action>
  <verify>
    <automated>corepack pnpm --filter @scholar-scout/web test -- --runInBand __tests__/components/CatalogueOpportunityCard.test.tsx __tests__/components/CatalogueFocusView.test.tsx; corepack pnpm --filter @scholar-scout/web run typecheck; corepack pnpm --filter @scholar-scout/web run lint</automated>
  </verify>
  <done>Overview cards visibly carry one source-backed factual reason with its date, fact-specific verification guidance, direct fact-source URL, and a separate `See all facts` path; focused detail carries the complete shared reason collection without adding a personal recommendation or unapproved media behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Complete comparison reasons and preserve each saved item's controlled metro</name>
  <files>apps/web/components/catalogue/CatalogueComparison.tsx, apps/web/__tests__/components/CatalogueComparison.test.tsx, apps/web/__tests__/app/programmes/[id]/page.test.tsx</files>
  <behavior>
    - Every resolved saved-choice card renders its full factual reasons collection with the same value/state/source/date/verification information as the focused detail; an empty collection remains explicit and factual.
    - A comparison detail link is built with `buildCatalogueDetailHref` and a complete controlled filter object whose metro is `item.regionId`, so the link cannot silently default to Greater Houston.
    - Regression coverage proves all six controlled metro IDs produce their own canonical detail URL and proves a non-Houston reviewed record resolves through the dynamic detail route when its metro is supplied.
  </behavior>
  <action>Write the comparison and route regressions first. Extend `ComparisonCard` with a labelled factual-reasons area that uses the existing fact-row evidence pattern for every `item.reasonsToConsider` entry, including an honest empty collection state. Replace its hand-built detail path with `buildCatalogueDetailHref(item.id, controlledFiltersForItemRegion)` where the filters set the item metro and otherwise use the existing neutral defaults; do not copy browser, profile, saved-list, or untrusted query state into this URL. Add parameterized comparison coverage for all six `CatalogueRegionId` values, asserting each details action carries its own canonical metro and no record falls back to the Houston context. Extend the dynamic detail-page regression fixture with a reviewed non-Houston record and prove the page finds it when the supplied controlled metro matches. Keep unavailable saved IDs unavailable and do not substitute a regional record. This implements D-01, D-05, D-06, D-07, D-08, and D-09 while closing the second missing item in `11-VERIFICATION.md`.</action>
  <verify>
    <automated>corepack pnpm --filter @scholar-scout/web test -- --runInBand __tests__/components/CatalogueComparison.test.tsx __tests__/app/programmes/[id]/page.test.tsx; corepack pnpm --filter @scholar-scout/web run typecheck; corepack pnpm --filter @scholar-scout/web run lint; corepack pnpm --filter @scholar-scout/web test -- --runInBand</automated>
  </verify>
  <done>Saved choice comparison shows the same verifiable factual reasons as discovery detail, and each of the six regional choices opens its own current reviewed detail model instead of a default-region not-found state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reviewed public snapshot → learner reasons | A presentation-specific reason must preserve public fact evidence and must not become a personal decision or fetch new provider data. |
| Saved browser IDs → detail route | A comparison action must retain the record's controlled region rather than selecting another regional model. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-07 | Information disclosure | discovery reason mapper | high | mitigate | Derive reasons only from `CataloguePublishedRecord` facts already exposed by the reviewed public snapshot; preserve evidence and do not add candidate, audit, profile, or sensitive data. |
| T-11-08 | Tampering | comparison detail action | medium | mitigate | Build the detail URL from the selected public item's validated `regionId` and fixed controlled defaults through the existing URL helper. |
| T-11-09 | Spoofing | reason verification action | medium | mitigate | Reuse each reason fact's validated stored source URL/label/date and explicit verification wording; no URL is constructed from visitor input. |
| T-11-SC | Tampering | package installation | high | mitigate | No packages are installed. |
</threat_model>

## Source Coverage Audit

| Source | Item | Plan coverage |
|---|---|---|
| GOAL | Source-first public discovery surfaces | Tasks 1–3 restore the missing factual-reasons flow and the broken cross-metro detail action only. |
| REQ | DISC-02 complete provider/type/place/status/reasons/facts/evidence/alternate/action contract | Tasks 1–3 provide source-backed reasons on all required surfaces and a working comparison detail action. |
| RESEARCH | Shared public DTO, canonical controlled URLs, source-first cards, finite detail, snapshot-only data | Tasks 1–3 preserve these established patterns while repairing their two missing links. |
| CONTEXT | D-01 through D-09 | Task actions cite the applicable hybrid, factual, choice-control, filtering, uncertainty, comparison, and official-action boundaries; no deferred media, provider-fetching, or profile-matching scope is added. |
| VERIFICATION | `CatalogueDiscoveryItem` drops reasons | Tasks 1–2 derive and render the bounded factual-reasons DTO. |
| VERIFICATION | Comparison detail link drops metro | Task 3 emits and proves the controlled item-region URL plus non-Houston resolution. |

<verification>
Run the focused model, card, focus, comparison, and dynamic detail-route suites; then run web typecheck, lint, and the full in-band web Jest suite. The separate Phase 11 browser accessibility acceptance remains required by `11-VERIFICATION.md` and is not changed by this narrow code-gap plan.
</verification>

<success_criteria>
- Every public discovery surface displays the same bounded, source-backed factual reasons or an honest empty state.
- Reasons never become a profile fit, eligibility, outcome, or provider-live-data claim.
- Comparison details retain the saved record's controlled metro, with regression coverage across all six approved metros.
- The captured full web suite, typecheck, and lint complete successfully.
</success_criteria>

<output>
Create `.planning/phases/11-choice-preserving-six-area-discovery/11-05-SUMMARY.md` when done.
</output>
