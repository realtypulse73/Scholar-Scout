---
phase: 11-choice-preserving-six-area-discovery
plan: 03
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - apps/web/app/shortlist/page.tsx
  - apps/web/components/catalogue/CatalogueComparison.tsx
  - apps/web/__tests__/components/CatalogueComparison.test.tsx
autonomous: true
requirements: [DISC-01]
must_haves:
  truths:
    - "A visitor's saved route IDs resolve against the current reviewed snapshot and can be compared without signing in."
    - "Comparison keeps missing or retired saved IDs visible as a factual unavailable state instead of silently discarding the visitor's choice."
    - "Each compared option shows source-backed facts, fact-specific status/date/actions, and neutral save/remove or official-verification actions without a recommendation or winner."
  artifacts:
    - path: apps/web/components/catalogue/CatalogueComparison.tsx
      provides: Responsive public-snapshot comparison with preserved saved-ID states.
      exports: [default]
    - path: apps/web/__tests__/components/CatalogueComparison.test.tsx
      provides: Browser-local compare, missing-ID, evidence, and reflow regression coverage.
  key_links:
    - from: apps/web/app/shortlist/page.tsx
      to: apps/web/lib/server/programme-records.ts
      via: getPublishedCatalogueSnapshot feeds buildCatalogueDiscoveryModel before the browser comparison component receives data.
      pattern: "getPublishedCatalogueSnapshot|buildCatalogueDiscoveryModel"
    - from: apps/web/components/catalogue/CatalogueComparison.tsx
      to: apps/web/lib/shortlist.ts
      via: Existing shortlist storage helpers retain visitor-chosen IDs and allow removal without ranking selected records.
      pattern: "parseShortlist|toggleShortlistId"
---

<objective>
Move student-controlled saving and side-by-side comparison onto the governed public snapshot while preserving every saved choice and its source evidence.

Purpose: make comparison practical on phone and desktop without letting the interface infer a winner, conceal stale evidence, or drop a retired choice.
Output: snapshot-driven comparison route/component and component-level factual/reflow tests.
</objective>

<execution_context>
@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/11-choice-preserving-six-area-discovery/11-CONTEXT.md
@.planning/phases/11-choice-preserving-six-area-discovery/11-RESEARCH.md
@.planning/phases/10-curated-import-and-governed-staff-publication/10-VERIFICATION.md
@.planning/phases/11-choice-preserving-six-area-discovery/11-01-SUMMARY.md
@apps/web/app/shortlist/page.tsx
@apps/web/components/shortlist/ShortlistComparison.tsx
@apps/web/components/shortlist/ShortlistButton.tsx
@apps/web/lib/shortlist.ts
@apps/web/lib/catalogue-discovery.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one saved public opportunity reaches source-first comparison</name>
  <files>apps/web/app/shortlist/page.tsx, apps/web/components/catalogue/CatalogueComparison.tsx, apps/web/__tests__/components/CatalogueComparison.test.tsx</files>
  <precondition>Plan 11-01 exports `buildCatalogueDiscoveryModel` and its `CatalogueDiscoveryItem` type from `apps/web/lib/catalogue-discovery.ts`.</precondition>
  <behavior>
    - A saved ID in browser shortlist storage resolves to the corresponding current public discovery item and renders provider, pathway, place/delivery, material facts, source date/state, detail, remove, and official verification actions.
    - The comparison route receives only the public Phase 10 snapshot model and remains usable before sign-in; existing account synchronization stays optional rather than becoming an access gate.
    - The comparison language describes records and facts, not personal fit, eligibility, likely outcomes, or a recommended winner.
  </behavior>
  <action>Write a red component test for one saved public discovery item. Change `/shortlist` to read `getPublishedCatalogueSnapshot()` and build the same public discovery model used by `/programmes`; remove the legacy `getGovernedProgrammes()`/`Programme` data path from this route. Create `CatalogueComparison` using existing shortlist storage/account-sync conventions so it resolves browser-saved IDs against passed discovery items. Render one factual comparison card path with the item identity, controlled pathway, place/delivery, all material facts with source/date/status/verification action, an explicit details link, and a student removal action. Use a labelled empty state that directs visitors to `/programmes`; no sign-in, onboarding, ranking, scoring, or application action belongs in this component. Implement D-01 and D-08.</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test -- --runInBand __tests__/components/CatalogueComparison.test.tsx</automated></verify>
  <done>One unauthenticated saved public record flows from browser shortlist storage into a source-backed comparison card on the snapshot-driven shortlist route.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve missing choices and responsive multi-record factual comparison</name>
  <files>apps/web/components/catalogue/CatalogueComparison.tsx, apps/web/__tests__/components/CatalogueComparison.test.tsx</files>
  <behavior>
    - Multiple saved records render as independent factual comparison cards on small screens; the page root has no wide fixed layout requirement.
    - A saved ID absent from the active snapshot remains represented with a clear retired-or-not-in-current-snapshot message and a browse/reset action.
    - Every visible material fact keeps its own source/status/date/verification action; unknown, stale, and conflicting facts cannot look confirmed.
    - Removing one saved item changes only that visitor-held shortlist state; no record receives a preferred/best/eligible label.
  </behavior>
  <action>Expand `CatalogueComparison` to resolve all saved IDs in their stable saved order, dividing them into present public records and visible unavailable entries. Preserve unavailable entries until the visitor removes them, label why their current factual detail is unavailable, and offer a return to browse rather than substituting an arbitrary record. On phone and tablet widths render stacked semantic cards/definition lists; if a larger-screen table is useful, constrain it inside its own labelled horizontal-scrolling region and preserve an equivalent stacked view at small widths. Give each material fact its own labelled value, status, source/date, and external verification link. Retain visible focus styles, native buttons/links, status messaging, and existing optional account synchronization. Add TDD coverage for two records, a missing ID, mixed evidence states, remove behavior, no outcome/winner language, and responsive class contracts that keep the document itself within its viewport. Implement D-07 and D-08 while supporting DISC-04's comparison portion.</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test -- --runInBand __tests__/components/CatalogueComparison.test.tsx; corepack pnpm --filter @scholar-scout/web run typecheck; corepack pnpm --filter @scholar-scout/web run lint</automated></verify>
  <done>Students can compare their chosen current and unavailable routes fact by fact on small or large screens without the product deciding which route is better.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser shortlist IDs → current snapshot records | Old/forged/missing IDs must not become a silent record substitution or private-record read. |
| Source evidence → comparison presentation | Fact status must stay specific, visible, and action-oriented rather than becoming a general ranking signal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-11-07 | Tampering | saved-ID resolution | medium | mitigate | Resolve IDs only against passed public items and display unmatched IDs as unavailable without an alternate record lookup. |
| T-11-08 | Information disclosure | shortlist route | high | mitigate | Route reads only the active public snapshot; the component receives no candidate, audit, profile, or staff field. |
| T-11-09 | Repudiation | browser save/remove choice | low | accept | Existing local/account shortlist mechanisms retain their established student-owned mutation semantics; this plan adds no privileged audit claim. |
| T-11-SC | Tampering | package installation | high | mitigate | No packages are installed. |
</threat_model>

## Source Coverage Audit

| Source | Item | Plan coverage |
|---|---|---|
| GOAL | Student-controlled comparison of governed pathways | Tasks 1–2 wire public snapshot records into save/compare and retained-choice states. |
| REQ | DISC-01 | Both tasks ensure public save, compare, and official action behavior. |
| RESEARCH | Responsive comparison, retained/missing IDs, no score/winner | Tasks 1–2 implement and test each constraint. |
| CONTEXT | D-01, D-07, D-08 | Comparison supports the hybrid model, visible uncertainty, and student-controlled factual choices. |

<verification>
Run the comparison suite, web typecheck, and lint. The final Phase 11 manual check exercises saving/removing two routes and comparing them at phone/tablet widths.
</verification>

<success_criteria>
- Saved choices resolve through the stored reviewed snapshot rather than legacy programmes.
- Missing snapshot records stay honest and under student control.
- The responsive comparison presents facts and verification actions without a hidden recommendation.
</success_criteria>

<output>
Create `.planning/phases/11-choice-preserving-six-area-discovery/11-03-SUMMARY.md` when done.
</output>
