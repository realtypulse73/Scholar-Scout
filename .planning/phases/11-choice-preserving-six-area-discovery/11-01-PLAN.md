---
phase: 11-choice-preserving-six-area-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/lib/catalogue-discovery.ts
  - apps/web/__tests__/lib/catalogue-discovery.test.ts
  - apps/web/components/catalogue/CatalogueDiscoveryOverview.tsx
  - apps/web/__tests__/components/CatalogueDiscoveryOverview.test.tsx
  - apps/web/app/programmes/page.tsx
autonomous: true
requirements: [DISC-01, DISC-03]
must_haves:
  truths:
    - "Any visitor can open /programmes without an account or onboarding profile, choose a covered metro, filter the reviewed snapshot, save a route, compare it, and open its stored official verification link."
    - "Every one of the six pathway classes remains visible as an explicit filter/coverage choice for the selected metro, including a truthful no-reviewed-record state."
    - "The browse order and URL filters are deterministic, reversible, and derived only from explicit visitor controls and the public reviewed snapshot."
  artifacts:
    - path: apps/web/lib/catalogue-discovery.ts
      provides: Public-record mapper, strict URL filter parser, deterministic ordering, coverage lookup, and filter application.
      exports: [CatalogueDiscoveryItem, CatalogueDiscoveryFilters, parseCatalogueDiscoveryFilters, buildCatalogueDiscoveryModel]
    - path: apps/web/components/catalogue/CatalogueDiscoveryOverview.tsx
      provides: Public metro/filter overview with governed cards and a result-state explanation.
      exports: [default]
  key_links:
    - from: apps/web/app/programmes/page.tsx
      to: apps/web/lib/server/programme-records.ts
      via: getPublishedCatalogueSnapshot followed by buildCatalogueDiscoveryModel
      pattern: "getPublishedCatalogueSnapshot|buildCatalogueDiscoveryModel"
    - from: apps/web/components/catalogue/CatalogueDiscoveryOverview.tsx
      to: apps/web/components/shortlist/ShortlistButton.tsx
      via: Existing browser-local, optional account-synchronized save action keyed by public catalogue record ID.
      pattern: "ShortlistButton"
---

<objective>
Prove the public, stored-snapshot discovery path end to end, then make its six-area filters and empty states fully deterministic.

Purpose: replace legacy profile-ranked programme browsing with a visitor-controlled catalogue surface that keeps the reviewed public record and every pathway class visible.
Output: typed discovery view model, URL controls, source-first overview cards, and focused regression tests.
</objective>

<execution_context>
@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-choice-preserving-six-area-discovery/11-CONTEXT.md
@.planning/phases/11-choice-preserving-six-area-discovery/11-RESEARCH.md
@.planning/phases/10-curated-import-and-governed-staff-publication/10-VERIFICATION.md
@apps/web/lib/catalogue-contract.ts
@apps/web/lib/catalogue-fixtures.ts
@apps/web/lib/catalogue-publication.ts
@apps/web/lib/server/programme-records.ts
@apps/web/app/programmes/page.tsx
@apps/web/components/shortlist/ShortlistButton.tsx
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove one public reviewed opportunity from snapshot to visitor action</name>
  <files>apps/web/lib/catalogue-discovery.ts, apps/web/__tests__/lib/catalogue-discovery.test.ts, apps/web/components/catalogue/CatalogueDiscoveryOverview.tsx, apps/web/__tests__/components/CatalogueDiscoveryOverview.test.tsx, apps/web/app/programmes/page.tsx</files>
  <precondition>Phase 10's `getPublishedCatalogueSnapshot()` remains available from `apps/web/lib/server/programme-records.ts` and returns only its stored public snapshot result.</precondition>
  <behavior>
    - A published record maps to one browser-safe discovery item with its provider title, controlled region/pathway, place/delivery, practical card facts, aggregate fact state, source label/date/state, direct official verification URL, and media-rights fallback state.
    - A visitor can see a governed card, open its detail link, save its ID, open comparison, and open a labelled official verification action without supplying a profile or signing in.
    - The programmes page reads the Phase 10 public snapshot only; it does not load onboarding data, session/profile ordering, legacy match scores, candidate data, audit data, or provider-network content.
  </behavior>
  <action>Start with failing unit and component tests. Create `apps/web/lib/catalogue-discovery.ts` as the named-export public mapper over `CataloguePublishedRecord`, `catalogueRegions`, and `catalogueCoverage`; accept an injected clock and preserve each material fact's own value/unresolved state and evidence rather than collapsing it into a prediction. Its initial model must expose one neutral stable ID order, a media state of `reserved-for-rights-review`, the record's stored official source URL, and explicit region/pathway coverage. Create `CatalogueDiscoveryOverview` as a server-safe presentation component using native links/forms and the existing `ShortlistButton`; render source label/date/state and an explicit official verification link on every card, plus a link into `/programmes/[id]`. Replace the legacy `/programmes` session/profile/score path with `getPublishedCatalogueSnapshot()` and this model. When the snapshot has no published records, render an honest reviewed-snapshot-empty state with the selected metro's boundary/coverage context and a browse action; do not fall back to seed/legacy programmes. Keep this first slice limited to one selected metro and its reviewed cards, but use the final public model and real routes rather than a disconnected demonstration. Implement D-01, D-02, D-05, D-06, D-07, and D-09 in this tracer.</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test -- --runInBand __tests__/lib/catalogue-discovery.test.ts __tests__/components/CatalogueDiscoveryOverview.test.tsx</automated></verify>
  <done>The public `/programmes` route visibly consumes only the stored reviewed snapshot and one card gives an unauthenticated visitor factual save, compare, detail, and official-verification actions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add reversible six-area filtering, coverage disclosure, and neutral ordering</name>
  <files>apps/web/lib/catalogue-discovery.ts, apps/web/__tests__/lib/catalogue-discovery.test.ts, apps/web/components/catalogue/CatalogueDiscoveryOverview.tsx, apps/web/__tests__/components/CatalogueDiscoveryOverview.test.tsx, apps/web/app/programmes/page.tsx</files>
  <behavior>
    - Only `metro`, `pathway`, `delivery`, `status`, `q`, and `page` are recognized from the URL; malformed values leave the public model in a safe, explainable state.
    - The overview always renders all six controlled pathway choices and each chosen metro's coverage state; filters narrow cards only and a reset restores the unfiltered selected-metro view.
    - Results are ordered by stable public ID after explicit filters; no account, location inference, academic proxy, sensitive circumstance, passive behavior, engagement signal, or score is an input.
    - Empty results explain whether active filters removed cards, a coverage cell is not yet verified, or the active reviewed snapshot has no matching record, with a safe reset/widen action.
  </behavior>
  <action>Extend the model with strict allowlisted query parsing, canonical query serialization helpers for return/detail links, deterministic stable-ID ordering, controlled text matching, explicit delivery/status filters, and coverage lookup for all six `CATALOGUE_PATHWAYS`. Render a labelled native filter form with covered-metro selection, pathway/delivery/status controls, a concise active-filter/sort summary, and a reset link. The pathway controls must remain present even when the reviewed snapshot has zero cards for a class; pair each choice with the relevant `verified` or `not-yet-verified` coverage disclosure so an absent card never implies availability or exclusion. Build all actions from model-owned controlled values, ignore unknown query values, and do not send filter terms to any provider. Remove the legacy profile/session imports and score language from this route/component. Add TDD coverage for malformed params, stable ordering, filter reversal, all six pathway/coverage entries, reviewed-snapshot empty state, filter-empty state, source action, and the absence of profile-shaped inputs from the discovery API. Implement D-05, D-06, and D-07 without adding Phase 12 qualification or Phase 14 referral behavior.</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test -- --runInBand __tests__/lib/catalogue-discovery.test.ts __tests__/components/CatalogueDiscoveryOverview.test.tsx; corepack pnpm --filter @scholar-scout/web run typecheck; corepack pnpm --filter @scholar-scout/web run lint</automated></verify>
  <done>A visitor can change or reset explicit six-area filters and understands the deterministic result order, coverage condition, and verification action without any hidden personalization or pathway suppression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Stored public snapshot → discovery model | The learner surface must not receive staff candidates, audits, or a provider-network dependency. |
| URL query → public filter state | Visitor-controlled query strings must not become profile, eligibility, location, or provider-request inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-11-01 | Information disclosure | programme discovery route | high | mitigate | Read only the cloned published snapshot and map allowlisted `CataloguePublishedRecord` fields into the browser DTO. |
| T-11-02 | Tampering | URL filter parser | medium | mitigate | Allowlist query keys/controlled values and test malformed values plus canonical reset behavior. |
| T-11-03 | Elevation of privilege | save/compare actions | low | accept | Reuse existing student-owned shortlist interaction; the public route has no new privileged mutation. |
| T-11-SC | Tampering | package installation | high | mitigate | No package installation; retain the pinned TypeScript/Next/Jest toolchain. |
</threat_model>

## Source Coverage Audit

| Source | Item | Plan coverage |
|---|---|---|
| GOAL | Accessible source-first six-area discovery | Tasks 1–2 create the public snapshot browse/filter path. |
| REQ | DISC-01 | Task 1 exposes public browse/save/compare/official action; Task 2 completes reversible filters. |
| REQ | DISC-03 | Task 2 fixes controlled ordering and all-class filter visibility. |
| RESEARCH | Public DTO, snapshot seam, canonical URL state, coverage states | Tasks 1–2 implement and test those patterns. |
| CONTEXT | D-01, D-02, D-05, D-06, D-07, D-09 | Tasks 1–2 cite and implement each decision. |

<verification>
Run the two focused Jest suites, then web typecheck and lint. The later phase-level check must include keyboard-only and small-width testing after focus and comparison surfaces land.
</verification>

<success_criteria>
- `/programmes` is public and begins with an orderly Realtor-style overview of stored reviewed records.
- The overview cannot silently hide a pathway class, misstate coverage, or use a visitor profile to order results.
- Each card presents a factual verification action alongside student-controlled save and compare actions.
</success_criteria>

<output>
Create `.planning/phases/11-choice-preserving-six-area-discovery/11-01-SUMMARY.md` when done.
</output>
