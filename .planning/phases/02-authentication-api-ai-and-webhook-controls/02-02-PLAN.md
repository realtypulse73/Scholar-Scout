---
phase: 02-authentication-api-ai-and-webhook-controls
plan: 02
type: tdd
wave: 2
depends_on: [02-01]
files_modified: [apps/web/package.json, pnpm-lock.yaml, .env.production.example, .env.prelaunch.local.example, apps/web/lib/server/rate-limit.ts, apps/web/lib/server/request-ip.ts, apps/web/lib/api-request.ts, apps/web/__tests__/lib/rate-limit.test.ts, apps/web/__tests__/lib/request-ip.test.ts, apps/web/__tests__/lib/api-request.test.ts]
autonomous: true
requirements: [SEC-03, SEC-05]
user_setup:
  - service: Upstash Redis
    why: "Provides externally atomic, cross-instance quota and credential reservations."
    env_vars:
      - name: UPSTASH_REDIS_REST_URL
        source: "Upstash console -> database REST API"
      - name: UPSTASH_REDIS_REST_TOKEN
        source: "Upstash console -> database REST API"
must_haves:
  truths:
    - Missing or unreachable limiter configuration rejects costly/credential operations with a safe 503.
    - Tests can deterministically prove quota reset and denial without a live provider.
  artifacts:
    - apps/web/lib/server/rate-limit.ts
    - apps/web/lib/api-request.ts
  key_links:
    - Routes consume a typed atomic reservation result before KDF, context reads, or OpenAI calls.
    - The credentials and registration boundaries derive an IP only from Vercel's platform-controlled client-IP header.
---

<objective>
Install the approved atomic counter client and establish fail-closed reservation and bounded-body contracts.

Purpose: Replace unsafe document-store counters with an externally atomic Upstash Redis adapter, per D-09--D-12 and D-17--D-20.
Output: Three file-disjoint, tested limiter/request-parser seams that later route plans consume. Executor scope partition: Task 1 owns the four package/environment files, Task 2 owns the four limiter/IP files, and Task 3 owns the two parser files; execute and verify each unit separately before continuing within this plan.
</objective>

<execution_context>@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/02-authentication-api-ai-and-webhook-controls/02-RESEARCH.md
@apps/web/lib/server/data-store.ts
<interfaces>
`ScholarScoutDataStore` only exposes whole-document `read()`/`write()`; it must not back counters. Export a separate injected limiter port with `reserve(key, limit, window): Promise<{ allowed; resetAt; retryAfterSeconds }>`.
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add the approved atomic limiter dependency and operational boundary</name>
  <files>apps/web/package.json, pnpm-lock.yaml, .env.production.example, .env.prelaunch.local.example</files>
  <behavior>- Missing REST configuration is documented as a 503 fail-closed condition.
- The runbook specifies production credentials, a non-production provider verification procedure, and no client exposure.</behavior>
  <action>After Plan 01 approval, add exactly `@upstash/redis@1.38.0` and `@upstash/ratelimit@2.0.8` using the pinned Node 20/Corepack pnpm workflow, matching the recorded provenance/integrity evidence. Add server-only Upstash REST URL/token names plus production and local verification guidance to the existing environment templates; require the recorded Upstash database provisioned by Plan 01 before production verification. Do not use Vercel Blob, the existing HTTP adapter, or process memory as a counter.</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web exec pnpm ls @upstash/redis @upstash/ratelimit; corepack pnpm --filter @scholar-scout/web run typecheck</automated></verify>
  <done>Approved packages are locked, provisioning is documented, and no browser-visible environment variable contains a Redis credential.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Implement fail-closed atomic reservation policies</name>
  <files>apps/web/lib/server/rate-limit.ts, apps/web/lib/server/request-ip.ts, apps/web/__tests__/lib/rate-limit.test.ts, apps/web/__tests__/lib/request-ip.test.ts</files>
  <behavior>- Advisor guest/day allows 10 reservations and denies the 11th with reset metadata (D-09).
- Advisor account/day allows 25 and denies the 26th (D-09).
- Sign-in email/IP allows five per 15 minutes and registration IP allows five per hour (D-17).
- On Vercel, only a single valid `x-vercel-forwarded-for` value supplies the client address; a conflicting/spoofed `x-forwarded-for`, missing address, or malformed multi-value header cannot select another limiter key (D-17).
- An unavailable limiter returns an unavailable result; it never allows work by default (D-12, D-17).</behavior>
  <action>Write failing unit tests first, then create a server-only adapter around Upstash's atomic limiter with injected clock/client seams. Export named policies for guest advisor, account advisor, sign-in email/IP composite, and registration IP keys; return typed reset/retry metadata. Also export the one trusted-IP policy: Scholar Scout's supported production deployment is Vercel, whose proxy overwrites the client address header; accept only one syntactically valid `x-vercel-forwarded-for` address, ignore all `x-forwarded-for` values, and return an unavailable result when the trusted address is absent or malformed. Prefix and hash identity material in provider keys, preserve a migrated guest-window binding through its UTC-day reset, and map missing configuration or provider failure to an explicit unavailable result. Do not reserve after a provider/KDF call or silently fall back to an in-process counter.</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test --runInBand --runTestsByPath __tests__/lib/rate-limit.test.ts __tests__/lib/request-ip.test.ts</automated></verify>
  <done>Reservations are atomic at the external provider boundary, trusted IPs cannot be spoofed through forwarded headers, and unavailable configuration/provider state fails closed.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Add reusable byte-bounded exact JSON parsing</name>
  <files>apps/web/lib/api-request.ts, apps/web/__tests__/lib/api-request.test.ts</files>
  <behavior>- Oversize declared or streamed bodies return a typed request failure before JSON parsing.
- Exact-object parsers reject unknown keys and invalid scalar/array bounds.</behavior>
  <action>Write tests before implementing a pure/request helper that checks Content-Length when supplied, streams to an explicit byte ceiling, parses JSON only after the ceiling, and delegates exact object validation to route-owned validators. Return typed expected-failure results rather than exposing parser errors. Keep provider, actor, and persistence logic out of this shared utility.</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test --runInBand --runTestsByPath __tests__/lib/api-request.test.ts</automated></verify>
  <done>Security-sensitive routes can reject oversized, malformed, and extra-field JSON before downstream work.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Next.js to Upstash Redis | Security limits depend on an external atomic reservation service. |
| client body to route | Untrusted bytes must be bounded before parsing. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-02 | Denial of service | quota/rate-limit adapter | high | mitigate | Atomic pre-work reservation plus 503 on unavailable configuration/provider. |
| T-02-03 | Tampering | request parser | medium | mitigate | Byte ceiling and exact schemas before downstream calls. |
| T-02-SC | Tampering | package install | high | mitigate | Execute only after Plan 01 approval. |
</threat_model>
<verification>Run each task's focused suite immediately after its isolated 4/4/2-file change set, then `corepack pnpm --filter @scholar-scout/web run lint`.</verification>
<success_criteria>All later API plans have a tested, externally atomic and fail-closed reservation seam plus bounded parsing.</success_criteria>
<output>Create `.planning/phases/02-authentication-api-ai-and-webhook-controls/02-02-SUMMARY.md` when done</output>
