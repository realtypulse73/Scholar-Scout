---
phase: 02-authentication-api-ai-and-webhook-controls
plan: 11
type: tdd
wave: 6
depends_on: [02-10]
files_modified: [apps/web/app/api/register/route.ts, apps/web/components/auth/AuthForm.tsx, apps/web/__tests__/api/register.test.ts, apps/web/__tests__/components/auth/AuthForm.test.tsx]
autonomous: true
requirements: [SEC-05, SEC-01]
must_haves:
  truths:
    - Registration has a trusted-IP rate limit and returns reset-aware 429 before account creation.
    - The browser uses the rate-limit-aware credentials boundary, displays only locked safe outcomes, and migrates same-device guest activity after successful sign-in.
  artifacts:
    - apps/web/__tests__/api/register.test.ts
    - apps/web/__tests__/components/auth/AuthForm.test.tsx
  key_links:
    - AuthForm -> POST /api/auth/credentials -> one-use grant -> NextAuth Credentials callback -> POST /api/account/guest-migration.
---

<objective>
Complete registration and browser credential integration on top of the tested credentials exchange.

Purpose: Deliver D-03 and D-17--D-20 without relying on unsupported NextAuth callback response customization.
Output: Reset-aware registration route, exact AuthForm request wiring, and regression tests.
</objective>

<execution_context>@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>
@.planning/phases/02-authentication-api-ai-and-webhook-controls/02-10-SUMMARY.md
@.planning/phases/02-authentication-api-ai-and-webhook-controls/02-03-SUMMARY.md
@apps/web/app/api/register/route.ts
@apps/web/components/auth/AuthForm.tsx
<interfaces>Plan 02 supplies the Vercel trusted-IP policy and atomic registration reservation. Plan 10 exposes POST `/api/auth/credentials`, which returns only allowlisted result codes, reset metadata, and an opaque one-use grant for `signIn('credentials')`; Plan 03 supplies the session-bound migration endpoint.</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Rate-bound exact registration before account creation</name>
  <files>apps/web/app/api/register/route.ts, apps/web/__tests__/api/register.test.ts</files>
  <behavior>- Five registrations per trusted Vercel IP/hour are allowed; the sixth returns 429 with `Retry-After` and reset metadata before any account write (D-17).
- Missing/malformed trusted IP or unavailable limiter returns safe 503; spoofed `x-forwarded-for` does not affect the reservation key (D-17).
- Registration accepts only its bounded account fields, derives the default student role server-side, and rolling expiry restores access without a permanent account lock (D-17, D-20).</behavior>
  <action>Write route tests first. Byte-bound and exact-validate the registration body, obtain the IP solely through the Plan-02 Vercel trusted-header helper, reserve the per-IP hourly window before account creation, and return reset-aware 429 or safe 503 without an account write. Remove the browser-selected role from the accepted body and create a student account by server policy; active staff remains governed by the live allowlist. Test five/six boundaries, reset expiry, unavailable/missing address, spoofed forwarded header, malformed input, and no-write ordering (D-17, D-20).</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test --runInBand --runTestsByPath __tests__/api/register.test.ts</automated></verify>
  <done>Registration is atomically rate-bounded with trustworthy IP derivation, safe reset metadata, and no browser-selected privilege.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Wire AuthForm to the supported credential boundary and migration flow</name>
  <files>apps/web/components/auth/AuthForm.tsx, apps/web/__tests__/components/auth/AuthForm.test.tsx</files>
  <behavior>- Credential sign-in posts only email/password to `/api/auth/credentials`, passes only the returned opaque grant to NextAuth, then requests guest migration before profile navigation (D-03, D-18).
- The client renders only the fixed unknown-account, incorrect-password, rate-limit/reset, unavailable, and migration-failure messages; it never displays raw server errors or guest credential material (D-03, D-18).
- Sign-up submits no role field and successful OAuth behavior remains with the Plan-10 session trigger.</behavior>
  <action>Write interaction tests first. Remove the client role selector and submit only the server-accepted registration fields. For credential sign-in, call the Plan-10 credentials endpoint, map its fixed status/code/reset fields to intentional user messages, call `signIn('credentials')` using only the opaque grant on success, then await the session-bound migration endpoint before navigation/refresh. Treat no-guest/idempotent migration as success and retain a recoverable migration error. Test exact fetch bodies, 401 code mapping, 429/reset display, 503 display, successful grant use, migration ordering, and absence of browser role/guest/context fields. Do not retry failed credentials client-side or add community behavior (D-03, D-04, D-18).</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test --runInBand --runTestsByPath __tests__/components/auth/AuthForm.test.tsx</automated></verify>
  <done>Students receive the locked detailed credential feedback and reset guidance through a supported HTTP boundary, while successful credentials safely migrate eligible same-device activity.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser/proxy to registration and credential UI | Untrusted requests may spoof identity, select a role, or exhaust account/KDF capacity. |
| credentials boundary to NextAuth | Only a server-minted one-use grant may establish a credentials session. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-24 | Denial of service | registration | high | mitigate | Trusted Vercel IP, atomic pre-write reservation, 429/reset, 503 on unavailable control. |
| T-02-25 | Information disclosure | credential UI | medium | accept | D-18 accepts two allowlisted account-state outcomes; rate limiting and no raw errors constrain disclosure. |
| T-02-26 | Tampering | client migration/role input | medium | mitigate | Server-only role/guest authority, one-use grant, exact request-body and migration-order tests. |
</threat_model>

<verification>Run the focused registration/AuthForm suites, then the full web Jest suite, lint, and typecheck under Node 20/Corepack pnpm.</verification>
<success_criteria>Registration and sign-in apply the locked rate/feedback/migration policies through testable HTTP and client boundaries, without trusting browser roles, forwarded headers, or guest identity.</success_criteria>
<output>Create `.planning/phases/02-authentication-api-ai-and-webhook-controls/02-11-SUMMARY.md` when done</output>
