---
phase: 02-authentication-api-ai-and-webhook-controls
plan: 04
type: tdd
wave: 4
depends_on: [02-03]
files_modified: [apps/web/app/api/memory/route.ts, apps/web/app/api/simulations/results/route.ts, apps/web/app/api/analytics/events/route.ts, apps/web/__tests__/api/user-data-routes.test.ts]
autonomous: true
requirements: [SEC-01]
must_haves:
  truths:
    - Memory and simulation reads/writes are restricted to the resolved actor.
    - Analytics writes use the resolved actor and public callers cannot read the global event collection.
  artifacts: [apps/web/__tests__/api/user-data-routes.test.ts]
  key_links: ["Each route passes `actor.storageKey` rather than a request value to platform-store."]
---
<objective>
Migrate personal memory, simulation, and analytics boundaries to the shared actor and exact-input contracts.

Purpose: Complete the first user-data ownership slice of SEC-01 while keeping public discovery open per D-01.
Output: Actor-scoped routes with regression coverage.
</objective>
<execution_context>@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md</execution_context>
<context>@.planning/phases/02-authentication-api-ai-and-webhook-controls/02-PATTERNS.md
@apps/web/lib/server/student-actor.ts
@apps/web/lib/api-request.ts</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Scope memory and simulation results to the resolved actor</name>
  <files>apps/web/app/api/memory/route.ts, apps/web/app/api/simulations/results/route.ts, apps/web/__tests__/api/user-data-routes.test.ts</files>
  <behavior>- A supplied identity cannot read or write another actor’s memory or results (D-01).
- Simulation POST validates its identifier and bounded answers before store writes.
- Account and guest records remain isolated; unauthenticated routes follow the actor policy.</behavior>
  <action>Write route tests first, then resolve the actor before any store access on GET/POST. Replace query/body ownership with the actor storage key; use bounded exact request parsing for simulation payload fields and preserve the existing result/recommendation response shape. Ensure tests attempt cross-actor identity substitution and prove no foreign store key is used.</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test --runInBand --runTestsByPath __tests__/api/user-data-routes.test.ts</automated></verify>
  <done>Memory and simulation operations derive ownership only from server actor state and validate business input.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Bound analytics access and actor-owned events</name>
  <files>apps/web/app/api/analytics/events/route.ts, apps/web/__tests__/api/user-data-routes.test.ts</files>
  <behavior>- Event POST ignores/rejects supplied identity and persists the actor key (D-01).
- A public GET cannot receive the application-wide analytics collection.</behavior>
  <action>Replace caller-selected analytics identity with the actor key and validate the finite analytics area, bounded event name, and bounded scalar metadata. Remove the global analytics GET response entirely in this phase; do not retain or add an aggregate operational view, so this plan has no dependency on the active-staff guard in Plan 06. Keep public programme/feed discovery routes open and do not implement Phase-5 community capabilities (D-04).</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web test --runInBand --runTestsByPath __tests__/api/user-data-routes.test.ts</automated></verify>
  <done>Analytics events cannot be attributed to another student and global analytics are not disclosed to arbitrary callers.</done>
</task>
</tasks>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| browser to personal API routes | Untrusted query/body fields must not select storage owners. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| T-02-07 | Spoofing / Information disclosure | memory and simulation routes | high | mitigate | Resolve actor before reads/writes and regression-test cross-actor requests. |
| T-02-08 | Information disclosure | analytics GET | high | mitigate | Remove/protect global event response; validate event writes. |
</threat_model>
<verification>Focused user-data routes test plus full web lint/typecheck after merge.</verification>
<success_criteria>Students can use only their own memory, simulation, and event data despite any identity value in a browser request.</success_criteria>
<output>Create `.planning/phases/02-authentication-api-ai-and-webhook-controls/02-04-SUMMARY.md` when done</output>
