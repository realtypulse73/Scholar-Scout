---
phase: 04-incremental-durable-persistence-boundaries
plan: 04
type: execute
wave: 4
depends_on: [04-03]
files_modified:
  - apps/web/lib/server/data-recovery.ts
  - apps/web/lib/server/data-store.ts
  - apps/web/__tests__/lib/data-recovery.test.ts
  - apps/web/__tests__/lib/data-store.test.ts
  - docs/http-data-adapter-runbook.md
  - docs/vercel-blob-data-adapter.md
autonomous: true
requirements: [DATA-01, DATA-02]
must_haves:
  truths:
    - "A recovery plan that becomes stale before apply returns a no-write conflict and preserves current data."
    - "Signed digest, exact confirmation, one-write apply, idempotent outcome, retention, incident hold, and audit guarantees remain unchanged."
    - "Opaque persistence versions never enter ScholarScoutData, signed envelopes, backups, previews, or audit payloads."
    - "Existing adapters and compatibility reads remain supported while raw unconditional mutation is restricted to no ordinary domain path."
  artifacts:
    - path: "apps/web/lib/server/data-recovery.ts"
      provides: "Version-bound one-write recovery apply"
    - path: "apps/web/__tests__/lib/data-recovery.test.ts"
      provides: "Phase 3 recovery regression plus CAS interleaving coverage"
    - path: "docs/http-data-adapter-runbook.md"
      provides: "Implemented HTTP concurrency and recovery semantics"
    - path: "docs/vercel-blob-data-adapter.md"
      provides: "Implemented Blob ETag/CAS and recovery semantics"
  key_links:
    - from: "apps/web/lib/server/data-recovery.ts"
      to: "apps/web/lib/server/data-store.ts"
      via: "versioned read captured during validation and one conditional final write"
    - from: "recovery envelope/digest"
      to: "ScholarScoutData"
      via: "canonical application content only; adapter version remains out of band"
---

<objective>
Bind Phase 3 recovery apply to the Phase 4 conditional-write contract and close compatibility/migration safeguards.

Purpose: Prevent a valid but newly stale restore/import from overwriting later data while preserving every signed recovery and adapter guarantee already verified.
Output: Conditional one-write recovery, regression tests, compatibility inventory, and accurate adapter runbooks.
</objective>

<execution_context>@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md</execution_context>

<context>
@.planning/phases/04-incremental-durable-persistence-boundaries/04-03-SUMMARY.md
@.planning/phases/03-administrative-and-data-operations-correctness/03-VERIFICATION.md
@.planning/phases/03-administrative-and-data-operations-correctness/03-SECURITY.md
@.planning/phases/03-administrative-and-data-operations-correctness/03-UAT.md
@apps/web/lib/server/data-recovery.ts
@docs/http-data-adapter-runbook.md
@docs/vercel-blob-data-adapter.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make signed recovery apply one conditional whole-document write</name>
  <reversibility rating="costly">Recovery format and behavior are already shipped guarantees; changes must remain wire-compatible and avoid migration of signed content.</reversibility>
  <files>apps/web/lib/server/data-recovery.ts, apps/web/lib/server/data-store.ts, apps/web/__tests__/lib/data-recovery.test.ts, apps/web/__tests__/lib/data-store.test.ts</files>
  <behavior>
    - A version captured for a validated restore/import plan is required by its final write.
    - An intervening commit makes apply fail with a safe conflict and zero recovery writes/audit outcomes claiming success.
    - Existing signed digest, exact confirmation, idempotency, retention, hold, and lifecycle audit cases remain green.
    - Provider version data is absent from signed/exported application content and privacy-minimal audit evidence.
  </behavior>
  <action>Thread the adapter's opaque expected version through server-side recovery plan state and use it only for the existing single final conditional document write. Keep the application digest as the signed-content binding and keep the provider version out of serialized envelopes, previews, backups, and audit records. On provider conflict, return a stable retry/replan outcome without applying data or recording success. Do not decompose restore/import into domain operations, change signing keys/formats, relax staff authorization, or alter retention/incident-hold rules.</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web run test -- --runInBand __tests__/lib/data-recovery.test.ts __tests__/lib/data-store.test.ts __tests__/api/admin-data-routes.test.ts</automated></verify>
  <done>Recovery remains one signed, guarded, idempotent write and now also rejects a provider-version race without changing current data.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce compatibility boundaries and document adapter guarantees</name>
  <files>apps/web/lib/server/data-store.ts, apps/web/__tests__/lib/data-store.test.ts, docs/http-data-adapter-runbook.md, docs/vercel-blob-data-adapter.md</files>
  <action>Inventory every remaining mutation call. Ordinary student, programme, operational, and platform writes must reach a named bounded conditional operation; the guarded recovery/import seam may retain whole-document composition but must use `writeVersioned`. Keep read compatibility for existing callers and adapters. Add a source-level regression or focused assertion that prevents migrated domain modules from reintroducing direct unconditional writes. Update both runbooks with the implemented ETag/version, first-create, conflict, retry, local-JSON scope, and one-write recovery behavior. State that no production migration or deployment occurred.</action>
  <verify><automated>corepack pnpm --filter @scholar-scout/web run test -- --runInBand __tests__/lib/data-store.test.ts __tests__/lib/data-recovery.test.ts &amp;&amp; corepack pnpm --filter @scholar-scout/http-data-service run test</automated></verify>
  <done>Every roadmap domain uses bounded conditional operations, recovery is the only guarded whole-document composition seam, and runbooks match tested adapter behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| signed recovery plan -> current store | A previously valid plan may become stale before its one final write. |
| adapter metadata -> recovery content | Provider versions must never become signed/exported application data. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-12 | Tampering | stale recovery apply | critical | mitigate | Bind final write to expected adapter version and prove no-write conflict. |
| T-04-13 | Information Disclosure | backup/envelope metadata | high | mitigate | Keep provider versions out of serialized and audit content. |
| T-04-14 | Repudiation | recovery outcome | high | mitigate | Record success only after conditional apply; preserve idempotent lifecycle evidence. |
| T-04-SC | Tampering | package installs | high | mitigate | No package install. |
</threat_model>

<verification>Phase 3 recovery suites plus new interleaving CAS cases pass; adapter runbooks agree with the tested contract.</verification>
<success_criteria>Phase 3 recovery behavior is regression-safe under concurrent writes, all supported adapters remain available, and migration remains incremental and reversible.</success_criteria>
<output>Create `04-04-SUMMARY.md` when done.</output>
