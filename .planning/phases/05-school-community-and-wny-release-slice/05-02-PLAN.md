---
phase: 05-school-community-and-wny-release-slice
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - apps/web/app/api/admin/community-moderation/route.ts
  - apps/web/app/admin/community-moderation/page.tsx
  - apps/web/components/admin/CommunityModerationQueue.tsx
  - apps/web/__tests__/api/community-moderation.test.ts
  - apps/web/__tests__/components/community-release.test.tsx
autonomous: true
requirements: [PROD-03]
must_haves:
  truths:
    - "Only freshly authorized staff can call listPendingReviewCampusNotes or resolve pending moderation records; unauthorized visitors receive the existing non-disclosing denial before any storage access (D-08)."
    - "A staff member can restore only a pending-review item to public or remove only a pending-review item to removed; an invalid/stale transition is rejected, a conflict retains an actionable queue row, and a report keeps content non-public (D-07, D-08)."
  artifacts:
    - "A focused staff-gated moderation page and API separate from ProgrammeAdminManager."
    - "An accessible queue component with safe excerpts, restore/remove confirmation, busy and retry states."
  key_links:
    - "Page and API both call requireActiveStaff before calling listPendingReviewCampusNotes or any other storage operation."
    - "Queue actions invoke restorePendingReviewCampusNote or removePendingReviewCampusNote from 05-01 and remove only a successfully resolved row locally."
---

<objective>
Expand the tracer into the focused staff moderation queue and resolution boundary required by D-08, retaining D-07's immediate-hide lifecycle.

Purpose: Make the safety state operational without coupling moderation to the programme administration manager.
Output: Fresh-staff API/page, responsive queue, and staff/denial/conflict tests.
</objective>

<execution_context>
@C:/Users/judge/.codex/gsd-core/workflows/execute-plan.md
@C:/Users/judge/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-school-community-and-wny-release-slice/05-SPEC.md
@.planning/phases/05-school-community-and-wny-release-slice/05-CONTEXT.md
@.planning/phases/05-school-community-and-wny-release-slice/05-UI-SPEC.md
@.planning/phases/05-school-community-and-wny-release-slice/05-PATTERNS.md
@.planning/phases/05-school-community-and-wny-release-slice/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add fresh-staff moderation read and resolution contracts</name>
  <files>apps/web/app/api/admin/community-moderation/route.ts, apps/web/app/admin/community-moderation/page.tsx, apps/web/__tests__/api/community-moderation.test.ts</files>
  <read_first>apps/web/lib/server/active-staff.ts; apps/web/app/api/admin/programmes/route.ts; apps/web/app/admin/feed/page.tsx; apps/web/lib/server/operational-records.ts; apps/web/__tests__/api/admin-programmes.test.ts</read_first>
  <behavior>
    - Fresh non-staff authorization denies queue reads and restore/remove mutations before any storage access or listPendingReviewCampusNotes call.
    - Fresh staff calls listPendingReviewCampusNotes only after requireActiveStaff succeeds, receives only the staff-safe pending-review DTO, and invokes restorePendingReviewCampusNote for pending-review -> public or removePendingReviewCampusNote for pending-review -> removed; invalid/non-pending transitions get a stable safe error.
    - A transition conflict returns a retryable response and preserves the reported record as non-public; a removed note cannot be restored or reappear in the public read fixture.
  </behavior>
  <action>Create the dedicated admin route and server page using requireActiveStaff with distinct read and resolve audit actions before parsing input, calling listPendingReviewCampusNotes, or reading any storage. The GET route and server page must call the named 05-01 list operation only after that fresh authorization succeeds; render/serialize its staff-safe pending-review DTO without supplementing it from raw note, review, author, reporter, or audit collections. Use notFound() for the page denial pattern and explicit non-sensitive JSON statuses for the API. Consume only the named 05-01 operations: restorePendingReviewCampusNote with expected status 'pending-review' for restore, and removePendingReviewCampusNote with expected status 'pending-review' for removal; do not directly edit note/review collections or broaden the data-store replacement API. Map invalid action, public/removed/non-pending target, and stale expected-status outcomes to the stable conflict contract. Test denial ordering, authorization-before-list ordering, pending-only staff DTO shape, named-operation calls, authorized restore/remove, invalid/non-pending rejection, stale conflict, and a removed-record fixture that stays absent from public reads.</action>
  <verify><automated>pnpm --filter @scholar-scout/web test --runInBand -- community-moderation</automated></verify>
  <acceptance_criteria>Only a freshly active staff member reaches queue storage or resolution; authorized restore/remove call the dedicated named pending-review operations; invalid or stale transitions are explicit; and removal leaves the record unavailable to every public-read fixture.</acceptance_criteria>
  <done>The staff server boundary is independently testable and preserves the tracer's persisted moderation invariants.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deliver the accessible focused moderation queue</name>
  <files>apps/web/components/admin/CommunityModerationQueue.tsx, apps/web/app/admin/community-moderation/page.tsx, apps/web/__tests__/components/community-release.test.tsx</files>
  <read_first>apps/web/components/campus-community/CampusNoteBoard.tsx; apps/web/components/ui/Button.tsx; apps/web/app/admin/feed/page.tsx; .planning/phases/05-school-community-and-wny-release-slice/05-UI-SPEC.md</read_first>
  <behavior>
    - Pending rows show safe wrapped excerpts/context and labelled Restore to community / Remove permanently actions.
    - Confirmation dialog defaults to the safe action, traps/returns focus, and resolution disables the row while announcing status.
    - Empty, load-error, and conflict/retry states are accessible; a failed resolve keeps the row actionable.
  </behavior>
  <action>Implement the UI-SPEC staff queue as a dedicated responsive Card list, not a new registry or an extension of ProgrammeAdminManager. Use existing local Card/Button patterns, the exact approved copy, 44px actions, visible focus, semantic headings, and safe long-Unicode wrapping with the specified disclosure for full staff content. On a successful server response remove the resolved row; on failure retain it and focus/announce retry guidance. Keep all moderation information staff-only and make removal confirmation visibly destructive only inside this staff screen.</action>
  <verify><automated>pnpm --filter @scholar-scout/web test --runInBand -- community-release community-moderation</automated></verify>
  <acceptance_criteria>The queue has accessible populated, empty, busy, failure, and conflict states; non-staff users cannot reach it; staff can resolve one pending item without exposing reporter or author identity to public UI.</acceptance_criteria>
  <done>D-08 is visibly and safely delivered on its own staff route.</done>
</task>

</tasks>

<threat_model>
## Security posture

OWASP ASVS Level 1 applies. Every high-severity threat is a release blocker until its named mitigation and automated check pass.

## Trust Boundaries

| Boundary | Description |
|---|---|
| browser → staff API | Caller attempts to read or resolve sensitive moderation records. |
| staff UI → resolution API | A destructive choice crosses from an authorized operational screen to persisted state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-05 | E | staff moderation API/page | high | mitigate | Fresh `requireActiveStaff` gate before parse/read and non-disclosing page denial. |
| T-05-06 | T | restore/remove transition | high | mitigate | Pending-state validation, server transition authority, conflict response, and confirmation UI. |
| T-05-07 | I | staff queue rendering | medium | mitigate | Render only staff-safe excerpt/context and keep queue behind server gate. |
</threat_model>

## Artifacts this phase produces

- Dedicated staff moderation page/API with fresh authorization and bounded state transitions.
- Accessible pending-review queue that supports restore/remove without public moderation leakage.

<verification>
Run the focused moderation API/component suites, then the full web Jest suite after Wave 2.
</verification>

<success_criteria>
D-07 and D-08 are reachable end-to-end: report hides an item, only staff see it in the queue, and staff can choose a safe resolution with conflict recovery.
</success_criteria>

<output>
Create `.planning/phases/05-school-community-and-wny-release-slice/05-02-SUMMARY.md` when done.
</output>
